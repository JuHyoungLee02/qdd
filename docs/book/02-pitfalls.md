# 02. 함정·실수와 예방 규칙 (새 작업 전에 먼저 읽는다)

형식: **증상 → 원인 → 예방 규칙 → 발생(커밋·회차·문서)**. 번호는 고정(새 항목은 끝에 붙인다). 근거 문서 약칭: `R/` = `docs/stage3/results/`, DL = `docs/draft-log.md`, HO = `docs/handoff.md`.

## 가. 기록·커밋·문서 일관성

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P01 | 줄 끝이 `\r\r\n`으로 두 번 바뀜 | CRLF 파일에 `\n`→`\r\n` 변환을 한 번 더 함 | 파일 전체를 LF로 풀고 → 덧붙이고 → 원래 CRLF였으면 한 번만 묶는다; 쓴 뒤 `\r\r\n`·외톨이 `\r`·탭 수를 출력해 0 확인 | R7 17회차(user-log.md), DL 459 |
| P02 | 기록 줄이 제어 문자로 쪼개짐, 역슬래시 경로가 탭으로 | 셸 heredoc 안 파이썬 문자열의 `\n`·`\t`가 실제 문자로 해석 | 경로·이스케이프가 든 편집은 **Write 도구로 만든 .py 파일**로만 실행 | R7 18회차 D-1(7fe42c6), DL 363(반복 3회) |
| P03 | 에이전트가 heredoc·`python -`·`python -c`·PowerShell 문자열로 스크립트를 만듦 | 습관 | 모든 지시문에 "Write 도구 스크립트만, heredoc·`cat >`·`python -`·`python -c` 금지"를 넣는다(그래도 R7 22·23·24·25, 계획·논문 에이전트에서 반복됨 → 보고서에 이탈 기록 의무) | R7 22–25회차 보고서, 계획·논문 에이전트 보고 |
| P04 | f-string 안 `{…}` 때문에 SyntaxError, 커밋에서 절이 빠짐 | 한국어 문장 속 중괄호, `&&` 사슬이 python 실패를 못 막음 | 문장에 중괄호가 들어가면 `{{ }}`; 명령 출력의 Traceback을 확인한 뒤 커밋 [→ 표시 2026-09-25 20:01 UTC, R7 26회차 N54: `ulog94.py`는 기록 스크립트 파일 번호이지 user-log 94가 아님] (표 칸 안으로 옮김, 2026-09-25 20:37 UTC, R7 27회차 N54) | DL 339(§25 누락), 이번 세션 ulog94 |
| P05 | **결정은 user-log·CLAUDE.md에만 있고 정본·handoff는 옛 상태** → R7 DOC | 결정을 적는 곳이 여럿인데 한 곳만 고침 | 사용자 결정 = **같은 커밋**에 user-log + 정본 절 + handoff(핵심 결정·대기 항목) + 책 03장. 정본에 [사용자 결정] 절을 더하면 handoff §2.7도 같이 | R7 11(§75→handoff), 23(22회차 줄), 24(§84→대기 항목), 25(user-log 87→정본 §85) |
| P06 | 결과 보고서를 커밋했는데 handoff §2.8·draft-log·direction-log 줄이 없음 | 보고서만 add | R7 보고서·결과 문서 커밋 = 기록 줄 3곳(+책 01장)을 **같은 커밋**에 | R7 23회차 D-1 |
| P07 | handoff의 정본 범위 숫자가 세 번 낡음 | 늘어나는 번호(정본 절 수)를 요약 문서에 적음 | 요약 문서는 "정본 마지막 절까지"로만 가리킨다 | R7 3·8·18·19·21회차 |
| P08 | 정정했다고 적었는데 실제로는 반쪽만 고침 | 적은 줄과 실제 diff를 대조하지 않음 | 정정 기록 전에 `git diff`로 실제 바뀐 줄을 확인 | R7 19회차 D-1 |
| P09 | 결과 문서에 새 절을 붙였는데 같은 문서의 요약·종합 절은 옛 결론 | 부분 갱신 | 절을 붙이면 요약·결론 절도 같은 커밋에서 갱신 | R7 17회차(se2e_diag) |
| P10 | 같은 사실을 적은 다른 문서를 놓침 | 문구로만 grep | 구현 이름(예: vLLM·HF·함수명)으로도 grep해 모든 소비처를 고친다 | R7 3·4회차(DL 442) |
| P11 | 시각을 짐작해 적음(01:50 대 실제 01:43), KST 날짜를 UTC 문서에 | 손으로 쓴 시각 | 시각은 `date -u` 출력 변수로만; 저장소 = UTC, 채팅 보고 = KST(user-log 72) | 정본 §21–§24 제목, R7 1회차 C1·2회차 K1, DL 335 |
| P12 | 등록 문서의 변경 기록 시각이 등록 시각보다 늦게 적힘(약 1시간 어긋남) | 사후에 기억으로 적음 | 변경마다 그 순간 `date -u`로 적고, 파일 mtime·user-log 시각과 대조 | R7 25회차 E-D1(`prereg_astra_motion.md`, 정정 8063733) |
| P13 | `[사용자]` 표시 줄·빨간 줄을 요약·수정해 검사에 걸림 | 편집 편의 | `[사용자]` 줄과 `\intent` 빨간 줄은 user-log 원문 그대로; 커밋 전 `tools/intent_check.py`·`user_line_check.py`·[사용자] diff 0 확인 | DL 343·438 |
| P14 | 다른 에이전트가 편집 중인 폴더를 `git add` 해 중간 상태가 커밋됨 | 폴더 단위 add | 파일을 하나씩 지정해 add; 남의 미커밋 파일은 건드리지 않는다 | DL 360(3152474) |

## 나. 사전 등록·실험 절차

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P15 | 등록 개정을 커밋하기 전에 유료 호출을 재개 | 에이전트가 "작성"을 "등록"으로 착각 | 등록 개정 → 메인 커밋 → **해시를 받은 뒤** 유료·학습 재개 | R7 25회차 E-D2(탐침 18:03:51Z 재개, 커밋 35029e5 18:27:27Z) |
| P16 | 사전 등록 전에 결과 일부를 봄 | GPU 점유 확인 중 평가 로그를 읽음 | 등록 전 결과 폴더 열람 금지; 봤으면 등록 문서에 "이미 본 것"과 문턱이 그 전에 정해졌음을 적는다 | `P/prereg_ma1b.md` §0(motion_s1 0.6956) |
| P17 | 표본이 작아 어떤 결정도 못 가르는 유료 단계 | 단계마다 "무엇을 결정하나"를 안 물음 | 등록에 단계별 "결정 / 이 표본으로 가를 수 있는가" 줄 필수(user-log 87, CLAUDE.md) | 탐침 S1 4편·G2 12회 삭제(user-log 87) |
| P18 | 관문 실패인데 설계를 그대로 두고 계속 | 관문 의미 무시 | 관문 실패·버그·무효 답 > 10 %·교란·비용/시간 2배 → 멈추고 재설계 → 등록 변경 커밋 → 재개(§85) | E-MA1 G0 → E-MA1b 재설계 |
| P19 | 구현 수치가 사전 등록과 다름(부트스트랩 2,000 대 10,000, γ 0.67 대 2/3, H = 1 미구현, Holm 없음 등) | 등록 원문 대신 기억으로 구현 | 구현 뒤 등록 원문에서 대조표를 다시 만들고 **경계값 입력으로 동작 확인**(CMP_EPS 1e-12) | R7 7·8·9·10·12·13회차(정본 §72–§80) |
| P20 | 파드에 옛 판정 스크립트가 남아 있음 | 배포 누락 | 판정 전 파드 사본 해시 = 커밋 블롭 해시 확인(`CODE_HASHES`) | 이번 세션(재배포·해시 확인) |
| P21 | 파드 코드 사본이 CRLF라 "무수정" 해시가 LF 블롭과 다름 | Windows 클론을 그대로 복사 | 고정 사본은 `git -c core.autocrlf=false archive`(LF); 해시는 LF 블롭 기준 | R7 22회차 N23 |
| P22 | 게이트를 편 단위로 계산해 단계 기준보다 일찍 멈춤 | 등록 문구(단계 단위)와 구현 단위 불일치 | 관문 집계 단위를 등록 문구대로 시험으로 고정 | 탐침 S2 병 F0(등록 §13 정정) |
| P23 | 두 유료 프로세스가 각자 합계만 보고 하드 정지를 검사 | 장부를 프로세스 메모리로 합산 | 공유 장부 파일 전체를 합산하고, 날아가는 호출은 최대 비용으로 예약 | 탐침(고침, G2부터) |
| P24 | 실험 코드가 기준 파일(`stageb_train.py`)을 고쳐 R7 기준점 카운트가 계속 0으로 | 실험 옵션을 기준 파일에 추가 | 새 기능은 새 옵션 파일로(E-TC 방식); 기준 파일 수정은 최소 + 기본 경로 바이트 동일 확인; R7은 기준점·실험 코드 절을 나눠 판정 | ded38d7 → R7 25회차 범위 분리 |
| P25 | 판정 스크립트가 입력 크기·중복을 검사하지 않음 | 방어 코드 없음 | 판정 입력의 항목 수·중복·집합(검증 1,799 등)을 스크립트가 확인 | R7 22–25 NOTE N3 |

## 다. 파드·셸·프로세스

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P26 | 파드 루트에 `/C:/Program Files/Git/data/...`가 생김 | Git Bash(MSYS)가 `/data/...` 인자를 Windows 경로로 바꿈 | Git Bash의 kubectl 호출은 항상 `MSYS_NO_PATHCONV=1`; 끝에 `/C:` 없음 확인 | R7 24회차(N41) |
| P27 | `pkill -f`가 자기 셸·`kubectl exec`까지 죽임 / 래퍼만 죽고 python은 남음 | 패턴이 명령줄 자신과 일치 | pkill 패턴은 스크립트 파일로, python은 environ(`IR_INST` 등)으로 식별해 PID로 종료; 남의 프로세스는 절대 안 건드림 | 메모리 feedback_kill_wrapper_pitfall, 움직임 확인 에이전트 보고 |
| P28 | 라벨 작업자가 시작하자마자 실패, 잠금 파일이 남음 | `IR_ROOT=cyclo`·`IR_INST` 없이 실행, 비정상 종료 뒤 잠금 | Isaac은 `ir_run.sh IR_ROOT=cyclo` + 작업자별 `IR_INST`; 재시작 전 잠금 소유 프로세스가 없는지 확인 후 제거 | 이번 세션 라벨 작업자 |
| P29 | GPU 사용률이 낮음(CPU 쿼터 조절) | OpenMP 바쁜 대기 | 모든 작업에 `OMP_WAIT_POLICY=PASSIVE`, 스레드 수 제한, `nice` | 이번 세션, [R/physx_hard_reset](../stage3/results/physx_hard_reset.md) |
| P30 | GPU 역할을 어기거나 과하게 막음 | 규칙 범위를 잘못 기억 | GPU 0·1 = Isaac 렌더, 2 = 계산만(렌더 금지), 3 = vLLM·보조 서버; 'GPU 2 금지'가 아니라 '렌더 금지'(user-log 62·64) | DL 421 |
| P31 | 파드 `/data` 밖(`/tmp`, `~/.cache`)에 파일 | 도구 기본 경로 | 캐시·tmp·모델 전부 `/data` 아래, `source /data/harvest/env.sh`(user-log 45) | DL 389 |
| P32 | 로컬 C: scratchpad에 임시 파일 | 하네스 기본 경로 | 작업 드라이브는 D:만(`D:\tools\scratch_qdd\…`); 에이전트 지시에 명시 | DL 399 |
| P33 | PowerShell에서 pytest 2개 실패 | `harvest/load/session.py`가 외부 `date` 실행 파일을 부름 | 전체 시험은 Git Bash에서 돌린다(또는 코드 수정) | R7 23회차 N31 |
| P34 | argparse가 `-inf`를 옵션으로 읽어 가드 시험이 엉뚱하게 막힘 | 음수 값 인자 | 음수·`-inf` 값은 `--opt=-inf` 형식 | R7 25회차(가드 14→28) |
| P67 | D: 여유 20 MB(60 GB 중) — 작업이 곧 멈출 뻔함(09-12 C: 사고와 같은 위험) | R7 회차·실험 에이전트마다 `D:\tools\scratch_qdd\<이름>`에 저장소 추출본·pytest 임시·파드 결과 사본을 쌓고 지우지 않음(r7c1–r7c26 등 수 GB). D:의 나머지(ipcam 31 GB·taskC_work 17.5 GB)는 사용자 자료라 손대지 않음 | 에이전트는 끝낼 때 자기 scratch의 큰 사본(추출 저장소·pytest basetemp·torch 캐시)을 지우고 요약·스크립트만 남긴다; R7은 직전 한 회차만 보관; 메인은 루프 점검 때 `Get-PSDrive D` 여유를 보고 5 GB 아래면 정리 먼저 | 2026-09-25 20:02 UTC, R7 26회차 보고(29 MB) → 옛 r7c·pytest 폴더 삭제 |
| P68 | R7 정정 커밋이 새 DOC를 만듦(26회차 정정 '지금 P0 생성 중' → 27회차 D-1; 표시가 표 칸 밖·문장 중간) | 정정 문장을 기억·추측으로 쓰고 원 문서·파드 상태와 대조하지 않음; 표 행 끝에 문자열을 붙임 | 정정에 넣는 사실은 원 문서 줄·파드 명령 출력으로 확인한 뒤 쓴다(현재형 상태는 UTC 시각과 함께); 표 행 수정은 칸 단위로(`split('\|')`), 쓴 뒤 칸 수 검사 | 2026-09-25 20:37 UTC, R7 27회차 |
| P69 | 책의 상태 꼬리표(`[진행 중]`·`[미커밋]`·'지금 … 생성 중')가 커밋 몇 개 만에 낡아 R7 DOC가 반복(27·28·29회차; 26회차 정정이 꼬리표를 만듦 — 2026-09-25 21:30 UTC, R7 29회차 N104) | 빨리 바뀌는 상태를 여러 문서(책·handoff·결과)에 적고, 결과 커밋 때 모든 사본을 고치지 못함 | **진행 상태는 handoff 한 곳에만**(UTC 시각과 함께); 책·지도는 위치·확정 결과만 적는다; 파일 존재만으로 '조립 끝' 같은 판단 금지 — 시각·내용(시드 수)까지 확인 | 2026-09-25 21:07 UTC, R7 28회차 |
| P73 | 파드 사본 CPU 시험이 13개 실패(`JSONDecodeError`, `eval/common.py` `git_commit`) | 미커밋 변경을 얹은 사본의 `CODE_VERSION`을 JSON이 아닌 글로 씀 — `git_commit`은 이 파일을 JSON으로 읽는다 | 파드 사본 `CODE_VERSION`은 JSON(`{"commit": …, "dirty": …}`, `tools/r6/sync.sh` 형식)으로; 미커밋 변경을 얹으면 `dirty: true` | 2026-09-26 00:58 UTC, [R/validator_gaps](../stage3/results/validator_gaps.md) 5절 |
| P74 | 로컬 전체 시험에서 `astra_motion` 시험 1개만 실패(`stream_time_limit`), 단독으로는 통과 | 벽시계 `time.sleep` 지연 모델로 실시간 비율을 재는 시험이라 기계 부하에 민감(P58 계열) | 전체 시험 실패는 이름을 그대로 보고하고, 바꾼 코드와 무관한지 확인(단독 재실행) 뒤 전체를 다시 돌려 그 결과를 적는다 — 무시하거나 '불안정'으로만 넘기지 않는다 | 2026-09-26 01:00 UTC, [R/validator_gaps](../stage3/results/validator_gaps.md) 5절 |

## 라. 시뮬·데이터·모델

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P35 | 같은 시드가 다른 궤적으로 재생(최대 수십 mm) | CPU PhysX 장면 내부 상태가 직전 실행 이력에 의존 | 에피소드마다 PhysX 장면 재생성(`hard_reset=True`, §78); 라벨은 `replay_maxabs == 0`만 신뢰(§80); 옛 풀은 새 물리로 재현 안 됨 | [R/pool_replay_debug](../stage3/results/pool_replay_debug.md), [R/physx_hard_reset](../stage3/results/physx_hard_reset.md) |
| P36 | 상태 쓰기로 복원하면 2.3–7.8 mm 어긋남 | 접촉 캐시가 사라짐 | 복원은 "리셋부터 재실행"(그리고 P35) | [R/pool](../stage3/results/pool.md) |
| P37 | 고유감각 속도가 미래 프레임을 읽음(누수) | `np.gradient` 중앙 차분 | 속도·파생 입력은 인과 후방 차분 v[k] = (x[k] − x[k−1])·hz, v[0] = 0; 새 판 `se2e_c1`, 옛 결과 문서에 한계 표시 | 정본 §83, 44c907d, R7 22회차 |
| P38 | 카메라 투영이 100 px 넘게 어긋남 | RB1에 머리 관절 기록 없음(고정 각 가정), 명목 카메라 모델 | 투영이 필요한 실험은 관문(G0)으로 먼저 재고, 실패하면 카메라가 정확한 시뮬로 | [R/ma1](../stage3/results/ma1.md) |
| P39 | 카메라 내부값 fx 272(102°)로 계산 | 센서 최대 화각을 모드 화각으로 착각 | 제조사 **모드별** 사양·실물 camera_info로 확인(ZED Mini VGA 85°, fx 367) | [R/e3st](../stage3/results/e3st.md) 8절, 정본 §47 |
| P40 | Isaac 카메라 `data.pos_w`·`quat_w`가 낡은 USD 값 | Isaac Lab 캐시 | 카메라 자세 = 부모 링크 실시간 자세 × 장착 변환(`mount_transform`) | [R/r1_perception](../stage3/results/r1_perception.md), 탐침 |
| P41 | 부하에서 팔이 약 1 cm 정상 지연 → 상대 명령이 누적 오차 | 측정 자세 기준으로 상대 명령을 쌓음 | 상대 명령은 **명령 기준점** 위에 쌓는다 | 탐침 |
| P42 | 그리퍼 토크가 한 틱 떨어지자 스킬이 "놓침"으로 다시 열음 | 스킬 S에 쥠 디바운스 없음 | 쥠 판정 0.15 s 디바운스(계획기와 같게) | [R/pre_r7_fixes](../stage3/results/pre_r7_fixes.md) #3 |
| P43 | 명령 TCP 기반 오라클 정답은 상한 0.73–0.92 | 명령 지연·타이머 의존 | 정답은 관측 자세로(labels_v2), 코드 규칙 상한 ≥ 0.95를 먼저 확인 | [R/e3lite](../stage3/results/e3lite.md), [R/labels_v2](../stage3/results/labels_v2.md) |
| P44 | 10 s 지평 결과 라벨은 거의 모든 보기가 동점 | 오라클 플래너가 대부분 회복 | 결과 라벨은 거부권 용도로만(§65); 적격 기준을 부적합 원천과의 일치로 두지 않는다 | [R/labeler](../stage3/results/labeler.md), DL 433 |
| P45 | 토크 기반 충돌 규칙이 명목 운동 토크와 섞여 오경보 | 관절 토크 문턱 | critic = V1h 단독, 로봇 쪽은 추종 잔차 방식으로(§64) | [R/e_m4b_meas](../stage3/results/e_m4b_meas.md) PC3 |
| P46 | vLLM 배치 불변을 켜도 서버 세션이 바뀌면 비트 동일 아님 | 캐시 적중·스케줄 차 | 결정성 주장은 "한 세션 안"으로 한정 | [R/stageA_generalize](../stage3/results/stageA_generalize.md) 5절 |
| P47 | 시험용 시드를 CAL 범위(500–549)에 둠 | 분할표 확인 안 함 | 시험 시드 3000–3199; 보호 분할(CAL·TEST·TEST-P5)은 `HARVEST_ALLOW_SPLIT` 없이는 거부 | [R/random5](../stage3/results/random5.md)(R7 2회차) |
| P48 | 이미지 3장을 주면 작은 VLM이 "잡았다"를 과대 주장(Qwen 12/14) | 보기 수·쥠 정의 부재에 따른 교란 | 무료 모델로 먼저 돌려 교란을 찾고, 쥠 정의(패드가 물체에 닫힘)를 프롬프트에 명시, 카메라 조건을 짝 비교 | `P/prereg_astra_motion.md` §12 |
| P49 | gpt-6-astra가 temperature·top_p·seed를 거부 | 모델 제약 | 답 흔들림은 설계로 줄인다(흐름 고정 요청·합의 규칙); 결정론 전제 금지 | `P/prereg_astra_motion.md` 수정 1 |
| P50 | RB1 데이터 라이선스 미표기 | 데이터 카드 없음 | RB1 체크포인트는 내부용, 논문 전 확인(§63 (6)) | [R/se2e_data](../stage3/results/se2e_data.md) |

## 마. 조사·논문

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P51 | "선행 없음"이라 적었는데 선행이 5편 넘게 있었음 | 요약 에이전트의 "못 찾음"을 부재 증거로 씀 | 부재 주장은 전용 검색(검색어 기록) 뒤에만; 가장 가까운 선행은 본문·부록·코드까지 | DL 166·241·320 |
| P52 | 학회 표기 오류 | 서지 DB(S2) 신뢰 | 학회 공식 페이지(OpenReview·CVF·Anthology)로 확인, 1.5년·신뢰도 규칙 | DL 291 |
| P53 | 우리 제안이 원문 설명에 섞임 | 표 한 칸에 둘 다 씀 | 원문 칸과 우리 접목안 칸을 나눈다 | DL 233 |
| P54 | 에이전트 보고를 요약하며 수치·설정 목록을 빠뜨림 | 요약 | 보고는 도착 즉시 원문 저장, 요약은 링크와 함께 | DL 322·328 |
| P55 | 논문 수치가 원문 조건을 잃음("7배") | 조건 생략 | 수치 인용 때 조건(무엇 기준, 설정)을 같이 | DL 180·300 |
| P56 | 파이프라인 시험 수치를 결과처럼 보고 | 성격 표시 누락 | 첫 줄에 "파이프라인 시험(§56)"과 "본 실험" 구분; 논문은 '예정/소규모' 표시, 숫자 날조 금지(user-log 88) | 정본 §56, DL 414 |

## 바. 탐침에서 (E-Astra-motion, 2026-09-25)

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P57 | Isaac 프로세스를 SIGTERM으로 죽였는데 50분 뒤에도 GPU 1에 살아 있음(두 번째 Isaac 프로세스가 됨) | 종료 처리 중 멈춤 | 종료 뒤 `ps`로 확인하고, 자기 python PID(명령줄로 식별)에 SIGKILL | 탐침 sanity(R/astra_motion 1절) |
| P58 | 실시간 맞춤 실행에서 시뮬이 실시간의 0.38배로 떨어져 답 나이(시뮬 초)가 작게 나옴 | 같은 파드 CPU 부하(다른 Isaac·유료 프로세스 동시) | 편마다 실효 실시간 비율(RTF)을 기록하고, 지연은 벽시계로도 잰다; RTF < 0.8 편은 표시 | R/astra_motion 5·6절 |
| P59 | 지연 0인 참값(흉내) 모델이 직렬 흐름에서 30호출을 2 s 안에 다 씀 | 시험 모델에 지연이 없음 | 흐름·실시간 스모크는 모델 지연을 흉내 낸다(`--truth-latency 4`) | 탐침 스모크 4·5 |
| P60 | 부드러운 실행기에서 같은 집기 요청이 새 답마다 창을 늘려 영영 실행 안 됨 | 새 답이 마감을 다시 설정 | 같은 집기 요청은 첫 마감 유지, keep은 대기 중 집기를 지우지 않음(시험) | 탐침 가짜 세계 시험 |
| P61 | "놓친 파지(miss)"로 만든 스냅샷이 실제로는 쥠(테를 한 손가락으로 걸어 듦) | 만든 방법 이름을 정답으로 믿음 | 정답은 시뮬 실측(holding)으로, 유료 호출 전에 정답 분포를 확인해 적는다 | 탐침 G1(쥠 25/안 쥠 15) |
| P62 | 모델이 한도 바로 위(5.1 cm, 한도 5 cm)를 내서 무효 → 흐름 방식에선 다시 묻기가 없어 호출 낭비 | 엄격한 한도 + 한도에 붙는 답 | 한도 근처 값을 어떻게 다룰지(잘라 쓰고 기록 / 거부)를 등록 때 정하고, 프롬프트 한도에 여유를 둔다 | 탐침 S2 병 F0 |

## 사. E-MA1·E-MA1b에서 (2026-09-25)

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P63 | R2_TRAIN을 "과제 폴더만 있고 행 조립 전"이라고 잘못 적음 [→ 정정 2026-09-25 21:07 UTC, R7 28회차 D-1: 실제로 틀린 것은 '과제 폴더만'뿐 — 행 파일은 파일럿 병합분이라 전체 기준 '행 조립 전'은 맞았다; 이 함정의 정정(ma1.md)이 오히려 새 오류였다] | 한 셸 명령에서 여러 `ls` 출력이 섞여 하위 폴더 내용(`dr/<과제>/P0.stageb.jsonl`)을 윗 폴더 것으로 읽음 | 상태 판단은 경로가 붙은 출력(`find … -maxdepth 3`, `wc -l <경로>`)으로 하고, 판단 근거 명령을 결과 문서에 적는다 | [R/ma1](../stage3/results/ma1.md) 1절 정정(19:55 UTC) |
| P64 | 전체 시험 요약 줄(통과·건너뜀 수)이 안 나옴 | `pytest.ini` addopts에 `-q`가 있는데 명령에 `-q`를 또 줘 요약까지 숨김 | 시험 수를 보고할 때는 `-o addopts=""`로 basetemp만 다시 주거나 `-q`를 빼고, 요약 줄을 그대로 옮긴다 | E-MA1b(1,165 통과·10 건너뜀) |
| P65 | 카메라 투영 검사의 기준점(VLM 포인팅)이 12 px 문턱보다 거칠다 | Molmo2-ER이 다른 팔(14/119)·안 보이는 곳을 가리킴 | VLM 포인팅을 기준으로 쓰기 전에 기준점 자체 오차를 소수 프레임으로 먼저 재고 문턱을 그에 맞춘다; 투영이 필요 없는 설계(로봇 기준 좌표)를 먼저 검토한다 | E-MA1 G0 → E-MA1b |
| P66 | 보조 목표가 결정 라벨과 같은 정보(미래 손끝 변위)면 더 촘촘해도 결정 정확도가 오르지 않았다(+0.003) | 새 정보 없음 | 보조 목표를 고를 때 "라벨에 없는 정보인가"를 먼저 묻는다(§84 보충 1 해석 주의가 맞았음) | [R/ma1b](../stage3/results/ma1b.md) |

## 아. E-CAM3·E-MA3에서 (2026-09-25)

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P70 | 정리 명령 `kubectl exec … bash -c '…; pkill -f "nvidia-smi -i 2,3 --query-gpu" …'`가 exit 143으로 자기 셸을 죽임(P27 재발) | `pkill -f` 패턴이 그 명령을 담은 `bash -c` 명령줄 자신과 일치 | 프로세스 정리는 항상 스크립트 파일로: `/proc/<pid>/cmdline`을 정확히 비교해 그 PID만 `kill`(`stop_gpumon.sh` 방식); 한 줄 명령에 `pkill -f`를 쓰지 않는다; 정리 뒤 `ps`로 남의 작업(Isaac 등)이 그대로인지 확인 | E-CAM3·E-MA3 종료 정리(22:15 UTC; R2_TRAIN 작업자는 21:56에 이미 끝나 있었음 — 파일 시각으로 확인) |
| P71 | KI stop인데 expert만 바꾼 판에서 결정 정확도가 +0.006 [+0.0009, +0.011] 달라짐 | 기울기 자르기(`clip_grad_norm_` 1.0)가 expert와 백본 LoRA의 **전체 노름**으로 계수를 정해 expert 기울기가 LoRA 갱신 크기를 바꾼다 | expert만 바꾸는 실험도 결정 지표를 "불변"으로 가정하지 말고 비열등 조건으로 재고(E-MA3 등록 방식), 결정 변화를 expert 개선의 근거로 쓰지 않는다 | [R/ma3](../stage3/results/ma3.md) 2절 |
| P72 | '기록만' 커밋이 다른 에이전트의 미커밋 편집(handoff §0 줄)을 함께 올려, 그 줄이 인용한 아직 없는 문서가 커밋에 빠짐(R7 34회차 D-1) | 기록 스크립트가 작업 트리의 파일 전체를 읽어 고친 뒤 파일 단위로 add — 남의 덩어리가 같이 들어감(P14 재발) | 커밋 전 `git diff <파일>`로 내 덩어리만 있는지 확인; 남의 편집이 있으면 그 에이전트에게 먼저 묻고 함께 커밋할지 정한다; 인용한 경로가 커밋에 있는지 기계 검사 | 2026-09-25 23:30 UTC, R7 34회차 |

## 자. E-MA2에서 (2026-09-26)

| # | 증상 | 원인 | 예방 규칙 | 발생 |
|---|---|---|---|---|
| P75 | 명령 문장을 읽은 결정층은 돌린 명령의 칸을 97 % 고르는데, 그 결정을 조건으로 준 expert 청크는 90°에서 28 %·180°에서 9 %만 명령 쪽으로 움직임(C0 우연 수준 13 %·7 %) | 학습 데이터(오라클 계획기)에서 결정·영상·궤적이 늘 같은 쪽이라 expert가 영상·고유감각이 정한 궤적을 내고 결정 임베딩을 거의 쓰지 않음(KI stop) | '결정이 따르면 행동도 따른다'고 가정하지 않는다: 조이스틱·명령 준수는 청크 수준(FK 손끝 변위 방향)으로 잰다; 행동을 조종하려면 결정과 궤적이 어긋나는 시연(경로 다양화)이나 코드 편향(§11)을 쓴다 | [R/ma2](../stage3/results/ma2.md) 2절 (E-SR0 설계 참고) |
| P76 | 로컬 기다림 루프 `until kubectl exec … bash -c '… ! pgrep -f "logs/ma2/dry.sh"'`가 대상이 죽어도 끝나지 않을 수 있었음(실행 뒤 발견해 멈춤) | `pgrep -f` 패턴이 그 검사를 담은 `bash -c` 명령줄 자신과 일치(P27·P70의 기다림 루프판) | 살아 있는지 검사는 argv를 정확히 비교하는 스크립트(`ps -eo args=`에서 `$1=="/bin/bash" && $2=="./run.sh"`)나 완료·실패 표지 파일(`*.done`, `EXIT $?` 줄)로; 기다림 조건에는 실패 신호(Traceback)도 넣는다 | 2026-09-25 23:19 UTC, E-MA2 사전 실행 |
| P77 | 머리 영상에 그린 명령 화살표(자홍)가 mug_marker 과제의 놓을 표지(자홍 원판)와 같은 색 | 장면 물체 색을 확인하지 않고 덧그림 색을 고름 | 덧그림을 쓰기 전에 과제·변형별 표본 영상에 그려 눈으로 확인하고(표본 그림 한 장), 장면에 없는 색을 고른다(E-MA2는 청록으로 바꿈) | 2026-09-25 23:17 UTC, E-MA2 투영 확인 그림 |
