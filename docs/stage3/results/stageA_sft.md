> **성격(정본 §56, user-log 56): 파이프라인 확인용 소규모 시험이다. 아래 수치는 논문 결과로 쓰지 않는다**(입력 좌표 = 시뮬 참값, 과제 1개, 120편, 무작위화 없음).

# 단계 A SFT 본 학습 (Jev-L typed 선택기, 새 풀) — 정본 §51–§54

## 사전 등록 (학습·DEV 채점 전 고정, 2026-09-24T15:51:46Z 파드 `date -u`)

작업 지시문 요지(뜻을 바꾸지 않음) + 코드에 옮기는 방법. 데이터를 본 뒤 바꾸지 않는다. 바꾸면 "사후 변경"에 두 판 모두 적는다.
(이 시각 전에 한 일: Step 1 풀 labels_v2 생성·게이트(결과 1절). 학습·DEV 모델 호출은 아직 없음.)

### 데이터·정답
- **학습** = 풀(`/data/harvest/data/pool`, 120편, 시드 2000–2119, 15:09 UTC 재생성판) 중 `split=fit` 60편의 결정 스냅샷(`decision=true`, 600개) × 5질문 = **3,000항목**. **조기 종료 검증** = `split=eval` 60편 결정 스냅샷 600 × 5 = **3,000항목 전부**(부분집합 없음).
- 정답 = labels_v2(§54 관측 정의), 파일 `/data/harvest/data/pool.labels_v2.jsonl`(풀 폴더 한 단계 위 — `ep*.jsonl` 글롭 무관). 풀의 `oracle` 필드는 읽는 순간 버린다(라벨 도구·학습 항목 모두). `progress`는 정답 원천이 없어 **학습하지 않는다**.
- 입력 = 추론과 같은 프롬프트: `jevl.SYSTEM` + [머리캠 JPEG, `jevl.question_text`(DecCall, S1 상태 **1 mm 격자** `step_cm=0.1`)], 보기 순서 고정(순환 없음), NONE_ESCALATE 마지막.
- **최종 시험(이 단계)** = DEV 0–29 × P0–P2 `jsel_dev` 스냅샷 중 `oracle ≠ null` 전부(E3-lite·labels_v2 게이트와 같은 2,387개; 학습에 한 번도 안 쓰임) × 게이트 5질문 = 11,935항목, 정답 = `jsel_dev/P*.labels_v2.jsonl`. CAL 500–549 · TEST 1000–1149 · TEST-P5 1300–1329는 읽지도 만들지도 않는다.

### 학습 하이퍼파라미터 (§52 그대로)
Qwen3-VL-4B-Instruct@ebb281ec BF16, LoRA r32 α64 dropout 0.05(LLM 36층 × q,k,v,o,gate,up,down; 비전·병합기·lm_head 동결), 손실 = 보기 집합 안 재정규화 NLL(`stagea_loss`), AdamW lr 1e-4 wd 0, cosine, 워밍업 3 %(= ceil(0.03 × 총 스텝)), 기울기 자르기 1.0, 유효 배치 64항목(`--accum 64`), **최대 2 에폭**(3,000/64 → 47 스텝/에폭, 총 94 스텝), 시드 0. 조기 종료: **12 스텝마다**(에폭당 약 4회) + 마지막 스텝에 검증 NLL, 좋아지면 `best/` 저장, 3회 연속 나빠지면 멈춤. **시험에 쓰는 모델 = `best/`**(검증 NLL 최저). GPU 1 한 장, `venv_train`, `TORCH_DISABLE_NATIVE_JIT=1`.

### 병합·서빙
`tools/stagea_merge.py --check`(fp32 병합 → BF16 저장, 기본 모델의 처리기·템플릿 그대로). 서빙 = `venv_vllm` vLLM 0.30.0, **GPU 3**, `VLLM_BATCH_INVARIANT=1`, `jsel/serve.sh`와 같은 옵션(메모리 0.85, prefix caching, raw_logprobs, seed 0). 영점 기준 모델도 같은 스크립트·옵션으로 GPU 3에서 따로 띄운다(동시에 두 서버 띄우지 않음).

### 주 지표 A와 채점
- 호출 = `tools/jevl_e3lite.py acc --conds S1:img:fixed --step-cm 0.1`(E3-lite와 같은 채점 코드 경로 `deccall_snap.score`, 동시 8, 재시도 3회 뒤 실패 = 오답), 영점 Qwen3-VL-4B와 SFT 병합 모델에 **같은 프롬프트**.
- 채점 = `tools/stagea_sft_report.py`: 행의 option_key(보기 집합 안 재정규화 확률 argmax, 동률 = 앞 보기)를 labels_v2와 비교. argmax NONE_ESCALATE = 오답.
- **A** = 게이트 5질문(dir_xy, dir_z, mag_coarse, target, phase) 11,935항목 합친 정답률. 짝 차이 = 항목별 정답 차의 평균, 판(kind, seed) 90개 단위 부트스트랩 10,000회(`cluster_mean_ci`, seed 0), 95 % 백분위.
- **최빈 기준선** = 질문마다 DEV labels_v2 최빈 보기를 늘 고르는 규칙(같은 항목, 같은 판 부트스트랩).
- 부 지표: 질문별 정답률(+CI), 섭동별 A, ECE(15구간)·AUROC(p_chosen → 정답; **보정 전**), NONE_ESCALATE 확률 평균·argmax 비율, 질문별 선택 분포. progress = 학습 안 함 → **표류만 보고**(영점 대 SFT argmax 일치율, 선택 분포, 최대 |Δp| 중앙·95 %, 옛 오라클 대비 정답률 참고값).

### 합격 기준 (지금 고정)
- **(a)** A_SFT − A_zero 짝 차이 95 % CI 하한 > 0.
- **(b)** A_SFT − 최빈 기준선 짝 차이 95 % CI 하한 > 0 (A_SFT가 최빈 기준선보다 크고 구간이 기준선 위). 참고로 "A_SFT CI 하한 > 기준선 점추정"도 적는다.
- **(c)** 병합 BF16 모델을 vLLM(BI 켬)으로 서빙해 `tools/jevl_bench.py l1`(jevl_model_select와 같은 L1 절차: S1 크기 DecCall, 이미지 = `/data/harvest/jevl/head_task0001_episode_000000.png`, N마다 워커당 10호출 버림, 200호출, 검열 2 s) **text+image N=4 p95 ≤ 0.33 s**, 그리고 `l2` **결정성 flip 0/20**(20 고정 페이로드 × 10 반복, 배경 N=4 부하). 측정 동안 파드 load(1분)를 시작·끝에 기록한다. 같은 세션에서 영점 기본 모델 L1도 참고로 잰다(판정 밖). 참고: 실제 DEV S1-1 mm 프롬프트 지연(`jevl_e3lite.py lat --states S1 --step-cm 0.1`, 판정 밖).
- **(d)** 질문별: SFT 정답률 점추정이 그 질문의 최빈 기준선보다 낮은 게이트 질문이 **없어야** 한다 — 낮은 질문을 이름으로 적는다(질문별 짝 CI도 함께).
- (a) 또는 (b)가 실패하면 "SFT insufficient"(방향 수준)로 보고하고, DEV를 보고 하이퍼파라미터를 고치지 않는다.

### 기타 고정
- 한 번만 학습한다(시드 0). DEV 결과를 본 뒤 재학습·재선택 없음. `best/` 대신 `last/`로 바꾸지 않는다.
- 파드 규칙: 모든 파일 `/data/harvest` 아래, 코드 사본 `/data/harvest/code_sftA`, 산출 `/data/harvest/ckpt/stageA/sftA_pool_v1/`, DEV 호출 결과 `/data/harvest/stageA_sft/`. GPU 1(학습·병합)·GPU 3(vLLM)만, GPU 0·2 사용 안 함.

---

## 결과 (2026-09-24 17:05Z 작성)

### 요약 — 판정(사전 기준 그대로)
| 기준 | 결과 | 판정 |
|---|---|---|
| (a) A_SFT − A_zero 하한 > 0 | **+0.5041 [+0.4977, +0.5106]** | **합격** |
| (b) A_SFT − 최빈 하한 > 0 | **+0.2892 [+0.2848, +0.2936]** (참고: A_SFT 하한 0.9259 > 최빈 0.6405) | **합격** |
| (c) 병합 BF16 vLLM text+image N=4 p95 ≤ 0.33 s, flip 0/20 | p95 **0.307 s**(p50 0.257), flip **0/20**(0/80 질문, max \|Δp\| 0, 200/200 성공) | **합격** |
| (d) 최빈 기준선 아래 게이트 질문 없음 | 없음(가장 작은 여유: mag_coarse 0.730 대 0.604) | **합격** |

→ 단계 A SFT는 사전 등록 기준 4개를 모두 통과했다. "SFT insufficient" 아님. DEV를 보고 바꾼 설정 없음(한 번 학습, `best/`).

### 1. Step 1 — 풀 labels_v2 (CPU 1프로세스, nice 10, 사전 등록 전 실행)
- 파일 `/data/harvest/data/pool.labels_v2.jsonl`: 풀 120편 전 줄 **3,361행**(결정 스냅샷 1,200 = fit 600 · eval 600, 편 fit 60 · eval 60). 풀 `oracle` 필드는 읽을 때 버림 → `progress` 없음. 도구 `tools/labels_v2_eval.py --pool`(새 모드).
- 게이트(S1 텍스트 1 mm 코드 규칙, 질문별 ≥ 0.95):

| 대상 | n | dir_xy | dir_z | mag_coarse | target | phase | (소단계 M) |
|---|---|---|---|---|---|---|---|
| 풀 전 줄 | 3,361 | 0.9955 | 0.9902 | 0.9878 | 1.0000 | 0.9988 | 0.9991 |
| 결정 스냅샷 | 1,200 | 0.9967 | 0.9908 | 0.9858 | 1.0000 | 0.9992 | 0.9983 |

전부 통과(DEV 판 0.9954/0.9908/0.9883/1.0/0.9992와 같은 수준). 잔차 원인: dir_xy 데드밴드 15, dir_z 데드밴드 30 · ALIGN 3, mag 구간 경계 38 · ALIGN 3, phase 도달 판정 4 — "그 밖" 0건.
- 분포(최빈 기준선): 전 줄 dir_xy none_xy 0.743 · dir_z down 0.503 · mag xlarge 0.583 · target o3 0.583 · phase continue 0.719(5질문 평균 0.626). 학습(fit 결정) 평균 0.618, 검증(eval 결정) 0.645. hold 정답은 fit 6 · eval 1개뿐.

### 2. 학습 (GPU 1, 15:52:49–16:51:49Z(config 기록 → done), 학습 루프 벽시계 54.7분)
- 항목 학습 3,000 / 검증 3,000(전부), 총 94 스텝(47/에폭 × 2), 조기 종료 발동 없음 — 검증 NLL이 매 평가 개선, **best = 스텝 94**(84와 0.15682 대 0.15682로 사실상 같음). 최대 GPU 메모리 27.3 GB, 약 2.0 항목/s(평가 포함).

| 스텝 | 0 | 12 | 24 | 36 | 48 | 60 | 72 | 84 | 94 |
|---|---|---|---|---|---|---|---|---|---|
| 검증 NLL | 4.367 | 0.617 | 0.310 | 0.287 | 0.259 | 0.182 | 0.163 | 0.157 | **0.157** |
| 검증 정확도 | 0.421 | 0.692 | 0.896 | 0.899 | 0.905 | 0.929 | 0.936 | 0.937 | 0.936 |

질문별 검증 NLL(0 → 94): dir_xy 7.71 → 0.013, dir_z 5.05 → 0.170, mag 5.08 → 0.582, target 3.32 → 0.0001, phase 0.68 → 0.019. 학습 손실 구간 평균: 1–6 2.95, 7–12 0.82, 13–24 0.43, 25–36 0.28, 37–47 0.37, 48–60 0.25, 61–72 0.19, 73–84 0.17, 85–94 0.18.
- 병합: `merged/`(8.3 GB), 확인 로짓 최대차 0.958 < BF16 반올림 바닥 1.132. vLLM 이어쓰기 재토큰화 sanity 4/4 OK.

### 3. DEV 시험 (2,387 스냅샷 × 5 = 11,935항목, 판 90개 부트스트랩, 호출 오류 0)

| | A (5질문) [95 % CI] | ECE | AUROC | p(NE) 평균 | NE argmax |
|---|---|---|---|---|---|
| 영점 Qwen3-VL-4B (S1 1 mm, 머리캠) | 0.4256 [0.4198, 0.4312] | 0.435 | 0.703 | 5.1e-8 | 0 |
| **SFT (병합 BF16, vLLM)** | **0.9297 [0.9259, 0.9334]** | **0.013** | **0.954** | 7.3e-5 | 0 |
| 최빈 기준선(DEV labels_v2) | 0.6405 [0.6366, 0.6446] | | | | |

질문별(정답률 [CI]; ECE·AUROC 영점 → SFT):

| 질문 | 영점 | SFT | 최빈 | SFT − 최빈 | ECE | AUROC |
|---|---|---|---|---|---|---|
| dir_xy | 0.139 | **0.992** [0.988, 0.995] | 0.742 | +0.250 [+0.241, +0.260] | 0.627 → 0.006 | 0.651 → 0.950 |
| dir_z | 0.496 | **0.939** [0.930, 0.948] | 0.496 | +0.443 [+0.432, +0.453] | 0.499 → 0.013 | 0.783 → 0.901 |
| mag_coarse | 0.122 | **0.730** [0.716, 0.744] | 0.604 | +0.126 [+0.113, +0.140] | 0.615 → 0.062 | 0.491 → 0.836 |
| target | 0.620 | **1.000** [1.000, 1.000] | 0.617 | +0.383 [+0.374, +0.391] | 0.368 → 0.0002 | 0.794 → — (오답 0) |
| phase | 0.751 | **0.987** [0.982, 0.992] | 0.744 | +0.244 [+0.237, +0.250] | 0.101 → 0.009 | 0.580 → 0.936 |

섭동별 A(영점 / SFT): P0 0.422 / 0.933, P1 0.429 / 0.919, P2 0.426 / 0.937.

선택 분포: 영점 쏠림이 사라짐 — dir_xy 영점 plus_x_plus_y 80 % → SFT 분포 ≈ 정답 분포(none_xy 1,768 대 정답 1,771), dir_z 영점 down 99.9 % → down 1,209 · up 701 · none_z 477(정답 1,185 · 748 · 454), target 영점 o3 99.7 % → 정답과 완전히 같음.
- **mag_coarse가 남은 약점**: 0.730(코드 규칙 상한 0.988보다 크게 낮음). SFT는 xlarge를 과다 선택(1,726 대 정답 1,441), medium(77 대 246)·small(52 대 129)을 덜 고른다 — 검증 NLL도 0.58로 가장 높았다.
- **phase `hold`**: 정답 19개(DEV)인데 SFT는 한 번도 고르지 않음(학습 정답 6개뿐).
- **NONE_ESCALATE**: p(NE) 평균 7e-5, argmax 0 → 예상대로 사실상 소멸(§52 결정 1: J5 집합 크기를 상위 호출 신호로 쓸 것).

### 4. progress 표류 (학습 안 함, 판정 밖)
- 영점 대 SFT argmax 일치 **0.929**. 선택: 영점 valid_progress 2,358 · failure 17 · allowed_change 12 → SFT valid_progress 2,232 · **failure 124** · recovering 19 · allowed_change 12.
- 보기 확률 최대 |Δp| 중앙 0.0006, 95 % **0.783** — 대부분 그대로지만 약 5–7 % 스냅샷에서 크게 움직인다. 옛 오라클 대비 정답률(참고) 0.914 → 0.861. → M7 progress 게이트·보정은 SFT 모델로 다시 재야 한다.

### 5. 지연·결정성 (GPU 3, BI 켬, 이미지 = ROBOTIS PNG, 200호출/조건)

| 모델 | 모드 | N=1 p50/p95 | N=2 | N=4 | N=8 | 실패 |
|---|---|---|---|---|---|---|
| **SFT 병합** | text | 0.041 / 0.049 | 0.065 / 0.076 | 0.102 / 0.113 | 0.134 / 0.199 | 0 |
| **SFT 병합** | text+image | 0.076 / 0.087 | 0.135 / 0.162 | 0.257 / **0.307** | 0.314 / 0.505 | 0 |
| 영점(참고, 같은 세션) | text | 0.041 / 0.046 | 0.065 / 0.072 | 0.099 / 0.111 | 0.130 / 0.186 | 0 |
| 영점(참고) | text+image | 0.074 / 0.078 | 0.129 / 0.154 | 0.248 / 0.273 | 0.379 / 0.500 | 0 |

- 파드 load(1분): SFT L1 시작 42.0 → 끝 34.0(다른 에이전트 라벨러·자체 점검 Isaac 6개 동시 실행), 영점 L1 28.6 → 47.6. 두 모델은 같은 구조라 차이는 부하 차이로 본다.
- 결정성 L2(SFT): 200/200 성공, **flip 0/20 페이로드, 0/80 질문, max |Δp| = 0**.
- 참고(판정 밖): 실제 DEV S1-1 mm 프롬프트(입력 토큰 중앙 824) text+image N=4 p95 — SFT **0.347 s**(load 25–29), 영점 0.303 s(load 56–61); text만 0.188 / 0.158 s. 실제 1 mm 프롬프트는 합성 S1 페이로드보다 길어 **0.33 s 문턱 근처**다(판정은 사전 등록대로 L1).

### 6. 해석 (판정 밖)
- labels_v2는 S1 1 mm 텍스트만으로 거의 결정되는 정답이라(코드 규칙 상한 ≥ 0.986), 이 결과는 "모델이 상태 텍스트의 좌표를 규칙대로 읽도록 배웠다"는 뜻이지 이미지 이해가 늘었다는 뜻이 아니다. 이미지 유무 판정(§53 결정 4)은 따로 재야 한다.
- 시험은 DEV(같은 장면·P0–P2 섭동)이고 풀과 분포가 같다 — random 5축 일반화(RD)는 재지 않았다.
- mag(0.73)는 규칙 상한과 0.26 차이 → 데이터(fit 600 스냅샷) 또는 반올림된 거리의 로그 구간 경계 학습 문제. DEV로 조정하지 않았다.

### 사후 변경
- 없음. (참고: best 스텝이 마지막 스텝 94라 조기 종료는 발동하지 않았다 — 사전 등록 절차 그대로.)

### 파일
- 로컬(D:\qdd, 커밋 안 함): `tools/labels_v2_eval.py`(풀 모드 `--pool`, `pool_lines`·`pool_label_rows`·`write_rows`), `tools/jevl_e3lite.py`(`--step-cm`, 결과 파일 이름 `_g0.1`), 새 `tools/stagea_sft_report.py`, 테스트 `tests/test_labels_v2.py`(+1), 새 `tests/test_stagea_sft_report.py`(2). 전체 **319 passed, 2 skipped**.
- 파드(`/data/harvest`): 코드 사본 `code_sftA/`; 라벨 `data/pool.labels_v2.jsonl`, 게이트 `labels_v2/pool_step01.json`(+`_mismatch.jsonl`); 학습 `ckpt/stageA/sftA_pool_v1/`(`config.json`, `log.jsonl`, `best/`, `last/`, `merged/`), `ckpt/stageA/run_sftA.sh`, `ckpt/stageA/_sftA_logs/train.log`; DEV·지연 `stageA_sft/`(`prereg_stageA_sft.md` sha256 9432b73c…, `acc_Qwen3-VL-4B-Instruct_S1_g0.1_img_fixed.jsonl`, `acc_jevl-sftA_S1_g0.1_img_fixed.jsonl`, `report.json`, `lat_sft.jsonl`, `lat_base.jsonl`, `devlat_*.jsonl`, `latsum.py`, `serve3.sh`, `post.sh`, 로그).
- `/data` 밖: 새 파일 없음(`/dev/shm` 디렉터리 시각만 바뀜 — vLLM 공유 메모리, 남은 파일 없음). vLLM 서버·학습 프로세스 모두 종료, GPU 1·3 비움. GPU 0·2는 건드리지 않았다.
