# E-CONF: VLA 결정 확신도로 모델 자신의 큰 오류를 미리 아는가 (새 표본 검증 + 확신도 개선 후보) — 결과: NONE (R2-새는 통과, S-E2E 실데이터에서 실패; 개선 후보 채택 없음)

작성 E-CONF 에이전트, 2026-09-26 UTC. 사전 등록 `docs/stage3/prereg_conf.md`(문서 시각 2026-09-26T07:18:17Z, 커밋 `6191369` — 새 표본 계산 07:19:44Z보다 먼저) + **변경 1**(`f37f8f8`·기록 `393a184`, 09:40Z, 판정 전: S-E2E G1 근접 동점 → 묶음 1 재추출) 규칙을 그대로 적용했다. 판정 스크립트 `tools/conf/conf_verdict.py`(등록 때 고정, 결과 뒤 수정 없음). 파드 고정 사본 `/data/harvest/code_conf` = `d225a84` LF archive + 등록 커밋의 `tools/conf/*`. 학습 없음, 유료 API 0. 정본 §95(user-log 106)의 '검증 먼저' 단계.

## 1. 판정

**Part V(확신도 `base` = −min_q(1등 − 2등 로그확률), 두 체크포인트 합동, 편 군집 부트스트랩 10,000회)**

| 데이터 | n (편) | 큰 오류 비율 | AUROC 합동 [95 %] (체크포인트별) | 90 % 재현율 호출 감소 [95 %] | 고정 문턱 이전 재현율 / 호출률 | 지연 p95 | Part V |
|---|---|---|---|---|---|---|---|
| **R2-새**(C0·C1, 학습된 적 없는 R2_TRAIN 실패 편) | 8,208 (244) | 0.386 | **0.806** [0.796, 0.817] (0.808 / 0.804) | **0.379** [0.369, 0.392] | 0.931 / 0.648 | 0.007 ms | ✓ |
| **S-E2E**(motion_s1·s2, 실물 검증 분할) | 1,799 (69) | 0.616 | **0.748** [0.724, 0.771] (0.742 / 0.754) | **0.244** [0.213, 0.258] | 0.908 / 0.763 | 0.005 ms | ✗ (AUROC < 0.75, 감소 < 0.30) |
| 문턱 | | | ≥ 0.75 | ≥ 0.30 | ≥ 0.85 | ≤ 5 ms | |

**Part I(개선 후보, `base` 대비 합동 AUROC 차, Holm 한쪽 하한)** — 두 데이터 모두 +0.03·하한 > 0·추가 지연 ≤ 5 ms를 넘은 후보 **없음**:

| 점수 | R2-새 AUROC [95 %] | R2-새 Δ [Holm 하한] | S-E2E AUROC [95 %] | S-E2E Δ [Holm 하한] | 추가 지연 p95 | Part I |
|---|---|---|---|---|---|---|
| `base` | 0.806 [0.796, 0.817] | — | 0.748 [0.724, 0.771] | — | 0.007 ms | — |
| `temp_min`(질문별 온도로 나눈 최솟값) | 0.806 | +0.000 [−0.000] | 0.748 | −0.000 [−0.002] | 0.005 ms | ✗ |
| `temp_joint`(보정된 '모든 결정이 맞을' 확률의 −log) | **0.848** [0.839, 0.856] | **+0.041** [+0.034] ✓ | **0.772** [0.748, 0.796] | +0.024 [+0.009] ✓ | 0.020 ms | ✗ (S-E2E +0.024 < +0.03) |
| `lr`(질문별 margin 로지스틱 결합) | 0.831 | +0.024 [+0.015] ✓ | 0.759 | +0.012 [+0.001] ✓ | 0.022 ms | ✗ |
| `flow`(흐름 청크 1 + 4 표본 분산) | 0.620 | −0.186 | 0.581 | −0.166 | 59.7 ms | ✗ |
| `lr_flow`(`lr` + 분산) | 0.848 | +0.042 [+0.030] ✓ | 0.766 | +0.018 [+0.006] ✓ | 59.7 ms | ✗ (S-E2E·지연) |
| `conf_set`(등각 집합 크기 합, α 0.1) | 0.456 | −0.350 | 0.741 | −0.006 | 0.020 ms | ✗ |

→ **판정 NONE**(등록 5절: Part I 통과 후보 없음 → `base`, `base`는 R2-새만 통과). 등록 0절의 결정대로 **확신도는 §95 (a) 흐름 우선 표시·(b) 보수 모드의 트리거로 아직 쓰지 않고, '왜 그 선택인가' 기록(질문별 1·2등과 차)으로만 둔다.** 새 표본에서도 R2 쪽 판별력은 E-NOV0 판정 밖 값(0.804)과 같게 재현됐지만(0.806, 보정 분할 C0 0.8036 = E-NOV0 재현), 실데이터 S-E2E에서는 문턱 경계 바로 아래(0.748, 구간이 0.75를 가로지름)이고 큰 오류 비율이 0.62로 높아 90 % 재현율을 지키면 호출을 24 %만 줄인다.

## 2. 판정 밖 값

**질문별 기여(`base`)** — 최솟값을 낸 질문 비율(전체 / 큰 오류 / 고정 문턱 표시), 질문 하나만의 AUROC, 그 질문을 뺀 최솟값 AUROC:

| 데이터 | 질문 | 틀림율 | 최솟값 질문 비율 | 단독 AUROC | 빼면 AUROC |
|---|---|---|---|---|---|
| R2-새 | `mag_coarse` | **0.244** | **0.629** / 0.702 / 0.712 | 0.754 | 0.775 |
| R2-새 | `dir_xy` | 0.071 | 0.181 / 0.123 / 0.123 | 0.657 | 0.798 |
| R2-새 | `phase` | 0.106 | 0.114 / 0.076 / 0.080 | 0.685 | 0.805 |
| R2-새 | `dir_z` | 0.069 | 0.077 / 0.099 / 0.085 | 0.812 | 0.798 |
| R2-새 | `target` | 0.000 | 0.000 / 0.000 / 0.000 | 0.715 | 0.806 |
| S-E2E | `mag_coarse` | **0.348** | **0.579** / 0.498 / 0.499 | 0.706 | 0.749 |
| S-E2E | `dir_xy` | 0.344 | 0.270 / 0.328 / 0.319 | 0.737 | 0.723 |
| S-E2E | `dir_z` | 0.186 | 0.151 / 0.174 / 0.182 | 0.701 | 0.742 |

- 최솟값의 60 % 안팎이 `mag_coarse`(문구와 라벨 뜻이 다른 질문, 프롬프트 검진 F16)에서 나온다. S-E2E에서는 `mag_coarse`를 빼도 AUROC가 그대로(0.749)라 그 질문의 확신도는 오류 예측에 거의 보태지 않는다 — 가장 자주 '헷갈림'을 내는 질문이 실데이터에서는 정보가 없는 셈이다(해석).
- **ECE(15개 동일 질량 구간)**: 결정 수준 원 확률 R2-새 0.028 → 온도 0.025, S-E2E 0.016 → 0.013(질문별 가장 나쁜 곳 R2 `phase` 0.074, `mag_coarse` 0.052 — 온도로 안 줄어듦). 큰 오류 확률: `base` Platt 0.069·`lr` 0.066·`lr_flow` 0.049(R2), 0.033·0.024·0.018(S-E2E). 결정 확률 자체는 이미 잘 보정돼 있고(원 ECE < 0.03), 온도는 판별력을 바꾸지 않는다(`temp_min` Δ 0.000).
- **민감도**: R2 배치도 새로운 212편(7,116스냅샷) 0.809; 평가 안 90 분위 라벨 0.808·0.748; 과제별 bottle_tray 0.794·mug_marker 0.928·mug_tray 0.835, 변형 standard 0.817·dr 0.796; S-E2E RB1 0.736·RB2 0.763. R2 보정 분할(E-NOV0 평가 7,308) C0 0.8036(E-NOV0 0.8035 재현)·C1 0.8131.
- **흐름 청크 분산은 반대 신호에 가깝다**(0.62·0.58): expert 흐름 표본의 흩어짐은 큰 오류와 약하게만 맞는다. 추가 지연도 1 + 4 표본 p95 55–60 ms(같은 파드 GPU 부하 — 사전 실행 한가할 때 10–16 ms)로 규칙(5 ms) 밖.
- **등각 집합 크기**: R2에서 무작위 아래(0.456) — 보정 분할(유효 편)에서 정한 q̂가 새 표본에서 대부분 집합을 1개로 만들어 동점이 많고, 동점 아닌 쪽이 쉬운 스냅샷에 몰렸다(해석, 따로 검증 안 함).

**§95 운용 모의(`base`, 흐름 지연 L 9.3 s, 보수 모드 = 표시부터 그 표시를 실은 다음 요청의 답까지)**

| 데이터 | 문턱 | 큰 오류 재현율 | 표시 비율 | 표시/분 | 우선 요청 비율 (/분) | 보수 모드 시간 비율 | 보수 구간 중앙 |
|---|---|---|---|---|---|---|---|
| R2-새 | τ90(보정) | 0.931 | 0.648 | 116.6 | 0.996 (6.42) | **0.930** | 11.3 s |
| R2-새 | 보정 표시율 0.1 | 0.271 | 0.136 | 24.6 | 0.757 (4.89) | 0.696 | 7.7 s |
| S-E2E | τ90(보정) | 0.908 | 0.763 | 91.6 | 0.961 (6.20) | **0.975** | 11.2 s |
| S-E2E | 보정 표시율 0.1 | 0.132 | 0.100 | 12.0 | 0.668 (4.31) | 0.635 | 7.5 s |

(결정/분 R2 180·S-E2E 120; 표시율 0.3·0.2 줄은 `D:\tools\scratch_qdd\conf\tables.md`.) → 90 % 재현율 문턱이면 **거의 모든 흐름 요청이 '우선'이고 로봇은 93–98 % 시간 보수 모드** — 채택됐더라도 §95 (b)를 이 형태로 켜면 사실상 늘 느린 로봇이 된다. 표시율 0.1까지 조여도 보수 시간 64–70 %(직렬 흐름에서 답까지 평균 약 1.5 L을 기다리므로). 보수 모드는 '표시 순간부터 짧은 창(예: 1 s)'이나 '연속 표시 n개' 같은 별도 정의가 필요하다(다음 등록 후보).

## 3. 해석 (판정과 별개, 근거 수준 표시)
- **확신도는 시뮬 분포 안에서는 새 표본에서도 선다**(R2-새 0.806, 두 체크포인트·과제·변형·배치 새로움 모두 0.79–0.93) — E-NOV0의 0.804는 우연한 표본 값이 아니었다. 그러나 **실데이터에서는 경계 아래**(0.748)이고 오류 비율이 높아(0.62, 결정 틀림 0.58) '90 %를 잡고 호출을 줄이는' 쓰임에는 약하다: S-E2E 결정 오류의 상당 부분이 휴리스틱 라벨(부호 패턴·남은 거리, F16·F17)과 실물 행동의 어긋남이라, 모델이 확신하고 '틀리는'(라벨과 다른) 경우가 많다(해석, 라벨 뜻 통일 뒤 재측정으로 가를 수 있음).
- **개선 트랙의 가장 싼 이득은 '최솟값' 대신 '모든 결정이 맞을 보정 확률'**(`temp_joint`): 추가 지연 0.02 ms로 R2 +0.041·S-E2E +0.024. 최솟값은 가장 헷갈린 질문 하나만 보지만, 여러 질문이 동시에 조금씩 헷갈리는 경우를 놓친다. S-E2E에서 등록 문턱 +0.03에 못 미쳐 채택하지 않았다(결과 뒤 문턱을 내리지 않는다).
- 질문별 온도는 순위를 거의 바꾸지 않는다 — 결정 확률은 이미 잘 보정돼 있어(ECE ≤ 0.03) 개선 여지는 보정이 아니라 **판별력**(어떤 특징을 더 보느냐)에 있다.

## 4. 바뀐 결정과 다음 (정본 §95 보충 1)
1. **§95 (a) 흐름 우선 표시·(b) 보수 모드에 확신도를 트리거로 쓰지 않는다**(판정 NONE). 확신도는 **기록(선택 이유 로그)** 과 (c) 추가 학습 라벨 후보 순위의 참고 신호로만 둔다.
2. 다음 판(새 사전 등록, 이 결과로 문턱을 바꾸지 않음): (i) `temp_joint`를 주 점수로 하는 재검증 — 라벨 뜻 통일(ser-A-min-3) 재학습 체크포인트(6질문)와 새 실데이터(RB3 `se2e_c2` 등)에서; (ii) 조정자 전달 문헌 후보(연구 문서 `docs/research/lit_sweep_2026-09-26.md` §4 L5): **SAFE식 은닉 상태 지도 탐침**(보이지 않은 과제 AUROC 77.04 대 토큰 확률 61.64), **FIPER식 시간 창 이중 조건**(확신도 낮음 AND 창 안 지속), **TD 보정**(ICML 2026) — 모두 현재 후보와 겹치지 않음; (iii) §95 (b) 보수 모드의 정의를 '표시 뒤 짧은 창·연속 n개'로 바꾼 운용 모의(지금 정의는 93–98 % 시간 보수).
3. `flow`(흐름 표본 분산)·`conf_set`은 다음 판에서 뺀다(판별력 0.46–0.62, 분산은 지연도 큼).

## 5. 런타임 훅 명세 (메인 → 결합 통제자 전달용; `harvest/runtime/*`는 이 작업에서 고치지 않았다)
- **입력(이미 있음)**: 융합 decide 결과 `answers_from`의 질문별 `probs`(보기 softmax)·`p_chosen`·`p_second`. 확신도 계산에 추가 순전파 없음.
- **기록(지금 켜도 되는 것, 판정과 무관)**: 결정마다 `conf = {"ver": "conf-base@v1", "min_margin": min_q ln(p_chosen/p_second), "argmin_q": q, "per_q": {q: {"top1", "top2", "margin"}}, "joint": −Σ_q ln p_chosen(q)}` 한 덩어리를 결정 기록·사이드카에 싣는다(선택 이유 로그, §95 '왜'). 계산 p95 0.005–0.02 ms(파이썬, 이 결과 1절).
- **게이트(지금은 끔 — NONE)**: 나중 판에서 채택되면 `flag = score ≥ τ`, τ는 체크포인트 × 질문 판(qid)마다 보정 분할의 90 % 재현율 문턱(`calibration.json`처럼 모델 지문에 묶어 불일치면 게이트 끔). 켤 때 우선 표시 문구 = `argmin_q`와 그 1·2등 보기. 보수 모드는 위 2절 모의대로 지금 정의로는 켜지 않는다.
- 새 직렬화(ser-A-min-3, 그리퍼 질문 포함 6질문) 체크포인트에서는 질문 집합이 바뀌므로 τ·온도는 다시 적합해야 한다.

## 6. 자체 검사 관문 (user-log 87)

| 관문 | 결과 |
|---|---|
| 등록 전 사전 실행 | 06:54–07:17Z 세 차례: 1·2차는 코드 기반의 `QUESTIONS`가 6개(ser-A-min-3)라 expert가 멈춤 → 기반을 `d225a84`(5질문)로, 드라이버 rc 출력 버그 수정; 3차 통과(등록 0.3절) |
| G0 데이터 | 통과: R2-새 244편·8,208(병합 불일치 0, `6f8889cc4c89`), cal 7,308 ×2, S-E2E 1,799 ×2, 판정 스크립트 입력 검사(개수·중복·요약 줄·유한·같은 순서) 통과 |
| G1 경로 재현 | 1차(09:39Z): c0 7,308 결정 일치 1.0·MSE 비 1.0000·margin 차 0.0 통과, c1 1,200 0.9983·1.0048 통과, **s1·s2 0.987 실패**(근접 동점, 묶음 8 대 1) → **변경 1**(판정 전 커밋) 묶음 1 재추출 → 2차(09:56Z) s1·s2 결정 일치 **1.0**·MSE 비 **1.0** 통과 |
| G2 시간 | 통과: 본 추출 62분(07:19:44–08:21:23Z; 예상 70분, 한도 140분) + 재추출 15분(09:41:25–09:55:58Z) |
| G3 NaN | 없음(입력 검사) |
| 재설계 | 변경 1 한 번(관문 실패 → 원인 확인 → 커밋 → 재개), 문턱·정의 불변 |

## 7. 검증 (결과 뒤)
- **독립 재집계**(`/data/harvest/logs/conf/recount.py` → `recount.json`, 판정 코드와 다른 구현: 자체 margin·라벨·반쪽 분할, `harvest.analysis.stats.auroc` 쌍별 정의): R2-새 AUROC 0.806298·감소 0.378960·이전 재현율 0.930643, S-E2E 0.747807·0.244024·0.907901, 보정 분할 0.803555·0.813076 — `verdict.json`과 소수 여섯째 자리까지 같다.
- 시험: 로컬 `tests/test_conf_{lib,build,verdict,gate}.py` 40 통과, 파드 고정 사본 `/data/harvest/code_conf`에서도 40 통과(10:0x UTC), 등록 때 로컬 전체 1,878 passed·29 skipped; 결과 커밋 전 로컬 전체 재실행과 기계 검사(`D:\tools\scratch_qdd\conf\check_results.py`: 1·2절의 소수 셋째 자리 수 122개가 `verdict.json`에서 뽑은 표에 있음 — 0.007은 0.0069의 반올림, 인용 경로 존재)는 아래 줄.
- 표 값은 `D:\tools\scratch_qdd\conf\tables.py`로 `verdict.json`에서 뽑았다.

## 8. 실행 기록

| 항목 | 값 |
|---|---|
| 코드 | 파드 `/data/harvest/code_conf` = `git -c core.autocrlf=false archive d225a84` + 등록 커밋 `6191369`의 `tools/conf/*`(해시 등록 7절과 같음) + 변경 1의 `run_conf.sh`(`4bcdd56cdf476acb`), `CODE_VERSION`, `logs/conf/CODE_HASHES{,_A2}.txt`; 드라이버 `tools/conf/run_conf.sh <코드> build → A → gate → A2 → gate → verdict` |
| 데이터 | R2-새 보기 `/data/harvest/data/conf/view/`(원본 `/data/harvest/r2/train` 실패 편, `heldout.json`), 보정 `/data/harvest/data/ma2/view/`, S-E2E `/data/harvest/data/se2e_c1/conv`; 체크포인트 `ckpt/ma2/c0/last`·`ckpt/sr1c/c1/last`·`ckpt/se2e_confirm/motion_s{1,2}/last` |
| 시각(UTC) | 등록 07:18:17(커밋 `6191369`), 본 추출 07:19:44–08:21:23, G1 09:39, 변경 1 09:40:15(`f37f8f8`·`393a184`), 재추출 09:41:25–09:55:58, G1 09:56, 판정 10:01:54–10:04:40(CPU), 재집계 10:04 |
| GPU | **GPU 2만**: 사전 실행 약 0.23 h + 본 1.03 h + 재추출 0.24 h → **≈ 1.5 GPU-h**. 다른 GPU·x2 파드·남의 프로세스 무접촉 |
| 산출 | `/data/harvest/logs/conf/`: `ext/{r2_c0_cal,r2_c0_test,r2_c1_cal,r2_c1_test,se2e_s1,se2e_s2}.jsonl`(묶음 8 S-E2E는 `*.b8.jsonl`, 판정에 안 씀)·`gate.out`, `verdict.json`·`verdict.out`, `recount.json`, `diag/`, `dry/`; 로컬 `D:\tools\scratch_qdd\conf\`(`verdict.json`·`recount.json`·`tables.md`) |
| 비용 | 유료 API 0건 |

## 9. 절차 이탈·기록 의무 (P03)
- 등록 전: 로컬 `python -c "print(1)"` 1회, 빈 heredoc 1회(아무것도 실행 안 함) — 결과와 무관(등록 8절).
- 변경 1의 첫 커밋(`f37f8f8`)에서 등록 문서 편집이 도구 오류(읽기 전 편집 거부)로 빠져, 기록은 바로 뒤 `393a184`(재추출 전)로 따로 커밋했다. 그 기록 커밋의 메시지에서 공동 저자·세션 줄이 편집 실수로 빠졌다(이미 푸시되어 강제 푸시하지 않음).
- **결과 커밋 `cd29570`이 다른 에이전트의 미커밋 기록 줄을 함께 올렸다(P72·P80 재발)**: 편집 전 가드는 깨끗했으나 편집과 커밋 사이(10:07–10:13Z)에 결합 계획 Task 21 에이전트가 같은 파일에 기록 줄을 더했고, `git commit -- <파일>`이 그 줄까지 담았다 — 정본 §84 보충 2 Task 21 구현 기록, handoff §0 결합 줄 갱신, draft-log 10:12 줄, 책 03 행·04 `harvest/eval`·`harvest/clients`·`harvest/couple`·`tests` 행·06 10:12 줄. 그 기록이 가리키는 Task 21 코드(`harvest/couple/*` 등)는 그 시점에 아직 미커밋이다. 이미 푸시되고 그 위에 다른 커밋(`11cbc39`)이 올라가 되돌리지 않았고 메인에 알렸다. 예방: 커밋 직전(편집 뒤)에 `git diff --numstat`가 내 줄 수와 같은지 확인한다.
- 본 추출이 끝난 뒤 이 에이전트 세션이 API 한도로 약 1시간 멈췄다(파드 실행은 이미 끝나 있었음, 고아 프로세스 없음 확인).
