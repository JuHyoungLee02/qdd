# R7 객관 검증 순회 — 11회차 (cycle 11, 연속 무결 카운트 0에서 시작)

- 검증자: 독립 검증 에이전트(이 코드의 작성자가 아님, `r7c10_fixes.md` §4 대조표에 기대지 않고 사전 등록 원문에서 대조표를 새로 만들고, 10회차 수정 에이전트가 "아직 행동 확인 안 함"으로 남긴 행(2·5·13·16·23·25·26·31·34·38·57)을 모두 **구성한 입력 → 실제 함수·런타임 출력**으로 확인함). 작성 2026-09-25 08:30 UTC 무렵(`date -u`; 로컬 시작 07:44 UTC, 파드 첫 명령 07:47:35 UTC, 파드 정리 끝 08:20:54 UTC).
- 대상: `D:\qdd` `dev` 커밋 `43cd1ff`(= 태그 `stage3-r7fix10`, 커밋 시각 2026-09-25 07:44:12 UTC). 재생 결정성 조사 에이전트가 같은 작업 트리에 커밋 안 된 변경(`harvest/sim/labeler.py`, `tests/sim/test_labeler_logic.py`, `docs/stage3/results/pool_replay_debug.md`)을 두고 있어 **작업 트리는 읽지도 쓰지도 않고** `git -c safe.directory=D:/qdd -c core.autocrlf=false archive 43cd1ff`를 `D:\tools\scratch_qdd\r7c11\repo`에 풀어 검토했다(비교용 `6522fda` 사본 `…\r7c11\base`). `6522fda..43cd1ff` = 코드 3개(`eval/closed.py`, `eval/e05.py` 설명만, `runtime/m4.py`) + 시험 2개(`test_m4.py` 수정, `test_r7c10_prereg.py` 새 15개) + 문서(정본 §75·§75 보충, handoff·draft-log·direction-log, `r7_cycle10.md`·`r7c10_fixes.md`, 정정 표시 6곳). 파드 사본 = 같은 archive + `CODE_VERSION`(tgz sha256 `34f8e164…`), 모든 산출 `meta.git.commit` = `43cd1ff8…`, `meta.code_sha` = `a937dcb555eff87a`.
- 기준: `r7_cycle1.md` 점검 틀(A–G)·분류, `r7_cycle2.md`–`r7_cycle10.md`, `r7c7_fixes.md`–`r7c10_fixes.md`, 정본 `00-interfaces.md` §1–§75(§75 메인 보충 07:44 UTC 포함, 뒤 절 우선; [사용자] 제목 절은 §67 규칙, 논문 .tex는 §70·§74·§75 논문 갱신 항목에 따라 NOTE), 계획 `2026-09-25-e2e-ready.md` 완료 정의 1–5, 사전 등록 `E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`·`M4-overlap-commit.md`(조건·변수 표).
- 분류(10회차와 같음): **DEFECT** = 코드가 사전 등록·정본과 다르게 동작하고 그 차이를 정한 정본 결정이 없는 것, 또는 완료 정의 1–5를 깨는 것. **DOC** = 현재 시제 서술이 커밋과 다르고 정정 표시가 없는 것, 사전 등록이 열어 둔 값을 코드가 정해 쓰는데 정본에 없는 것.
- 규칙 준수: 저장소 수정·커밋·푸시 없음(이 보고서 파일만 새로 씀). 로컬 임시 = `D:\tools\scratch_qdd\r7c11`(스크립트는 모두 Write 도구로 쓰고 경로로 실행; heredoc·`cat >`·`python -` 사용 0). 파드 = `/data/harvest/tmp/r7c11`(코드 사본·pytest·산출·가짜 날짜 카나리 루트, 1.1 GB)와 내 Isaac 판(07:50:18 UTC 시작)이 만든 `ir/kitcache/cyclo-r7c11_standard`(208 MB)·`cache/pyc_r6/data/harvest/tmp/{r7c11,tmpcenhezv7}`·`tmp/{carb.JQbIUo,tmpcenhezv7}` — 시작 전 목록(`pod_before.txt`)과 생성 시각(07:50:19–07:50:37 UTC)으로 가려 **모두 지웠다**(08:20 UTC). 같은 시간대의 다른 새 항목(`kitcache/cyclo-replaydbg_demo{E,F,G}`, `tmp/carb.*`·`tmp/tmp*` 08:00:54–08:13:19 UTC)은 재생 조사 에이전트 것이라 두었다. GPU: 단계 B 학습·CUDA 시험 = GPU 2(렌더 없음), Isaac = GPU 1(`CUDA_VISIBLE_DEVICES=1`, `IR_ROOT=cyclo`, 고유 `IR_INST` = `r7c11_standard`), GPU 0(재생 조사)·GPU 3 건드리지 않음, 끝에 0–3 = 901/1/1/1 MiB(GPU 0은 재생 조사 판). 시드 DEV만(`HARVEST_ALLOW_SPLIT`는 가드 음성 시험 1건에서 `=dev`만). 유료 API 없음(Astra 모의). 비밀값 출력·검색 없음. 남의 프로세스 건드리지 않음.

## 판정: **FAIL**

| 분류 | 건수 |
|---|---|
| DEFECT | 0 |
| DOC | 2 |
| SCOPED | 12 |
| NOTE | 11 |

10회차 수정(H = 1 앞당겨 묻기 `m4.early_ask_steps`, `lead_max` 1.0 s, 나이 경계 `older_than`)은 **행동으로 확인했다**: 가짜 세계 폐루프에서 주 지연 0.307 s·lead 1.0의 H = 1은 스텝당 묻기 3·받아들인 표 중앙값 2·확정 294(C5·C3·C6), 스텝 k의 요청 시각 = `e05.vote_steps`의 (−0.33, −0.66, −0.99)(실행한 57스텝 모두) = `latency.votes_per_step` 3; 파드 Isaac C5 H = 1 받아들인 표 중앙값 2·`commits` 187, C3 240. H = 3은 **내 무작위 대조(씨앗 2111, 210판 × C0–C6, 스텝 격자 밖 시작 시각 포함)에서 나이 경계만 되돌린 HEAD가 6522fda와 105/105 × 7조건 비트 동일**이고, HEAD 그대로와의 차이는 지연이 정확히 1.5 s(C0·C1·C3–C6)·5.0 s(C2)인 판에서만 난다(§75 N1의 의도된 변경, N1). 사전 등록 대조표 66행(§5)에서 동작 불일치는 없다(DEFECT 0). 완료 정의 1–5도 모두 다시 돈다(§6.3). 그러나 문서 두 건: **handoff가 §75 보충(같은 커밋, 07:44 UTC)이 SCOPED로 닫은 판정 7(H 비교 한계)을 아직 "[결정 필요: 메인 세션]"으로 적음**(D-1, 10회차 D-1과 같은 모양), 그리고 **사전 등록이 정의하지 않은 풀의 "경계 사례"(30 % 과표집 층)를 코드가 정해 쓰는데 정본에 기록이 없음**(D-2, 8회차 D-2와 같은 부류; 대조표 행 2를 처음으로 행동 확인하면서 드러남). 연속 무결은 **0회 그대로**.

---

## 1. DEFECT

없음. 근거: §5 대조표 66행 전부를 구성 입력 또는 파드 실행 산출로 확인(로컬 새 행동 점검 `hand11.py` 48/48, 10회차 경계 점검 `hand_checks.py` 재실행 98/98, 무작위 대조 1,470판 × 3판본, 파드 가드 16건·카나리 가짜 날짜 5회·Isaac 폐루프 2칸). 특히 10회차 수정 에이전트가 행동 확인하지 않았다고 적은 행 — E0.5 판정 5·7·8(행 23·25·26), E1 행 31·34·38, 행 13·16, 시뮬 쪽 행 2·5·57 — 을 모두 경계값 입력으로 돌렸고 사전 등록 문구대로 나왔다(행 2의 **값**은 맞고 정의의 정본 기록만 없음 → D-2).

## 2. DOC

### D-1. `docs/handoff.md:102` — 판정 7(H 비교 한계)을 "**[결정 필요: 메인 세션]**"으로 적음(정본 §75 보충이 같은 커밋에서 SCOPED로 결정)
- 정본 §75 보충(`00-interfaces.md:657`, "2026-09-25 07:44 UTC, 메인 결정")이 "(1) **lead_max 기본 1.0 s 확정** … 최종값은 E0에서 d_p95가 나온 뒤 E §2.7-4 규칙(스텝당 표 중앙값 ≥ 2)으로 다시 정한다 … (2) **판정 7(H=3 대 H=1) → SCOPED** … (3) 창 안에 스텝이 없을 때 … 대체 규칙 채택"으로 닫았는데, 새 세션이 가장 먼저 읽는 handoff의 10회차 줄(`:102`)은 "알려 둘 한계: 단계 A DecCall은 질문 하나라 이 런타임에서 H = 1과 H = 3의 차이는 표 창뿐(판정 7 해석, 정본 §75 **[결정 필요: 메인 세션]**)"과 "`closed --m4-lead-max`(기본 1.0 s, 후보 {1.0, 1.5}는 E0 뒤 메인 확정)"로 적었고 해결 표시가 없다. 머리 `마지막 갱신: 2026-09-25 07:40 UTC`(`:3`)도 보충(07:44)·커밋(07:44:12) 이전이다.
- 같은 사실: `docs/stage3/results/r7c10_fixes.md:72`(현재 상태 표 행 51 판정 칸 "열린 값 `lead_max`(E0 뒤) + H 비교 한계(§75, **메인 확인**)"), `:83`–`:87`(§5 "판단이 갈릴 수 있는 곳 (메인 확인 요청)" 1–3 — 셋 다 보충 (1)–(3)이 답함) — 해결 표시 없음. (정본 §75 제목 "[… 열린 값 1건 메인 확인 요청]"과 본문 `:648`·`:649`의 [결정 필요]는 같은 절 보충 `:657`이 덮으므로 정본 관례상 문제 아님. `direction-log.md:58`의 "메인 확인 요청"은 07:00 시점 기록 줄이라 NOTE 범주.)
- 선례: 10회차 D-1(handoff 9회차 줄의 N4 "[결정 필요]"가 §74 보충 뒤에도 그대로 → DOC), 5회차 P1, 정본 §71 grep 목록("`[결정 필요]`를 후속 정본 절과 대조").

### D-2. 풀 "경계 사례"(30 % 과표집 층)의 정의 — 사전 등록이 열어 둔 값을 코드가 정해 쓰는데 정본에 없음
- 사전 등록: E §1.5(`E-first-experiments.md:121`) "문턱 근처(히스테리시스 띠 안) 술어는 `ambiguous` 표시. **경계 사례를 풀의 30%로 과표집**하고, 분석은 자연 가중치와 과표집 가중치 둘 다 보고" — "경계 사례"의 정의는 없다(`ambiguous`와 따로 적힘). 풀의 소비처: E §1.5 표(`:119`) "스냅샷 풀(E1·E-M3-1·E3)", E §3.4 "경계 과표집(§1.5)이 오답 수를 확보하는 장치", 정본 §73(`:623`) "과표집 모집단은 `--decision-only`로 남긴다"(`calib --decision-only`, `calib.py:70`).
- 코드(43cd1ff): `harvest/sim/snapshot.py:102-116` `boundary_flags` = **`ambiguous`이거나, 그 스냅샷에서 다음 연속 스냅샷(0.33 s)까지 등록부 술어 값이 바뀌는 경우**(앞 방향만), `:119-142` `select_decision`(판당 round(10 × 0.30) = 3개를 이 층에서, 모자라면 다른 층에서 채움, `w_natural` = 역포함확률 × n/N), 상수 `:30-31`. 행동 확인(`hand11.py`): 경계 20/100 → 경계 3·나머지 7, 자연 가중치 합 10.0; 경계 1개뿐 → 1 + 9; 6스냅샷 판 → 6개 모두; `boundary_flags([{a:1},{a:1},{a:0},{a:0}], [F,F,F,T])` = [F, T, F, T]. 즉 **값·과표집 비율은 사전 등록대로**이고, "경계 사례"를 무엇으로 볼지만 코드가 정했다.
- 기록: 결과 문서 `docs/stage3/results/pool.md:25`에만 정의·근거(near 띠만으로는 자연 4.6 %라 판당 3개를 못 채움, 앞뒤 두 방향이면 자연 35 %라 "과표집"이 아니게 됨 → 앞 방향만, 자연 22.8 % → 풀 29.7 %)가 있고, 정본에는 없다(`grep "경계 사례|boundary_flags|과표집"` → §73 `:623`의 "과표집 모집단" 언급뿐, 정의 없음). 옛 계획서 `2026-09-24-stage3-experiments.md:1394`는 "히스테리시스 띠 안 술어가 있는 결정 시점을 우선 뽑는다"로 다른 정의(정정 표시 없음, 옛 계획서라 NOTE 범주). 같은 부류로 풀 섭동 배정(P0·P1·P2 각 40편 고정 순열, `snapshot.py:46-52`)도 `pool.md:23`에만 있다(사전 등록은 "P0~P2만"; fit/eval 60/60은 정본 §64에 언급).
- 영향: 지금 기본 판정 경로(E0.5·E1은 연속 스냅샷 전부, 정본 §73)는 이 층을 쓰지 않아 동작 결함은 아니다. E-M3-1·E3·`calib --decision-only`·자연/과표집 가중치 보고가 이 정의를 쓴다.
- 선례: 8회차 D-2("사전 등록이 열어 둔 평가 값을 코드가 정해 쓰는데 정본(§72 원칙)에 적히지 않았다" → 정본 §73 E0.5·E1 열린 값 목록). 1–10회차 대조표 행 2는 코드 값(10 × 120, 30 %)만 보고 "일치"로 적었다.

## 3. SCOPED (통과에 영향 없음, 근거 재확인)
- S1. `LatencyChargingController` 스텁(정본 §71 보충) — 로컬·파드 모두 `tests/runtime/test_latency_ctrl.py:11` 1개 건너뜀.
- S2. 본 단계 B·R2_TRAIN 대량 생성(§66) — `gen gen --seeds 10005-10006` 확인 인자 없이 거부(§6.4).
- S3. CAL 보정·TEST 평가(메인 세션, `HARVEST_ALLOW_SPLIT`) — 가드 §6.4.
- S4. RB1 라이선스(§63-(6)).
- S5. §67 C8 — `core.py` Astra patch/replace = epoch만.
- S6. Astra 카나리 id "none" — 파드 C5·C3 판 `astra` 행 2 + 2 모두 `canary_id: "none"`.
- S7. S-E2E 체크포인트 런타임 tau 마스크 차이(`r7_fixes.md` §7).
- S8. CONTRADICT-soft(§68 K6·§69).
- S9. §73 D5 M4 설계 확장(경계 직후 W 2·γ 1.0, 큐 임계 g, 장면 변화 조기 호출, 미확정 실행 (b) 엄격화) — `m4.py:28-32` "Not implemented".
- S10. §73 N5 E1 범위(`calib` = 판정 1·8).
- S11. §74 보충 N4 (가) 비례 STALE_MAX(지연 큐와 함께 E-M4-lat 준비 때)와 런타임에 없는 조건 C2'·C2'-S·C2-match·C3'·C3''·C5-A3·C-FIX·C5'(`closed.json` `meta.not_in_runtime`).
- S12. (새) §75 보충 (2) **E-M4 판정 7(C5 H = 3 대 H = 1) → SCOPED**: 단계 A DecCall은 결정 종류마다 질문 하나라 H = 1·3의 입력 길이·질문 내용 차이가 없다. 행동 확인: 가짜 세계 0.3 s 격자에서 H = 3과 H = 1(lead 1.0)의 원장이 같다(확정 294 대 294, 호출당 슬롯 {3: 60} 대 {3: 60}) — 보충이 적은 그대로.

## 4. NOTE
- N1. **"H = 3 비트 동일(나이 경계 N1 포함)"의 범위**: 정본 §75 D1(`:647`), `r7c10_fixes.md:65`·`:88`, handoff `:102`("H = 3 판 155/155 모든 조건 비트 동일")는 수정 에이전트 표본(지연 {0.2, 0.33, 0.5, 0.75, 1.0, 1.4, 1.6, 2.5} s + 흔들림)에서 참이다. 지연이 정확히 1.5 s·5.0 s인 판을 넣으면 H = 3도 6522fda와 달라진다(내 대조: C0·C1·C6 102/105, C3·C4·C5 99/105, C2 97/105 동일 — 다른 판은 전부 지연 1.5 s(C2는 5.0 s)); `older_than`만 옛 원시 비교로 되돌리면 **105/105 × 7조건 동일**이므로 그 차이는 §75 N1(정확히 STALE_MAX인 표 유지)의 의도된 변경이다. 결함 아님 — 다음 정리 때 "(나이 경계 판 제외)" 정도로 적을 것을 권함.
- N2. **H = 1 앞당긴 묻기의 질문 문구**: 호출은 `build_live_request(slots[0], …)`로 창의 첫 스텝을 묻고(`core.py:192-194`, 질문 "during step ds{slots[0]}"), 그 답이 창 안 모든 스텝의 표가 된다. 그래서 스텝 k의 세 표는 "step k", "step k−1", "step k−2"로 물은 답이다. `question_id`는 `{ds}`를 틀로 바꿔 해시하므로(`run_r5.question_ids:32`) 같은 질문 id이고, E0.5 재생도 각 스냅샷 자기 `ds_id`로 묻는다 — 재생과 런타임은 같은 방식. M4 §4.1 "같은 스텝을 … (질문 문구 동일)"의 문자 그대로는 아니고, 정본 §75의 읽기("스텝 번호에 묶이지 않은 답")와 판정 7 SCOPED(S12)가 이것을 덮는다. E-M4 결과 문서에 이 읽기를 적을 것.
- N3. **"d_p95 > 약 0.67 s면 lead 1.5" 문턱은 격자 정렬 호출 기준**: 창 [t + d̂, t + lead_max]의 스텝 수는 호출 시각이 0.33 s 격자에 맞을 때 d̂ ≤ 0.66이면 2 이상이지만, 격자 밖 호출(시작 시각 오프셋, N_max로 밀린 송신)에서는 창 길이 lead − d̂ < 2 T_c(d̂ > 0.34 s)면 1·2표가 번갈아 나온다(내 대조: 격자 밖 시작 C5 H = 1 지연 0.5 s·lead 1.0 판 중앙값 1). 파드 Isaac 판은 첫 호출 t = 0.0으로 격자에 맞다(받아들인 표 {1: 5, 2: 43, 3: 4}). E0 뒤 `lead_max`를 다시 정할 때 E §2.7-4 오프라인 수와 함께 런타임 원장의 스텝당 표 수를 볼 것.
- N4. `M4Params(lead_max=inf)`는 검증을 통과하고 첫 `target_slots`에서 `OverflowError`(`math.floor(inf)`)로 죽는다(`--m4-lead-max inf`면 워커가 첫 호출에서 실패). 0·−1·NaN은 거부(파드 가드 3건 rc 1). `math.isfinite` 검사를 권함.
- N5. E0.5 판정 5 값 `j5_flip_minus_retest` = 성공 궤적 flip(반올림 없음) − 재시험 flip(전 스냅샷, 표시용 4자리 반올림 값)(`e05.py:446-448`). 보고 값이라 판정 영향 없음; 재시험의 모집단(전 스냅샷 대 성공 궤적)은 사전 등록이 정하지 않았다 — E0.5 결과 문서에 적을 것.
- N6. E1 판정 7 결속: 모델 지문·`question_id`(카메라 배치·상태 형식이 legend에 들어감)가 다르면 `Calibration.load`가 거부하고 **런타임이 시작하지 않는다**(`core.py:124-127`) — 사전 등록 "해당 질문군 게이트 끔"보다 엄격하나 정본 §73 "읽기 거부"와 같다. 지문이 없는 보정 파일, 또는 `RuntimeConfig.question_ids`가 빈 경우 결속 검사를 건너뛴다(`calibration.py:260-266`) — `closed`는 항상 둘 다 채우므로 지금 경로는 안전.
- N7. 시뮬 쪽: 행 57의 D-time τ 동점 처리(큰 τ를 "단순"으로, `labeler.py:141-142`)는 사전 등록에 없지만 §65 선택(plan)에 영향 없음. 행 5의 오라클 플래너 조기 종료 규칙(FSM fail, `done` 뒤 3 s)은 생성기 규칙이고 폐루프 평가(`aiworker.py:162-168`)는 1 s·60 s·낙하 그대로.
- N8. 단계 B S-E2E 스모크(6스텝, 검증 4개): dec 3.526 → 2.890 → 2.377은 줄지만 fm 3.320 → 3.024 → 3.303은 단조가 아니다(10회차 N5와 같은 모양). 재적재 차 0.0. 소규모 확인이라 판정 없음(§56) — S-E2E 사전 등록 때 "손실 감소"에 어느 손실을 볼지 적을 것.
- N9. 10회차 N2(소수 γ 0.67), N6(카나리 `dev_v1` 3편, 이번 `meta.canary` `cn20260924_mock_ae0d1a`, `stale: true`), N7(논문 — §74·§75 논문 갱신 항목), N8(옛 계획서 초안 코드, E §3.6 반올림 [가정], E0.5 분당 400 [가정], `handoff.md:27` 날짜 붙은 옛 절, TEST2가 `splits.RANGES`에 없음, RD 1차판 Score [가정], 자연/과표집 가중치 보고, S-E2E 사전 등록 항목) 그대로 — 모두 열림, 완료 정의 1–5를 막지 않음. `direction-log.md:58`·`draft-log.md:448`의 "메인 확인 요청"은 07:00·07:40 시점 기록 줄.
- N10. 위생: 로컬 C:에서 이 검토 시작(16:44 KST) 뒤 새로 생긴 파일은 `AppData\Local\Temp`의 앱 캐시 `.tmp`, `2026-09-25_mservice.txt`, `killreh.sh`·`killsim.sh`(내용 = rehearsal·`sim_server_rb20` 프로세스 정리 — 다른 프로젝트), `mat-debug-*.log`뿐 — 이 저장소 산출물 0. 공용 `D:\tools\scratch_qdd\pytest_tmp\p35108`(08:00:52 UTC)은 내 로컬 판(07:45–07:48 UTC, basetemp = `r7c11\pt1`) 뒤라 다른 에이전트 것. 파드 `/data` 밖: `find / -xdev -newermt "07:47 UTC"`(`/proc`·`/sys`·`/dev`·`/run`·`/data` 제외) 0건, 루트에 "C:" 폴더 0.
- N11. 내 절차 메모: 첫 `git -C D:/qdd` 명령이 safe.directory 오류로 실패(읽기 전용, 전역 설정은 쓰지 않음 — 이후 `-c safe.directory=D:/qdd`). 파드 시작 전 목록은 `/data/harvest/tmp/r7c11` 폴더를 만든 직후에 떠서 그 폴더가 "전" 목록에 들어 있다(정리 때 지움). 규칙 위반 없음.

---

## 5. 사전 등록 대조표 (과제 1, 원문에서 새로 만듦, 행마다 행동 확인)

`prereg.json` 해시: 파드 모든 산출 `meta.prereg.check` = "OK"(e05·rd·calib·closed, 해시 5개 = `prereg.json`, `utc` 다음·`model` 앞). 사전 등록 문서는 `6522fda..43cd1ff`에서 바뀌지 않았다. 코드 줄은 `43cd1ff` 기준. "확인" 열: **로컬** = `D:\tools\scratch_qdd\r7c11\hand11.py`(새 행동 점검 48/48 PASS, `hand11.txt`), `hand_checks_c10.py`(10회차 경계 점검 재실행 98/98, `hand_c10_on_43cd.txt`), `char11.py`·`char_cmp11.py`(무작위 대조); **파드** = §6.3·§6.4(`pod_check.txt`, `pod_canary.txt`). 굵게 = 이번에 처음 행동으로 확인한 행.

| # | 사전 등록 값·절차 (출처 file:line) | 구현 (file:line) | 확인(구성 입력 → 출력) | 판정 |
|---|---|---|---|---|
| 1 | 시드 DEV 0–29 / CAL 500–549 / TEST 1000–1149 / TEST-P5 1300–1329 / POOL 2000–2119 / R2_TRAIN 10000–59999 (E :115-120, 정본 §66) | `eval/splits.py:13-15,27-42`, `datagen/gen.py` | `RANGES` 끝값; 파드 가드 16건 rc 1(출력 폴더 0) | 일치(TEST2 N9) |
| **2** | POOL 120 × 10 = 1,200, 경계 사례 30 % 과표집, 자연·과표집 가중치 (E :121) | `sim/snapshot.py:30-31,102-142` | 경계 20/100 → 3 + 7·가중치 합 10.0; 경계 1개 → 1 + 9; 짧은 판 6개 전부; `boundary_flags` [F,T,F,T]; 10 × 120 = 1,200·P0–P2만 | 값 일치, **정의 정본 미기록 → DOC D-2** |
| 3 | `ambiguous` = 히스테리시스 띠 (E :121; M1 :140) | `snapshot.py:89-99` (5, 6] cm | 0.05 아님·0.0500001 참·0.06 참·0.0600001 아님 | 일치(§74) |
| 4 | 분할·재표집 = 에피소드 (E :122, :130) | `calib.py`, `e05.py`, `rd`, `canary.py` | 파드 `meta.bootstrap.unit` 4종(episode / layout seed) | 일치 |
| **5** | 성공 1 s 연속·60 s·낙하 즉시 실패 (E :107) | `sim/planner.py:26-38`, `runtime/aiworker.py:162-168`, `config.py:12-13` | 20 Hz 곱 시각에서 정확히 1.0 s 유지 → 성공, 0.95 s → 아님, 1 s 안의 None → 끊김, 100 Hz 누적 합 시각 1.0 s → 성공; 60 s·1 s 설정 | 일치(N7) |
| 6 | 모든 호출 요청 해시·이미지 해시·카나리 id (E :125-127, 정본 §28) | `core.py:214-217,270-271,373-374` | 파드 C5 `call` 50/50·C3 49/49 64자리 hex·이미지 {cam_head, cam_wrist_right}·`cn20260924_mock_ae0d1a`, `astra` 2 + 2행 hex·{cam_head}·"none" | 일치 |
| 7 | 95 % 구간 = 군집 부트스트랩 10,000 (E :130, EVAL :184) | `stats.py:4` | 파드 4개 `meta.bootstrap.n_boot` 10000 | 일치 |
| 8 | 짝 비교 = 같은 시드 짝 (E :130) | `stats.cluster_diff_ci:57-` | 판정 8 구성 입력에서 치환·바닥이 같은 군집 뽑기(구간 폭 0) | 일치 |
| 9 | Holm 판정 2 "LA-2 또는 C2''"(하한 > 0) (E :131, :257) | `e05.py:218-227`, `stats.py:87-101` | 98/98(음의 가설 기각 아님·수준 안 풂) | 일치(§74 N1) |
| 10 | Holm 판정 10 (E :265) | `e05.py:202-237` 양측 | 98/98 | 일치(§74) |
| 11 | 카나리 "하한 > 0, Holm" (E :139) | `harvest/canary.py:31-55` | 98/98; 파드 모의 카나리 `drift_suspect` False | 일치 |
| 12 | 판정 절 해시·시각을 실행 기록 첫 줄에 (E :133) | `eval/common.py:419-437` | 파드 4개 check OK·해시 = `prereg.json`·`model` 앞 | 일치(§73) |
| **13** | 카나리 기준일 = 세트를 처음 돌린 날 (E :140) | `eval/canary.py:210-218` | 파드 가짜 날짜(scratch 루트, 모의): 세트 t_a 09-20 첫 판 기준 없음, 09-22·**09-25 기준 = 09-20**(09-22 아님), 다른 세트 t_b 09-23 기준 없음·09-26 기준 = 09-23 | 일치 |
| 14 | "모델 식별 필드가 바뀌어도 같은 처리" (E :139) | `calibration.py:255-267` | 다른 지문 → 읽기 거부·런타임 시작 거부 | 일치(§73, N6) |
| 15 | E0.5 표 = t_s − d_p95 − {0, .33, .66} (E :236) | `e05.py:44-58` | 런타임 H = 1 요청 시각과 57스텝 모두 같음(0.307 s) | 일치 |
| **16** | 같은 시각 K = 3 동시 (E :238) | `e05.py:482,535-541`(`asyncio.gather`) | 파드 `calls.jsonl` 117/117 스냅샷에 `A0#0–#2` 3개, `meta.same_k` 3 | 일치 |
| 17 | newest / LA-2·γ 2/3 / C2'' / C2' (E :240) | `replay.py:10-30`, `e05.py:61-65` | 98/98(`a,b,a` 확정, `a,b,c` 첫 표) | 일치(§74) |
| 18 | `C_flip` 두 식 (E :242) | `e05.py:77-87`, `m4._update_flip` | 코드(무변경) | 정본 기록(§73) |
| 19 | A0–A4, 층 (E :245-246) | `options.layer`, `e05` | 판정 8 구성 입력이 L7_17 층으로 판정됨 | 일치 |
| 20 | 성공·섭동 창 (E :250) | `e05.py:162-164` 1 s | 정본 §73 | 정본 기록 |
| 21 | 판정 1–3 (E :256-258) | `replay.py:38-50`, 입력 `e05.py:421-425` | 98/98 | 일치(§74) |
| 22 | 판정 4 (E :259) | `replay.py:51-56` | 98/98 | 일치 |
| **23** | 판정 5 재시험 빼기 (E :260) | `e05.py:446-448` | 구성: 재시험 10/56·flip 0 → `j5` −0.1786 = round(flip − retest) | 일치(N5) |
| 24 | 판정 6 < 1 % (E :261) | `replay.py:57-58` | 98/98 | 일치 |
| **25** | 판정 7 C2' − LA-2 ≥ +2 %p·하한 > 0, C2' − C2·S1 단독 보고 (E :262) | `e05.py:449-453` | 구성(10편 × 50스텝): 정확히 1/50 → 참(구간 [0.02, 0.02]); 1/51 → 거짓; 평균 0.0204·하한 −0.0041 → 거짓; 보고 3값 있음 | 일치 |
| **26** | 판정 8 치환 flip − 바닥 짝 하한 > 0, 층별 (E :263, :246) | `e05.py:454-459` | 구성: 치환 10/56·바닥 0 → 참; 둘 다 0(하한 정확히 0) → 거짓; 치환 10/56 대 바닥 5/56 → 참 | 일치 |
| 27 | 판정 9 (E :264) | `replay.py:59-68` | 98/98 | 일치 |
| 28 | d_p95 = E0 값, 재실행 규칙 (E :236, :278) | `e05.py:485` 0.307(정본 §55) | 정본 기록 | 일치 |
| 29 | E0.5 분당 400 [가정] (E :277) | 없음 | 로컬 서버 | N9 |
| 30 | E1 자료 반분 (E :296 → 정본 §52) | `calib.py:28-31` | 정본 §73 | 정본 기록 |
| **31** | ECE = 15 동일 질량 (E :320) | `calibration.py:71-83` | 독립 구현(안정 정렬, 앞 n mod 15 묶음 +1)과 n ∈ {1, 7, 15, 16, 17, 29, 30, 31, 100, 437} × 20회(동점 포함) 최대 차 8.3e-17 | 일치 |
| 32 | 오답 < 30 → 판정 불가 (E :320) | `calibration.py:215` | 30 가능·29 불가 | 일치 |
| 33 | J5 q̂ = ⌈(n+1)(1−α)⌉번째 (E :330) | `calibration.py:59-64` | 98/98 | 일치 |
| **34** | log p 1e-6 자름 (E :333) | `calibration.py:20,23-30,37-39` | p = 0 보기 → 로짓 log 1e-6(T 2에서 확률 1e-3/1.001), NLL(정답 p 0) = −log 1e-6 | 일치(반올림 [가정] N9) |
| 35 | 질문별 온도, 등위 회귀 비교 없음 (E :334) | `calibration.py:42` | 정본 §73 | 정본 기록 |
| 36 | 원 확률: ECE ≤ .03 ∧ T ∈ [0.8, 1.25] (E :335) | `calibration.py:93` | 98/98 | 일치 |
| 37 | 판정 1 (i)–(iv) (E :339) | `calibration.py:210-234` | 모든 문턱 정확히 경계에서 켬 | 일치(§74) |
| **38** | 판정 7 재보정 결속: 모델·문구·보기·상태 형식 (E :345-346) | `calibration.py:255-267`, `run_r5.question_ids:20-35`, `core.py:124-127` | 같음 → 읽음; 다른 지문·상태 IMG·카메라 HW → 거부; 불일치 파일이면 런타임 시작 거부 | 일치(N6) |
| 39 | 판정 8 J5, 적합 400 미만 "보장 없음" (E :347) | `calibration.py:230-233` | 경계 켬, 400 보장·399 없음 | 일치 |
| 40 | `ambiguous` ECE 제외 (E :359) | `calibration.evaluate`(무변경) | 10회차 파드 `n_ece` 96 + 애매 11 = 107 | 일치 |
| 41 | E1 판정 2–6 등 | 없음 | 정본 §73 | SCOPED S10 |
| 42 | N_max = ⌈d̂/T_c⌉+1 — C5; C2 상한 없음 (M4 :258,:263) | `m4.py:177-181` | 시험 `test_inflight_counts…`(C5 0.6 s 최대 2 ≤ 3, C0·C1·C6 1) 통과 | 일치 |
| 43 | γ = 0.67 = 3표 중 2 (E :487, M4 :276) | `m4.py:46,72-77,287` | 98/98 | 일치(§74) |
| 44 | W = 1, W = 0 즉시, W < 0 거부 (정본 §72) | `m4.py:109-110,245-254` | 대조 W ∈ {0,1,2} H = 3 105/105(N1 되돌림) | 일치 |
| 45 | 비가역 W+1 (M4 :291) | `m4.py:242,250` | 코드 | 일치 |
| 46 | τ = 1(접촉 근처 0), near ≤ 5 cm (E :487) | `core.py:44-56`, `m4.py:198-207` | 98/98(5.00 참·5.01 거짓) | 일치 |
| 47 | conformal α 0.01·w 5 (E :487) | 정본 §61·§64 | 정본 기록 | 정본 기록 |
| 48 | STALE_MAX 1.5 s (E :487, M4 :288) | `m4.py:88,216` | 틱 0.01·0.02·0.05 s × 곱·반올림·누적 합 × 20,000 위치: 정확히 1.5 s 유지·+1틱 폐기 위반 0(옛 원시 비교면 174/6,000 폐기) | 일치(§75 N1) |
| 49 | C2 = VLM Stream, 5 s 타임아웃 (E :495, M4 :332) | `conditions.py:22`, `m4.py:219-225,299-303` | 4,000 위치 5.00 s 적용·5.05 s EMPTY 위반 0; 대조 C2 H = 3 105/105(N1 되돌림) | 일치(§74 D2, §75 N1) |
| 50 | C0·C1·C3·C4·C5·C6 정의 (M4 :329-346) | `conditions.py:20-26` | 대조 H = 3 모든 조건 105/105(N1 되돌림) | 일치 |
| 51 | H 1과 3 비교, H = 1은 같은 스텝을 앞당겨 2~3회 묻기 (정본 §2 :18, M4 :188-189, :273, E :183, :197, :236) | `m4.py:61-69,103-112,183-189`, `closed.py:151-166,290-292,405,433` | 가짜 세계 0.307 s·lead 1.0: 스텝당 묻기 3·받아들인 표 2·확정 294(C5·C3·C6); 파드 Isaac C5 받아들인 표 중앙값 2·`commits` 187, C3 240; 대조 H = 1 확정 있는 판 C3·C5 74 → 84, C6 26 → 51 | 일치(§75 D1) |
| 52 | n_LA 2 (M4 :275) | `m4.py:285-287` | 98/98 | 일치 |
| 53 | 조기 호출은 주기 슬롯을 당겨 씀 (M4 :262) | `core.py:414-421` | 시험 `test_early_call_pulls…`(총 호출 수 같음) 통과 | 일치 |
| 54 | FLIP_TH 끔 (정본 §6) | `m4.py:89,277` | 시험 `test_flip_gate_off…` 통과 | 일치 |
| 55 | 경계 직후 W 2·γ 1.0 등 (M4 §3 #5, §4.3) | 없음 | 정본 §73 | SCOPED S9 |
| 56 | P1 2 cm·near 5 cm, P2 0.5 s, h_lift 3 cm, tilt 30° (E :420-421) | `config.py:8-11` | 설정값; near 경계 98/98 | 일치 |
| **57** | 라벨 규칙 적격 ≥ 0.90·차 ≤ 0.02는 단순한 쪽 (prereg_labeler :11-12) | `sim/labeler.py:131-142` | 27/30 적격·0.8999 부적격; 차 0.02 → plan, 0.0201 → 판별력 큰 쪽; 적격 없음 → None; D-short 동점 → 긴 h | 일치(N7) |
| 58 | 폐루프 RD 재표집 = layout 짝 (EVAL :184) | `closed.py:56,59-131` | 파드 `meta.bootstrap.unit` "layout seed …" | 일치 |
| 59 | 주 RD = Score 기반 (EVAL :182) | 성공률 | 1차판 [가정] | N9 |
| 60 | H1/H2/H3 판정 (EVAL :186-190) | 없음 | 기준선 | SCOPED S1 |
| 61 | M4b 자기 사전 등록 2,000회 | `m4b/*` | 정본 §72 예외 | 일치 |
| 62 | E-M4-lat STALE_MAX 비례 (M4 :378, 정본 §74 보충) | 없음(지연 큐 없음) | 정본 기록 | SCOPED S11 |
| 63 | (새) H = 1 창이 비면 d̂ 뒤 첫 스텝 (정본 §75 (ii), 보충 (3)) | `m4.py:67-69` | 7 d̂ × 7 lead × 700 송신 틱 = 34,300경우 정확 유리수 창과 불일치 0, 20 Hz 곱·누적 합 4,000경우 0; lead 0.2 → [첫 스텝] | 정본 기록 |
| 64 | (새) `lead_max` ∈ {1.0, 1.5}, 기본 1.0, 최종값 E0 뒤 (E :183, :197, 정본 §75 보충 (1)) | `m4.py:106,111-112`, `closed.py:291-292,405,433` | 0·−1·NaN 거부(파드 rc 1 × 3), inf는 통과 후 첫 호출에서 실패(N4); 파드 `spec`·`meta.m4_lead_max` 1.0; H = 3에서 무관(`m4_config("C5", 3)` = 기본) | 일치(N3·N4) |
| 65 | (새) E0 판정 4 오프라인 스텝당 표 수, lead_max 1.5 (E :183, :197) | `analysis/latency.py:85-88` | 0.307 s·lead 1.0 = 3 = 런타임 묻기 수 | 일치(E0 미실행) |
| 66 | (새) E-M4 판정 7 H = 3 대 H = 1 (M4 :365) | 다스텝 DecCall 없음 | 0.3 s 격자 H = 3 = H = 1 원장 | SCOPED S12 |

### 5.1 10회차 표(`r7_cycle10.md` §5)·수정 대조표(`r7c10_fixes.md` §4)와의 차이
- 행 51: 10회차 불일치(D1) → 이번 **일치**(스텝당 표·확정·요청 시각을 원장·Isaac에서 셈).
- 행 2: 1–10회차 "일치"(코드 값만) → 이번에 선택 함수·경계 정의를 구성 입력으로 돌려 값은 맞고 **정의가 정본에 없음(D-2)**.
- 행 5·13·16·23·25·26·31·34·38·57: `r7c10_fixes.md:81`이 "행동으로 바꾸지 않은 행"으로 남긴 것 — 모두 구성 입력·파드 실행으로 확인, 일치.
- 행 48–50: `r7c10_fixes.md` "H = 3 비트 동일(N1 포함)" → 경계 지연 판에서는 N1 때문에 다름(N1, 의도된 변경).
- 새 행 63–66(§75·§75 보충).

## 6. 확인한 것 (근거)

### 6.1 10회차 수정의 독립 확인 (과제 2)
- **H = 1 표 수·확정**(가짜 세계, 모의 선택기, 2,000틱, `hand11.py`; 표 = (묻기 중앙값, 받아들인 표 중앙값, `commits`)):

| 지연 | C5 lead 1.0 | C5 lead 1.5 | C6 lead 1.0 |
|---|---|---|---|
| 0.15 s | 3, 2, 299 | 4, 2, 309 | 3, 2, 299 |
| **0.307 s** | **3, 2, 294** | 4, 2, 304 | 3, 2, 294 |
| 0.45 s | 2, 2, 265 | 3, 2, 293 | 1, 1, 58 |
| 0.6 s | 2, 2, 259 | 3, 2, 288 | 1, 1, 0 |
| 0.9 s | 1, 1, 5 | 2, 2, 249 | 1, 1, 0 |

  C3은 C5와 같은 수. C6은 겹침이 없어(in-flight 1) 지연 ≥ 0.45 s에서 스텝당 1표 — 조건 정의대로. 받아들인 표 중앙값이 묻기 수보다 작은 것은 LA-2가 두 번째 표에서 확정하면 세 번째 표가 확정 불변 규칙대로 `log_only`이기 때문(파드 C5 `log_only` 32).
- **요청 시각 일치**: 0.307 s, 격자 위 호출에서 실행한 57스텝 모두 스텝 k를 묻는 호출 시각 오프셋 = `e05.vote_steps` (−0.33, −0.66, −0.99), 개수 3 = `latency.votes_per_step(0.307, 0.33, lead 1.0)`. 파드 Isaac C5: 3표 스텝의 오프셋 (−0.32, −0.65, −0.98) 등(송신이 한 틱 밀린 경우), 재생 오프셋과 ≤ 0.01 s.
- **무작위 대조**(`char11.py`, 수정 에이전트·10회차 스크립트와 따로 씀: 씨앗 2111, 지연 {0.2, 0.307, 0.33, 0.5, 0.75, 1.0, 1.4, **1.5**, 1.6, 2.5, **5.0**} s + 흔들림(0.15–0.45 s, 7 % 1.3–1.8 s), **시작 시각 0–8 s 무작위(호출 격자가 스텝 격자 밖)**, H ∈ {1, 3}, W ∈ {0, 1, 2}, 잡음 선택기 0/15/30 %, HEAD lead ∈ {1.0, 1.5, 0.2, 2.0}, 1,200–2,400틱; 210판 × C0–C6 × 3판본(6522fda / 43cd1ff / 43cd1ff에서 `older_than`만 원시 비교로 되돌린 판); 비교 = 행동 바이트 sha256·원장 counts·슬롯·실행 계정·호출 수·단계·epoch, `char_cmp11.txt`):
  - **H = 3(105판): 6522fda = 43cd1ff(N1 되돌림) 105/105 × 7조건 모두 비트 동일.** 6522fda 대 43cd1ff: C0·C1·C6 102, C3·C4·C5 99, C2 97 — 다른 판은 모두 지연 1.5 s(C2는 5.0 s) = §75 N1(N1 NOTE).
  - H = 1(105판): 6522fda와 같은 판 18–19(의도된 변경); 확정이 한 번이라도 있는 판 C3·C5 74 → 84, C6 26 → 51, C0·C1·C2·C4 0 → 0.
- **`lead_max` 검증·meta**: 파드 `closed --m4-h 1 --m4-lead-max 0 | -1 | nan` → "M4 lead_max = … must be > 0 s (canon §75)" rc 1 × 3; 폐루프 판 `spec_standard.json`·`meta.m4_lead_max` 1.0, `meta.m4_H` 1. inf는 N4.
- **나이 경계**(`older_than`): 틱 간격 0.01·0.02·0.05 s × 시각 형식 3개(곱 `k·dt` = Isaac `_steps·step_dt`, 6자리 반올림 = 가짜 세계, 누적 합) × 한도 1.5·5.0 s × 20,000 위치 — 정확히 한도인 나이 유지·한 틱 더 폐기 **위반 0**. 원장 수준(0.05 s 곱 4,000 위치): C5 1.5 s 표 유지, C2 `decision` 5.00 s 적용·5.05 s EMPTY 위반 0.

### 6.2 10회차 이후 바뀐 기록
| 위치 | 주장 | 확인 |
|---|---|---|
| `handoff.md:102` | 10회차 수치, 수정, 로컬 860/11(841 + 15 + 조사 에이전트 4), 파드 898/3, Isaac C5 `commits` 187, 연속 0 → 11회차 | 수치 모두 이번 판과 같음(로컬은 커밋 사본이라 856 = 841 + 15, 파드 898/3, Isaac C5 187·C3 240 재현); "[결정 필요]" 판정 7은 **D-1** |
| `handoff.md:3`·`:5`·`:83` | 정본 §1~§75 | 맞음(머리 시각 07:40 < 보충 07:44 → D-1) |
| `handoff.md:101`, `r7c9_fixes.md:15` | [해결 R7 10회차 D-1: §74 보충 (가)] | 표시 있음 |
| 정본 §75·보충 | 코드 줄·동작 | 코드와 맞음(§5 행 15, 48–51, 63–66) |
| `r5_closed_loop.md:33`, `r7c8_fixes.md:20`·`:93`, `r7_cycle9.md:124` | [정정 R7 10회차 D1, §75] | 표시 있음 |
| `e3st.md:66`, `planner_dev.md:102`, 옛 계획서 `:400` | [정정 R7 10회차 N4] | 표시 있음 |
| `direction-log.md:58`, `draft-log.md:448` | 10회차 판정·수정 | 보고서와 같음(07:00·07:40 시점 줄) |

### 6.3 완료 정의 1–5 (직접 돌린 것, 과제 4)
| 정의 | 판정 | 실행 |
|---|---|---|
| 1 데이터 | 충족 | 파드 `venv_e3st`로 R2 DEV `/data/harvest/r2/dev/*/*/P0` 전 편 `validate_episode`: **36/36 errors 없음**, 모두 DEV, 성공 mug_tray 12/12·mug_marker 12/12·bottle_tray 8/12(07:52:59 UTC). LeRobot 내보내기 시험 **6 passed**(07:53 UTC). |
| 2 모델 | 충족 | **단계 B S-E2E**(GPU 2): `stageb_train train --data se2e --run r7c11_se2e --max-train 40 --max-steps 6 --batch 2 --max-val 4 --eval-every 3 --reload-check --seed 44` → rc 0(07:49:32–07:50:18 UTC), 검증 dec 3.526 → 2.890 → 2.377(fm은 N8), `save_load` `max_abs_action_diff` **0.0**, 재적재 전후 eval 같음. CUDA 시험(GPU 2, `tests/train` + `test_fused_action.py`, 서빙 포함) **102 passed**(07:53:38–07:54:15). |
| 3 폐루프 | 충족 | 모의 `closed --model mock --split dev --seeds 7 --conditions C5,C3 --max-seconds 40 --astra mock --isaac-gpu 1 --inst-prefix r7c11 --m4-h 1`(07:50:18–07:52:59) → `CLOSED_DONE`, C5·C3 성공 1.0. 로그 행: `call` 50 + 49행 전부 64자리 hex `request_sha256`·`image_sha256` {cam_head, cam_wrist_right}·`canary_id` `cn20260924_mock_ae0d1a`, `astra` 2 + 2행 hex·{cam_head}·"none". `meta.bootstrap` n_boot 10000(= `result.bootstrap`), `meta.prereg` OK, `meta.m4_H` 1, `meta.m4_lead_max` 1.0, 호출당 슬롯 3, 받아들인 표 스텝당 중앙값 2, `commits` C5 187·C3 240, `dropped_stale` 0. |
| 4 평가 | 충족 | 모의 한 명령(`venv_vllm`, GPU 없음): `e05 --data jsel_dev/P0,P1 --split dev --episodes 2` → `E05_DONE`(claim `a_as_stabilizer`, 판정 키 j2·j5·j7·j8·j10 모두 있음, 같은 시각 K 3), `rd … --episodes 1` → `RD_DONE`, `calib --fit-data pool --fit-split pool --heldout jsel_dev/P0 --heldout-split dev --episodes 4` → `CALIB_DONE`. 모두 rc 0, n_boot 10000, prereg OK. |
| 5 운영 | 충족 | 산출 모두 `/data/harvest` 아래, `meta`에 git·`code_sha`·카나리·시드·모델·부트스트랩·사전 등록 해시·`m4_H`·`m4_lead_max`, 정리 뒤 흔적 없음(N10). |

### 6.4 C. 가드 (파드, 변수 없이, `CUDA_VISIBLE_DEVICES=""`; 거부 뒤 출력 폴더 없음 — "no guard output dirs")
- `e05 --split cal`, `calib --fit-split cal`, `rd --split test`, `e05 --split test`, `closed --split test --seeds 1000`, `closed --split cal --seeds 549` → "--split X refused: set HARVEST_ALLOW_SPLIT=X (main session only …)". `closed --split dev --seeds 30`·`--seeds 1149` → "not in split dev range(0, 30) (refused, never opened)". `--isaac-gpu 2` → "0 or 1 only (GPU 2 never renders)". `--m4-h 0` → ValueError. `--conditions C2p` → "runtime has [C0…C6]". **`--m4-h 1 --m4-lead-max 0`·`-1`·`nan` → ValueError(§75)**. `HARVEST_ALLOW_SPLIT=dev closed --split test --seeds 1000` 거부. `gen gen --seeds 10005-10006` → "R2 generates DEV 0-29 only (… --confirm-train)". **16건 모두 rc 1.**

### 6.5 A. 테스트
- 로컬(`D:\tools\scratch_qdd\r7c11\repo` = 43cd1ff archive, `python -m pytest -q -rs -p no:cacheprovider --basetemp=…\r7c11\pt1`, `PYTHONDONTWRITEBYTECODE=1`, TMP = scratch, 07:45:40–07:48:00 UTC): EXIT 0, **856 passed · 11 skipped**(진행 표시 856, F·E 0; 건너뜀 = torch 없음 7, inspect_robots 2, pyarrow 1, TODO(P3) 1). 856 = 841(6522fda) + 15(`test_r7c10_prereg.py`); 재생 조사 에이전트의 커밋 안 된 시험 4개는 사본에 없음.
- 파드 CPU(`venv_train`, PYTHONPATH = 코드 사본 + `ir/env.sh`, `CUDA_VISIBLE_DEVICES=""`, TMPDIR·pyc·카나리 루트 = scratch, 07:49:03–07:50:31 UTC): **898 passed, 3 skipped**, EXIT 0(건너뜀 CUDA 없음·pyarrow·TODO(P3), 경고 1). 파드 CUDA 102 passed, LeRobot 6 passed.

### 6.6 같은 사실 grep (과제 3, `D:\tools\scratch_qdd\r7c11\sweep11.py` → `sweep11.txt`, 추적 파일, `third_party/` 제외, 정정 표시 유무 분리)
- H = 1 "한 요청 = 한 스텝"·"호출당 슬롯 1": 사전 등록·설계 문장(정본 §2, M3·M4·D1 문서 — H = 1의 정의 문장)과 기록 줄뿐, 현재 시제로 "런타임 H = 1 = 스텝당 1표"라 적은 곳 0(`r5_closed_loop.md:33` 정정 표시 있음).
- `lead_max`·앞당겨 묻기: 코드(`m4.py`·`closed.py`·`e05.py` 설명·`latency.py`)·정본 §75·기록 문서 — 서술 일치(기본 1.0, 후보 {1.0, 1.5}, H = 3 무관, §59 "lead"와 다른 뜻).
- `[결정 필요]`·"메인 확인" + H·lead·판정 7: **D-1**(`handoff.md:102`, `r7c10_fixes.md:72`·`:83`–`:87`); 정본 §75 본문 `:648`·`:649`는 같은 절 보충 `:657`이 덮음; `direction-log.md:58` 기록 줄(N9); 옛 설계 문서의 D11 "[결정 필요] H = 1 대 3"(`SUMMARY.md:280`, `M3:240`, `M4:438`, `D11:196`)은 단계 2 설계 기록(E-M4로 정한다는 뜻, §75와 모순 없음).
- "H = 3 비트 동일"·155/155: 정본 §75, `r7c10_fixes.md`, handoff — 표본 범위 N1.
- 나이 비교(`older_than`·`T_EPS`·원시 `now - v.t_state >`): 코드 두 곳 모두 `older_than`; 원시 비교는 사전 등록 의사코드 `M4:203`뿐(사전 등록 인용).
- 0.67 s 문턱: 정본 §75 보충·`r7c10_fixes.md:85` — 격자 기준(N3).
- 시험 수(8xx passed): 기록 줄은 모두 그 판 수치와 같음(§6.2).
- 정본 범위 `§1~§N`: `handoff.md:3`·`:5` = §75(맞음), 나머지는 날짜 붙은 옛 문서.
- `마지막 갱신`: `handoff.md:3` 07:40 UTC(D-1).
- 옛 사실 재확인: γ 소수 0.67(옛 계획서 초안 `:1430`, `m4.py:29` 사전 등록 인용, `test_m4.py:82` 3/4 경우 — N9), "C2 = 겹침 + newest"(`r6_eval.md:46` 정정 표시), 부트스트랩 2,000(m4b 예외·시험 인자·기록 줄), "W=2 흡수"(정본 §5 원문·정정 표시 있음), near "< 5 cm"(정정 표시 또는 0.049 m 참 서술) — 새 누락 0.
- 구현 이름 대조: `early_ask_steps`(`m4.py:61`, `target_slots :186-187`, `e05.vote_steps` 설명 :47, 정본 §75), `older_than`(`:216`, `:301`), `m4_lead_max`(spec `:405`, meta `:433`), `share_at_least`·`agree == "stream"`·`direction="greater"` 10회차와 같음.
- 풀 "경계 사례": **D-2**(정본 0건, `pool.md:25`, 옛 계획서 `:1394` 다른 정의).
- 카메라(§47): 폐루프 이미지 키 {cam_head, cam_wrist_right}(§57), 이번 변경 파일에 카메라 서술 없음. GPU: "GPU 2 = 렌더 금지" 서술·가드 일치. 시드·날짜: 정본 §75 07:26 UTC·보충 07:44 UTC·커밋 07:44:12 UTC·수정 보고서 07:40 UTC 서로 맞음.

### 6.7 F. 규칙·위생
- 저장소: 작업 트리는 읽지 않고 쓰지 않음(archive만), 이 보고서 한 파일만 추가. 로컬 C:: N10(이 저장소 산출물 0). 파드: 시작 전·후 목록 대조로 내 항목만 삭제(머리 규칙 준수 줄), 내 프로세스 0(끝에 `ps`), 재생 조사 Isaac(GPU 0)·`/data/harvest/data`·`r2/dev`·`canary`는 읽기만(카나리 시험은 scratch 루트 `HARVEST_CANARY_ROOT`).

## 7. 다음 순회 전에 할 일 (제안)
1. **D-1**: `handoff.md:102`의 판정 7 "[결정 필요: 메인 세션]"과 lead_max "E0 뒤 메인 확정"에 [해결: 정본 §75 보충 07:44 UTC — (1) lead_max 1.0 확정·E0 뒤 §2.7-4 규칙으로 재결정, (2) 판정 7 SCOPED, (3) 빈 창 대체 규칙 채택] 표시, `r7c10_fixes.md:72`·§5에도 같은 표시, handoff 머리 시각 갱신.
2. **D-2**: 정본에 풀 설계의 열린 값 한 줄 — "경계 사례 = `ambiguous` 또는 다음 0.33 s 안의 등록부 술어 변화(앞 방향), 판당 round(10 × 0.30)개, 부족 시 다른 층에서 채움, `w_natural` = 역포함확률; 섭동 배정 P0·P1·P2 각 40편 고정 순열" + 근거(`pool.md:25`), 옛 계획서 `:1394`에 [정정] 표시.
3. N1 문구("나이 경계 판 제외"), N4(`lead_max` 유한값 검사)는 다음 코드·문서 정리 때. N2·N3·N5는 E-M4·E0·E0.5 결과 문서에 해석으로.
