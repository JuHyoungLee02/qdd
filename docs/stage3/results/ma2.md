# E-MA2 VLA가 Astra `edit` 명령을 따르는가 — 결과: NONE (VLA 입력에 명령을 넣지 않고 §11 코드 편향만)

작성 E-MA2 에이전트, 2026-09-26 UTC. 사전 등록 `docs/stage3/prereg_ma2.md`(문서 시각 2026-09-25T23:32:45Z, 커밋 `fde4f6a` 23:33:15Z — 어느 본 판보다 먼저; 본 실행 23:33:49Z 시작) 규칙을 그대로 적용했다. 판정 스크립트 `tools/ma2/ma2_verdict.py`(등록 때 고정, 결과 뒤 수정 없음, 파드 고정 사본 `/data/harvest/code_ma2` = `fde4f6a`로 실행). 근거: 연구 문서 `molmoact_deepdive_2026-09-26.md` §6.2, 설계 §11·§14, 정본 §84.

## 1. 판정 (사전 등록 6절, 평가 1,200 스냅샷 — R2_TRAIN eval 분할, sha `61b2bce64ee1`)

| 조건 | C0 없음 | C1 문장 | C2 화살표 | 문턱 | |
|---|---|---|---|---|---|
| 준수율(e90·e180 쌍 1,250개: 결정 칸 일치 **그리고** 청크 cos > 0.5) | 0.0008(참고 바닥) | **0.1768** | **0.0008** | 둘 다 < 0.6이면 NONE | 둘 다 < 0.6 ✗ |
| 준수율 C2 − C1 | | | **−0.1760** [−0.198, −0.155] | ≥ +0.10 | ✗ |
| 명령 없는 정확도(6,000항목) | 0.9388 | 0.9233 | 0.9293 | | |
| 정확도 − C0 | | **−0.0155** [−0.021, −0.010] | **−0.0095** [−0.015, −0.004] | ≥ −0.01 | C1 ✗ · C2 ✓ |
| FULL 결정 p95 | 96.0 ms | 95.8 ms | 91.0 ms | C2/C1 ≤ +10 %, C1/C0 ≤ +10 % | −5.0 % ✓ · −0.2 % ✓ |

→ **판정 NONE**(규칙 1: C1·C2 준수율이 모두 0.6 미만). 등록 0절의 결정대로 **융합 VLA 입력에는 Astra `edit`을 넣지 않고, 설계 §11의 코드 편향(조이스틱·손끝 기준에 더해 부드럽게 반영)만 쓴다**(설계 유지). 규칙 2(C2)는 준수율 차가 음수라 실패, 보충 규칙 3(C1)은 준수율 0.177 < 0.6과 정확도 −0.0155 < −0.01 두 조건으로 실패. 등록 0절의 '학습 규모' 표시 조건(참 명령 e0 준수율이 **둘 다** < 0.6)은 해당하지 않는다(C1 e0 0.762, C2 0.470) — C1은 제자리 명령을 배웠다.

## 2. 판별 값 (판정 밖 포함, `summary.json` — 원 기록에서 따로 다시 셈, 판정 파일과 같음)

**준수율 성분**(쌍마다 eligible 625; 결정 = 예측 `dir_xy`가 명령 칸, 청크 = FK 손끝 변위 xy와 명령 xy cos > 0.5):

| 모델 | e0 결정 / 청크 / 둘 다 | e90 결정 / 청크 / 둘 다 | e180 결정 / 청크 / 둘 다 |
|---|---|---|---|
| C0(명령 못 봄) | 0.381 / 0.770 / 0.350 | 0.006 / 0.134 / 0.002 | 0.000 / 0.070 / 0.000 |
| C1 문장 | **0.976** / 0.781 / 0.762 | **0.970** / **0.275** / 0.266 | **0.976** / **0.093** / 0.088 |
| C2 화살표 | 0.610 / 0.754 / 0.470 | **0.006** / 0.146 / 0.002 | **0.008** / 0.080 / 0.000 |

- **C1: 결정층은 명령을 따르고 expert 청크는 따르지 않는다.** 돌린 명령에서도 예측 `dir_xy`가 97 %로 명령 칸을 고르는데(숫자 문장을 읽음), 그 결정을 조건으로 준 청크는 90°에서 28 %, 180°에서 9 %만 명령 쪽으로 움직인다 — C0의 우연 수준(13 %·7 %)과 비슷하다. expert는 KI stop으로 백본 문맥(명령 문장이 섞인 은닉 상태)과 결정 임베딩을 보지만, 학습 데이터에서 결정과 영상이 늘 같은 쪽을 가리켜 **영상·고유감각이 정한 궤적을 그대로 낸다**. 준수율 실패의 원인은 결정이 아니라 청크다.
- **C2: 화살표는 제자리(참) 명령에서만 도움이 되고 반대 명령은 무시한다.** e0 결정 0.610(C0 0.381보다 높음 — 화살표가 영상과 같은 쪽일 때는 읽는다), e90·e180 결정 0.6–0.8 %(C0과 같음). 머리 영상의 짧은 화살표(xy 2 cm ≈ 수 px–십여 px, 손끝이 팔에 가리는 자세 있음)가 영상 속 목표와 어긋날 때 모델은 목표 쪽을 고른다.
- 질문별 명령 없는 정확도: C0 dir_xy 0.933 · dir_z 0.924 · mag 0.860 · target 1.000 · phase 0.978 / C1 0.926 · 0.910 · 0.818 · 1.000 · 0.963 / C2 0.933 · 0.904 · 0.839 · 1.000 · 0.970. C1의 하락은 주로 `mag_coarse`(−0.042)와 `dir_z`(−0.014) — 명령을 준 절반의 표본이 방향 정답을 명령 칸으로 바꿔(등록 2절) 명령 없는 표본의 방향·크기 감독이 줄어든 몫으로 본다(해석, 따로 검증 안 함).
- 과제별 명령 없는 정확도: C0 bottle 0.932 · marker 0.947 · mug 0.939, C1 0.921 · 0.929 · 0.921, C2 0.926 · 0.932 · 0.930.
- 지연(`tools/ma2/ma2_latency.py`, H200 GPU 2, 평가 집합 50스냅샷 × 4회 교차, 예열 30, 가중치 c0, 다른 줄 끝난 뒤 01:05–01:08Z, 부하 평균 7.6 → 4.8): FULL p50 84.8 / 86.8 / 84.6 ms, p95 96.0 / 95.8 / 91.0 ms, GPU p95 78.5 / 79.5 / 79.6 ms; 시각 토큰 356(세 형식 같음), 프롬프트 토큰 485.8 / 515.1 / 485.8. C2의 메모리 그리기는 FULL p95에 보이는 비용이 없다(C2가 C0보다 5 % 빠른 것은 측정 잡음 범위).
- 학습 중 검증 150(원천별 50) `dec`·`dec_acc`(step 0 → 500 → 1000 → 1500 → 2000): C0 5.37/0.312 → 0.41/0.853 → 0.28/0.884 → 0.22/0.916 → 0.20/0.923, C1 5.09/0.397 → 0.34/0.885 → 0.24/0.919 → 0.22/0.923 → 0.19/0.933, C2 5.29/0.317 → 0.47/0.832 → 0.33/0.884 → 0.28/0.909 → 0.27/0.908(검증 표본의 절반은 명령을 준 입력) — NaN·발산 없음. 청크 관절 오차(`sample_mae_arm_rad`) 마지막 0.0181 / 0.0177 / 0.0182 rad.
- **해석 한계**: 오라클 계획기 시연(한 장면에서 경로가 거의 하나 — MolmoAct 조종 시연처럼 "같은 목표로 다른 경로"를 모으지 않음), 오프라인(폐루프 아님) 준수율, 시드 하나, §56 소규모(2,000스텝, 명령 표본 약 8,000). "VLA 입력의 명령이 행동을 조종하지 못한다"는 이 데이터·규모에서의 결과이며, 청크가 결정을 따르게 하려면 결정과 궤적이 어긋나는 시연(경로 다양화)이 필요하다는 MolmoAct의 교훈(연구 문서 §2.9·§4.1)과 맞는다.

## 3. 자체 검사 관문 (user-log 87)

| 관문 | 결과 |
|---|---|
| 등록 전 확인 | R2_TRAIN 결과 문서 `b2b0b3e`를 파드에서 다시 셈(`logs/ma2/verify_r2.json`): 행 1,495,348·라벨 152,409·유효 5,126(fit 4,877 / eval 249)·폴더별 시드 집합과 섭동 띠·행 시드 범위 모두 일치. FK 확인 cos 중앙값 0.9999(1,161). 투영 그림 2장 확인(자홍 → 청록 교체). |
| 싼 사전 실행 | 23:18–23:26Z 통과(등록 0.3절). 첫 시도에서 `ma2eval`이 학습 적재기를 불러 평가 전용 행(학습 0)으로 멈춤 → 평가는 `stageb_data.load_for_training`으로 고치고 다시 통과(등록 전) |
| (3) 데이터 수 | 통과: 세 판 `n_train + n_val` = 144,562 + 7,308 = **151,870**, `ma2_apply` C1·C2 `given` 75,895(49.97 %)·`missing` 0, C0 `given` 0; 평가 세 파일 모두 1,200 스냅샷 × 조건 수 정확히 한 번(판정 스크립트 입력 검사) |
| (1) NaN·발산 | 없음(세 판 학습 기록 NaN 0, 검증 `dec` 5.1–5.4 → 0.19–0.27) |
| (2) 시간 2배(90분) | 없음: 학습 루프 2,306 / 2,360 / 2,317 s(판 전체 40.6 / 41.8 / 41.1분) |
| (4) 실행 경로 | 통과: 등록 커밋 `fde4f6a`의 변경은 새 파일(`harvest/train/r2_ma2.py`, `tools/ma2/*`, 시험 4개)과 문서뿐 — `stageb_*`·`prefix_share`·`se2e_*`·`harvest/runtime`·`harvest/datagen`·검증기 무수정(`git show --stat fde4f6a`); 고정 사본 해시 = 등록 해시(`CODE_HASHES.txt`); C0 판 `ma2_apply` given 0 |
| 재설계 | 없음 |

- `prompt_config` sha: C0 `b41669e54c39`, C1 `07b43550d6fc`, C2 `54a769eb221e`(모두 `ma2@v1` 표지 — 기본 체크포인트와 섞이지 않음).

## 4. 검증(결과 뒤, user-log 87)
- 판정 파일과 따로 쓴 집계 스크립트(`logs/ma2/summarize.py`, 원 평가 기록에서 칸·코사인을 다시 계산)가 준수율 0.0008 / 0.1768 / 0.0008, 명령 없는 정확도 0.9388 / 0.9233 / 0.9293을 같은 값으로 냈다. 평가 기록의 돌린 명령은 판정 스크립트가 다시 계산한 값과 1e-5 안에서 같다(입력 검사).
- 시험: 로컬 전체 1,156 통과·21 건너뜀(등록 때), 파드 새 시험 + `test_se2e_cam3_cli.py`·`test_stageb_torch.py` 58 통과.
- 절차 이탈(결과와 무관, 기록 의무 P03): 파드에서 `python3 -c` 2회(사전 실행 뒤 `views.json` 요약 출력, 중간 요약 출력 — 둘 다 읽기 전용), 로컬에서 `dry.sh` 초안에 빈 heredoc 한 줄을 썼다가 실행 전에 지움. 기다림 루프 하나가 `pgrep -f`로 자기 `bash -c`와 일치해 끝나지 않을 수 있음을 발견하고 멈춤(남의 프로세스 영향 없음 — 새 함정 P76).

## 5. 실행 기록

| 항목 | 값 |
|---|---|
| 코드 | 파드 `/data/harvest/code_ma2` = `git -c core.autocrlf=false archive fde4f6a`(LF), `CODE_HASHES.txt`; 드라이버 `logs/ma2/run_ma2.sh A|B` |
| 데이터 | `/data/harvest/data/ma2/`(뷰 18폴더 — img는 원본 링크, 결정 행 151,870, `cmd/*.jsonl`, `eval_set.json`, 화살표 79,495장 약 23:23–23:24Z) |
| 시각(UTC) | 사전 실행 2026-09-25 23:18–23:26, 등록 23:32:45(커밋 23:33:15), GPU 2: c0 23:33:49–00:14:28 → c2 00:14:28–00:55:34 → c2 평가 –01:05:38 → 지연 –01:07:57; GPU 3: c1 23:33:49–00:15:37 → c0 평가 –00:18:31 → c1 평가 –00:28:48; 판정 01:08:18 |
| GPU | GPU 2 ≈ 1.6 h(사전 실행 약 0.1 h 포함 ≈ 1.7 GPU-h), GPU 3 ≈ 0.9 GPU-h → **합 ≈ 2.6 GPU-h**(예산 2–4). GPU 0·1 미사용 |
| 체크포인트 | `/data/harvest/ckpt/ma2/c{0,1,2}/last`(학습 기록 사본 `train.out`) |
| 로그·판정 | `/data/harvest/logs/ma2/`: `train_c*.out`, `eval_c*.jsonl`, `latency.json`, `verdict.json`, `summary.json`, `verify_r2.json`, `probe1.json`, `proj_sheet*.jpg`, `dry/`; 로컬 요약 `D:\tools\scratch_qdd\ma2` |
| 비용 | 유료 API 0건 |
