# E-MA1 MolmoAct식 손끝 궤적 2×2 — 결과: 관문 G0 실패 → R2 미준비로 중단

> **후속 [2026-09-25 17:54 UTC, 메인 결정]**: 요인 A는 카메라가 필요 없는 로봇 기준 좌표 3D 궤적 보조 손실로 **E-MA1b**(`prereg_ma1b.md` → `results/ma1b.md`)에서 먼저 잰다. 이력 덧그림(O)과 영상 좌표 궤적(trace5)은 **R2_TRAIN 완료 뒤 E-MA1 재등록**을 기다린다.

작성 E-MA1 에이전트, 2026-09-25 UTC. 사전 등록 `docs/stage3/prereg_ma1.md`(2026-09-25T17:32:31Z, 커밋 `06355f0` 17:32:42Z, push 17:32:46Z — G0 실행·어느 학습보다 먼저; 문서 sha256(LF) 앞 16 `cc49b650680a489d`) 규칙을 그대로 적용했다. 근거: 정본 §84·§84 보충 1(user-log 85), 연구 문서 `docs/research/molmoact_deepdive_2026-09-26.md` §6.1.

## 1. 판정 (사전 등록 2절 규칙 그대로)

| 단계 | 유효 기준점 | 오차 중앙값 (≤ 12 px) | 90분위 (≤ 30 px) | 판정 |
|---|---|---|---|---|
| 명목 카메라 모델(120장 중 유효 119) | 119 (실패 1, 사람 대체 0) | **100.9 px** ✗ | **264.5 px** ✗ | 실패 |
| PnP 보정(원천별 6자유도, 보정 반 60 → 검사 반 59) | 59 | **86.9 px** ✗ | **212.1 px** ✗ | 실패 |

- **G0 실패** → 등록 규칙: "R2 시뮬로 이동 — R2_TRAIN 데이터가 준비돼 있으면 새 등록, 아니면 보고하고 멈춘다".
- **R2_TRAIN 상태(2026-09-25 17:3x UTC 확인)**: 하드 리셋 빌드(`code_r2train_e8e1864`, 시드 10000–10999 계획)로 **생성 중** — 작업자 5개가 `--kinds P0 --seeds 10000-10599`를 돌리는 중(큐 잠금 시드 10494–10495), `/data/harvest/r2/train`에 과제 폴더(dr·standard × 3과제)만 있고 단계 B 학습 행 조립 전. **준비 안 됨 → E-MA1은 여기서 멈춘다.** 네 칸(`none+none`·`A+none`·`none+O`·`A+O`) 학습, 예측, 지연 측정, 2×2 판정, 시드 1 반복은 **돌리지 않았다**. R2_TRAIN 작업자는 건드리지 않았다. [정정 2026-09-25 19:55 UTC, 메인 확인 요청: 경로 `/data/harvest/r2/train`은 **있다**(19:07Z `ls -ld` 확인, `/data/harvest/data/r2`와 다른 곳 — 거기엔 옛 `train_pilot_prefix_6b013ac`만 있다). 다만 "과제 폴더만 있고 행 조립 전"은 틀렸다: `dr|standard/<과제>/` 아래에 편 폴더 `P0/`(각 1,802항목)와 과제별 `P0.stageb.jsonl`(dr 5,539·8,657·8,609행, standard 7,579·11,518·11,740행)이 이미 있었고, 19:07Z에는 P1 섭동 종류를 생성 중(큐 잠금 시드 10690–10728, 생성 프로세스 15개)이었다. 계획한 종류·시드가 끝나지 않았다는 뜻에서 "준비 안 됨" 판단은 그대로다.]
- 따라서 A·O 어느 요인도 판정되지 않았다. §84 보충 1의 "O 불채택 → VLA 입력 덧그림 버림" 규칙은 **발동하지 않았다**(판정 없음). 설계 §13의 Astra 영상 덧그림은 이 결과와 무관하게 그대로다.

## 2. G0 세부 (사전 등록 2절)

| | n | 중앙값 | 90분위 | 평균 | 최대 |
|---|---|---|---|---|---|
| 명목 전체 | 119 | 100.9 | 264.5 | 137.6 | 752.9 |
| 명목 RB1 | 59 | 107.6 | 473.9 | 177.1 | 752.9 |
| 명목 RB2 | 60 | 71.3 | 228.4 | 98.8 | 363.3 |
| PnP 검사 반 전체 | 59 | 86.9 | 212.1 | 103.1 | 377.5 |
| PnP 검사 반 RB1 | 29 | 91.2 | 212.1 | 107.0 | 325.0 |
| PnP 검사 반 RB2 | 30 | 75.1 | 198.7 | 99.4 | 377.5 |
| (참고) PnP 보정 반 적합 전체 | 60 | 54.6 | 246.7 | 95.4 | 397.5 |

- 표본: `se2e_c1` 검증 분할, 양손 행 제외, 원천 × 활성 팔마다 30장(서로 다른 에피소드, 시드 0) = 120장, 보정 반 60. [정정 2026-09-25 18:35 UTC, R7 25회차 E-D3: 검증 분할의 에피소드가 모자란 칸이 있어 서로 다른 에피소드는 RB1-left **21**·RB1-right 30·RB2-left **28**·RB2-right 30이다. 모자란 자리는 등록 규칙(사전 등록 2절 "모자라면 고른 에피소드의 다른 행으로 채움")대로 같은 에피소드의 다른 행으로 채웠다 — 코드(`se2e_trace.g0_select`)는 등록대로다.]
- 기준점: Molmo2-ER `allenai/Molmo2-ER`@`dab22564403d2607855bb1fffb0721285b445081`(Apache-2.0, `/data/harvest/models/Molmo2-ER`, 19 GB), transformers 4.57.1(모델 카드 고정판, `/data/harvest/pylib_molmo2` 오버레이 + `venv_train` torch 2.13), bf16, 탐욕 복호, GPU 2(H200), 프레임당 중앙값 0.55 s. 기준점 실패 1장(`RB1_ep37_k0`, 답 "There are none.") — 사람 대체 없이 제외(등록대로).
- PnP 보정값(원천별, 판정 밖 참고): RB1 ω = (0.53, −0.49, 0.15) rad·t = (0.22, 0.18, 0.25) m, RB2 ω = (−0.49, 0.36, −0.04) rad·t = (−0.23, −0.28, 0.05) m — 물리적으로 말이 안 되는 크기다. 보정 반 자체에도 맞지 않는다(적합 중앙값 54.6 px) → 기준점과 명목 모델 사이의 관계가 **단일 강체 변환으로 설명되지 않는다**.

## 3. 사후 진단 (판정 밖, 결과를 본 뒤의 읽기)

`/data/harvest/logs/ma1/g0_diag.json`, 그림 `ma1_g0_sheet.jpg`(12장: 초록 + = Molmo2-ER 점, 빨강 원 = 활성 팔 명목 투영, 파랑 원 = 다른 팔 명목 투영).

| | RB1 | RB2 |
|---|---|---|
| 오차 ≤ 30 px 비율 | 0.017 | 0.150 |
| 오차 ≤ 60 px 비율 | 0.102 | 0.467 |
| 명목 투영이 영상 밖 | **31 / 59** | 5 / 60 |
| Molmo 점이 **다른 팔** 투영에 더 가까움 | 8 | 6 |
| 그 프레임 제외 시 중앙값 / 90분위 | 102.1 / 220.4 px | 56.3 / 162.0 px |
| 기준점 − 투영 중앙값 (du, dv) | (−17, **−88**) px | (−40, +2) px |

- **RB1**: 투영의 절반 이상이 영상 아래 밖으로 떨어지고 기준점이 투영보다 평균 88 px 위에 있다 → RB1 머리 각 가정(RB2 중앙값 피치 0.5492 rad 고정, [가정])이 틀렸을 가능성이 크다(실제 RB1 머리가 더 숙였거나 에피소드마다 다름). RB1 상태에는 머리 관절이 없어 데이터만으로는 알 수 없다.
- **RB2**: 머리 관절이 기록돼 있는데도 중앙값 56–71 px, 가로 편향 −40 px. 그림에서는 빨강 원이 대체로 그리퍼 근처(손목·손가락 사이)에 오지만 Molmo 점은 그리퍼 몸통·손가락 끝·때로 다른 물체를 가리킨다.
- **기준점 자체의 잡음**: 다른 팔을 가리킨 경우 14/119, 그리퍼가 안 보이는 곳을 가리킨 경우(그림 2번째 칸) 등 → Molmo2-ER 포인팅은 12 px 문턱을 재기에 충분히 정확한 기준이 아닐 수 있다(MolmoAct 라벨도 프레임별 포인팅이라 떨림이 있다는 연구 문서 §4.2의 지적과 같은 방향).
- 두 원인(카메라 모델 오차, 기준점 잡음)을 이 자료로는 분리할 수 없다. 어느 쪽이든 등록 판정은 바뀌지 않는다.

## 4. 구현 상태 (커밋됨, 기본 경로 불변)

- 등록 때 고정: `harvest/train/se2e_trace.py`(카메라·투영·보정·trace5·덧그림·드롭아웃·G0 선택/판정), `tools/ma1/g0.py`, `tools/ma1/g0_point.py`, `tools/ma1/ma1_verdict.py` + 시험 3개.
- G0 뒤 추가(TDD, 시험 먼저 실패 확인): `harvest/train/se2e_trace_model.py` — A 요인 torch 쪽(`TraceAuxHead` 21회귀, `StageBTrace` 보조 손실, `as_trace`/`with_trace_head`, 판정 밖 지표 `extra_metrics`: 궤적 픽셀 오차·예측 보기 조건 expert 청크 오차), 시험 `tests/train/test_se2e_trace_model.py`(모의 백본). R2로 옮길 때 그대로 쓸 수 있는 부분이다(보조 목표가 데이터와 무관).
- **하지 않은 것**(중단 때문): `stageb_train.py` 옵션 배선, `ma1_c1` 데이터 생성(덧그림 영상·trace5 라벨), 지연 도구, 파드 학습. `stageb_data.py`·`stageb_model.py`·`PROMPT_FILES`·`harvest/runtime` 무수정.

## 5. 다음 (결정 필요)

- 등록 규칙상 E-MA1은 **R2_TRAIN 완료 뒤 R2 시뮬 데이터로 새 사전 등록**(카메라 정확 → G0 불필요 또는 자명 통과; 시뮬 머리 카메라 = §47 설정)으로 다시 연다. R2 행은 30 Hz라 이력 2 s = 60프레임, trace 상한 3 s = 90프레임으로 옮겨야 한다(등록 때 정함).
- S-E2E에 남기려면 새 등록이 필요하다: 예) RB1 머리 각을 에피소드별로 추정하거나(첫 프레임 PnP), 기준점을 사람 표시로 바꾸거나, RB2만으로 범위를 좁히기. 이 문서는 어느 쪽도 권하지 않는다 — 이 G0 결과는 **"명목 FK 투영을 S-E2E 머리 영상 라벨로 쓰기에는 부족하다"**까지만 말한다.

## 6. 실행 기록

| 항목 | 값 |
|---|---|
| 코드 | 파드 `/data/harvest/code_ma1_g0` = `git -c core.autocrlf=false archive 06355f0`(LF, 고정 파일 sha 앞 16이 등록과 같음 확인: `86a4831c6d64d7e6`·`1876c5cad61766f4`·`fa4905ed42e990f4`·`a4859739452818a4`) |
| 시각(UTC) | select 17:33:14, 기준점 17:33:19–17:34:45, 판정 17:34:45–17:34:46 |
| GPU | GPU 2만(움직임 줄 확인 실험 종료·프로세스 없음 확인 17:30Z 뒤), 약 1.5분. GPU 0·1 미사용 |
| 파일 | `/data/harvest/logs/ma1/`: `g0_frames.jsonl`(sha256 앞 16 `accc3f207e520012`), `g0_points.jsonl`(`9db6fd1301b3a831`), `g0.json`(`a8ee53060ecd4a56`), `g0_diag.json`, `g0_sheet.jpg`, `run_g0.out`, `setup_molmo2.out`; 로컬 사본 `D:\tools\scratch_qdd\ma1` |
| 비용 | 유료 API·Astra 호출 0건(상한 0원) |
