# E-Couple 무료 사전 실행(셰이크다운) 결과 — 배관 점검, P3 판정 없음

- 작성 2026-09-26 07:26 UTC. 사전 등록 [`prereg_couple_dry.md`](../prereg_couple_dry.md) `9322853`(06:06 UTC), 변경 1 `f9903d7`(06:18 UTC, 본 실행 결과 전; 분석 지표 추가만).
- **성격(§56)**: 셰이크다운. P3(결합이 VLA 단독보다 낫다)에 대한 판정·결론 **없음**. 유료 호출 0회(장부 72행 합계 **0원**, 가격 모델 `mock`).
- 실행: 메인 파드 **GPU 1만**(Isaac 렌더 + 융합 VLA 서버 `ckpt/sr1c/c1/last` + Qwen3-VL-8B vLLM 포트 8371, 이미지 상한 3), 고정 사본 `/data/harvest/code_couple_dry_9322853`(결합 배선 = 553a8ad, Task 12 판 올림·Task 17 이전), 분석 사본 `code_couple_dry_ana_f9903d7`. 스모크 G0 06:07–06:12 UTC 통과, 본 실행 06:14:39–07:22:40 UTC(작업자 standard 2,022 s·dr 2,041 s), 24/24편.
- 출력: 파드 `/data/harvest/out/couple_dry/{smoke,main}/`(`closed.json`·부가 로그·`dry_calls_*`·`dry_trace_*`·`ledger.jsonl`·`dry_analysis.json`·`sheets/` 영상 mp4 24개 + 프레임 시트 24장 + 요청 영상 시트 12장), 로그 `/data/harvest/logs/couple_dry/`.
- **해석 한계**: Astra 대역은 Qwen3-VL-8B + `astra-couple@v1`이다. 프롬프트 검진(`prompt_health.md`, 1b68a6a)에서 v1은 개념 정의·축/카메라 자세 줄이 없고 그리지 않는 화살표를 설명하며(Qwen이 없는 화살표를 27/33 인용), Qwen은 문구 바꿈에 6–28/40 뒤집힌다. 그래서 **이 조건의 행동 지표(성공·시간·저크)는 정보가 없다** — 배관만 본다. 게다가 VLA 단독이 0/12라 짝 차 자체가 정의상 0이다(B5).

## 1. 배관 (결합 팔 12편, 사전 등록 §3 번호)

| # | 지표 | 결과 | 기대(등록) |
|---|---|---|---|
| 1 | 충돌 | 작업자 Traceback 0, `eval_runs` 4/4 success, 24/24편 | 0 |
| 2 | 시간 초과·늦은 답 | 시간 초과 **5**, 늦은 답 **5**(주입 6 — dr 시드 5 편은 요청 3이 날아가는 중 off_table로 끝나 시간 초과 전 종료) | 주입이 닿은 편 수 |
| 3 | 동시 1개 불변식 | (a) 부가 로그 순서 위반 **0**/72 보냄; (b) 클라이언트 실제 동시 호출 최대 **2**, 2였던 호출 **1**회(주입 시간 초과 뒤 — 설계대로 자리 비움); (c) `max_outstanding` 2, `max_inflight`는 이 판(9322853)에서 **보낼 때마다 1로 고정 기록되어 증거가 못 됨**(B2, b0ee916에서 수정) | 1, 시간 초과 직후만 2 |
| 4 | 늦은 답 폐기 | 게이트 `stale` **0**; 답 나이 p50 8.78 s·p95 12.08 s·최대 12.77 s(15 s 초과 0); 늦은 답 5개는 모두 `late`로 적용 안 됨 | 로그정규 p95 약 14 s라 0–2 |
| 5 | 도착 시 대조(§91) 판정 수 | **측정 불가 — 이 판에 구현 없음**(결합 계획 Task 19 미커밋) | 미구현 |
| 6 | 권한 a | 이 판에 런타임 적용 없음(Task 17은 실행 뒤 b0ee916에 커밋). 대리값 a_priv: 결합 편 틱의 대부분 a = 1(편별 0.74–1.00); **a_priv = 0에서 실제로 적용된 편향 0.42 mm**(전체 편향 경로 22.7 mm 중) — Task 17이 막았어야 할 양 | 측정 |
| 7 | 편향 크기·상한 | `edit` 2회 적용(각 50 %, dr 시드 2·4), 편향 경로 합 22.7 mm, 편향 속도 최대 **0.0084 m/s ≤ 0.08**, `a_max_seen` **0.32 = 상한(지킴)**, 틱 궤적의 속도 변화 최대도 0.32 m/s²; 축소(`offset_contradicted`) 1·진전 없음 1 | 지킴 |
| 8 | T1 관문 | `deny:no_evidence` 18, `allow:t1_astra_stale` 10, `allow:t1_fresh` 4, `layer_mismatch` 사건 15; Astra 거부로 막힌 전이 0 | 분포 보고 |
| 9 | 답 형식·게이트 | 유효 답 52 / 받은 답 55(늦은 답 5 제외), **스키마 오류 3(5.5 %, 모두 `command` 값)**, 게이트 `ok` 52, 명령 continue 50·edit 2, 층 none 50·apply 2; 메모 `claim_vs_t1:grasped` 15(Qwen이 '잡았다'고 했으나 T1은 안 쥠 — 게이트가 버림), `other_wrist_claim` 1 | 측정 |
| 10 | 장부 | 72행, **0원**, charge 60·unanswered 12(편 끝에 날아가던 요청 — 매 편 1개), 가격 모델 `mock` | 0원 |
| 11 | 지연 | 붙잡은 지연 p50 9.34 s·p95 25.0 s(주입 포함); **Qwen 실제 지연 p50 5.03 s·p95 6.26 s·최대 7.71 s**(GPU 1을 Isaac·VLA와 나눠 씀) → 로컬 VLM은 Astra p50 9.3 s보다 빠름; 청크 대 확정 결정 준수(편별) 0.92–1.00, 청크 대 편향 준수는 편향이 켜진 한 편 n 1(값 0.0) — 편향은 청크 밖 IK 치우침이라 청크 자체가 따를 이유가 없다(정의대로) | — |

## 2. 행동 (서술만, 판정 없음 — 위 해석 한계)

| 변형 | 팔 | 성공 | 종료 | 마지막 단계 | 저크 RMS 중앙 (m/s³, 100 Hz 틱) | 최대 속도 중앙 | RTF 중앙 | 명령 계단: 관절 > 0.04 rad/틱 합 · 그리퍼 > 5 mm/틱 합 |
|---|---|---|---|---|---|---|---|---|
| standard | A0 VLA 단독 | 0/6 | 시간 5·탁자 밖 1 | approach 4·lift 1·carry 1 | 232 | 0.72 | 0.355 | 100 · 30 |
| standard | A1-dry | 0/6 | 시간 6 | approach 5·carry 1 | 447 | 0.83 | 0.330 | 426 · 25 |
| dr | A0 | 0/6 | 시간 5·탁자 밖 1 | approach 3·carry 2·lift 1 | 431 | 0.73 | 0.349 | 315 · 62 |
| dr | A1-dry | 0/6 | 시간 4·탁자 밖 2 | approach 4·lift 1·carry 1 | 492 | 1.02 | 0.319 | 364 · 49 |

- 저크는 등록한 식(`astra_motion.harness.motion_stats`)을 100 Hz 틱 TCP 경로에 그대로 쓴 값이라 탐침 값(0.83 m/s³, 다른 실행기)과 비교하지 않는다(변경 1). **두 팔 모두** 관절 명령이 한 틱에 0.04 rad를 넘는 계단이 편마다 수십–수백 번 있다 — 청크 전환 계단(결합 전부터 있던 것, 계획 Task 10 미해결 부수 항목 '보류 → 재생 청크 전환 때 현재 행으로 뜀')으로 보이며 결합 팔이 standard에서 더 많다(원인 미분리).
- 프레임 확인(책 규칙): `sheets/standard_…cp-off_dev2…jpg`(A0): 팔이 머그를 지나쳐 쟁반 위에서 약 40 s 'approach'로 떠 있음 — VLA 단독이 이 과제를 끝내지 못한다. `sheets/req_dr_C5_cp-serial_dev2…jpg`(흐름 모델이 받은 요청 3장): 머리 영상 흰 고리·굵은 예정 화살표·손목 모서리 상자(청록 예정·자홍 점선 편향) 모두 그려짐, #2에서 `edit` 적용 뒤 #3에 편향 점선 화살표가 보임. 스모크 `sheets/…cp-serial…jpg`: 잡고 들어 carry 중 머그를 탁자 밖으로 떨어뜨림(off_table), 그 사이 그리퍼 명령이 두 층 관문에 약 2.3 s 묶였다가 한 틱에 14 mm 닫힘(B1).

## 3. 버그 목록 (심각도 · 재현 · 담당)

| # | 심각도 | 내용 | 재현 | 담당 · 상태 |
|---|---|---|---|---|
| B1 | 중 | 두 층 관문이 붙잡은 그리퍼 닫기를 풀 때 한 틱 계단(0.10475 → 0.0908, 14 mm) — `_couple_fused`가 막힌 동안 `a[7] = w_prev`, 허용되면 청크 현재 값을 바로 씀 | 스모크 `smoke/standard/C5_cp-serial/actions/20260926_061130_476edc8d/dev0-P0-standard-e0.jsonl` t 0.98–3.30 s | 결합 통제자 · **b0ee916에서 수정**(허용 뒤 속도 제한) — 이 실행에서는 재검증 안 함 |
| B2 | 중하 | `SerialStream.max_inflight`가 보낼 때마다 `max(…, 1)` → 동시 1개 불변식의 증거가 못 됨 | `harvest/couple/stream.py` 9322853 판 52행 | 통제자 · **b0ee916 수정** |
| B3 | 중 | 편향 적용기 로그(명령·리셋·축소·버림)와 두 층 관문 로그(변경 시각·이유)가 부가 JSONL에 안 나감, `applied_m`는 부호 있는 순합 | 아무 결합 부가 로그(`couple_kind` = send/answer/timeout/adherence/event만) | 통제자 · **b0ee916 수정**(`offset`·`irrev_gate` 행, `applied_abs_*`) |
| B4 | 높음(E-Couple 필수) | 이 판에 권한 a 적용·도착 시 대조(§91)·구간 계획(§90)·움직임 줄(TickView `motion=None`) 없음 → a_priv = 0에서 편향 0.42 mm 적용 | 1절 6행 | 통제자 · a는 b0ee916(Task 17), §91·§90은 Task 18·19 대기, 움직임 줄은 Task 12(ser-A-min-3) |
| **B5** | **차단** | **바닥 효과: VLA 단독 0/12**(E-SR1c C1, DEV 0–5 P0 standard·dr) — 대부분 approach에서 멈춤. 짝 성공 차가 정의상 0이라 유료 E-Couple이 아무것도 못 가른다 | 2절 표·프레임 | 메인(VLA 레시피·재학습) — 재학습 체크포인트로 **VLA 단독 폐루프 온전성 관문**(예: DEV 성공 ≥ 30 %)을 E-Couple 전에 둔다 |
| B6 | 중 | `b_contradict`가 결정 스텝마다 사건으로 올라감(결합 12편 892회) → `stream.last_event_t`가 늘 새로워 **`phase_pause`(A2 비용 대비책)가 절대 켜지지 않고** 모든 요청이 사건 목록을 실음 | 아무 결합 부가 로그 `couple_kind = event` 행(예: `main/standard/C5_cp-serial/ours/*/dev1-P0-standard-e0.jsonl`) | 통제자 · 미수정(보고함) — 사건 올림을 가장자리 트리거·빈도 제한으로 |
| B7 | 하 | 매 편 끝에 날아가던 요청 1개가 예약값(최대 출력 1,200토큰)으로 `unanswered` 청구 → 12/72행; 유료면 호출의 약 15–20 %가 상한값으로 잡혀 예산을 보수적으로 과대 계상 | `main/ledger.jsonl` kind 분포 | 통제자·E-Couple 등록 · 예산 계산에 반영(버그라기보다 설계 결과) |
| B8 | 하(대역) | Qwen 스키마 오류 5.5 %(`command` 값), `grasped` 과대 주장 15 | 1절 9행 | 프롬프트(v2 준비 중) — Astra 조건에서 다시 잼 |

## 4. 본 E-Couple 전에 고칠 것 (우선순위)

1. **B5 VLA 단독 온전성 관문**: 재학습(ser-A-min-3) 체크포인트로 VLA 단독 폐루프를 먼저 돌려 성공률이 바닥이 아님을 확인(0 %면 E-Couple 유료 실행 금지 — 짝 차를 가를 수 없음). 원인 후보(미검증): R2 다과제 2,000스텝 오프라인 체크포인트의 폐루프 능력 부족, 청크 전환 계단.
2. **B4 나머지**: §91 도착 시 대조(Task 19)·§90 구간 계획(Task 18)이 들어간 판으로 이 사전 실행을 한 번 더(같은 도구 `tools/couple_dry/`, 무료) — 대조 판정 수·권한 a 궤적(a = 0 편향 0 기대)을 실제로 세기 위해.
3. **B6** 사건 올림 빈도 제한(A2를 쓸 계획이면 필수).
4. B1–B3 수정(b0ee916)은 다음 사전 실행에서 동작 확인: 그리퍼 계단 ≤ 청크 자체 속도, `max_inflight` 1(시간 초과 직후만 2), `offset`·`irrev_gate` 행 존재.
5. `prereg_couple.md` §0의 '무료 사전 실행' 칸은 이 문서와 다음 사전 실행 결과로 채운다(초안은 이번에 건드리지 않음).

## 5. 비용·시간·이탈
- 유료 0원. GPU 1 약 **1.3 GPU-h**(Qwen 서버 06:07–07:23 UTC, 그 안에 스모크 4분·본 실행 68분). 다른 GPU·프로세스 안 건드림(정지는 `stop_dry.sh`로 자기 PID만: vLLM 3개 프로세스).
- 이탈: 로컬에서 빈 `python -` heredoc 1회(실행 내용 없음, 멈춰 끝냄, P03); `run_dry.sh` 한 줄을 sed로 고치다 깨뜨려 Edit로 복구(커밋 전). 세션 재시작으로 대기 중이던 로컬 감시가 끊겼으나 파드 실행은 계속돼 끝난 편을 다시 돌리지 않았다.
