# E-OPEN8 — 공개(오픈소스) 데이터만으로 8B를 학습하면 우리 과제로 옮겨 가나 (무료, 사전 등록)

- **상태: 등록**(학습 전 커밋, P15). 결과는 `docs/stage3/results/open8.md`에 쓴다.
- 작성: 변환 방법 에이전트. 초안 2026-09-27 02:5x KST(7faca71), 등록 2026-09-27 03:xx KST.
- **사용자 원문**(user-log 123):
  - "일단 오픈소스를 가지고 학습을 할건데 어떤걸 써서 어떻게 할거임? 대규모는 아니고 소규모로 해두돼"
  - "근데 저 4개의 방법론은 오픈소스데이터로 일단 해야해 이해했으?"
- **데이터 구성과 관문 수치**: `docs/research/public_data_conversion_howto_2026-09-26.md` 10–11절. 세트는 파드 `/data/harvest/out/xemb/open8/`(`train.jsonl`·`counts.json`).
- **E-PT·E-XEMB8과 맞춘 것**(`docs/stage3/prereg_pt.md`):
  - 같은 모델·레시피·걸음 수(`--max-steps 816`).
  - 같은 DEV·OOD-H 스냅숏.
  - 같은 지표 정의(접근 xy·3D 오차, 유효 JSON). 이 시험은 v2 xyz 답이라 계산은 L8 채점기 `teach_l8.metrics`로 한다(4절).
  - 같은 부트스트랩 판정 규칙.
- **비용**: 유료 0원, GPU만. 팔당 약 1.4 GPU-h [추정].

## 0. 자체 검사

### 0.1 무엇을 결정하나
- **O-A**: 우리 시뮬 데이터 **0**, 공개 데이터만으로 LoRA한 8B가 우리 요청 형식(런타임 그대로)에서 대상에 다가가는가(영샷 대비 접근 오차).
- **O-B**: L8 자체 데이터에 공개 세트를 같은 걸음 수로 섞으면 L8 단독보다 DEV·OOD-H가 나아지는가.
- 둘 다 **좌표 누수 0**이어야 한다.

### 0.2 선행 조건 (등록 시점 상태)
1. 공개 세트의 관문을 통과할 것(howto 10–12절). 결과: T1(RB2 카메라)·T3는 불통이어서 그 스트림 몫은 0이다. 나머지 스트림은 통과했다.
2. OOD-H: E-PT의 v2 xyz 요청 판 OOD-H 파일(`/data/harvest/out/teach_pt/data/ood_h_xyz.jsonl`)이 평가 때 있으면 쓴다. 없으면 **DEV만 판정**하고, O-B의 OOD-H 판정은 '없음'으로 적는다.
3. 코드 고정 사본 `/data/harvest/code_open8_<등록 커밋>`(git archive, LF).

### 0.3 이미 본 것
- L8 결과, E-PT 등록.
- 변환 관문 수치(howto 9–10절).

O-A·O-B의 어떤 모델 출력도 아직 보지 않았다.

## 1. 팔

| 팔 | 학습 데이터 | 기준 |
|---|---|---|
| **O-0** 영샷 | 없음(Qwen3-VL-8B 원판) | L8 영샷 값 재사용: DEV 접근 xy 중앙 125.5 mm |
| **O-A** 공개만 | E-OPEN8 세트(에폭당 약 6,500행, 우리 시뮬 0) | O-0 |
| **L8** 자체만 | L8 어댑터 그대로 | — |
| **O-B** 자체 + 공개 | L8 `train.jsonl` 6,521행에서 무작위 3,261행(시드 0) + E-OPEN8에서 무작위 3,250행(시드 0) = 6,511행 | L8 |

- 걸음 수는 모두 816이다(= L8 총 걸음). 명령은 `harvest.teach_l8.train --epochs 3 --max-steps 816 --micro 8 --accum 2`로, 나머지 인자는 L8 기본값이다. 코사인 일정 길이는 816이다.
- 공개 행의 영상 칸 이름은 L8 로더(`dataset.user_content`)의 'head camera'·'right wrist camera'를 그대로 따른다. 공개 행의 글에는 그 로봇 카메라 이름이 들어 있다.
- O-A는 우리 런타임 요청을 **한 번도 보지 않는다.** 공개 C′ 요청(그 로봇의 자기 정보·`frame: base_<로봇>`)으로 형식을 배운다. 그래서 이 팔은 '형식 + 자기 정보 읽기'의 전이를 잰다.

## 2. 데이터 (E-OPEN8, 최종 수는 howto 11절과 `counts.json`)

| 스트림 | 원천(라이선스) | 방법 | 목표 행 |
|---|---|---|---|
| RB2 AI Worker(우리 로봇 계열) | ROBOTIS 공개 실데이터 RB2(`Task_0002_OrderPicking`) | T1 관문 불통 → C′(`camera: unknown`, FK 목표) 1,000 + 추적·포인팅 일치 그리퍼 점 950 + 궤적 400 | **2,350** |
| MolmoAct 궤적 | allenai/MolmoAct-Pretraining-Mixture(CC BY 4.0: BC-Z·RT-1·Bridge·보조 궤적) | T0 픽셀 궤적(0–255 → 픽셀, 튐·되돌림 필터) | **1,000** |
| MolmoBot RBY1 점 | allenai/molmobot-data(ODC-BY) | 보정 있음: 그리퍼·물체·놓을 곳 점, EE 투영(G1 필터) | **1,000** |
| MolmoBot Franka C′ + 점 | 같음 | 위에서 잡기 ≤ 30°, `frame: base_franka`; 점은 EE(≥ 0.6 m)·물체·놓을 곳 | **1,000 + 500** |
| T3 유사 깊이 3D | RB2 + MoGe-2 | T3 관문 불통 | **0** |
| 일반 공간 | RefSpatial 부분 집합(Apache-2.0) | 형식만(다른 지시문) | **650**(10 %) |
| **합** | | | **6,500**(`counts.json`) |

- 모든 행에 `source:`/`frame:` 머리 줄과 카메라 줄(또는 `camera: unknown`)을 단다.
- 빌더(`tools/xemb/build_open8.py`)는 다음 행을 거부한다: 영상이 없는 행, 머리 줄이 없는 행, C′ 답에 공개 형식 키가 있는 행.

## 3. 학습
- E-PT 3절과 같다: L8 레시피, `--max-steps 816`, bf16, seed 0.
- 학습 자체 검사: 첫 20걸음 손실 하강, 로더 행 수 = 파일 행 수(P115), 스트림 비율 = 계획 ± 1 %.

## 4. 평가 (L8 채점기 `harvest.teach_l8.evaluate`·`metrics` — L8 기준과 같은 계산, E-PT와 같은 지표 정의)

### 4.1 세트
- DEV: L8 `dev.jsonl` 제어 스냅숏 305.
- OOD-H: 0.2의 2번을 따른다.
- 공개 보류분(held-out open): 8스트림 × 50행 = 400행. `train.jsonl`에 들어가지 않은 변환 행에서 무작위로 뽑는다(시드 1). 파일은 `open8/heldout.jsonl`이다.
  - 같은 편의 다른 프레임이 학습에 있을 수 있다. 그래서 이 값은 '같은 분포 안' 정확도로만 적는다.
- 서빙: 마지막 어댑터를 병합(`teach_l8.merge`)해 vLLM에 올리고 탐욕 복호한다(L8과 같음).

### 4.2 지표
- 유효 JSON률.
- 그리퍼 행동 정확도.
- 접근 xy 오차, **접근 3D 오차(주)**.
- **좌표 누수율**: `fmt.leak_flags`. 우리 요청의 답에 `frame`·`point`·`trace` 등 공개 키가 나오거나, 우리 작업 상자 ± 5 cm 밖의 목표가 나오면 누수다.
- 공개 보류분 정확도: 점 픽셀 오차, C′ 목표 오차(그 로봇 기저, mm).

## 5. 판정 (결과 전 고정)

- **관문(모든 팔)**: 누수 0건, 유효 JSON ≥ 0.95. 못 넘으면 그 팔은 판정하지 않고 형식을 고친다.
- **O-A 전이**: DEV 접근 xy 중앙 ≤ 0.5 × O-0(= 62.8 mm) → **TRANSFER**. ≤ 0.8 × O-0 → **PARTIAL**. 그 밖은 **NONE**. 폐루프 DEV 4판(standard·dr × s0·s1)은 기술만 하고 영상을 남긴다.
- **O-B 일반화**: E-PT 5.2 규칙을 그대로 쓴다. 주 지표는 OOD-H 접근 3D 오차 중앙이다.
  - 팔 ≤ 0.8 × L8이고, 스냅숏 짝 부트스트랩(10,000회, 시드 0) 95 % 구간 상한 < 0 → **BETTER**.
  - 구간이 0을 포함 → **SAME**. 하한 > 0 → **WORSE**.
  - DEV 비열등: ≤ L8 + 5 mm.

## 6. 결정 규칙
- **O-A TRANSFER 또는 PARTIAL**: 공개 데이터가 형식·자기 정보 읽기를 가르친다는 근거로 본다. 35B 혼합의 공개 몫을 유지·확대한다(교차 기체 문서 3.2).
- **O-A NONE**: 공개 데이터만으로는 우리 요청을 못 푼다. 공개 데이터는 보조(O-B) 역할로만 쓴다.
- **O-B BETTER**: E-XEMB8 E팔(자체 + 공개)의 출발 구성으로 쓴다. **SAME**: 필수 원칙(user-log 116)대로 유지하고 OOD-O·D에서 다시 잰다. **WORSE/누수**: 해당 스트림을 고친다.

## 7. 자원·규칙
- GPU: **빈 카드에서만** 돌린다. 등록 시점에 빈 카드는 x3 파드 GPU 0 하나였다. 35B 학습·L8-D 생성·E-PT가 쓰는 카드는 선점하지 않는다.
- 폐루프 4판(Isaac)은 렌더가 허용된 빈 카드가 날 때만 돈다. 나지 않으면 '폐루프 없음'으로 적는다.
- 모든 파일은 `/data` 아래 둔다. 유료 0원.

## 8. 말하는 것·못 하는 것
- **말하는 것**: 소규모 공개 데이터(우리 로봇 계열 RB2 포함)가 우리 과제 형식으로 옮겨 가는지, 그리고 자체 데이터와 섞을 때 탁자 높이 ± 3 cm 일반화를 바꾸는지.
- **못 하는 것**: 대규모 효과, 실물 폐루프, 35B, 한 시드.

## 변경 기록
- 등록 판(이 커밋): 초안(7faca71)에서 달라진 것은 다음과 같다.
  - 스트림 최종 수를 채웠다.
  - O-B 구성을 정했다(L8 3,261 + 공개 3,250).
  - 채점기를 L8의 것으로 적었다(E-PT의 teach_pt 채점기는 점·ND 형식용이고, 이 시험의 답 형식은 v2 xyz라 L8 채점기와 같다).
  - OOD-H 조건부를 적었다.
  - GPU 규칙을 적었다.
  - 판정 문턱은 바꾸지 않았다.
