# L8-D — L8 데이터 다양화 생성기(탁자 높이·방해물·외관·과제)와 보호 OOD 세트 (무료, 생성 전 설계·등록)

- 작성: L8D 에이전트, 2026-09-26T18:10Z(UTC, KST 27일 03:10). **본 데이터 생성 전에** 커밋한다(P15). 결과는 `docs/stage3/results/l8d.md`.
- 근거(사용자 원문, 통제자 경유, user-log 다음 번호): "관 동의가 필요한 공개 데이터를 쓰긴할거임 근데 일단은 없는 것들에서 먼저 보고 나중에 꼭 말해줘 잘된다면 그거 하자고. 그치 저 L8이라는 것 자체를 여런 테스크에다가 여러 높이 장애물을 더 훨씬 다변화할 수 있음 좋겠어"
- 자리: 정본 §97 보충 1(다양한 데이터는 필수), `docs/research/diversity_plan_2026-09-26.md` 1절(자체 시뮬 다양화)·3절(OOD). L8 레시피(`prereg_teach_l8.md`)는 읽기만 한다.
- 게이트(약관 동의) 공개 데이터: **비게이트 먼저**. 결과가 좋으면 통제자가 사용자에게 게이트 데이터 사용을 제안한다(사용자 결정, 이번 원문).
- 비용: 유료 호출 0, 렌더 GPU만.

## 0. 자체 검사
- **무엇을 결정하나**: 이 등록은 학습 판정을 하지 않는다. 결정하는 것은 (1) 고정 리프트 로봇이 다룰 수 있는 탁자 높이 범위(관문 G-H), (2) 리프트를 쓰면 그 범위가 얼마나 넓어지는가(측정만, 기본값 아님 — 사용자 결정 대기), (3) L8과 같은 형식의 다양화 데이터 L8-D와 보호 OOD 세트의 구성.
- **이미 본 것(P16)**: L8 결과, E-PT 중간 결과(통제자·보드: L8-xyz는 OOD-H ±3 cm 폐루프 4/4, ±7 cm 0/4; ND-1 오프라인 DEV 1.2 mm), E-STRIP8 중간 결과(L8은 손 정보 의존, z를 외움). 이것들은 다양화의 필요를 보여 줄 뿐 이 등록의 문턱은 계획 문서(8 cm·0.8)에서 왔다.
- **형식은 나중에 고른다**: E-PT/ND·E-STRIP8의 입력·출력 형식 결론이 아직 없다. 그래서 수집은 E-PT 경로(`teach_pt.collect.PtCollector` + `PtEpisode`, 깊이 켬)로 하여 호출마다 v2 요청(격자·탁자 높이·물체 크기 = 손 정보 있음), ND 요청 3개(격자·탁자 높이 없음, 고리만 영상), 점 요청, 머리 깊이, 카메라를 모두 저장한다. 빌드 때 스위치(격자 on/off, 손 정보 on/off, 깊이 on/off, source·frame 태그 on/off)로 형식을 고른다.

## 1. 다양화 축 (1단계)
- **탁자 높이**: 프로세스(버킷)마다 한 높이(탁자는 정적 충돌체). 높이마다 작업 상자 x = 머리 시야 띠 ∩ 위에서 닿는 띠(관문 G-H, 2절). y는 L8과 같은 −0.40–−0.06.
  - 학습 높이 = G-H 통과 범위 [lo, hi] 안 1 cm 격자, 단 모든 OOD-H 높이(E-PT 0.78·0.82·0.88·0.92와 바깥 lo·hi)에서 **1.5 cm 이상** 떨어진 것만(`spec.heights_from_gate`). 0.85는 포함(L8과 같은 높이).
  - 머리 각도는 고정(0.69 rad, 실물 45° 정책), 무작위화하지 않는다.
- **방해물 0–6**: 새 변형 `drx` = TRAIN 풀과 같은 방해물 7종(기존 물체 세트)에서 0–6개 + 과제 배치의 o3·o8·o9(있을 때). 배치 거부는 기존 `placement_ok`(경로 띠·물체 간격).
- **조명·재질 넓힘**: `drx`는 별도 풀 파일 `harvest/sim/randomization_pools_x.json`의 TRAIN_X(TRAIN 자산 + 탁자 재질 14·바닥 7·방해물 색 6 추가, 조명 세기·색온도 바깥 띠 추가). 원래 풀 파일·digest·`dr`/`random` 표본은 바이트 불변(시험). 과제 물체 색(빨강·초록·파랑·자홍)은 방해물 색에서 뺀다(1단계에서 지시 대상 혼동 없음).
- **변형 비**: standard 1/3 · drx 2/3(시드별). 깨끗한 편 25 %(L8과 같음).
- **과제**: 참값 모델이 푸는 기존 셋 mug_tray 40 %·bottle_tray 30 %·mug_marker 30 %(시드별). box_marker는 참값 0/3이라 뺀다.
- **행동·라벨**: L8 수집기 그대로(교란 확률 0.35·편당 ≤ 4·회복 규칙 넷·막힘 반복 버림, 한도 30호출·120 s).
- **편마다 기록**: `scene.json`(탁자 높이, 리프트, 작업 상자, 변형, 과제, 배치, 무작위화 전부, 방해물 수와 이름, 정착 오프셋). `labels.jsonl` 행에도 `table_z`·`n_distractors`.

## 2. 관문 G-H (높이마다; 결과 전 고정)
- 후보: 0.72, 0.74, 0.76, 0.78, 0.80, 0.82, 0.84, 0.85, 0.86, 0.88, 0.90, 0.92, 0.94, 0.96, 0.98.
- **시야 띠**: 실제 머리 카메라(탐침이 기록한 K·R·t)로 x마다 y ∈ {−0.40, −0.23, −0.06}의 물체 밑면 중심과 윗면 중심(높이 10 cm)이 영상 안(여백 10 px)이면 통과; 가장 긴 연속 구간.
- **도달 띠**(`probe_reach`): 탁자를 0.70으로 내리고 물체를 치운 뒤, 같은 y 셋 × x 0.30–0.62(2 cm) 기둥마다 z 1.20에서 2 cm씩 내려가며 위에서 내려다보는 목표를 25틱 유지, 정착 오차 ≤ 8 mm(실행기 도달 허용). 높이 tz의 필요 z = tz + 0.07 … tz + 0.24(잡기·놓기 ~ 운반 + 2 cm)의 모든 탐침 z에서 통과한 x의 가장 긴 연속 구간.
- **통과**: 작업 상자 x 폭 = 시야 ∩ 도달 ≥ **8 cm** **그리고** 깨끗한 참값 편(GATE 시드 35000–, 과제 비율 그대로, standard, 높이마다 10편, 그 높이의 작업 상자) 성공 ≥ **0.8**.
- 범위 = 0.85에서 이어지는 통과 높이 [lo, hi]. 계획 추정은 −5/+10 cm였다.
- **리프트(측정만, 기본 끔)**: `make_env(lift=…)`로 몸통 리프트 시작·유지 값을 바꾼다. 같은 탐침을 리프트 두 값(한계 안 낮은·높은 쪽)에서 돌려 도달 띠가 z로 그만큼 옮겨지는지, 한계(soft limits)를 기록한다. 리프트로 넓어지는 높이 범위 = [lo + 리프트 하한 이동, hi + 상한 이동]을 **숫자로 보고만** 한다. 참값 편 성공 확인은 양 끝 높이에서 5편씩. 기본 데이터에는 쓰지 않는다(사용자 결정 전).

## 3. 데이터 L8-D
- 경로: `/data/harvest/out/teach_l8d/collect/<split>/<variant>_tz<z>/<task>_s<seed>/`, 빌드 `/data/harvest/out/teach_l8d/data/`.
- **TRAIN 1,200편**(L8의 4배; 시드 30000–31199, `spec.plan_train`, 높이는 균형 배정). 행 수는 L8 비례로 약 1만 조종 행 예상.
- 빌드(`harvest/teach_l8d/dataset.py`): L8 행 형식(조종 = 요청 + 참값 답, 반복 가중 `repeat_of`, 인식 보조 QA 1개) + 행마다 `scene` 필드. 형식 스위치 `--format v2|nd-xyz|nd-est|pt`(v2 = L8 그대로), `--tags`(요청 앞에 `source: qdd_sim/ffw_sg2`·`frame: base_ffw_sg2`, 작전T open8과 같은 형식; 답에는 frame 키 없음). 세는 파일 `counts.json`: 과제 × 높이 구간(낮음 < −3 cm, 가운데 ±3 cm, 높음 > +3 cm, 그리고 높이별) × 방해물 구간(0, 1–2, 3–4, 5+) × 변형.
- 분할 가드: train 빌드는 OOD 시드(70000–70999)·DEV(0–29)·`random`·`randx` 변형을 거부한다.

## 4. OOD 세트 — 절대 학습하지 않음 (보호 시드 70000–70999, 생성은 `--confirm-ood`)
- **OOD-H**(70000–70099): E-PT/E-STRIP8과 같은 높이 0.78·0.82·0.88·0.92 + G-H 바깥 높이 lo·hi. 높이마다 standard 8편 + drx 8편(과제 비율 그대로). E-PT OOD-H(DEV 0–9 × standard)와 시드가 겹치지 않고 높이는 같다(PT-ND 동의, 보드 03:xx KST). 학습 높이는 이 높이들에서 1.5 cm 이상 떨어진다.
- **OOD-D**(70300–70339): 변형 `randx` = TEST_X 풀(TEST 자산만: 방해물·재질·HDR, 조명 띠 모두 학습 풀과 겹치지 않음), 방해물 3–4개, 높이 0.85, 과제 비율 그대로, 40편.
- **OOD-O**(70100–70299): 새 물체가 들어오는 2·3단계에서 이름 해시로 뺀 물체로 만든다(1단계에는 없음 — 기존 물체만으로는 '본 적 없는 물체'가 없다).
- OOD 세트도 L8 수집기(교란 섞임, 같은 규칙)로 모아 오프라인 스냅숏 평가에 쓴다.

## 5. 2·3단계 (1단계 뒤, 변경 기록으로 등록)
- 2단계 과제 변형(참값이 풀 수 있는 것만, 최소 확장 + 시험): 높은 받침에 놓기, 색·크기로 고르기(혼동체 머그), 관계 놓기(left of / next to), 그릇에 넣기.
- 3단계 새 물체: MolmoSpaces Isaac USD(ODC-BY/Apache) 부분 집합, 참값 집어 놓기 3회 중 2회 이상 성공한 것만. 물체 높이 > 10 cm이면 `resolve.ROBOT_FLOOR_M`(0.105) 조정 필요(PT-ND 주의).

## 6. 자원·규칙·자체 검사
- GPU: Isaac 렌더만. 메인 GPU 2·4, x2 GPU 0 금지(`tools/teach_l8d/isaac.sh`가 거부). 35B 학습 카드는 선점하지 않는다(동승은 30 GB 이하). 잡고 놓을 때 보드 `events.log`에 적는다.
- 모든 파일 `/data` 아래. 코드는 고정 사본 `/data/harvest/code_teach_l8d_<커밋>`(git archive, LF).
- 자체 검사: 전(시험 통과, 기본 경로 불변 시험, G-H) / 도중(첫 20편 성공·버림 비율, 방해물 정착 오프셋, 높이별 첫 프레임 눈 확인) / 끝(구간별 수, 영상 10편 눈 확인, 참값 깨끗한 편 성공률 과제별 ≥ 0.8 — 병 과제는 L8에서 7/10이었으므로 따로 적는다).

## 변경 기록
- **변경 1 (2026-09-26T19:01Z, 참값 관문 편을 돌리기 전 — 본 것: 도달 탐침 오차표 두 개(base·lift 0.0)와 그로 낸 띠)**: 도달 띠 판정에 **z 방향 3점 중앙값**을 넣는다(`spec.median3`). 이유: 탐침 기둥에서 1–2 mm 이웃 사이에 한 점만 114–237 mm로 튀는 일회성 흔들림(25틱 유지가 스윙 중간에 끝남)이 5곳 있었고, 그대로 두면 L8이 참값 55/55로 성공한 x 0.36–0.40(탁자 0.85)이 '닿지 않음'으로 나왔다. 연속 두 점 이상 실패는 그대로 실패로 남는다(시험). 문턱 8 mm·필요 z 구간·시야 규칙은 그대로.
  - 이 규칙으로 낸 기하 띠(참값 편 전): 고정 리프트 **0.74–0.98 통과**(0.72는 도달 폭 0), 0.85 작업 상자 x 0.36–0.54. 리프트 0.0(몸통 +9.93 cm, 머리 z 1.405 → 1.504)에서는 띠가 그만큼 위로 옮겨져 0.84–0.98(후보 끝)까지 통과. 리프트 관절 한계 [−0.50, 0.00](기본 −0.0993, 측정 시작값 −0.125: 2.6 cm 처짐).
- **범위 추가(L8-X, 같은 시각)**: 사용자 범위 변경(user-log 다음 번호, "지금의 10배")에 따라 `objset="x"` 물체(받침대 o12, 파란 머그 o13, 작은 빨간 컵 o14, 회색 통 o15, 보이지 않는 관계 지점 o17·o18)와 과제 8종(`tasks.X_TASKS`: mug_stand, stand_mug_tray, mug_bin, bottle_bin, bluemug_tray, smallcup_tray, mug_left_of_bottle, mug_right_of_bottle)을 더한다. 기본 env·과제·배치는 바이트 불변(시험). 참값 라벨은 받침면 높이를 쓰는 `teach_l8d.xlabels`(탁자 하나면 L8 라벨과 같음 — 3,000 무작위 상태 시험). 과제별 참값 관문(깨끗한 편 3회 중 2회 이상, 탁자 0.85)을 통과한 과제만 데이터에 넣는다. 설계 `docs/research/l8x_env_suite_design_2026-09-27.md`.
- **변경 2 (2026-09-26T19:24Z, 학습 전 — 본 것: 과제 관문 24편 결과(7종 3/3, bottle_bin 2/3)와 높이 관문 중간값(0.74 9/10, 0.76 8/10, 0.78–0.84 10/10·9/10))**: L8-X 학습·보류 과제를 **학습 전에** 고정한다(`teach_l8d.spec`).
  - 학습 X 과제 7종 `X_TRAIN_TASKS`: mug_stand, stand_mug_tray, mug_bin, bottle_bin, bluemug_tray, mug_left_of_bottle, mug_right_of_bottle(시드 31200부터, 과제 균등, 높이·변형은 1단계와 같은 규칙, `spec.plan_x`).
  - **OOD-O**(70100–70299): smallcup_tray — 작은 빨간 컵 o14는 학습의 어떤 과제(대상·놓을 곳·방해물)에도 나오지 않는다(시험). 40편.
  - **OOD-T**(70700–70899): 보류 조합 bluemug_bin(학습된 파란 머그 × 학습된 통), bottle_stand(병 × 받침대). 각 20편. 참값 관문(3회 중 2회)을 먼저 본다.
  - OOD-S(70500–70699)는 가구 장면(도우미 L8X-assets의 `sample_scene`)이 들어올 때 보류 가구 종류로 만든다.
  - train 빌드는 보류 과제 행을 거부한다(`dataset.check_row`, 시험).
  - 운영 메모: x3 파드는 CPU 쿼터 2개라 Isaac 생성에 쓰지 않는다. 가운데 높이(0.84–0.86) 1단계 생성은 가장자리 높이 관문 결과 전에 먼저 시작했다(가운데 높이는 기하 여유가 크고 0.84는 이미 10/10) — 관문을 못 넘는 높이의 편은 버리고 수를 적는다.
  - 사용자 결정(통제자 전달): 여러 단계 과제는 **별도 프롬프트 판 v2m**(새 PROMPT_ID, 기존 판 불변)을 최소 형식과 함께 같은 편에서 만든다 — 다음 변경으로 등록. 리프트 높이는 사용자 결정 전까지 OOD·한계 탐침에만(플래그).
- **변경 3 (2026-09-26T19:32Z, 여러 단계 과제 편을 하나도 모으기 전)**: 여러 단계 과제 2종 `X_MULTI_TASKS` — mug_tray_bottle_marker(머그를 쟁반에, 그다음 병을 표지에), clear_to_bin(머그와 병을 차례로 통에, 통 안 좌우 3.5 cm 자리).
  - 에피소드(`teach_l8d.multistep.XEpisode`, PtEpisode 하위): 과제 정보·참값·행동 정책·감시는 **지금 단계**의 단일 단계 그대로. 중간 단계는 on·놓음·똑바름이 처음 성립한 틱에 다음 단계로 넘어가고(1 s를 기다리면 참값이 stop을 답함), 최종 성공은 마지막 단계의 1 s 규칙 + 앞 단계가 모두 아직 성립. 잡는 폭은 단계 대상마다 바꾼다. 러너 조기 종료 한도는 2배(60호출·240 s), 프롬프트 한도는 그대로.
  - **프롬프트 = 별도 판 `<판>+m`**(`PROMPT_ID_M`): 모든 인터페이스(v2·pt·ND 셋)의 정적 부분에서 성공 줄을 '모든 단계를 순서대로' 줄로, 물체 목록의 역할 꼬리표를 '첫째·둘째로 옮길 물체 / k단계에 놓을 곳 / 둘 다 놓을 곳'으로 바꾼다. 그 밖의 줄은 바이트 동일(시험). 기존 판·PROMPT_ID는 불변. 최소 형식(s-min)은 저장된 v2m 요청에서 `strip`으로 만든다(같은 편).
  - 관문: 과제마다 깨끗한 참값 3편(탁자 0.85) 중 2편 이상. 통과하면 학습에 넣는다(시드 32400부터, 높이·변형은 같은 규칙, 약 300편).
