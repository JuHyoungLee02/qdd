# 인수인계: 새 세션이 가장 먼저 읽을 문서

마지막 갱신: 2026-09-26 02:03 UTC (R1–R6 완료, R7 순회 중 — 회차별 판정·연속 무결 횟수는 §2.8 마지막 줄; 정본 범위는 적지 않는다: 정본 `docs/design/00-interfaces.md`의 마지막 절까지가 현재 판이다)

> 현재 판본: 정본 `00-interfaces.md` **§1부터 마지막 절까지**(뒤 절이 앞 절을 덮는다). 단계 3 이전 판본(plan v6.8, SUMMARY v3.10, §1의 v5.6)은 옛 기록이다. 단계 3 이후 최신 상태는 §2.7·§2.8.

> **기록책(user-log 89)**: [`docs/book/README.md`](book/README.md) — 실험 장부·**함정과 예방 규칙(새 작업 전에 먼저 읽기)**·결정 연표·지도·비용. 결과·정정과 **같은 커밋**에 해당 장을 갱신한다.

## 0. 진행 중 작업 (이 절만 진행 상태를 적는다 — 책 P69; 각 줄에 확인 시각 UTC)

- **R7** (확인 2026-09-26 00:46 UTC): **E2E 준비 기준점 도달** — 36·37회차 연속 무결(대상 ffe335c·3d4738a), 태그 `stage3-e2e-ready` = 3d4738a. 이후 코드 변경은 새 회차로 검토한다(기준점은 태그로 고정). 태그 뒤 할 일: 결합 설계 구현 계획 실행(subagent-driven), 논문 N192.
- **결합 구현(계획 2026-09-26, subagent-driven)** (확인 2026-09-26 14:38 UTC, 파드 상태는 통제자 보고): Task 1–26 끝 — 1–16 기본(직렬 흐름·두 층 관문·편향·비용 장부·폐루프 팔), 17 편향 × 권한 a(정본 §84 보충 8), 18 astra-couple@v2(§90·§86 보충 3), 19 도착 시 대조(§91), 20 프롬프트 검진 가드(§59 보충), 21 사건 불응기·미응답 별도 보고·`insufficient_quota` 치명 정지(§84 보충 2), 22 E-CONF 기록 훅(§95 보충 2), 12 직렬화 판본 `ser-A-min-3`(재학습 때 `--segment-dropout` 기본 0.3 필수, 기존 체크포인트는 재학습 전까지 모두 거부; 새 ser-A-min-3 체크포인트 뒤 절차(카나리·보정·폐루프 스모크, 융합 보정 qid 한계) = 계획 `docs/superpowers/plans/2026-09-26-astra-vla-coupling.md` Task 12 Step 17 + 정본 §83 구현 기록 4), 25 C5 교착 수정(§4·§58·§64 보충), 26 유료 가드 user-log 114(§85 후속); 최종 검토 수정(§4 보충 정정·§84 보충 2·§85 후속 2·§91)도 끝. 현재 동작(F25): hold 3.0 s 뒤 한 스텝 탈출, 탈출 3연속 → `hold_stuck` 실패 경로; lift·carry에서 VLA가 쥔 물체는 패드가 50.1 mm 아래로 닫혀야 `object_lost`, 힘만 떨어지면 `grip_force_low` 기록만; hold·탈출 중 재생 청크의 그리퍼는 T1 전제가 설 때만 움직이고(결합 켜짐·꺼짐 모두) 결합은 그 청크를 실행 움직임으로 본다. 유료 규칙(user-log 114): 실제 Astra 클라이언트는 heartbeat 포함 모두 `--approval`/`astra_approval`(통제자 경유 사용자 명시 승인) 없이 거부, `run_r5` 기본 `--astra mock`. **파드 검증 막힘**: Task 25 C 재측정과 Task 24 dry-run r2 — HEAD의 ser-A-min-3이 기존 체크포인트를 모두 거부(`ckpt/sr1c/c1/last`: serializer_ok·last_step_ok·files_ok)하고 GPU 3 사용 중; 얼린 코드 사본 `/data/harvest/code_couple_dry_1d44f26` 보존. [결정 필요] ser-A-min-3 재학습 vs 옛 판 이식 vs 배선만 확인.
- **E-Astra-solo 전체 팔 AS(§92 P3 기준선, 정본 §93)** (확인 2026-09-26 09:41 UTC): 사전 등록 변경 2(`docs/stage3/prereg_astra_solo.md`) 커밋 — Astra low 동기, E-Couple 짝 DEV 0–19 × {standard, dr} = 40편, 하드 정지 25,000원, P107 문구·API 가드 고침. **유료 보류: OpenAI 크레딧 소진(메인 HOLD) — 메인이 충전을 확인한 뒤** 고정 사본으로 `tools/astra_solo/full.sh <사본>`(메인 파드 GPU 3, 출력 `/data/harvest/out/astra_solo_full/`, 장부 `/data/harvest/logs/astra_solo/ledger_full.jsonl`). 결과 문서 `docs/stage3/results/astra_solo_full.md` 커밋 때 이 줄을 지운다.
- **E-SR1e** (확인 2026-09-26 09:59 UTC): 실데이터 S-E2E 먼 구간 분기 레시피 두 팔(A 6,000스텝·분기 50 %, D 3,000스텝·분기 75 %) + L1 대조 안내(E-SR1d C1, w 1.5·2·3) — 사전 등록 `docs/stage3/prereg_sr1e.md` 커밋 뒤 메인 파드 GPU 2(A s1 → D s1)·GPU 3(A s2 → D s2) 학습, 이어 GPU 2 평가 단계(`tools/sr1e/lane_sr1e.sh main`, 고정 사본 `/data/harvest/code_sr1e`, 로그 `/data/harvest/logs/sr1e/`, 체크포인트 `/data/harvest/ckpt/sr1e/`). 결과 커밋 때 이 줄을 지운다.

## 1. 먼저 할 일
1. `dev` 브랜치인지 확인한다. **모든 작업(글, 코드)은 `dev`에서 시작하고 `dev`에만 푸시한다. `main`은 절대 건드리지 않는다**(사용자가 직접 반영).
   - Windows 클론이면 `git -c safe.directory=D:/qdd ...`가 필요할 수 있다.
2. 다음 순서로 읽는다.
   1. `CLAUDE.md`: 모든 규칙과 제약.
   2. `docs/user-log.md`: 사용자 발언 전체(번호는 계속 늘어난다 — 최신 번호는 파일 끝을 볼 것; 2026-09-25 11:10 UTC 기준 70번, R7 13회차 D-3 정정). 의도는 여기서.
   3. `docs/plan.md`: 현재 계획 **v5.6** ([사용자] / [제안] / [결정 필요]). §5에 [결정 필요] 10개와 첫 실험.
   4. `docs/draft-log.md`: v0~v4.1 기록과 교훈. **같은 실수를 반복하지 않는다.**
   5. `docs/research/v3/README.md`(에이전트 공통 규칙) + `v3/01~19`(원문 확인 보고서). v2는 참고만(일부 정정됨).
   6. `paper/`: Overleaf 초안(CVPR 공식 author kit 구조, 한국어 마인드맵, 빨간색 = 사용자 의도).
3. 네트워크: `curl -s -o /dev/null -w "%{http_code}" https://arxiv.org/abs/2506.07339` → 200이면 원문 가능. 두 번째 세션에서는 arXiv, docs.typesafe.ai, developers.openai.com, GitHub, OpenReview, Semantic Scholar 모두 됐다.

## 2. 현재 위치
- **단계 1 초안 조사**: 2026-09-23 19:25 UTC 시작 → **21:32 UTC 마감**(약 2시간 07분, 범위 안). 마감 이유는 draft-log.
  - 첫 세션: 약 20분, 검색 요약만(v2).
  - 두 번째 세션: 19:49부터. 원문 확인 보고서 18개(v3/01~18), plan v3 → v4.1, Overleaf를 CVPR 템플릿으로.
- **단계 2 모듈 설계**: 21:32 UTC 시작. 최소 6시간(→ 03:32 UTC 이후), 최대 24시간(±1~2시간). 결과는 `docs/design/`.
  - [사용자] 파이프라인 설계가 아니다. 각 부분(M1~M10)에 대해 어디의 어떤 연구가 최고였고, 무엇을 가져올 수 있고, 우리 것에 어떻게 접목할지를 최대한 모은다. 로봇·VLA 분야일 필요 없다. LLM/VLM 방법 중 아직 로봇에 안 쓰인 것도 찾는다. 1년 반 이내(큰 주제의 기초 문헌은 예외, 표시).

## 2.5 단계 2 진행 상황 (2026-09-24 01:30 UTC)
- **먼저 읽을 것**: `docs/design/SUMMARY.md`(현재 설계 정리본 v2, 23:30 기준) → `docs/design/00-interfaces.md`(정본 §1~§23, 뒤 절이 앞 절보다 우선; SUMMARY v2 뒤의 변경은 §19~§23과 D8~D10 문서) → 필요한 모듈 문서.
- 완료: 설계 1판 M1~M10, 검증 D1·D2(원문), D3(근거 상향), D4(반대 검토 + 로봇 밖 분야 2차 + 그 원문 확인), D5(일관성 49건 정리), D6(모의 심사: 3/4/2점, 두 논문 문제), D7(선점 재검사: A3, RoboDawn), D8(RoboDawn·A3 정독), D9(Slow Brain·Show-Harness·GPT-as-Policy 정독), D10a(Harness VLA·CaP-X 정독 + 코드), D10b(Critic in the Loop·CheckVLA 정독), 첫 실험 프로토콜(`E-first-experiments.md`), 평가 설계(`EVAL-evaluation-design.md`), 설계 정리본 v2.
- 가장 중요한 사실
  - 사용자 핵심 주장 지지 근거 둘: RoboDojo Astra standard→random −11.1% 대 π0.5 −72.2%(우리가 RoboProbe 원자료로 계산), RoboDawn C2R Astra 무학습 53.2% 대 π0.5 46.0%.
  - M4 새로움: (a) 시간차 겹침 typed 호출 합의 + (b) 실행 뒤 예상 대 측정을 **함께** 쓰는 확정 규칙(A3·Slow Brain은 각각 일부만).
  - 학회: CVPR 2027 제출 2026-11-16 AoE, ICRA 2027 마감됨.
- 완료(01:30): §21·§22 반영, SUMMARY v3, plan v5.8, D11 최종 일관성 점검(60건) 반영, Overleaf v4(빨간 줄 33개 user-log 글자 일치, `tools/intent_check.py`로 확인).
- 완료(01:55): D12 선점 재검사 → 정본 §24, SUMMARY v3.1, plan v5.9.
- 완료(02:22): D13 M1·M2·M5 원문 정독 → 정본 §25, SUMMARY v3.2, plan v6.0, Overleaf.
- **단계 2 마감 03:37 UTC**: 보고서 `docs/design/STAGE2-CLOSE.md`. 사용자 결정(user-log 25·26) 반영 완료 — 논문 한 편, effort 기본 low + low·high 비교, D32 → 정본 §27, M1 기본 후보 A, Overleaf 논문형 본문(`paper/main.tex`) + 마인드맵(`paper/mindmap.tex`).
- **03:37 뒤 연장·추가 작업(05:03 UTC 기준)**: user-log 26–31.
  - Overleaf: `paper/main.tex`는 논문형 글(16쪽), 그림 5장(`paper/figures/`, 스크립트 `figures/src/make_figs.py`, 실행 `MPLCONFIGDIR=D:/tools/mplcache PYTHONPATH=D:/tools/pylib`), 마인드맵은 `paper/mindmap.tex`(빨간 줄 36, `tools/intent_check.py`).
  - 점검: D15(오류)·D16(자료·신뢰도, bib 39편 서지 확인) 반영. 22.58 → 원문 22.48 등.
  - API 일관성: 정본 §28(Astra A1–A6, Jev J1–J4, E0 반복·매일 카나리). §29 다수결(A5) 보류. §30 D18 결과 — 3개는 §31에서 결정됨(user-log 32, Claude 결정·근거 포함): A5′ 검사→수리(다수결 삭제), A4′ 적응형 patch|replace, J5 Jev conformal 게이트(E1 뒤). 설계 문서·논문 반영 완료(05:24 UTC).
  - 규칙 위반 1건 발견·수정: C 드라이브 사용 → 임시 파일은 `D:\tools\scratch_qdd`만.
- **07:02 UTC 추가**: user-log 33–34 — 학습 실행기 B(소규모 E2E, 모든 스킬 실행을 하나의 학습 실행기가 맡고 스크립트 스킬은 GT 생성기·백업, 결정 층은 학습 안 함). 정본 §32·§33, 근거 `docs/design/D19-action-expert.md`, 논문 '학습 실행기' 절·E-AE-0~5·개요도 반영. D9 해소(허용). 새 사용자 결정 대기: D35 도메인 무작위화 허용 여부, D36 주 표 실행기 S(잠정) 대 B. 글꼴은 나눔고딕(paper/fonts). PDF 출력은 D:/tools/pdf_out.
- 다음(제안, 사용자 지시 대기): 단계 3 첫 실험 E0·E0.5(같은 날) → E1 → E2a, 병행 EVAL S0·S1. 남은 사용자 결정은 SUMMARY §5.1 "나중" 묶음과 실물 실험(D30). E0 전 할 일: R1 금지어 보기 이름 교체(정본 §27 끝).
- D10에서 바뀐 것: M7 "적시 재현율(CheckVLA 방식)"은 원문 정의가 아니었음 → 고정 창판(판정 기준) + 원문판(병기). E-M8a에 원문 충실 기준 A2-CV·A1-cal·A1-wait·A6-CiL-sync. RPent Astra 92.63%는 논문 아닌 리더보드 값(8칸 T/S, 메모리 두 묶음, 에피소드당 412 s 정지형). 반대 증거 둘: CheckVLA d_lat = 10에서 경계 대기가 청크 안 수리보다 나음, CaP-X 검증 강화 프롬프트 68.29 → 65.43.
- 남은 계획(마감 전 기록, 완료됨): 단계 2 마감 보고. 이전 계획: 마지막 일관성·사용자 의도 점검 → 단계 2 마감 보고(최소 03:32 UTC 이후).
- 사용자 [결정 필요]: `SUMMARY.md` §5.1(D1~D28) + plan §8의 18(논문 틀)·19(실물 실험). 결정 전에는 두 안을 대칭으로 둔다.

## 2.6 07:02 UTC 뒤 결정 (정본 §34~§42, user-log 35~41)
- **§34·§35**: 도메인 무작위화 넣음(D35), 주 표 실행기 = 스크립트 스킬 S(D36), action expert = 구간 제한 잔차 R(`D20-expert-role.md`).
- **§36~§39** [사용자 결정]: 로봇 = ROBOTIS AI Worker, 카메라 = ZED 스테레오(머리 ZED Mini + 손목 D405), 실물 = FFW-SG2 베이스 고정, R 힘 입력 = 관절 `effort` + 그리퍼 전류(손목 F/T 추가 안 함). GT·모듈 실험 장면 = cyclo_lab AI Worker 한 팔 + `ZED_M` 쌍둥이(`D21-aiworker-zed.md`). 남은 하드웨어 결정 없음.
  - (sweep6 정정) 위 줄의 `ZED_M` 쌍둥이는 정본 §43(user-log 44)에서 폐기됐다 — AI Worker 기본 카메라 그대로, 시뮬 설정은 §47 복사(머리 ZED Mini 왼쪽 정류 672×376, VGA 수평 85°·fx 367; 좌·우 손목 D405 424×240).
- **§40**: Inspect Robots(`robocurve/inspect-robots`, MIT, 논문 없음) 판정 — 선행 겹침 없음, Robocurve Astra 실물 수치는 LOW라 근거로 안 씀(`D22-inspect-robots.md`).
- **§41** [사용자 결정, user-log 41]: **1차 평가 = Inspect Robots + AI Worker SG2 한 팔 시뮬, 2차 = RoboDojo.** B1a-real 해소(agent + Astra = 범주 1 기준선).
- **§42** (`D23-inspect-robots-integration.md`): 시계 sync/simlat/wall 주입, 주 표 = 지연 충실 트랙(우리 simlat, 동기 기준선 `LatencyChargingController`) + sync-fair 병기, RTF 기록, 오라클 상태 정보 동등, `aiworker` 몸체·`OursPolicy` 어댑터, 기준선 B1a-IR·B5-IR·B3b-IR, 단계 P0~P4. 반영 문서: EVAL, E-first, SUMMARY v3.10, plan v6.8, 논문 평가 절.
- **다음(단계 3, 사용자 go 대기)**: E0·E0.5(같은 날, E0.5 먼저) / ROBOTIS HF 데이터로 오프라인 스테레오 안정성 시험(E3-ST, E §5.10) / Inspect Robots **P0** 설치·스모크(v0.59.0·커밋 `7e506e3` 고정). 사용자 지시 전에는 시작하지 않는다.

## 3. 단계 2 진행 방법 (제안, 자율 진행)
- 모듈마다 `docs/design/Mx-*.md` 한 개: (1) 역할과 입출력 (2) 분야 전체의 최고 후보 표(어디서 최고, 조건 포함 수치, 신뢰도, 기간) (3) 가져올 것과 접목 방법 (4) 대안과 비교 실험 (5) 반대 증거 (6) 열린 질문·[결정 필요].
- 순서 제안: M4(가장 강한 컨트리뷰션) → M3·M5(M4와 얽힘) → M7·M8·M9(실패 흐름) → M1·M2(사용자 결정 대기 부분은 후보만) → M6 → M10 → 평가·첫 실험(E1 보정, E2 마차 시험, E3 형식 비교) 설계.
- 모듈마다: 조사 에이전트(요약) → **원문 확인 에이전트(독립)** → 메인 세션이 설계를 바꾸는 주장 직접 재확인 → plan·Overleaf 갱신 → draft-log 기록 → dev 푸시.
- 30분마다 루프 점검(세션 cron). 사용자 승인 없이 계속 진행한다.

## 4. 사용자 결정을 기다리는 것 (`plan.md` §5 요약)
- (sweep6 표시) 아래는 단계 2 초기 목록이다 — 대부분 정본 §26·§31·§34–§51에서 해소됐다. 지금 사용자 대기 항목은 §2.7 '사용자 대기 항목: 없음'.
1. 핵심 주장 문구 2. M1 변환 방법·인식 앞단 3. 실험 환경(RoboDojo-Sim + LIBERO-Plus 제안) 4. 기간 밖 문헌 허용 5. Astra effort 원칙 적용 6. M3 기본안(촘촘 대 목표 지정형) 7. M8 자체 판단 구현·다중 프레임 기본값 8. M6 5위·"기존 스킬" 정의 9. M10 메모리 주입·로컬 학습 진행 모델 10. 정밀 구간 정지 허용
- 결정 전에는 두 안을 모두 설계에 남기고 멈추지 않는다.
- 저장소 이름 변경(qdd → harvest)은 사용자가 직접 한다.

## 5. 작업 습관과 함정 (두 세션에서 정착)
- 커밋 메시지 끝에 Co-Authored-By 줄(시스템이 주는 것)을 붙이고 바로 `git push origin dev`.
- 조사 에이전트는 공통 규칙 파일(`docs/research/v3/README.md`)을 주고 결과를 `docs/research/<버전>/`에 쓰게 한다. 커밋은 메인 세션만.
- **API 한도**: arXiv 검색 API·Semantic Scholar·GitHub API는 같은 IP를 공유해 429가 난다. 한 번에 한 에이전트만 arXiv 검색 API(5초 간격)를 쓰게 하고, 나머지는 abs/html 페이지만.
- **원문 확인**: 요약 에이전트는 표 한 행을 일반화하거나 우리 제안을 원문처럼 적는 일이 반복됐다. 요약 뒤에는 반드시 독립 원문 확인 단계를 둔다. 원문 칸과 우리 접목안 칸을 나눈다.
- **부재 주장**은 전용 검색(검색어 기록) 뒤에만. "못 찾음"은 부재 증거가 아니다.
- **사용자 의도**: 조사가 의도와 달라도 [사용자] 줄은 그대로, 제안은 [결정 필요]로. 기본값도 사용자 예시 먼저.
- 시각은 `date -u`로 찍는다(추측 금지).
- Windows 체크아웃은 CRLF라 `sed` 줄 치환이 조용히 실패하거나 `\t`를 탭으로 바꾼다. 수정은 Edit 도구나 python으로 하고 grep으로 확인한다.
- `paper/` 컴파일: Tectonic 0.15.0 `D:/tools/tectonic/tectonic.exe`. 절차: `rm -rf D:/tools/texbuild; mkdir -p D:/tools/texbuild; cp -r paper/. D:/tools/texbuild/; cd D:/tools/texbuild; TECTONIC_CACHE_DIR=D:/tools/tectonic/cache D:/tools/tectonic/tectonic.exe main.tex`. PDF 확인은 `PYTHONPATH=D:/tools/pylib` pymupdf. 참고 문헌은 arXiv abs 페이지 메타데이터로 만든다(예: 로컬 `D:/tmp_main/mkbib.py`).
- 작업 드라이브는 D만 쓴다(C 사용 금지, 사용자 환경 규칙).
- 사용자에게는 한국어로 짧게 보고한다.

## 2.7 단계 3 이후 상태 (2026-09-24 21:03 UTC, user-log 42~66, 정본 §43~§64)
- **먼저 읽을 것**: `docs/superpowers/plans/2026-09-25-e2e-ready.md`(현재 계획: 관문 R1–R7 → S-E2E), `docs/design/00-interfaces.md` §43부터 끝까지(지금 마지막 절), `docs/stage3/direction-log.md`(방향 검사 기록), 진행 기록(ledger) `.superpowers/sdd/2026-09-24-stage3-experiments/progress.md`, 결과 문서 `docs/stage3/results/`.
- **핵심 결정**: Jev 사용 불가 → 로컬 VLM Qwen3-VL-4B typed 선택기(§44, §50, Gemma 4 E4B는 지연 탈락) / Astra = 위층 계획기 + 하트비트 5 s·단계 경계·사건(§45) [→ 정본 §82(user-log 74·76): Astra는 J1–J6(과제 컴파일·실패 진단/복구·검사 술어 합성·새 물체 이름·저빈도 진행 감사·경험 증류)에만 쓰고, 5 s 하트비트·단계 경계 확인·성공 판정은 작은 모델·코드로 강등; effort low·high 모두 보고; 유료 한도 약 10만 원; 구현은 R7 관문 뒤] / 인식 이름 `detect_phrase`는 Astra가(§46) / 카메라 = AI Worker 기본, 시뮬 설정은 humanoid-challenge-env 복사, ZED Mini VGA 85°·fx 367(§43·§47) / 시뮬 CPU PhysX(§48) / 실물 추론 = RTX PRO 6000 서버, 지금은 H200에서 가정해 검증(§49·user-log 62) / 영점 선택기는 최빈 미만 → 정답 재정의 labels_v2(§53·§54) / 학습 경로 C→A→B(§51·§52), **융합 원칙: 런타임은 단일 VLA(결정 토큰 + action expert + 확인 헤드)**, 모듈형 스택은 기준선으로 병기(§58·§60) / 입력 = 머리 + 활성 손목 다중 이미지, lead 호출(§57·§59) / S-E2E = ROBOTIS AI Worker 공개 데이터 1,575편·10 Hz(§62·§63) / **M4 (b)·critic 측정 = 세계 쪽 확인 헤드 V1h + 로봇 쪽 고유 감각 T1, critic 경보 = V1h 단독(§61·§64)**. / **VLA는 조이스틱형 손끝 정밀 보정기, 계획은 Astra(user-log 75·82)**, **결정 입력에 움직임 줄 채택·2프레임 비디오 불채택(§83, E-TC 사전 등록 판정)**, S-E2E 고유감각 속도 누수는 인과 후방 차분으로 수정(데이터 se2e_c1, 44c907d) / **정본 §84(2026-09-25 17:54 UTC 반영)**: 결합 설계 승인(직렬 1개 Astra 흐름 호출·부드러운 반영·카메라 3대·덧그림, 설계 문서 §11–§16), falsify 먼저 평가(재사용 한 번 상한), MolmoAct 주 참고(E-MA1: G0 실패 → E-MA1b 3D 궤적 보조 손실, 이미지 궤적·덧그림은 R2 뒤), 움직임 줄 확인 실험 CONFIRMED(+0.032), 유료 실험은 Claude가 유익성 자체 검사 후 실행(사용자 승인 불필요, user-log 87·CLAUDE.md) / **정본 §85**: 유료·GPU 실험은 Claude 자체 검사(user-log 87), 논문은 가설 기반 완성판(user-log 88), E-MA1 G0 실패 → E-MA1b(3D 궤적 보조) [→ 2026-09-25 18:28 UTC, R7 25회차 N41: E-MA1b는 §84가 아니라 §85] / **정본 §86(탐침 결과)**: F0, 늦은 답 폐기 15 s, 파지 확정은 T1·V1h(Astra 영상은 참고), 흐름 단가 약 50–58원/회, 탐침 총 6,933원 / **정본 §84 보충 6(2026-09-26 02:03 UTC)**: SigLIP 별도 통합 안 함 — VLA 영상 탑이 이미 SigLIP2 계열(user-log 92) / **정본 §96 보충 2(2026-09-27 16:2x UTC)**: 최종 상위 = Qwen3.5-35B-A3B(8B는 학습 부담 때문의 임시 대리) — **상위↔VLA 의도 공유 프로토콜 필수**: 상위 행동은 구간 의도(§90 now/do/next)와 함께 내려오고 VLA(0.33 s)는 그 의도를 기준으로 공백을 메움, 파지 타이밍("언제 잡지")은 VLA 소유(§87), 상위는 VLA 실행 결과를 보고 더 정확히 검증(§91); 35B 학습·추론 출력은 반드시 의도/구간 + 검증 판정 포함(astra-couple@v2 형식), solo 형식(E-TEACH-L8·E-STRIP8·E-PT/ND)만으로는 불가(user-log 125)
- **완료 관문**: R1 인식 기준선(3D 오차 35 mm — 융합의 근거), R3 처리량(6.5–12배), R4 단계 B 코드(KI 기울기 0 확인, GPU 스모크), R5 폐루프(Inspect Robots에서 한 판 성공), E-M4b-meas. **진행 중**(21:03 기준; R2·R6은 이후 완료 — §2.8): 결과 라벨(T14, GPU 0 장시간). **다음**: R7 객관 검증 순회 2회 연속 무결 → S-E2E 소규모 E2E 학습(사전 등록 뒤). [→ 2026-09-25 21:50 UTC, R7 30회차 N117: 이 줄의 진행 상태는 옛 기록 — 지금 진행 상태는 §0]
- **성격 주의(§56)**: 지금까지의 학습·평가 수치는 모두 파이프라인 확인용 소규모 시험 — 논문 결과로 쓰지 않는다.
- **운영 규칙(추가)**: 파드 작업 폴더 `/data/harvest`(옛 경로는 링크), 모든 파드 파일은 /data 아래(`source /data/harvest/env.sh`), GPU 0·1 = Isaac 렌더, GPU 2 = 학습·vLLM(렌더 금지, user-log 64 허용), GPU 3 = vLLM; 로컬 임시 파일은 D:\tools\scratch_qdd만(하위 에이전트는 C scratchpad를 기본으로 쓰려 하므로 지시문에 명시), pytest basetemp는 pytest.ini가 D로 고정; 키: HF `/data/.hf_token`, OpenAI `/data/.openai_token`(값 출력 금지); Jev 키는 없음.
- **사용자 대기 항목**: 없음(주 표 병기 유지 합의, RB1 라이선스는 논문 전 확인). [→ 갱신 2026-09-25 15:27 UTC, R7 19회차 N13: 지금 사용자 확인 대기 = (1) 정본 §82 'falsify 먼저 평가 뒤 Astra 호출' [결정 필요], (2) Astra 유료 실행(E-Astra-necessity·E-Astra-motion 탐침) 승인 — 한도 약 10만 원, (3) Astra 움직임 인터페이스 설계(브레인스토밍 진행 중, user-log 77·78)] [→ 갱신 2026-09-25 16:08 UTC: user-log 79–82 반영 — E-Astra-motion 탐침(3D 경로 P-pc·P-plane·P-tri 대 보고 조종 S, RGB만, 예산 ≤ 3만 원) 준비 중이며 첫 유료 호출 전 예상 비용 보고 대기; Astra 움직임 인터페이스(A+B·손끝 목표 순서) 설계는 탐침 결과 뒤 확정] [→ 갱신 2026-09-25 16:47 UTC, R7 22회차 N12: 탐침 범위는 user-log 83에서 바뀜 — 공간 목표 표현 없이 일반 조종(S 방식: continue/edit/stop + 평가)만, 카메라 3대·덧그림 비교 포함; 정의는 탐침 사전 등록 `docs/stage3/prereg_astra_motion.md`(커밋되면)와 설계 초안 `docs/superpowers/specs/2026-09-26-astra-vla-coupling-design.md`] [→ 갱신 2026-09-25 17:54 UTC, R7 24회차 D-1: 정본 §84(17:11 UTC)로 (1) falsify 먼저 평가 결정(재사용 한 번 상한, 보충 1)·(3) 결합 설계 승인; (2) 유료 실행은 user-log 87로 사용자 승인 대신 Claude 자체 검사(탐침은 재범위 G1·S2·표적 G2, 누적 상한 15,000원). **지금 사용자 대기 항목 없음**]

## 2.8 R7 진행 (2026-09-24 23:32 UTC)
- R1–R6 관문 + R7 전 사전 수정 완료(태그 `stage3-r1r6`, 커밋 519de26). 추가 결정 §65(결과 라벨 = 거부권 용도), §66(R2 생성기·R2_TRAIN 시드 10000–59999), §67(날짜 정정·두 호출 구조·SCOPED 목록).
- **R7 1회차 FAIL**(DEFECT 2: §63 코드 미구현·S-E2E 학습 진입점, §42 로그의 요청 해시·카나리 id / DOC 8) → 코드 수정 완료(커밋 b4a58ce, 태그 `stage3-r7fix1`), 문서는 메인이 정정(§67). **연속 무결 횟수는 0부터 다시 센다**(2회 연속 무결이면 S-E2E).
- 보고서: `docs/stage3/results/r7_cycle1.md`, 수정 기록 `docs/stage3/results/r7_fixes.md`.
- **R7 2회차(2026-09-25 00:30 UTC 무렵) FAIL**: DEFECT 0, DOC 6(날짜·계획·gen.py 주석·handoff·se2e_data §9·CONTRADICT-soft 범위 표기), SCOPED 7, NOTE 9. 코드 결함 없음 — 1회차 D1·D2 해소를 실행으로 확인(stageb se2e/r2 `--reload-check` 차 0.0, 로그 행 요청 해시·카나리 id). DOC 6건은 메인이 정정(정본 §68). 연속 무결 여전히 0회 → 3회차 진행. 보고서 `docs/stage3/results/r7_cycle2.md`.
- **R7 3회차(2026-09-25 00:55 UTC) FAIL**: DEFECT 0, DOC 4(L1 se2e_data 날짜, L2 r2_datagen 시드 서술, L3 런타임 주석의 '한 번 호출', L4 handoff 정본 범위) — 모두 2회차 정정 때 같은 사실을 적은 다른 곳을 놓친 것. 메인 정정(정본 §69). 연속 무결 0회 → 4회차. 보고서 `docs/stage3/results/r7_cycle3.md`.
- **R7 4회차(2026-09-25 01:14 UTC) FAIL**: DEFECT 0, DOC 2(M1 `fused_action.py` 머리 설명, M2 `r5_closed_loop.md`의 fused decide = vLLM 서술) — 코드 무결 4회 연속(→ **정정(R7 5회차 P1)**: 3회 연속 — 2·3·4회차, 1회차는 코드 DEFECT 2). 정정(+ `r6_eval.md` 같은 서술) → 정본 §70. 연속 무결 0회 → 5회차.
- **R7 5회차(2026-09-25 01:31 UTC) FAIL**: DEFECT 0, DOC 2(P1 이 문서 머리 상태 줄이 §70 뒤 갱신되지 않음·코드 무결 횟수 오기, P2 `r6_eval.md:54`·`r5_closed_loop.md:98`의 '단계 B 실체크포인트 어댑터 없음' 서술 — pre-R7 519de26에서 해결됨), SCOPED 8, NOTE 13. **코드 무결 4회 연속(2–5회차)**. 보고서 `docs/stage3/results/r7_cycle5.md`. → 문서 전수 정리 sweep6(2026-09-25 01:48 UTC): P1·P2, 결과 문서·D2x 열린 문제 목록의 해결·범위 표시, 102°·`ZED_M` 옛 카메라 서술, user-log 62 풀이 표시, 코드 주석(`stagea_train` 사용법, `gen` 거부 문구, `m4.py`·`closed.py` 머리 설명, Jev 옛 도구) — 기록 `docs/stage3/results/r7_sweep6.md`, 정본 §71. 연속 무결 0회 → 6회차.
- **R7 6회차(2026-09-25 02:20 UTC) PASS**: DEFECT 0, DOC 0, SCOPED 8, NOTE 14 — sweep6 표시 28개 표본 대조 모두 사실, 단계 B 실체크포인트 융합 폐루프까지 실행. **연속 무결 1회** → 7회차(코드 무변경 상태에서). 보고서 `docs/stage3/results/r7_cycle6.md`. NOTE N1(`closed.py` 융합 판 `meta.prompt_config`가 모듈형 구성)은 2회 연속 무결 확정 뒤 수정.
- **R7 7회차(2026-09-25 02:45 UTC) FAIL**: DEFECT 1(E1 — 평가 스크립트 `e05`·`rd`·`calib`·`closed`의 부트스트랩 기본 2,000회 ≠ 사전 등록 10,000회(E §1.7·EVAL §4.2), 출력에 횟수 없음), DOC 0, SCOPED 8, NOTE 14. 보고서 `docs/stage3/results/r7_cycle7.md`. → 수정(2026-09-25 03:11 UTC, `docs/stage3/results/r7c7_fixes.md`, 정본 §72): 기본값 10,000 + 출력 `meta.bootstrap`(횟수·seed·재표집 단위), 사전 등록 대조 N2(M4 W = 첫 도전 표 뒤 더 필요한 표 수, W=0 즉시 교체, 정본 §5 'W=2' → W=1 정정, 기본 C0–C6 동작 불변)·N3(판정 10 Holm 쌍별 결과)·N4(E1 ECE 판정 = 15개 동일 질량 구간)·N5(폐루프 RD 재표집 = 레이아웃 시드)·N7(융합 판 `meta.prompt_config` = 체크포인트 구성), N1(이 문서 머리 시각). 파드 1회차 잔여 `code_r7c1*`·`cache/pyc_r7c1*`·`ir/kitcache/cyclo-r7c1*`(약 1.7 GB) 삭제. 시험 로컬 800 passed/11 skipped, 파드 CPU 842/3. **코드 변경 → 연속 무결 0회** → 8회차.
- **R7 8회차(2026-09-25 03:50 UTC) FAIL**: DEFECT 5(사전 등록 대조표 47행에서 — D1 E0.5 판정 2 Holm 없음, D2 E1 본 ECE에 `ambiguous` 포함, D3 카나리 표류 판정의 기록 없는 "> 2×바닥" 조건·재표집 단위, D4 C5 "접촉 근처 τ = 0" 미전달, D5 M4 경계 직후 W 2·γ 1.0·큐 임계 g·장면 변화 조기 호출 미구현·미기록), DOC 2(D-1 이 문서 정본 범위 줄, D-2 사전 등록이 열어 둔 값의 정본 미기록), SCOPED 8, NOTE 14. 보고서 `docs/stage3/results/r7_cycle8.md`. → 수정(2026-09-25 04:27 UTC, `docs/stage3/results/r7c8_fixes.md`, 정본 §73): D1 `e05.gain_holm`(가설별 결과 `j2_gain_holm`), D2 ECE에서 `ambiguous` 제외·따로 보고, D3 정본 §28 그대로(질문별 에피소드 군집 구간 + 질문 Holm, 2배 조건 삭제), D4 `core.near_contact` → `on_vote`·`try_commit_prefix`(newest 조건 C0·C1·C2·C4 비트 동일, C3·C5·C6은 접촉 근처만 달라짐), D5 SCOPED 기록, D-1·D-2, N5(E1 범위), N8(`closed --m4-h`), 대조 중 찾은 E §1.7 해시 줄(`meta.prereg`). 시험 로컬 813 passed/11 skipped, 파드 CPU 855/3. **코드 변경 → 연속 무결 0회** → 9회차.
- **R7 9회차(2026-09-25 05:20 UTC) FAIL**: DEFECT 2(사전 등록 대조표를 원문에서 다시 만들어 — D1 런타임 γ 0.67이 "3표 중 2"를 확정하지 못함(2/3 < 0.67, E0.5 재생과도 다름), D2 기준 조건 C2(VLM Stream)가 사전 등록 정의와 다름(STALE_MAX 1.5 s 폐기·FROZEN log_only·N_max 상한·5 s 타임아웃 없음)), DOC 1(`r7_fixes.md:45` "Holm 없음"), SCOPED 10, NOTE 12. 보고서 `docs/stage3/results/r7_cycle9.md`. → 수정(2026-09-25 06:16 UTC, `docs/stage3/results/r7c9_fixes.md`, 정본 §74): D1 γ = 정확히 2/3(`m4.share_at_least`, 런타임·재생 같은 함수), D2 C2 = `agree "stream"`(상한 없음·실측 in-flight, 매 틱 가장 새 유효 응답, 5 s 타임아웃 → 기본 행동), N1 Holm은 "하한 > 0" 방향만 기각(판정 2·카나리; 판정 10은 양측), N4 E-M4-lat 지연 점의 STALE_MAX = 지연에 맞춰 다시 잡되 척도 규칙은 **[결정 필요: 메인 세션]**(코드 없음) [해결 R7 10회차 D-1: 정본 §74 보충(2026-09-25 06:20 UTC, 메인 결정)이 **(가) 비례 규칙** STALE_MAX = 1.5 s × d*_p95 / d_p95(E0)를 채택 — 구현은 지연 큐와 함께 E-M4-lat 준비 때, 주 지연 동작 불변], 수치 문턱 경계 대조(판정 입력 반올림 제거·부동소수 허용 `CMP_EPS`·near 들어가기 ≤ 5 cm), D-1, N5, N6(SUMMARY SCOPED 표시, 논문은 §74 갱신 항목). 무작위 대조 200판: C0·C1·C4 비트 동일, C2 전부(의도), C3·C5·C6은 정확히 2/3 경계 판만. 시험 로컬 841 passed/11 skipped, 파드 CPU 883/3. **코드 변경 → 연속 무결 0회** → 10회차.
- **R7 10회차(2026-09-25 07:00 UTC) FAIL**: DEFECT 1(D1 — E-M4의 H = 1이 사전 등록 정의 "같은 스텝을 시각을 당겨 2~3회 묻기"(정본 §2, M4 §4.1, E §2.6·§2.7-4·§2A.3)를 구현하지 않아 스텝당 표 1개·확정 0, 파드 Isaac C5 `commits` 0), DOC 1(D-1 이 문서 9회차 줄·`r7c9_fixes.md:15`의 N4 "[결정 필요]"가 §74 보충 뒤에도 그대로), SCOPED 11, NOTE 10. 보고서 `docs/stage3/results/r7_cycle10.md`. → 수정(2026-09-25 07:40 UTC, `docs/stage3/results/r7c10_fixes.md`, 정본 §75): D1 `m4.early_ask_steps` — H = 1 호출은 시작 시각이 [t_send + d̂, t_send + lead_max]인 모든 스텝의 앞당긴 표(E §2.6 스케줄, E0.5 재생·오프라인 E0 수와 격자 일정에서 같음), `closed --m4-lead-max`(기본 1.0 s, 후보 {1.0, 1.5}는 E0 뒤 메인 확정), H = 3은 그대로; N1 나이 비교 경계(`m4.older_than`, 정확히 1.50·5.00 s 유지); D-1; N4 near "< 5 cm" 옛 서술 표시; N7 논문 갱신 항목; "값 전달"만 본 대조표 행을 행동 시험으로(H 스텝당 표 수·조기 호출 예산·FLIP_TH 끔·in-flight). 무작위 대조 300판: **H = 3 판 155/155 모든 조건 비트 동일**, H = 1은 의도대로 달라짐(확정 있는 판 C5 32 → 88). 파드 Isaac C5 H = 1 `commits` 187. 알려 둘 한계: 단계 A DecCall은 질문 하나라 이 런타임에서 H = 1과 H = 3의 차이는 표 창뿐(판정 7 해석, 정본 §75 **[결정 필요: 메인 세션]**). 시험 로컬 860 passed/11 skipped(841 + 새 15 + 재생 조사 에이전트의 커밋 안 된 시험 4), 파드 CPU 898/3. **코드 변경 → 연속 무결 0회** → 11회차. [해결 R7 11회차 D-1: 정본 §75 보충(2026-09-25 07:44 UTC, 메인 결정) — lead_max 1.0 s 확정(E0 뒤 E §2.7-4 규칙으로 재결정), 판정 7 SCOPED(다스텝 DecCall 전까지), 빈 창 대체 규칙 채택]
- **R7 11회차(2026-09-25 08:30 UTC) FAIL**: DEFECT 0, DOC 2(D-1 handoff 10회차 줄의 판정 7·lead_max가 §75 보충 뒤에도 '결정 필요', D-2 풀 '경계 사례' 층 정의·섭동 배정이 정본에 없음), SCOPED 12, NOTE 11 — 10회차 수정(H = 1 앞당겨 묻기, H = 3 불변, 경계 나이)은 동작으로 확인. 메인이 정정 + N4(`lead_max = inf` 거부) → 정본 §76. 연속 무결 0회 → 12회차. 보고서 `docs/stage3/results/r7_cycle11.md`.
- **R7 12회차(2026-09-25 09:10 UTC) FAIL**: DEFECT 4(D1 E0.5 FLIP_TH 후보 분위가 사전 등록 α 0.01이 아님, D2 E0.5 같은 시각 뒤집힘 성공/섭동 분리·A2·A3의 A0 대비 flip 없음, **D3 C5·C4의 (b) 범주가 결정 모델 입력으로 되먹임되지 않아 런타임 C5 = C5'**, D4 호출 기록에 질문별 확률·요청/응답 원문·Astra 원응답·최대 토큰 없음), DOC 3(handoff 정본 범위, `r7c9_fixes.md:88`, `snapshot.py` 주석), SCOPED 12, NOTE 12. 보고서 `docs/stage3/results/r7_cycle12.md`. 같은 무렵(약 09:10 UTC) 사용자 승인으로 S-E2E 학습과 R2_TRAIN 대량 생성이 병렬로 시작됐다. → 수정(2026-09-25 10:00 UTC, `docs/stage3/results/r7c12_fixes.md`, 정본 §77): D3 DecCall 상태 끝에 `last_step: <범주>` 줄(형식 **ser-A-min-2**, C4·C5·C6은 마지막으로 끝난 스텝의 (b) 범주, C0–C3·새 런타임 조건 **C5'**는 none; 학습 자료는 그 편이 기록한 실행 뒤 확인에서 — R2는 기존 `verify.prev_step`이라 생성기 변경 없음) → **지금까지의 모든 체크포인트 무효**(옛 판본은 `check_prompt`·`training_prompt_config`가 거부, 진행 중인 S-E2E 체크포인트 포함 — 메인 확인 요청 [해결 R7 13회차 D-1: 정본 §77 보충(2026-09-25 10:25 UTC, 메인 결정) — S-E2E 판정은 6b013ac 고정 사본으로, 그 체크포인트는 폐루프에 쓰지 않음; 범주 희소성은 본 단계 B 사전 등록 때; C5' = none 줄 유지 확정; `serializer` 필드는 다음 `stageb_train.py` 수정 때]); D4 `probs`·요청/응답 blob(`<run>/blobs/<sha256>`)·Astra `output_text`·`max_output_tokens`·JPEG 원본(결정 호출 이미지는 해시만); D1 q0.99(α 0.01)·q0.999·q0.95; D2 `same_time_flip_by_trajectory`·`a1flip`–`a4flip`; N6 카나리 표류 의심이면 J5 끔(`closed.j5_after_canary`); N7 E0 판정 4 중앙값 집계(`latency.judgment4`); D-1·D-2·N1 표시, **D-3 보류**(재생 조사 에이전트 파일). 무작위 대조 160판: 행동·원장 모두 6b013ac와 같고 요청 마지막 줄만 다름. **코드 변경 → 연속 무결 0회** → 13회차.
- **R7 13회차(2026-09-25 11:05 UTC) FAIL**: DEFECT 3(D1 E0.5 FLIP_TH 초기값이 사전 등록 "split conformal, `question_id@vN`별"이 아님 — 5질문 평균 점수 + `np.quantile`, D2 카나리 기준일이 `question_id` 판본을 보지 않음(ser-A-min-2 판이 ser-A-min-1 기준과 비교), D3 표류 의심 뒤 재보정 없이 다음 날 깨끗한 카나리로 J5가 다시 켜짐), DOC 4(D-1 §77 보충이 해결한 "메인 확인 요청" 표시 없음, D-2 `r6_eval.md:46` C5', D-3 이 문서 user-log 번호, D-4 CLAUDE.md user-log 69·70 제약 — D-3·D-4는 메인이 93c68d7에서 고침), SCOPED 14, NOTE 14. 보고서 `docs/stage3/results/r7_cycle13.md`. → 수정(2026-09-25 11:55 UTC, `docs/stage3/results/r7c13_fixes.md`, 정본 §79, 커밋 안 함): D1 `e05.flip_th_conformal` 질문별 ⌈(n+1)(1−α)⌉번째 값, n이 모자라면 문턱 없음(None, α 0.01은 n ≥ 99); D2 `canary.select_baseline` = 같은 세트 × 같은 `question_ids`의 첫날(새 판본 = 새 기준일); D3 `latest_canary.last_drift` — 가장 최근 표류 카나리 이후 보정 파일이 있어야 J5 켬; N1 경계 틱 호출도 방금 끝난 스텝 범주를 싣도록 호출을 경계 판정 뒤에 만듦(학습 자료 유도 불변, 조기 호출은 다음 틱 그대로; 무작위 대조 160판 중 156판 93c68d7과 비트 동일, 달라진 4판은 경계 틱 호출의 줄·epoch뿐, 성공 불변); N5 없는 `--data` → rc 1; 워커 환경 `OMP_WAIT_POLICY=PASSIVE`(메인 요청); D-1·D-2 표시. 시험 로컬 953 passed/13 skipped, 파드 CPU 1001/4. **코드 변경 → 연속 무결 0회** → 14회차. [R7 14회차 N4: 이 수정은 커밋 2c4a30d(11:56 UTC)·태그 `stage3-r7fix13`으로 커밋됨]
- **§78 PhysX 하드 리셋**(커밋 e8e1864, 2026-09-25 11:14 UTC): `Env.reset`이 에피소드마다 PhysX 장면을 다시 만든다(24/24 새 프로세스와 비트 동일, +0.28 s) — 결과 라벨 신뢰 = 재생 비트 동일, 옛 물리 녹화(풀·R2 DEV·E0.5 스냅샷)는 새 빌드로 재생되지 않음(29편 재라벨 방식은 R2_TRAIN 뒤), R2_TRAIN은 하드 리셋 빌드(`code_r2train_e8e1864`)로 재기동.
- **S-E2E 학습 통과**(커밋 bc1bd54, 11:58 UTC, `docs/stage3/results/se2e_train.md`): 두 시드 모두 사전 등록 (a)–(d) 통과, (e) 재개 재현 "tolerance"(총손실 상대차 중앙값 0.46 %·0.39 %) — 파이프라인 확인(§56), 판정 스크립트는 결과 전 커밋판(0b056b8). 진단 D1–D3(커밋 a4a6257, 12:32 UTC, 사전 등록 `prereg_se2e_diag.md`): 상수 lr에서 계산량 한계 아님, 과적합 시험 결론 없음(1k에서 train 0.885 / val 0.56), 라벨 모호성 한계 아님.
- **R7 14회차(2026-09-25 12:30 UTC, 대상 2c4a30d) FAIL**: DEFECT 1(D1 결과 라벨을 읽는 코드(`--truth outcome:`, 단계 A `OutcomeLabels`)가 §78 (1) 신뢰 기준(재생 비트 동일)을 적용하지 않음 — `replay_maxabs` 0.37 행이 정답에 들어감), DOC 2(D-1 `r7c12_fixes.md:17` §79 N1 정정 표시 없음, D-2 `labeler.md:40` 등 "새 빌드로 29편 재라벨"이 §78 (2)와 어긋남), SCOPED 18, NOTE 13 — 13회차 수정(§79)과 §78 하드 리셋(같은 워커 C5 → C5' 행동 1,592개 비트 동일)은 동작으로 확인. 보고서 `docs/stage3/results/r7_cycle14.md`. → 수정(2026-09-25 13:10 UTC, `docs/stage3/results/r7c14_fixes.md`, 정본 §80, 커밋 안 함 [정정 R7 15회차 N7·N15, 정본 §81: 커밋됨 — 0790dd4, 태그 `stage3-r7fix14`, 커밋 시각 12:57:49 UTC(위 "13:10 UTC"는 짐작 시각)]): D1 `stagea_data.replay_bit_identical`(필드 없음·null도 불신) — `load_truth` outcome·`OutcomeLabels`·`labels_v2_eval` 자기 점검이 그 행을 빼고 뺀 수를 `meta.truth_label_trust`·단계 A `label_trust`에 기록(`stagea_data.py` 프롬프트 해시 바뀜 — 기존 체크포인트는 §77로 이미 무효, S-E2E 고정 사본 영향 없음); N1 판정 스크립트 입력 검사((b) 처음·끝 3점 = 0·4000·4500·4686, (e) 창 = 2001–2050, 드라이버 폴더 필수) — 실제 로그 재실행 `verdict_v2.json` 판정 불변; N3 `--seeds` 0편 → rc 1, `determinism`은 시드 거부 뒤 폴더 생성; D-1·D-2 표시. 시험 로컬 987 passed/13 skipped, 파드 CPU 1041/4. **코드 변경 → 연속 무결 0회** → 15회차.
- 14회차 수정 뒤 메인 커밋: 9cee78b(S-E2E 데이터 규모 곡선 사전 등록 `prereg_se2e_diag.md` §7, 12:57:49 UTC; 13:48 UTC에 파드 `stageb_train` 학습 프로세스 2개 진행 중 — 읽기만 확인), b283da2(논문), 0b853c2(E-TC 시간 맥락 2×2 사전 등록 + 선택 옵션 코드, 파드 고정 사본 `code_se2e_temporal`).
- **R7 15회차(2026-09-25 13:25 UTC, 대상 9cee78b) FAIL**: DEFECT 0, DOC 2(D-1 `pool.md:32`·`:57`·`e_m4b_meas.md:19`·`D28:149`의 "고정 선행 실행으로 재실행 비트 동일"이 §78 사실과 어긋나는데 정정 표시 없음, D-2 정본 §80·`r7c14_fixes.md:24`의 자체 점검 "597행·13편" = 실제 13시드·20편), SCOPED 18, NOTE 15 — 14회차 수정(§80)은 모든 소비처에서 동작으로 확인(파드 실제 풀 886 / 6,627행), 같은 워커 C5 → C5' 행동 1,877개 비트 동일. 보고서 `docs/stage3/results/r7_cycle15.md`. → 수정(2026-09-25 13:47 UTC, `docs/stage3/results/r7c15_fixes.md`, 정본 §81, 커밋 안 함): D-1 표시(+ 같은 사실 grep: `pool.md:33`, `r2_datagen.md:99`, `draft-log.md:412`, `cli_pool`·`gen` 주석), D-2 정정(파드 재확인 90편 중 20편); N2 `stageb_train.RESUME_KEYS` + 진단·시간 맥락 옵션 12개, 모든 `train` 옵션을 `RESUME_KEYS`/`RESUME_FREE`로 분류하는 시험, 옛 체크포인트의 없는 키 = 기본값; N10 `cli_label`이 재생 거리 없는 결과를 null(불신)로 적음; N3 `truth_label_trust`·`label_trust`에 `questions`(5질문 805 대 전체 886); N15·N7 시각·커밋 정정. 시험 로컬 1006 passed/15 skipped, 파드 CPU `tests/train`+`tests/eval` 381 passed. **코드 변경 → 연속 무결 0회** → 16회차.
- **R7 16회차(2026-09-25 14:16 UTC) PASS**(대상 f91ad52): DEFECT 0, DOC 0, SCOPED 18, NOTE 16 — 15회차 수정·E-TC 사전 등록 대 코드 (a)–(e) 모두 동작으로 확인. **연속 무결 1회** → 17회차(코드 무변경 상태에서). NOTE N1(predict·evalck의 형식 대조), N2(replay_max NaN 순서), N3(temporal_verdict 검증 집합 확인)은 2회 연속 무결 확정 뒤 처리. 그 사이 커밋은 문서·논문·데이터 규모 결과(3478cf2)·user-log뿐.
- **R7 17회차(2026-09-25 14:46 UTC) FAIL**(대상 9a0ae52): DEFECT 0, DOC 1(se2e_diag.md 결론 요약·6절이 8.3의 D2 해소를 반영 안 함) — 메인이 정정. 연속 무결 0회로 돌아감 → 18회차. 사고: 메인의 기록 덧붙이기 스크립트가 CRLF 파일을 한 번 더 변환해 docs/user-log.md 줄 끝이 \r\r\n이 됐다(내용 불변) — 복원. [정정 2026-09-25 15:06 UTC, R7 18회차 D-1: 이 줄 자체도 제어 문자로 쪼개졌던 것을 복원]
- **R7 18회차(2026-09-25 15:06 UTC) FAIL**(대상 7fe42c6): DEFECT 0, DOC 3 — D-1 7fe42c6 기록 줄이 제어 문자로 쪼개짐(draft-log·handoff, 복원), D-2 handoff가 §82 미반영(정본 범위·핵심 결정 줄 갱신), D-3 D25·D28이 5 s 하트비트를 설계 기본으로 적음(§82 표시). 연속 무결 0회 → 19회차. [정정 2026-09-25 15:27 UTC, R7 19회차 D-1: 이 커밋에서 정본 범위 줄 가운데 바뀐 것은 머리 줄(:3)뿐이었다(핵심 결정 줄 :84의 §82 표시는 들어갔음) — :5·:83은 19회차 정정에서 §82로 갱신]
- **R7 19회차(2026-09-25 15:27 UTC) FAIL**(대상 25370d5): DEFECT 0, DOC 2 — D-1 handoff :5·:83 정본 범위가 §81로 남음(18회차 정정이 머리 줄만 고침), D-2 steering_representation 문서의 'Astra는 경계·사건에만' 서술에 §82 표시 없음. 정정: 고친다고 적은 줄과 git diff 실제 변경 줄을 대조했다. 연속 무결 0회 → 20회차.
- **R7 20회차(2026-09-25 15:48 UTC) PASS**(대상 7e0aeba): DEFECT 0, DOC 0, SCOPED 19, NOTE 23 — **연속 무결 1회**. NOTE N12(정정 표시 문구)·N14(draft-log의 \tentative가 탭으로 깨짐)·N15(handoff 끝 줄바꿈·목록 사이 빈 줄) 정리. 21회차는 코드 무변경 상태(문서·결과·정본 §83만 추가된 HEAD)에서.
- **R7 21회차(2026-09-25 16:08 UTC) FAIL**(대상 874cb33): DEFECT 0, DOC 1 — handoff 정본 범위가 §82로 남음(정본은 §83). 같은 원인(정본 절이 늘 때마다 handoff 범위 숫자가 낡음)이 세 번째라 handoff에서 정본 범위 숫자를 없애고 '마지막 절까지'로 바꿨다. NOTE N20·N22·N21·N13·N12 정리. HEAD는 속도 누수 수정 코드(44c907d)를 포함하므로 22회차는 새 코드를 포함한 HEAD에서 0부터.
- **R7 22회차(2026-09-25 16:45 UTC) PASS**(대상 3a3d76c): DEFECT 0, DOC 0, SCOPED 20, NOTE 30 — 속도 누수 수정(인과 후방 차분)·`reconvert`·확인 판정 스크립트를 동작으로 확인(se2e_c1 42,813행 재계산 오차 0.0). **연속 무결 1회**. [→ 추가 2026-09-25 17:19 UTC, R7 23회차 D-1: 이 줄은 보고서 커밋(a4cddad) 때 빠졌던 것을 넣었다]
- **R7 23회차(2026-09-25 17:11 UTC) FAIL**(대상 a4cddad): DEFECT 0, DOC 1(D-1 handoff §2.8에 22회차 줄 누락 — 보고서는 커밋했으나 이 절·draft-log·direction-log를 갱신하지 않음), SCOPED 20, NOTE 37. 코드 무변경·회귀 전부 통과. 연속 무결 0회 → 24회차(HEAD = 정본 §84 + 보충 1·2, 결합 설계 승인, MolmoAct 연구 문서; 코드 무변경). 교훈: R7 보고서를 커밋할 때 handoff §2.8·draft-log·direction-log 줄을 **같은 커밋**에 넣는다.
- **S-E2E 움직임 줄 확인(2026-09-25 17:24 UTC 기록, `prereg_se2e_motion_confirm.md` 15:52Z → `results/se2e_motion_confirm.md`) CONFIRMED**: 고친 데이터 `se2e_c1`, 2,000스텝 × 시드 1·2, 검증 전체 1,799 — none 0.672/0.677, motion 0.707/0.708, 합동 효과 +0.032 [+0.022, +0.043](규칙 ≥ +0.015·하한 > 0), 전이 층 +0.030 [+0.016, +0.043](규칙 ≥ −0.01). 정본 §83 채택이 확인됨. GPU 2·3 비움 → E-MA1(정본 §84)이 이어 쓴다.
- **E-MA1 → E-MA1b(2026-09-25 19:58 UTC 기록)**: E-MA1(`prereg_ma1.md` 17:32:31Z) 관문 G0 실패(명목 101 px·PnP 87 px, 문턱 12 px; `results/ma1.md`) → 메인 재설계로 카메라 없는 로봇 기준 3D 궤적 보조 손실 **E-MA1b**(`prereg_ma1b.md` 17:54:05Z, `results/ma1b.md`): 기준 motion_s1·s2 재사용(예측 비트 동일), a3d 시드 1·2, 검증 1,799 합동 **+0.0031 [−0.0022, +0.0085]**, 전이 0.000 → **불채택**(단계 B 레시피에 궤적 보조 손실 넣지 않음). 이력 덧그림(O)·영상 궤적(trace5)은 R2_TRAIN 뒤 E-MA1 재등록. `stageb_train.py`에 `--aux-extra a3d@v1` 옵션(기본 경로 바이트 동일, R7 26회차 동작 검토).
- **E-CAM3·E-MA3(2026-09-25 22:21 UTC 기록, 둘 다 불채택)**: 사전 등록 `prereg_cam3.md`(20:30:33Z, `5c46056`)·`prereg_ma3.md`(20:34:02Z, `40895f8`), 기준 motion_s1·s2 재사용(검증 1,799 예측 비트 동일), GPU 2·3 병렬. **E-CAM3**(머리 + 양 손목): 합동 +0.0014 [−0.0041, +0.0069]·FULL p95 +11.7 % → §57 유지(`results/cam3.md`). **E-MA3**(expert 층별 KV): 청크 오차 합동 −2.19 % [하한 1.48 %] < 문턱 5 % → §58 한 층 조건 유지(`results/ma3.md`). 정본 §57 보충·§84 보충 3. 기준 파일(`stageb_train.py` 등) 무수정 — 옵션은 새 파일 CLI 포장(`se2e_cam3.py`·`se2e_kvcond.py`). ≈ 3.4 GPU-h, 유료 0원.
- **R7 24회차(2026-09-25 17:45 UTC) FAIL**(대상 6e3fda6): DEFECT 0, DOC 1(D-1 handoff §2.7 '사용자 대기 항목'이 정본 §84로 해소된 falsify 순서·결합 설계를 여전히 대기로 적음, 핵심 결정 줄에 §84 없음), SCOPED 21, NOTE 44. 코드·시험·사전 등록 무변경, 회귀 전부 통과. 연속 무결 0회 → 25회차(HEAD에 E-MA1 코드·탐침 사전 등록·확인 실험 결과 포함 → 새 코드 행동 확인). 교훈: 정본에 [사용자 결정] 절을 더하는 커밋에서 handoff §2.7(대기 항목)·핵심 결정 줄을 **같은 커밋**에 고친다.
- **R7 25회차(2026-09-25 18:15 UTC 무렵) 기준선 FAIL**(대상 c767c3a): DEFECT 0, DOC 1(D-1 정본 §82 보충 '실행은 사용자 승인'이 user-log 87·CLAUDE.md와 반대 — 정본에 옮긴 절이 없었음), SCOPED 21, NOTE 45. 실험 코드 절(카운트와 별개) FAIL: 코드 결함 0, DOC 3(E-D1 탐침 등록 변경 기록 시각, E-D2 탐침 재범위 개정 미커밋 상태에서 유료 호출 재개 — 35029e5로 늦게 커밋·이탈 기록, E-D3 ma1.md 표본 서술). 정정: 정본 §85(2026-09-25 18:28 UTC). 연속 무결 0회 → 26회차(기준 파일 `stageb_train.py` 수정 ded38d7 포함 → 행동 확인). 교훈: 사용자 결정은 user-log·CLAUDE.md와 **같은 커밋에** 정본 절로 옮긴다; 등록 개정은 커밋 해시를 받은 뒤에만 유료 호출 재개.
- **R7 26회차(2026-09-25 19:40 UTC 무렵) 기준선 FAIL**(대상 6483397): DEFECT 0, DOC 1(D-1 책 01장이 R7 PASS 회차에서 6회차를 빠뜨림), SCOPED 22, NOTE 54. 실험 절(E-MA1b) PASS. `stageb_train.py` 기본 경로가 수정 전과 비트 동일함을 실제 학습으로 확인. 정정(2026-09-25 20:01 UTC): 책 01·02·03·04, 정본 N47·N48 표시, E-MA1b 불채택을 정본 §85 보충으로. 연속 무결 0회 → 27회차. 교훈: 책도 기준선 범위 — 책 숫자는 원 문서와 대조한 뒤 쓴다.
- **R7 27회차(보고 2026-09-25 20:3x UTC → 정정 커밋 47edadb 20:37:43 UTC [2026-09-25 21:07 UTC, R7 28회차 N86]) 기준선 FAIL**(대상 7a02943): DEFECT 0, DOC 1(D-1 책 04 R2_TRAIN 줄의 '지금 P0 생성 중' — 26회차 정정 자체가 틀림), SCOPED 21, NOTE 61. 실험 절 FAIL(코드 결함 0, DOC 2: astra_motion 결과 수치 3곳·Qwen 프롬프트 판본 혼재 미기재). 정정(2026-09-25 20:37 UTC): 책 01–06, 결과·등록 정정, 정본 §86 보충(시간 초과 20 s). 연속 무결 0회 → 28회차. 교훈: **정정 문장도 사실 확인 뒤 쓴다** — 26회차 정정이 새 오류를 만들었다(함정 P68).
- **R7 28회차(보고서 머리 21:05 UTC, 파드 정리 끝 21:02 UTC [2026-09-25 21:50 UTC, R7 30회차 N119]) 기준선 FAIL**(대상 47edadb): DEFECT 0, DOC 2(D-1 27회차 정정 '19:07Z P0 행 조립 끝'도 틀림 — 행 파일은 12:20Z 파일럿 병합분뿐, D-2 책 04 상태 꼬리표 낡음), SCOPED 21, NOTE 70. 실험 절 PASS(탐침 정정·E-CAM3/E-MA3 등록 확인). 정정(2026-09-25 21:07 UTC): **구조적 조치 — 진행 상태는 handoff에만, 책에는 적지 않음(P69)**. 연속 무결 0회 → 29회차. **R2_TRAIN 운영 주의(N94)**: 생성이 끝나면 병합·검사를 다시 돌려야 단계 B가 전체 행을 읽는다.
- **R7 29회차(보고 2026-09-25 21:2x UTC, 파드 정리 21:23:27Z) 기준선 FAIL**(대상 0cc45ae): DEFECT 0, DOC 3(D-1 책 01 R2_TRAIN 상태 칸 `[진행 중]` 남음, D-2 책 04 시드 수를 P0 값으로 전체에 적용, D-3 책이 넘긴 진행 상태가 handoff에 없음), SCOPED 21, NOTE 77. 실험 절 PASS. 정정(2026-09-25 21:30 UTC): handoff 0절 '진행 중 작업' 신설, 책 01 표 D 상태 칸 통일, 04 시드 수 종류별. 연속 무결 0회 → 30회차. [→ 2026-09-25 21:30 UTC, R7 29회차 N103: 28회차 줄 '보고 21:02'는 파드 정리 끝 시각, 보고서 머리 21:05]
- **R7 30회차(보고 2026-09-25 21:49 UTC 무렵) 기준선 FAIL**(대상 272432e): DEFECT 0, DOC 1(D-1 책 01 표 D의 앞 세 줄 상태 칸이 새 규칙과 다름 — 29회차 정정을 일부 줄에만 적용), SCOPED 21, NOTE 89. 실험 절 PASS. 정정(2026-09-25 21:50 UTC): 책 01 모든 표 상태 칸을 스크립트로 정리하고 **기계 검사**(네 값 외 0). 연속 무결 0회 → 31회차.
- **R7 31회차(보고서 머리 2026-09-25 22:09 UTC, 파드 정리 22:07 UTC [2026-09-25 22:36 UTC, R7 32회차 N139]) 기준선 PASS**(대상 ac7a314): DEFECT 0, DOC 0, SCOPED 21, NOTE 100. 실험 절 PASS. **연속 무결 1회**. 상태 칸 기계 검사(40칸 모두 네 값), 행별 내용 보존(32줄) 확인. 남은 NOTE: 검증기 `validate.py:78`(검증 대상 판정이 줄의 `decision` 표지를 읽음, N131)·`:82-85`(npz에 `grip` 없으면 `:83`에서 KeyError, N132) [줄 번호 2026-09-25 22:36 UTC, R7 32회차 N149]; 32회차가 실데이터 336편에서 불일치 0을 확인. 추가 N142(중간 프레임 시각 NaN 통과)·N143(행 시드 변경·`phase_id` null 통과) — 기준점 태그 뒤 TDD로 고친다.
- **R7 32회차(보고 2026-09-25 22:3x UTC) 기준선 FAIL**(대상 4c532d2): DEFECT 0, DOC 2(D-1 handoff §0 R2_TRAIN 줄 '아직 파일럿 병합분'이 확인 시각에 이미 틀림, D-2 책 01 R7 행 결과 칸이 31회차 PASS를 빠뜨림), SCOPED 21, NOTE 109. 실험 절 PASS. 코드는 ac7a314와 바이트 동일. 정정(2026-09-25 22:36 UTC): §0 줄을 파드 스크립트 출력으로 다시 씀, 책 01 R7 행은 회차 목록 대신 handoff §2.8 참조. 연속 무결 0 → 33회차.
- **R7 33회차(대상 d0e25f4) 기준선 PASS**: DEFECT 0, DOC 0, SCOPED 20, NOTE 117. 실험 절(E-CAM3·E-MA3) PASS — 판정을 검증자 자체 코드로 다시 계산해 `verdict_full.json`과 일치. **연속 무결 1회**. 새 NOTE 중 태그 뒤 처리: 검증기 N154(행 `kind`·라벨 행 비-`k` 필드 변경 통과, N142·N143과 같은 계열), 결합 구현 때 N155(에피소드 끝에 날아가던 Astra 호출이 로그 행 없음).
- **R7 34회차(대상 972b0be) 기준선 FAIL**: DEFECT 0, DOC 1(D-1 handoff §0 R2_TRAIN 줄이 인용한 `results/r2_train_gen.md`가 커밋에 없음 — 메인이 '기록만' 커밋에 R2 에이전트의 미커밋 handoff 편집을 함께 올림, 함정 P14 재발 → P72), SCOPED 20, NOTE 128. 그 줄의 수치는 검증자가 파드 원자료로 모두 확인(유효 5,126편·행 1,495,348·결정 152,409, 18폴더 병합 검증). 정정(2026-09-25 23:30 UTC): 결과 문서를 책 01·04·05·06·handoff와 한 커밋으로. 연속 무결 0 → 35회차. [보고서 머리 작성 2026-09-25 23:20 UTC — 2026-09-25 23:58 UTC, R7 35회차 N181]
- **R7 35회차(대상 b2b0b3e, 보고서 작성 2026-09-25 23:5x UTC 무렵 — 보고서 머리의 '09-26 00:0x'는 틀려 정정 표시함) 기준선 FAIL**: DEFECT 0, DOC 2(D-1 R2_TRAIN 병 놓기 실패 798 → 794, D-2 본 생성 시작 12:15 → 실제 12:00·파일럿 검사는 관문이 아니었음), SCOPED 20, NOTE 143. 실험 절: 검토할 것 없음. R2_TRAIN의 나머지 수치는 검증자 자체 스크립트로 모두 일치. 정정(2026-09-25 23:58 UTC). 연속 무결 0 → 36회차.
- **R7 36회차(대상 ffe335c, 보고서 머리 2026-09-26 00:20:35 UTC) 기준선 PASS**: DEFECT 0, DOC 0, SCOPED 20, NOTE 152. 실험 절(E-MA2 사전 등록·코드) PASS. **연속 무결 1회**. 태그 뒤 처리: 검증기 N193(행에 `k`가 없으면 `validate.py:65`에서 KeyError, N132 계열); 다음 논문 갱신 때 N192(논문의 E-MA2 '사전 등록 중' → 등록·학습 중).
- **R7 37회차(대상 3d4738a, 보고서 머리 2026-09-26 00:42:18 UTC) 기준선 PASS**: DEFECT 0, DOC 0, SCOPED 20, NOTE 164. 실험 절: 새 코드 없음. **연속 무결 2회 → E2E 준비 기준점 도달**, 태그 `stage3-e2e-ready`(3d4738a).
- **검증기 약점 묶음 TDD(태그 뒤, 기록 2026-09-26 01:07 UTC) 끝**: N131·N132·N142·N143·N154·N165·N190·N193·N210이 모두 예외·조용한 통과 대신 구조 오류로 기록된다 — `harvest/datagen/validate.py`만 변경(`stageb_data.py`는 PROMPT_FILES_B라 무변경; 행 고유감각 유한성·행동 = npz 청크 대조는 validate.py에서). 새 시험 37개(약점 32개 RED 32 failed → GREEN, 경계 보존 5개), 실데이터 R2 DEV 36/36·R2_TRAIN 306편 오류 0(도장 메타 불일치 0), 로컬 1,203 passed·파드 CPU 1,352 passed. 태그 뒤 코드 변경이라 다음 R7 회차 검토 대상. [`results/validator_gaps.md`](stage3/results/validator_gaps.md)
- 2026-09-26 01:20 UTC E-MA2 결과(사전 등록 `fde4f6a` 2026-09-25T23:32:45Z): Astra `edit`을 VLA 입력에 문장(C1)·머리 영상 화살표(C2)로 준 두 판 모두 돌린 명령 준수율 < 0.6(0.177·0.001) → **NONE**: VLA 입력에 명령을 넣지 않고 §11 코드 편향만(정본 §84 보충 4). C1 결정은 명령을 따르나(0.97) expert 청크가 안 따름(0.28·0.09) — E-SR0·결합 구현은 청크 수준 준수로 잰다. 재설계 없음, GPU 2·3 ≈ 2.6 GPU-h. 이탈: 파드 `python3 -c` 2회(읽기 전용)·빈 heredoc 초안 1줄(실행 전 삭제).
- 2026-09-26 01:52 UTC E-SR0 결과(사전 등록 `7b42b97` 2026-09-26T01:36:22Z, 변경 1 `fb64b20`): 학습 없이 기존 체크포인트의 decide → chunk 경로에서 expert에 주는 조이스틱 결정만 반사실 값으로 바꿔 청크 FK 손끝 변위 방향을 잼 — 반사실 방향 준수율(cos > 0.5) S-E2E(motion_s1 + s2, 1,799 × 2) **0.340**(우연 0.335) · R2(E-MA2 C0, 1,200) **0.431**(우연 0.333), 둘 다 < 0.6 → **WEAK**. S-E2E는 방향 토큰에 따른 청크 퍼짐 1.1 mm(청크 15.6 mm), R2는 9.3 mm로 바뀌되 강제한 쪽이 아님; `phase_id` close/open은 그리퍼 0.95로 따름 → 결합은 E-SR1b(조건 강화, 새 사전 등록) 전까지 결정 토큰이 아니라 청크·손끝 기준 경유로(정본 §84 보충 5). 권고: 결정 드롭아웃 + CFG(π*0.6 RECAP, 학회 미확인 기술 보고서), 결정–궤적 독립 대응 데이터(MolmoAct ICRA 2026) — 결과 5절. GPU 2·3 ≈ 0.3 GPU-h, 변경 1 = 판정 입력 검사 버그(R2 키 중복, 값 계산 전). 이탈: 로컬 `python -c` 1회(판 확인)·등록 커밋 메시지 heredoc 1회. user-log 90 SigLIP 결정은 이 결과 뒤로 미뤄져 있었음.
- 2026-09-26 04:50 UTC E-SR1b 결과(사전 등록 `ad85725` 2026-09-26T02:30:01Z): 결정 드롭아웃 + CFG(A), 되짚기 결정 라벨(B), AB를 E-MA2 C0 레시피로 시드 2개씩(+ A02 1개, C0 s1) 학습 — x2 파드 GPU 0·1 + 메인 GPU 2·3 → 두 지표 모두 **NOT_REACHED**: 스트레스 A_xy 최고 B 0.598(기준 0.438), 그럴듯한 P_xy 최고 B 0.398(기준 0.358), B는 청크 MSE +14.6 %·그리퍼 −0.030으로 비열등 실패; 드롭아웃은 준수를 낮추고 CFG는 청크·그리퍼를 무너뜨림; 근처 > 자유. 결합은 청크·손끝 기준 경유 유지(정본 §84 보충 7). ≈ 7.9 GPU-h. 평가·판정 도구 `tools/sr1b/`(두 지표·거리 층·그리퍼 비열등·CFG 격자)는 E-SR1c에서 재사용 가능.
- 2026-09-26 05:28 UTC E-SR1c 결과(사전 등록 `a79bd64` 2026-09-26T03:09:14Z, 변경 1 `2a2ad34`·2 `beacc0d`): 먼 구간 반사실 분기 50 %(C1)와 + 권한 게이트 adaLN(C2)을 E-MA2 C0 레시피로 학습(메인 GPU 1, Isaac G-br는 GPU 0 — 끝난 뒤 비움) → **ADOPT_C1(FULL)**: 먼 구간 반사실 준수 0.991(C0 0.344), 근접 비열등 N1–N5 통과, C2 이득 없음 → 결합은 먼 구간(a = 1)만 결정 경유 조종, 근접은 VLA 자기 결정, §6·§11 × a, 레시피에 분기 데이터(정본 §84 보충 8). 결과 `docs/stage3/results/sr1c.md`, 체크포인트 `/data/harvest/ckpt/sr1c/c1/last`.
- 2026-09-26 06:15 UTC 프롬프트 검진 결과(user-log 96·97, 유료 부분 사전 등록 `5b62187`, `docs/stage3/results/prompt_health.md`): 운영 프롬프트는 바꾸지 않음(판본 기록 누락 1건만 고침). **결합 `astra-couple@v1`은 판 올림 전에 E-Couple·E-Astra-necessity를 돌리지 말 것**: 판정 정의 없음·그리지 않은 화살표 범례·확정 화살표 = 결정 토큰 중심값(F1–F3), 로컬 자리 서버는 영상 ≥ 3(F12), 로컬 팔은 문구 2–3개(Qwen 문구 뒤집힘 최대 43–70 %; Astra 잡기 3/30으로 강건). VLA `mag_coarse`·`dir_xy` 라벨 뜻 ≠ 질문 문구(F16·F17) → Task 12 직렬화 판 올림(ser-A-min-3)에 반영 검토. 제안 목록 9절(메인 승인 대기). 유료 2,093.9원.
- 2026-09-26 06:25 UTC MolmoAct 실데이터 준비(user-log 99·정본 §89, 계획 `27ade09`, 변경 1 `febe4f1`·2 `56291df`, 학습 없음): R2 시뮬 MolmoAct 데이터는 파일럿에서 중단(옵션 코드는 남김, 기본 경로 비트 동일 5/5). S-E2E Molmo2-ER 포인팅 라벨은 원시 오류 27–30 %(다른 팔 17–20 %) → 거르개 v2(명목 머리 카메라 투영 + 편별 오프셋으로 좌우 배정 + 150 px) 검증 표본에서 남긴 점 오류 3.7 %·유지 94.5 %(RB2 2 %·RB1 11 %); 궤적 끝 = 쥐기 뒤 놓기, MolmoAct 청록 그대로. **RB2로 E-MAR-real(A 궤적 보조 × O 이력 덧그림) 등록 가능 — 초안 6절**, RB1은 놓는 순간 그리퍼가 영상 밖(C3). 궤적 조건 조종(S)은 같은 장면·대체 경로 실물 시연(대상 2 × 직접 50 + 대체 50)이 필요 [결정 필요: 수집]. ≈ 0.56 GPU-h. [`molmoact_real_readiness.md`](stage3/molmoact_real_readiness.md)
- 2026-09-26 06:26 UTC E-NOV0 결과(사전 등록 `d225a84` 2026-09-26T05:36:22Z, 변경 없음): 학습 없이 E-MA2 C0 자신의 백본 특징(주 = 마지막 층 결정 위치 은닉)으로 R2_TRAIN fit 기억 15,000개까지 코사인 k-NN 새로움 점수 → 평가 분할 7,308에서 큰 오류(결정 틀림 또는 청크 MSE 상위 10 %) AUROC **0.570**, 90 % 재현율 호출 감소 **13 %**(R1 문턱 0.75·30 % 실패), dr 이동 **0.742**·과제 빼기 **0.751**(R2 문턱 0.80 실패); 지연 GPU p95 0.17 ms·실물 대 시뮬 1.000 → **NONE**: 결합 흐름 호출에 새로움 게이트 안 둠, T_nov 현행 유지(정본 §84 보충 9). 판정 밖: 결정 확신도가 큰 오류 0.804·호출 45 % 감소, 문맥 평균 은닉이 dr 0.964 — 새 등록으로만. GPU 2 ≈ 1.0 GPU-h, 유료 0. 도구 `tools/nov0/`(특징 추출·k-NN·AUROC·호출률 표)는 확신도 게이트 재등록에 재사용 가능.
- 2026-09-26 07:09 UTC E-SR1d 결과(사전 등록 `32065de` 2026-09-26T05:58:59Z, 변경 없음): E-SR1c 레시피(먼 구간 반사실 분기 50 %)를 실데이터 S-E2E에 시뮬 없이 — 기구학 분기 61,993행(G-kin 통과), 그리퍼 사건 대용 권한(G-a 통과, 참 거리 검증 불가), C1 시드 1·2 (x2 GPU 0·1) 대 motion_s1·s2 → **NOT_REACHED**: 먼 구간 A_xy 0.339 → 0.536(목표 0.8), 근접 비열등 모두 통과 → 실데이터 결합은 청크·손끝 기준 경유 유지(정본 §84 보충 10). 결과 `docs/stage3/results/sr1d.md`, 체크포인트 `/data/harvest/ckpt/sr1d/c1_s{1,2}/last`(불채택). ≈ 2.2 GPU-h.
- 2026-09-26 07:30 UTC E-Couple 무료 사전 실행 결과(사전 등록 `9322853` 06:06 UTC, 변경 1 `f9903d7`; 셰이크다운 — P3 판정 없음, 유료 0원): 메인 GPU 1에서 E-SR1c C1 + Qwen3-VL-8B(Astra 지연 맞춤·시간 초과 주입) × DEV 0–5 × {standard, dr} × {VLA 단독, 결합} 24/24편, 충돌 0, 동시 1개 위반 0, 주입 6 → 시간 초과·늦은 답 5, stale 0, 스키마 오류 5.5 %, 편향 상한 지킴, 장부 0원. 버그 B1–B3(그리퍼 계단·max_inflight 고정·편향/관문 로그 누락)은 b0ee916에서 수정, **B5 차단: VLA 단독 0/12(바닥) → 재학습 체크포인트의 VLA 단독 폐루프 온전성 관문 먼저**, B6 `b_contradict` 사건 범람으로 `phase_pause` 무력, §91·§90 미구현이라 대조 판정은 못 셈. 결과 `docs/stage3/results/couple_dry.md`, 도구 `tools/couple_dry/`(다음 판에서 같은 무료 사전 실행 재사용).
- 2026-09-26 07:38 UTC E-Astra-solo 결과(사전 등록 `50336ce` 07:10Z, 변경 1 포함; user-log 103·정본 §93): 동기(로봇이 기다림)·절대 목표 eef·최소 저크 실행기(VLA 없음)·탁자 좌표 격자 + 낙하선·v2 프롬프트로 **Astra low 단독 mug_tray 4/4 성공**(DEV standard 0·1, dr 0·1; 편당 9–17호출, 성공까지 움직임 중앙 16.35 s, 원/성공 591.5원, 무효 0) — 탐침 0/6은 조건 탓이라는 판단과 맞음. D1 ADOPT(전체 팔 `AS`는 `astra-solo@v2`), D2 effort undecided(→ low + medium 부분집합), 전체 팔 40편 예산 ≈ 23,700원. **메인 확인 필요**: E-Couple 사전 실행 VLA 단독 0/12와 대비해 §92 P3 비교에 AS 팔을 넣을지. 유료 3,080.9원. `docs/stage3/results/astra_solo_pilot.md`.
- 2026-09-26 07:44 UTC E-ACC 결과(사전 등록 `e4f8891` 06:52Z, 변경 1 `7865928`·`d02cbac`, 변경 2 `eb42189`, 변경 3 이 커밋; `docs/stage3/results/eacc.md`): **중단 — OpenAI 크레딧 소진**(31번째 호출부터 `insufficient_quota`, 운영 `AstraClient`는 스트림 `error`·`response.failed`를 오류로 표시하지 않아 빈 답이 됨 — 결함 D1). 과금 1,352.7원(on 10장 × v1·v2·v2cp 30호출; 장부의 나머지 5,043원은 과금 안 된 예약액). off 스냅숏 답 0 → M1 없음, R1·R2 판정 불가, P2 안 함. on 관찰: 무효 0/30, 구간 now v2 8/10·v2cp 9/10, 명령 v1 10/10·v2 9/10·v2cp 8/10(on_08 병 전도 의심으로 오라클 불확실), 원/호출 35.3·47.8·52.2, 지연 p50 4.3·5.6·6.1 s. 잠정 v2 사양 = `tools/eacc/prompt_v2.py`(카메라 자세 줄 기본 끔). 재개 = 크레딧 충전 뒤 off 20장부터(변경 3).
- 2026-09-26 10:14 UTC E-ACC P1 결과(재개, 변경 4 `a09c5ed`; `docs/stage3/results/eacc.md`): off 20장 × v1·v2·v2_ax(AxisGuide 축 덧그림) 60호출 + on 30호출. **병목은 방향이 아니라 알아채기** — edit가 난 답의 방향은 13/13 맞음(각 중앙 5–9°), 그러나 엉뚱한 곳·엉뚱한 놓을 곳(7–10 cm)은 세 팔 모두 0/13("progressing/aligned"), 엉뚱한 물체만 v1 1/7·v2 5/7·v2_ax 7/7. **R1 v2 채택**(M1 +0.20 [0.05, 0.40] 우월, M3 +0.10, 무효 0/90), **R2′ 축 덧그림 미정**(+0.10 [0.00, 0.25]) → 기본 끔, R2 카메라 자세 줄 실행 안 함. astra-couple@v2 사양 = `tools/eacc/prompt_v2.py`(결과 5절). 과금 E-ACC 4,113.4원(상한 8,000). 2단계(effort medium·목표 확인 질문, 약 1,740원)는 보고 뒤 새 변경으로.
- 2026-09-26 10:27 UTC E-ACC 2단계 결과(변경 5 `0dd95ad`; `docs/stage3/results/eacc.md` 6절): 알아채기 수단 × off_a·off_c 13장. (A) effort medium 1/13·지연 p95 17.6 s → **low 유지**. (B) 목표 확인 문장 7/13(+0.54 [0.23, 0.77], off_a 6/7·off_c 1/6)으로 크게 오르나 계획대로 5장에서 괜한 수정 2장(v2 5/5 → 3/5, 허용 1) → 등록 규칙 S2B상 **v2에 넣지 않음**. 괜한 수정 원인 = 도착 예상 손끝 외삽(0.5 s 속도 × 9.3 s)이 내려가기·쥐기에서 6–9 cm 튐(P113). 다음 후보 B′(기준점 = 지금 손끝 + 확정 화살표, 약 1,030원 — 80 % 정지를 넘어 상한 변경 필요). E-ACC 과금 5,907.1원.
- 2026-09-26 10:34 UTC E-ACC B′ 결과(변경 6 `47e6ff6`, 정지 비율 95 %; `docs/stage3/results/eacc.md` 6.3절): 목표 확인의 기준점을 '지금 손끝 + 확정 화살표'로 바꾸니 괜한 수정은 사라졌으나(on 5/5) 알아채기 3/13(+0.23 [0.0, 0.46]) → S2B′ **채택 안 함**; off_c(쥔 채 엉뚱한 놓을 곳)는 0/6 그대로 — 평면도(APC)는 띄우지 않고 보고(다음 후보: 결합 `predict_ee` 고친 뒤 B 재측정, 또는 APC — 둘 다 새 등록·상한 검토). astra-couple@v2 사양은 v2 그대로(목표 확인·축 덧그림 끔). E-ACC 과금 6,892.2원.
- 2026-09-26 09:55 UTC RB3 데이터 추가(학습 없음, 0원): 새 판 **`/data/harvest/data/se2e_c2`**(= se2e_c1 RB1·RB2 행 바이트 동일 + RB3 627편 16,721행, SHA256SUMS, RB3 행 sha `37bc3dd00f26`), 로더 합 56,004(train 53,390 / val 2,614); RB3는 `--se2e-kinds RB1,RB2,RB3`로만(기본 RB1,RB2 = se2e_c1과 같은 표본); 코드 421833f; 라이선스 직원 개인 계정 → 내부용·Astra 전송 금지 전 확인; 빈 편 179 제외는 메인 확인 대상. `docs/stage3/results/rb3_data.md`.
- 2026-09-26 10:07 UTC E-CONF 결과(사전 등록 `6191369` 07:18:17Z, 변경 1 `f37f8f8`·`393a184` 판정 전: S-E2E G1 근접 동점 → 묶음 1 재추출): 결정 확신도(질문별 1·2등 로그확률 차 최솟값)가 새 표본 R2-새(학습 안 된 실패 편 8,208, C0·C1) 큰 오류 AUROC **0.806**·호출 감소 0.379·이전 재현율 0.931로 통과, S-E2E 실데이터(1,799, motion_s1·s2) **0.748**·0.244로 실패 → **NONE**: 확신도는 §95 흐름 우선·보수 모드 트리거로 안 쓰고 선택 이유 기록만(런타임 훅 명세 = 결과 5절, 통제자 전달). 개선 `temp_joint` R2 +0.041·S-E2E +0.024(미달). 정본 §95 보충 1. GPU 2 ≈ 1.5 GPU-h, 유료 0. [`results/conf.md`](stage3/results/conf.md)
- 2026-09-26 10:38 UTC E-MAR-real 결과(사전 등록 `prereg_marr.md` `7245787` + 변경 1 `3e2f5f6`; `docs/stage3/results/marr.md`): Molmo2-ER 포인팅 + 거르개 v2(RB2) MolmoAct 2D 궤적 보조(`trace5-point@v1`) 검증 1,799 합동 **+0.0047 [−0.0006, +0.0103]**(문턱 +0.02 미달, 하한 ≤ 0), 전이 +0.0026, 라벨 행 −0.003 [−0.017, +0.011]; aux 궤적 오차 62 px 대 평균-궤적 기준선 145 px(궤적은 배웠으나 결정 정확도로 전이 안 됨); 준수 불변(A_xy 0.341 대 C0 0.340) → **불채택**: 단계 B 레시피에 안 넣음. 실데이터 MolmoAct 이식은 대체 경로 시연 뒤 E-MAR-S로만, O(이력 덧그림)는 보류(§84 보충 1: 일반화 근거 아님). 정본 §84 보충 11. ≈ 3.6 GPU-h(메인 GPU 0 1.55 h + x2 GPU 0·1 2.05 h), 유료 0.
- 2026-09-26 10:58 UTC 선생-학생 설계(user-log 108, 정본 §96, `docs/research/teacher_student_design_2026-09-26.md`, 실행 0·0원): 시뮬 선생 데이터(E-TEACH-0, 학생이 간 상태 × 선생 라벨, DAgger식)는 **본학습 자리만**, 실물 동기 선생 조종·개입(E-TEACH-R)은 **추가학습 자리**(실물 접근 없음 → 선행 조건 달린 계획, 200편 대체 경로 수집 대안). 팔 C-cont·D-O(오라클)·D-T(선생), 선생 자격 S1 150상태(Astra ≤ 8,000원 / 개방형 0원). **선행 = 런타임 멈춤 고리(0/12 중 8편) 수정 — 결합 통제자 + E-VLA-solo 에이전트**; 선생 선택은 `open_vlm_solo.md` 뒤.
- 2026-09-26 11:00 UTC 선생-학생 목표 재중심(user-log 109 "나는 학습할 수 있는 llm크기에서 아스트라를 대체하는게 목적이야", 정본 §96 개정 1): **주 트랙 E-TEACH-L** = Astra → 학습 가능 크기 오픈 VLM(Qwen3.5-27B·35B-A3B 급 LoRA, H200 1–2장; 확장 122B-A10B QLoRA) 증류, astra-solo@v2 형식·학생 상태 DAgger식, 양은 시뮬 참값 모델(무료)·Astra는 핵심 상태만(단계마다 ≤ 8,000원 ≈ 148호출), 팔 L-T·L-A·L-TA, 성공 = DEV 40 짝에서 Astra 단독 − 3/40 이상·지연 p50 ≤ 1/3·API 0원. VLA DAgger는 보조 트랙 E-TEACH-V(선행 = 멈춤 고리 수정). 다음: L0 무료 영점 + 대역 결과 → 사전 등록.
- 2026-09-26 11:4x UTC 자동 맥락 설계(user-log 110 + "댑스를 쓴다는걸 가정을 해야겠다", 정본 §97, `docs/research/auto_context_design_2026-09-26.md`, 실행 0·0원): 로봇 자기 정보(R 16)는 URDF·관절·camera_info·자기 시험에서 코드로 자동 생성, 장면(P 5: 탁자 높이·물체 위치·크기·역할)은 깊이(시뮬 렌더 GT 깊이 — astra_solo는 지금 `depth=False`; 실물 ZED Mini 스테레오 깊이 계산·D405) + VLM 점 + 코드, 요령(L 3)은 학습, 격자·정의·조언(X 3)은 절제로 뺄지 판단; 시각 자기 인식(말단·접근 축)은 상위 VLM 점 출력으로 학습·FK 대조; 모델 목표 2개(상위 VLM + VLA). **다음 = A0 무료**(TRAIN 시드 깊이 켠 스냅샷에서 평면·물체 군집·자기 점 대 GT) → A1 무료(참값 모델 입력 자동 값 교체) → A2 유료 ≤ 8,000원(단계 1 ≤ 3,000원).
- 2026-09-26 14:3x UTC 다양한 데이터 필수(user-log 116, 정본 §97 보충 1, `docs/research/diversity_plan_2026-09-26.md`, 실행 0·0원): 상위 VLM 학습은 (a) 공개 변환(인식 QA + 좌표계 명시 조종 C′, 절차 `public_data_conversion_howto_2026-09-26.md`) + (b) 자체 생성기 다양화(탁자 높이·크기·색, MolmoSpaces 물체, 방해물 0–6·혼동체, 받침, 조명·질감, 카메라 장착 흔들림 — 머리 각도 고정, 새 과제 4)가 **필수**; 일반성은 OOD-H·O·D·X(보호 분할 `ood` 70000–70999, 학습 금지)에서 L8 지표 + 프레임 누수 0으로 잰다. **다음** = L8 1단계 결과 뒤 생성기 다양화(TABLE_TOP_Z 모듈 상수 → env 값, 기본 경로 바이트 동일) + 관문 G-H·물체 선별 → OOD 목록 커밋 → E-XEMB8(A·B·D·E, 선택 E′) 사전 등록 → 실행. 사용자 질문 2개(리프트로 높이 맞추기, 게이트 HF 동의) 대기.
- 2026-09-26 14:5x UTC E-TEACH-L8 끝(user-log 117, `docs/stage3/results/teach_l8.md`, 0원): 8B 참값 SFT(1.39 GPU-h)로 오프라인 접근 xy 125.5 → 2.6 mm, 폐루프 DEV 4/4(영샷 0/4) — TREND·STRONG·WORKS.
  - 모델: 병합 가중치 `/data/harvest/out/teach_l8/merged_ep2`, 어댑터 `run_main/epoch2`. 서빙은 `tools/teach_l8/vllm.sh`, 폐루프는 `closed.sh`.
  - 다음 후보: (1) OOD·다양화 평가(ul 116)에서 8B 대 35B-A3B; (2) 35B-A3B LoRA 50걸음 처리량 스모크; (3) Astra 라벨 팔(L-A, 유료 — 사용자 허락 필요, ul 114).
- 2026-09-26 15:20 UTC E-PT 등록(user-log 120–122, 정본 §97 보충 2, `docs/stage3/prereg_pt.md`, 유료 0원): 주 팔 = 깊이 없이 모델이 거리를 아는 ND-1 `nd-xyz`(격자·탁자 높이 없이 xyz)·ND-2 `nd-est`(추정 먼저), 비교 = 깊이 점 `pt`·L8-xyz; F0 영샷 점 + F2 같은 L8 편·816걸음 LoRA를 DEV + OOD-H(탁자 0.82·0.88)에서. 진행 상태는 §0이 아니라 결과 문서 `docs/stage3/results/pt.md`에.
- 2026-09-26 16:4x UTC(09-27 01:4x KST) 결합 학습 계획(user-log 127·128, 정본 §96 보충 3·4, `docs/research/coupled_training_plan_2026-09-27.md`, 실행 0·0원): 런타임 배선은 끝; 막힌 첫 단추 = **ser-A-min-3 VLA-sim 재학습**(V0) → VLA 단독 ≥ 4/12(V1) → 결합 사전 실행 r2(V2) → 결합 형식 상위 E-TEACH-L8C(참값 라벨러 `couple-truth` + 결합 DAgger, V3) → 결합 폐루프 학생 8B + VLA 대 8B 단독 대 VLA 단독(V4) → 35B(V5) → 유료 E-Couple은 승인 뒤. VLA는 E2E(E-VLA-E2E: KI식 이산 행동 토큰·예측 결정 조건·의도 교란) 팔로 단계식과 비교. 사용자 결정 D1 `verify` 필드·D2 재학습 우선·D3 구간 줄 세분도.
