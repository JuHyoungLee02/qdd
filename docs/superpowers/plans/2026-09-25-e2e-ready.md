# E2E 학습 준비 완료 계획 (user-log 59)

> 사용자 지시(user-log 59): "그냥 계속 다 자동으로 해주면돼 아직 엔드투엔드는 아니고 모든 파이프라인 전체를 엔드투엔드 학습할 준비까지 모두 준비를 마쳐주면 되는거야 알겠지? 검증해보면서 계속 자동으로"

> 개정 17:58 UTC (user-log 60, 정본 §58): 융합 원칙 — 런타임은 단일 VLA(결정 토큰 + action expert). R1은 보조 라벨·기준선용, R3·R4는 한 백본에 통합, R5 런타임은 모델 한 번 호출.

**목표:** 엔드투엔드(단계 B: Qwen3-VL-4B 백본 + action expert, §51·§52) 학습을 **돌리기 직전 상태**까지 전 파이프라인을 만들고 검증한다. 본 E2E 학습은 하지 않는다.

**완료 정의(모두 충족):**
1. 데이터: 인식 추정 상태(M1) 입력, 여러 과제·물체, 학습용 DR, 행동(관절) 기록, 결과 기반·관측 정의 라벨이 한 형식(LeRobot 호환 + 우리 스냅샷)으로 대량 생성된다 — 생성기·검증기·소규모 샘플 통과.
2. 모델: 단계 A(결정 토큰, 다중 이미지 = 머리 + 활성 손목, D27 배치)와 단계 B(같은 백본 + flow-matching expert, expert→백본 기울기 차단) 학습 코드가 테스트 + 소규모 스모크(손실 감소·저장·재적재·서빙)를 통과한다.
3. 폐루프: 학습된 모델을 넣어 돌릴 실행 계층(Astra 하트비트 → Jev-L DecCall → M4 확정 → 스킬 S + 잔차 R 훅, simlat 시계)이 Inspect Robots `aiworker` 몸체 위에서 한 판 끝까지 돈다.
4. 평가: E0.5 재생·RD(standard/random) 평가·보정(CAL 온도·J5) 스크립트가 학습 산출물을 받아 한 명령으로 돈다.
5. 운영: 학습 처리량(묶음 처리)·파드 경로(/data/harvest)·재현 판본 고정·문서.

**원칙:** 각 항목은 사전 등록한 검증(테스트/스모크/프레임 확인)으로만 "완료". 소규모 시험 수치는 결과로 쓰지 않는다(§56). 방향 질문 6개는 관문마다.

## 관문

| 관문 | 내용 | 통과 조건 |
|---|---|---|
| R1 인식 | M1 앞단(시뮬 기본 카메라 → SAM 3.1(`detect_phrase`) → 렌더러 깊이 → 3D 중심 → S1 좌표) | DEV 스냅샷에서 추정 좌표 오차 분포(mm) 산출, 파이프라인 지연, 추정 S1로 labels_v2 코드 규칙 정확도 보고 |
| R2 데이터 | 다과제(머그→트레이 + 2과제 이상, 물체 풀), DR 장면, 관절 행동 30 Hz 기록, LeRobot 내보내기, 병렬 생성기 | DEV 소규모 생성·검증기 통과, 프레임 확인, 한 편 재생으로 기록 정합 |
| R3 모델 A′ | 단계 A 다중 이미지 + 추정 S1 입력, 묶음 학습 | 스모크 + 학습/추론 확률 일치 테스트 + 지연 |
| R4 모델 B | 단계 B 코드(백본 + flow expert, KI 차단, 결정 토큰 + 연속 행동 공동 손실) | 스모크(손실 감소, 기울기 차단 테스트, 저장·재적재, 추론 속도) |
| R5 폐루프 | Inspect Robots `aiworker` 몸체(P1) + `OursPolicy`(P2) + simlat + M4 + 스킬/R 훅 + Astra 하트비트 | DEV 한 판 완주, 로그 스키마, 결정성 |
| R6 평가 | E0.5 재생·RD·CAL 보정 스크립트, 한 명령 실행 | 소규모 입력으로 끝까지 돈다 |

순서: R1·R4·R5 병렬 → R2(R1 필요) → R3(R1·R2·D27) → R6. GPU: 0 = 결과 라벨 작업, 1 = Isaac(프로세스 ≤ 3), 3 = vLLM/학습 스모크(번갈아), 2 금지.
