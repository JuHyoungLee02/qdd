# 단계 2 모듈 설계: 공통 규칙 (에이전트용)

> 개정 2026-09-25 01:48 UTC (R7 sweep6, 정본 §43·§44·§47·§58·§60 — 단계 3 결정 안내만 붙임, 본문은 단계 2 당시 기록이라 고치지 않음): 빠른 typed 선택기 Jev는 쓸 수 없어(user-log 46) 로컬 VLM Jev-L(Qwen3-VL-4B, 같은 JevCall 형식 = DecCall)로 바꿨고(§44·§50), 런타임 주 설계는 결정 토큰 + action expert + 확인 헤드를 한 백본에서 내는 융합 모델이며 결과 표는 모듈형 스택과 나란히 둔다(§58·§60). 카메라는 AI Worker 기본 카메라 그대로다(§43: `ZED_M` 쌍둥이·트랙 D-stereo 가상 카메라 폐기). 시뮬 카메라 설정은 humanoid-challenge-env 복사다(§47: 머리 ZED Mini 왼쪽 정류 672×376, VGA 수평 85°·fx 367 — 'ZED Mini 102°×57°'는 센서 최대 화각이라 VGA 모드 값이 아니다; 좌·우 손목 D405 424×240). 최신 상태는 `docs/handoff.md` §2.7·§2.8.

시작: 2026-09-23 21:32 UTC. 목표 최소 6시간, 최대 24시간.

## 사용자 정의 [사용자]
- 모듈 설계는 파이프라인 설계가 아니다. 각 부분(M1~M10)에 대해 **어디의 어떤 연구가 그 부분에서 최고였고, 무엇을 가져올 수 있고, 우리 것에 어떻게 접목할지**를 최대한 모은다.
- 모듈 단위로만 최고이면 된다. 로봇·VLA 분야일 필요 없다. **LLM, VLM 자체의 방법 중 아직 로봇에 적용되지 않은 것**도 찾는다.
- 자료는 1년 반 이내(arXiv 첫 공개 2025-03-23 이후). 아주 큰 주제의 기초 문헌은 예외("기간 밖, 기초 문헌" 표시).
- 매우 꼼꼼하게. 끊임없이 의심하되 없는 사실을 만들지 않는다.

## 먼저 읽을 것
- `D:\qdd\CLAUDE.md`, `D:\qdd\docs\plan.md`(v4.3, 자기 모듈 절과 §1 모델 제약, §5), `D:\qdd\docs\draft-log.md`(교훈), `D:\qdd\docs\research\v3\README.md`(조사 규칙 — 그대로 따른다), 자기 모듈 관련 `docs/research/v3/*.md`.

## 산출물: `docs/design/Mx-이름.md` (한국어)
1. 역할과 입출력(우리 구조: Astra 느린 상위, Jev 텍스트 전용 typed 하위, 기존 스킬, 코드 critic)
2. **분야 전체 최고 후보 표**: 이름 | 분야(로봇/VLA/LLM/VLM/기타) | 어디서 최고였나(벤치마크, 조건 포함 수치, 표 위치) | 신뢰도(학회, 인용, 스타) | 기간 | 학습 없이 쓸 수 있나 | 로봇 적용 여부
3. **가져올 것과 접목 방법**: 원문 칸과 우리 접목안 칸을 반드시 분리한다.
4. 설계안: 1순위와 대안(사용자 [결정 필요]가 걸린 곳은 두 안 모두)
5. 비교 실험(조건, 지표, 판정 기준을 미리 적는다)
6. 반대 증거와 위험
7. 열린 질문, [결정 필요]
8. 확인 못 한 것

## 반복 금지 (draft-log 교훈)
- 요약만 보고 단정하지 않는다. 수치는 표·절 위치와 조건(평균/최대, 모델, 설정)을 함께.
- 우리 제안을 원문처럼 쓰지 않는다.
- 부재 주장은 본문·부록·코드까지 본 뒤, 검색어를 기록하고.
- 사용자 의도([사용자] 줄)를 바꾸지 않는다. 다르면 [결정 필요].
- Jev 제약: 텍스트만, 수치 약함, #2(c) 보간 금지, #8 질문 사이 확률 항등식 불성립, 보정 측정 전 확률은 게이트로만.
- Astra 제약: 이미지 O 비디오 X, logprob 불가, effort 지연 날마다 변동, effort 낮추기는 기본안 금지(사용자 발언).

## API 규칙
- 이 라운드에서 arXiv 검색 API(`https://export.arxiv.org/api/query`, 5초 간격)를 쓸 수 있는 에이전트는 프롬프트에 명시된 하나뿐이다. 나머지는 arxiv abs/html 페이지(2초 간격), 학회·프로젝트 페이지만.
- Semantic Scholar·GitHub API는 쓰지 않는다(필요하면 "미측정").
- `D:\qdd`의 다른 파일은 고치지 않는다. 커밋하지 않는다.
