# E. 첫 실험 프로토콜 (E0 → E0.5 → E1 → E2a → E3) — 실행 수준 설계

> 개정: 2026-09-23 D5 반영 (`D5-consistency.md` 1-11·1-19·2-3·4-2): E1 `QN-accept`(Noul) → `QC-accept`(M9 복구 제안 중 선택 Choice), 진행 질문 보기에 `NONE_ESCALATE` 추가, `QS-dist`를 M3 `dist_cat` 4단계로, effort 표기를 "잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5, 00 §16"로.
> 개정: 2026-09-23 정본 §14–§15 반영 (`00-interfaces.md` §14·§15, `D4-cross-field.md` §2, `D4-cross-field-verification.md` #14): E3에 **질문 앵커 PageRank 줄 순서 조건 A-QPR** 추가(TabGR 2601.08444, 무학회 LOW, 순서 강건성의 직접 근거는 부록 Table 12, §5.2·§5.3·§5.6). §14의 C3'(Adaptive-Consistency식 베이즈 정지, 임계값 명시)·C3''(보기 순서 돌리기)는 **E-M4 조건**이라 이 문서의 실험에 넣지 않는다(§4.12).

> 개정: 2026-09-23 D4 반영 (`D4-devils-advocate.md` F2~F5·F7·F9·D-1·C-1·C-2·C-6·C-10, 00-interfaces §13): E0.5 오프라인 표 재생 추가(§2A), E2를 E2a(룰 대 Jev C1, M4 없이, 결과 기반 라벨, Astra 켠 대응 쌍)로 바꾸고 C5 조건은 E-M4로 이동(§4.12), 방향 재검토는 95% 상한 < +5%p일 때만(그 밖은 "결론 유보"), 환경 역할(E0~E3 = 단일 팔 자작 장면) 확정, Astra effort 기본 high, 방향 질문 두 질문 분할(M3 §4.2) 반영.

작성: 2026-09-23 UTC(첫 판 표기 "2026-09-24"는 KST 날짜, D4 C-10), 설계 에이전트. 근거: `00-interfaces.md`(정본, §2 JevCall·§6 확률 게이트·§7 설정 표·§8 순서·§9 C1), `plan.md` v5 §1·§3·§5·§6, `M3-action-representation.md` §4·§5, `M4-overlap-commit.md` §4·§5, `M1-state-representation.md` §4·§5, TypeSafe 공식 문서(2026-09-24 읽음: `models.md`, `api.md`, `model-jaggedness/jev-1.13.md`, `confidence.md`, `sdk/python/api/retries.md`, `patterns/fan-out.md`).
이 문서는 **코드가 아니라 절차**다. 판정 기준은 실행 전에 고정하며, 실행 뒤 바꾸려면 "사후 변경" 절에 이유와 함께 적고 원 기준 결과도 같이 보고한다.

표기: **[사용자]** 사용자 결정(바꾸지 않음) / **[제안]** 이 문서의 제안 / **[가정]** 확인 안 된 전제(실행 전 확인하거나 결과에 표시) / **[결정 필요]** 사용자가 정할 것.

---

## 0. 한눈에 보기

| 실험 | 묻는 것 | 핵심 판정(사전 등록) | Jev 비용 | Astra 비용 | 벽시계 |
|---|---|---|---|---|---|
| E0 지연 | 한국에서 실제 `JevCall`(H=1·3 + 감시 질문) p50/p95, 시간대·동시 in-flight 영향 | p95 대 T_c(0.33 s)로 겹침의 주 목적(지연 숨기기 / 자기 확인) 결정, `d_p95`·`N_max` 초기값 확정. **겹침은 어느 경우에도 끄지 않는다** | 약 $14~18 | 0 | 4일(자동 세션) |
| E0.5 표 재생 | 기록 궤적의 연속 스냅샷(0.33 s 간격)에서 같은 스텝 표들 사이 불일치(flip rate), newest-wins 대 LA-2 대 감쇠 융합의 결과 기반 정답률 | flip rate ≈ 0(성공 궤적 < 5%)이고 합의 이득 < +2%p면 **M4 주장을 (b) 중심으로 좁힘**(E-M4 전). 겹침은 끄지 않는다 | 약 $1~5 | 0 | 약 1일 |
| E1 보정 | 질문 유형(Choice/Score/Noul)·보기 이름·보기 수·위치별 보정 | 질문군별 "게이트 켬" 조건(ECE·AUROC·임계 구간 정확도) 통과 시에만 §6 게이트 허용 | 약 $3~10 | 0 | 1~2일(자료 풀 뒤) |
| E2a 마차 시험 | 같은 텍스트 상태에서 Jev C1(D-줌/G/H, **M4 없이**)이 룰보다 나은가 + 오프라인 결과 기반 라벨 대조 + Astra 켠 대응 쌍(보조) | **모든 Jev 조건의 (Jev − R0) 95% 상한 < +5%p일 때만 방향 재검토 보고**, 구간이 +5%p를 걸치면 "결론 유보"(멈춤 아님) | 약 $4~7 | 약 $60~440(대부분 Astra 켠 쌍) | 2~3일(실시간 실행) |
| E3 M1 형식 | 오라클 정답 위에서 상태 표현 A/B/C·대조군 비교 | M1 §5 판정 1~7(그대로), 결정은 사용자 | 약 $2~5 | 약 $85~300(V-free·C) | 1~2일 |

순서와 의존(00 §13): 환경·술어·오라클 플래너 구축 → **스냅샷 풀 생성**(Jev 불필요, 연속 스냅샷 열·시뮬 상태 포함) → E0(병행 가능, 자료 풀 없이도 시작) → **E0.5**(풀 + E0의 `d_p95` + 롤아웃 라벨러) → E1(풀 필요, E2a 질문 형식을 고정) → **E2a**(E0의 `d_p95`, E1의 형식 결론 필요, M4 구현 불필요) → E-M4(C5 포함). E3는 풀이 생기면 언제든(00 §8 "E1·E3 오프라인 병행").

환경 역할(00 §13, D4 C-1): **E0~E3는 단일 팔 자작 장면**(싸고 빠름)에서, **본 평가는 RoboDojo(양팔)**에서 한다. 둘을 섞지 않는다. Astra effort는 **잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5, 00 §16**. RoboDojo 원문 재현용 medium은 EVAL의 재현 조건에서만 쓴다.

---

## 1. 공통 규칙 (네 실험 모두)

### 1.1 모델·언어·버전
- `model = "jev-1.13.0"`(버전 ID 고정, 별칭 `jev-latest` 금지 — 공식 models.md: 별칭은 새 릴리스에 따라 움직인다). 응답의 `model` 필드를 매 호출 기록하고, `jev-1.13.0`이 아닌 응답이 1건이라도 나오면 그 세션을 분리 보고한다.
- 모든 상태·질문·보기 설명은 **영어**(공식: 영어가 가장 정확).
- 엔드포인트 `POST https://api.typesafe.ai/v1/systemone`. 공식 한도(2026-09-24 읽음): 64k 토큰/요청, state + 가장 긴 질문 32k, **1,200 요청/분·250k 토큰/초(예고 없이 바뀔 수 있음)**, Choice ≤ 255 보기, Score 2~10단계. 가격 $0.042 / 1M 입력 토큰, 출력 무료.
- Python SDK를 쓰면 **재시도를 끈다**(`RetryPolicy(max_retries=0)`). SDK 기본값은 재시도 2회·백오프 0.5 s·타임아웃 30 s라서(공식 retries 문서) 429·5xx를 지연 속에 숨긴다. 재시도가 필요한 폐루프(E2a)는 재시도 층을 우리 코드에 두고 재시도 횟수를 따로 기록한다.

### 1.2 `JevCall` 형식 (00-interfaces §2를 API 요청으로 옮긴 것) [제안]
- 한 호출 = 결정 질문 H개(미래 결정 스텝 k..k+H−1) + 감시 질문(M7 진행 범주 Choice 1개 + 필요 시 M8 T3b Noul 1개).
- 질문 키(`questions` 맵의 키)는 추론에 쓰이지 않는다(공식 api.md). 키 규칙: `ds<번호>.<질문>` / `mon.progress` / `mon.t3b`.
- 스텝별 전제(M4 §4.1 "그 전 스텝들이 코드 예상대로 끝났다고 가정한 상태", 바뀐 술어만)는 **구조화된 `instructions` 객체의 한 필드**로 넣는다(공식 api.md가 허용하는 "질문 필드 + 데이터 필드" 형식). state는 모든 질문이 공유한다.
- 보기 설명(`criteria` 값)에 M3 `expected_after` 술어를 붙인다(E-M3-1이 효과를 재기 전까지는 붙인 형식을 기본으로 둔다).
- `NONE_ESCALATE` 보기는 모든 결정 질문에 둔다(M3 §4.1).

H=1, D-줌 예(값·문구는 예시, 실제 템플릿은 E1 결과로 고정):
```json
{
  "model": "jev-1.13.0",
  "state": "t_state: f1287 (t=42.90s)  contract: c7  stage: S2 \"place mug o3 on tray o5\"\nrobot: gripper=closed_holding(o3) wrist_force=light arm=moving\nobjects:\n  o3 mug red | held_by_gripper | upright\n  o5 tray blue | on(table) | clear=yes\nfacts: above(o3,o5)=no near(o3,o5)=yes aligned_xy(o3,o5)=no ...\nstage S2: exit=on(o3,o5)=no invariants=holding(o3)=yes elapsed=normal\nchanges (last 3s): -0.4s aligned_x(o3,o5): no->yes",
  "questions": {
    "ds412.dir": {
      "type": "choice",
      "instructions": {
        "assumed_before_step": "step ds411 finished as planned: aligned_x(o3,o5)=yes",
        "question": "Which direction should the gripper move during step ds412 to make progress toward the `exit` of stage S2 without violating `forbidden`?"
      },
      "criteria": {
        "plus_y": "Move toward +y (robot left). expected_after: aligned_xy(o3,o5)=yes, in_contact(o3,o5)=no",
        "minus_z": "Move down. expected_after: in_contact(o3,o5)=yes",
        "hold": "Keep the current position. expected_after: no change",
        "NONE_ESCALATE": "None of the options fits the situation."
      }
    },
    "ds412.mag_coarse": { "type": "choice", "instructions": "...", "criteria": { "tiny": "about 0.5 cm", "small": "about 1 cm", "medium": "about 2 cm", "large": "about 4 cm", "xlarge": "about 8 cm", "NONE_ESCALATE": "..." } },
    "ds412.mag_fine":   { "type": "choice", "instructions": "...", "criteria": { "...": "9 sub-bins inside the previous step's coarse bin (M3 §4.2 (a))" } },
    "ds412.grip":       { "type": "choice", "instructions": "...", "criteria": { "open": "...", "close": "...", "keep": "...", "NONE_ESCALATE": "..." } },
    "mon.progress":     { "type": "choice", "instructions": "Considering the change since the last step, how is stage S2 going?", "criteria": { "valid_progress": "...", "allowed_change": "...", "failure": "...", "recovering": "...", "NONE_ESCALATE": "Cannot tell from the state." } },
    "mon.t3b":          { "type": "noul", "instructions": "Given elapsed = `normal` of expected for step 'place', should this step have been completed by now?" }
  }
}
```
- 방향은 M3 §4.2 개정(D4 C-6)대로 **두 질문으로 나눈다**: `ds412.dir_xy`(수평 8방향 + `none_xy` + `NONE_ESCALATE` = 10개), `ds412.dir_z`({up, down, `none_z`, `NONE_ESCALATE`} = 4개). 위 예의 `ds412.dir`은 모양만 보인 것이다. 한 질문 28개(26방향 + `hold` + `NONE_ESCALATE`)는 E1·E-M3-1의 비교 조건 `D-dir28`로만 쓴다. 크기 구간의 cm 값은 **[가정]**(M3는 "로그 간격 5구간"만 정했고 값은 튜닝 대상).
- H=3: `ds412`, `ds413`, `ds414` 각각 `dir`·`mag_coarse`·`grip` + 전제 필드, `mag_fine`은 첫 스텝(`ds412`)에만(M3 §4.2, D1 B1), 감시 질문은 한 벌.
- G/H안: `ds.target`, `ds.approach`, `ds.phase`, (접촉 근처) `ds.fine_dir` — M3 §4.3·4.4.

### 1.3 크기 등급 (E0이 실제 `usage.input_tokens`로 교정) [가정: 토큰 수는 추정]
| 등급 | 구성 | 입력 토큰 추정 |
|---|---|---|
| S1 | H=1, D-줌(위 예) | 1.0k~1.5k |
| S3 | H=3, D-줌 | 2.5k~4k |
| G1 | H=1, G/H안(목표·접근·단계 + 감시) | 0.6k~1k |
| X8 | 응력 시험(관련 없는 물체·술어를 채워 약 8k) | 약 8k |
- 공식 fan-out 문서: 질문들은 병렬 평가되므로 질문을 더해도 응답 시간 증가는 보통 작다. **이것을 E0이 우리 크기에서 직접 확인한다.**

### 1.4 환경과 상태 원천
- **첫 환경(00 §13으로 확정): 시뮬 단일 팔 탁상 pick-and-place.** E0~E3 전용이며 RoboDojo 본 평가와 섞지 않는다. 7자유도 팔 + 평행 그리퍼, 탁상 1개, 대상 물체 1(머그/블록), 놓을 곳 1(트레이), 방해물 0~2개. 과제 문장: "Put the red mug on the blue tray."
  - 엔진: RoboDojo-Sim과 같은 Isaac Sim 위 자작 장면(00 §13: 초기 실험은 자작 장면, 본 평가는 RoboDojo. 첫 판의 "벤치마크가 정해지면 그 안으로 옮긴다"는 쓰지 않는다). 요구 조건만 고정한다: (1) 참 자세 접근(오라클), (2) 시드로 초기 배치·섭동이 결정됨, (3) 섭동 주입 훅, (4) **벽시계 동기 실시간 실행**(시뮬 시간을 벽시계에 묶어 실제 Jev 지연이 그대로 작용).
  - 성공: `on(o3,o5) ∧ ¬holding(o3) ∧ upright(o3)`가 1 s 연속 참(T1 술어). 시간 제한 60 s. 물체가 탁상 밖으로 떨어지면 즉시 실패.
- **상태 원천**: E0.5·E1·E2a·E3a는 **오라클 상태**(시뮬 참 자세 → M1 술어 등록부 계산기 → 텍스트). 인식 앞단(SAM 3.1 + 깊이)은 이 단계에서 쓰지 않는다. 이유: 마차 시험은 **결정 층**을 재는 것이고, 인식 오류가 섞이면 결정 층 차이를 가린다. 인식 조건은 E3의 잡음 주입 하위 조건과 뒤 단계(E3-P, 환경 일반화 평가)에서 다룬다.
- M1 변환: 사용자 결정 전이므로(00 §11.3) E0~E2a는 **등록부 술어를 그대로 줄 표로 쓴 최소 직렬화**(M1 후보 A와 모양이 같다)를 **측정용 고정 형식**으로 쓴다. 이것은 M1 결정이 아니다. E3 결과로 사용자가 다른 후보를 고르면 E2a 핵심 조건만 재실행한다(§4.9).

### 1.5 시드 세트와 스냅샷 풀
| 세트 | 시드 | 용도 | 누가 볼 수 있나 |
|---|---|---|---|
| DEV | 0~29 | 룰 작성, 질문 템플릿 작성, 디버깅 | 설계자 |
| CAL | 500~549 | M4 conformal 경계·FLIP_TH 보정(섭동 없는 성공 실행), E1 보정 적합 | 설계자(결과만) |
| TEST | 1000~1149 | E2a 본 평가(TEST2 = 1150~1299, 연장 시) | 실행 전 아무도 안 봄 |
| POOL | 2000~2119 | 스냅샷 풀(E1·E-M3-1·E3), 오라클 플래너 + 섭동으로 생성 | 설계자 |
- 스냅샷 풀: POOL 시드 120 에피소드(오라클 스크립트 플래너가 실행, 섭동은 **DEV 공개 섭동 P0~P2만** — 보류 섭동 P3·P4는 E2a TEST 전용으로 남긴다) × 에피소드당 결정 시점 10개 = **1,200 스냅샷**. 추가로 에피소드마다 **0.33 s 간격 연속 스냅샷 열과 시뮬 전체 상태**를 저장한다(E0.5 재생, 결과 기반 라벨용 짧은 롤아웃 재시작). 각 스냅샷에 오라클 정답(목표 ID, 최적 방향·크기 구간, 단계 완료 여부, 진행 범주, 금지 술어 위반 여부, 술어 참값)을 붙인다. 문턱 근처(히스테리시스 띠 안) 술어는 `ambiguous` 표시. 경계 사례를 풀의 30%로 과표집하고, 분석은 자연 가중치와 과표집 가중치 둘 다 보고.
- 분할은 **에피소드 단위**(같은 에피소드 스냅샷은 한쪽에만).

### 1.6 기록 (모든 Jev 호출)
`call_id, experiment, condition, seed, ds_ids, H, size_class, t_send(monotonic), t_first_byte, t_done, http_status, retry_n, usage.input_tokens, usage.output_tokens, model, question_id(문구·보기 해시), 질문별 {choice, probabilities, confidence | noul | score, legend}, t_state, 지역(PC/파드), 시각(KST·PDT)`. 요청·응답 원문 JSON을 그대로 저장(재분석용).

### 1.7 통계 공통
- 비율 차이: 같은 시드 짝 비교(McNemar/짝 부트스트랩), 95% 구간은 **에피소드(또는 스냅샷의 에피소드) 군집 부트스트랩 10,000회**.
- 다중 비교: 한 판정에 여러 조건을 걸면 Holm 보정.
- 중간 확인 금지. 예외는 각 실험에 적은 **1회 사전 등록 연장**뿐.
- 실행 전: 이 문서의 판정 절을 해시(SHA-256)로 남기고 해시와 시각을 실행 기록 첫 줄에 적는다 **[제안]**.

---

## 2. E0 — Jev 지연 (한국, 실제 `JevCall` 크기)

### 2.1 목적
M4의 고정 구간 길이 `d_p95`와 동시 in-flight 상한 `N_max`의 초기값을 정하고, **겹침의 주 목적이 "지연 숨기기"인지 "같은 스텝 여러 표(자기 확인)"인지**를 정한다. 겹침 자체는 끄지 않는다(00 §9 C1 [사용자]).

### 2.2 가설 (사전)
- H0-a: 한국발 S1 p50은 0.35~0.7 s, p95는 0.6~1.2 s이다 **[가정 근거]**: 업체 측정은 미국 서부(v3/01 블로그 단서), OpenRouter 전 지역 P50 0.37 s, 제3자 데모 p50 0.53/p95 0.81 s(LOW), 한국–미국 서부 왕복 약 0.13~0.16 s(측정 안 함, 일반 값).
- H0-b: S3 − S1의 p95 차이는 0.1 s 미만이다(공식: 질문 병렬 평가).
- H0-c: 동시 in-flight 4개까지 p95 증가는 25% 미만이다.
- H0-d: 시간대 사이 p95 최대/최소 비는 1.5 미만이다. 미국 업무 시간(PDT 09~18 = KST 01~10)에 느리다.

### 2.3 설정
- **측정 위치 2곳**: (주) 폐루프를 돌릴 기계(시뮬이 도는 클러스터 파드, **[가정] 한국 소재**), (보조) 로컬 PC(한국). 결과는 위치별로 따로 보고, 설계값은 주 위치 값.
- 연결: HTTP keep-alive(연결 재사용). 첫 요청(새 연결) 지연은 따로 기록(`cold`). 네트워크 분해용으로 세션 시작마다 `curl -w`의 `time_namelookup/time_connect/time_appconnect/time_starttransfer/time_total`을 20회 기록.
- 페이로드: §1.3의 S1, S3, G1, X8. 스냅샷 풀이 없으면 템플릿 생성기로 만든 현실적 상태를 쓰고, 풀이 생긴 뒤 **확인 세션 1회**를 실제 스냅샷으로 돌린다. 두 결과의 p95가 10% 넘게 다르면 실제 스냅샷 값을 쓴다.
- 부하 방식 2가지:
  1. **닫힌 루프 동시성 스윕**: in-flight 수 N ∈ {1, 2, 3, 4, 6, 8}을 유지(응답이 오면 즉시 다음 요청). 각 (크기, N)마다 300 요청.
  2. **열린 루프 주기 재생**(실제 스케줄러 모양): T_c ∈ {0.2, 0.33, 0.5} s마다 무조건 송신, in-flight가 N_cap(=6)에 닿으면 그 슬롯은 건너뜀(건너뜀 수 기록). 각 (크기 S1·S3, T_c)마다 60 s.
- 시간대: KST 02·06·10·14·18·22시 6슬롯 × 평일 3일 + 주말 1일 = 24 세션. 세션 하나 약 15~20분.
- 결정성(재시험 flip, M4 §5 전제 실험): 고정 페이로드 20개를 세션마다 1회씩 보내 24회 반복 → 같은 입력에서 최빈 보기가 바뀌는 비율, 확률 최대 변화 |Δp|.
- **Astra 첫 토큰(부수 측정, D4 W4)** [제안]: E0 첫 평일에 Astra effort별(high·xhigh·max, 비교용 medium) 첫 토큰 지연을 실제 T_fail 입력(격자 1장 약 3,600 토큰 + 텍스트 약 2k)으로 20회씩 잰다. 결과는 M9 §7 [결정 필요](사용자 원칙 세 개의 충돌)에 "effort X면 실패당 대기 Y초" 표로 붙인다. medium은 측정만 하고 기본값 후보로 쓰지 않는다. 비용은 E0 표에 넣지 않은 Astra 약 $5~20 **[가정]**.
- 속도 제한: 공식 1,200 요청/분 = 20 요청/초. N=8에서 p50이 0.4 s면 초당 20회에 닿는다. 429는 **지연 표본에서 빼지 않고 "실패"로 따로 센다**(아래 지표).

### 2.4 조건 요약
크기 {S1, S3, G1, X8(동시성 1·4만)} × 부하 {닫힌 N=1..8, 열린 T_c 3값} × 시간대 24 세션 × 위치 {파드, PC(평일 1일만)}.

### 2.5 표본과 근거
- 세션당 약 (3 크기 × 6 N × 300) + (X8 2 × 300) + (2 × 3 × 60 s × ~3 rps) ≈ 7,000 요청, 24 세션 ≈ 17만 요청.
- p95 정밀도: 한 칸 n=300이면 p95 순위 285의 이항 표준편차 약 3.8 → 95% 구간 약 p92.5~p97.5 (칸별 참고용). **설계값은 시간대를 합친 n ≥ 7,200(300 × 24)에서 계산** → p95 순위 구간 약 p94.5~p95.5로 충분.
- 시간대 비교: 슬롯당 n = 300 × 4일 = 1,200 → 슬롯별 p95 95% 구간 폭 약 ±1.2 백분위.

### 2.6 지표
- 요청별 총 지연(t_done − t_send), 첫 바이트까지, 네트워크 분해(DNS·TCP·TLS·TTFB).
- 칸별 p50/p90/p95/p99, 실패율(429·529·5xx·타임아웃 2 s 초과). **p95 계산 시 실패와 2 s 초과는 +∞로 넣는다**(검열 자료; 실패가 5%를 넘으면 p95 자체가 ∞ → 그 칸은 "사용 불가").
- 크기 효과: p95(S3) − p95(S1), p95(X8) − p95(S1), 토큰당 기울기(선형 회귀).
- 동시성 효과: p95(N)/p95(1).
- 시간대 효과: 슬롯별 p95의 최대/최소 비, 요일 효과.
- 열린 루프: 실효 응답률(초당 답), 건너뜀 비율, **스텝당 표 수** — 지연 기록으로 M4 스케줄러를 오프라인 재생: 스텝 시작 전 `lead_max` ∈ {1.0, 1.5} s부터 `d_p95` 전까지 T_c 간격으로 같은 스텝을 묻는 H=1 방식(M4 §4.1)에서 고정 구간 경계 전에 도착한 답의 수.
- 결정성: 재시험 flip 비율, |Δp| 중앙값·최대.

### 2.7 판정 기준 (사전 등록)
운영점 L = **파드 위치, 선택 크기(S1과 S3 각각), N = N_max 후보에서의 전체 합산 p95**. T_c = 0.33 s(설정 표).
1. **`d_p95` 초기값** = L(크기별). 추가로 "최악 시간대 슬롯 p95"를 설정 표 비고에 적는다. 최악/전체 비가 1.3을 넘으면 E2a 이후 모든 폐루프 실험에서 **시간대를 기록하고 조건을 같은 시간 블록 안에 교차 배치**(블록 무작위화)한다.
2. **`N_max` 초기값** = ceil(L / T_c) + 1(M4 §4.3). 단 p95(N_max)/p95(1) > 1.25면 동시성 벌점이 있다는 뜻 → N_max를 벌점이 1.25 이하인 가장 큰 N으로 낮추고, 초당 호출 예산을 M4 C0~C6에서 같게 맞춘다.
3. **겹침의 목적** (00 §9 C1, M4 §5 전제 실험의 문턱을 그대로 씀):
   - (a) L < T_c/2(0.165 s): 지연 숨기기 목적 약함 → **주 목적 = 자기 확인(같은 스텝 여러 표)**. 겹침 유지. "겹침 끄는 안"은 [결정 필요]로 사용자에게 보고만.
   - (b) T_c/2 ≤ L ≤ T_c: 두 목적 모두. 주 목적은 자기 확인, 지연 숨기기는 보조.
   - (c) T_c < L ≤ 1.0 s: **겹침 없이는 3회/초 불가능** → 주 목적 = 지연 숨기기, 자기 확인은 스텝당 표 수가 허락하는 만큼.
   - (d) 1.0 s < L ≤ 2.0 s: (c)와 같되, 확정 행동이 1 s 이상 낡은 상태 위의 결정이 된다 → M3 D-줌(촘촘한 이동)에 불리하다는 경고를 E2a 해석에 붙이고 [결정 필요]로 보고(T_c 또는 D-줌 결정 빈도).
   - (e) L > 2.0 s 또는 운영점 실패율 > 2%: "Jev를 초당 3회 루프 안에"라는 전제가 흔들림 → E2a 전에 사용자와 방향 논의(E2a는 G/H 중심으로 축소 실행 제안).
4. **자기 확인이 실제로 가능한가**: 열린 루프 재생에서 T_c=0.33, lead_max=1.5 s일 때 스텝당 표 수 중앙값 ≥ 2이면 M4 (a)의 LA-2 확정이 성립 가능. < 2이면 (a)는 대부분 "미확정 실행"이 된다 → M4에 보고(E-M4의 "미확정 실행 스텝 비율" 감시 기준을 사전에 높게 잡음).
5. **H 선택에 지연이 영향 주나**: p95(S3) − p95(S1) ≤ 0.05 s면 E-M4의 H 비교에서 지연은 무시(정확도·토큰만). > T_c(0.33 s)면 H=3에 지연 벌점이 있다고 E-M4 판정 7 해석에 붙인다.
6. **결정성**: 재시험 flip = 0이고 |Δp| 최대 < 0.01이면 "Jev는 같은 입력에 결정적" → M4 (a) 합의의 불일치는 **상태 변화에서만** 온다고 해석(M4 §5 기록 그대로). flip > 0이면 그 비율을 M4 `flip_score` 기준선으로 쓴다. 상태 변화에서 오는 불일치가 실제로 있는지는 **E0.5(§2A)**가 잰다.
7. **위치 차이**: PC − 파드 p95 차이가 0.1 s를 넘으면 폐루프는 반드시 파드에서 돌린다(당연하지만 기록).

### 2.8 결과별 설계 분기
| 결과 | M4 | M3 | M5 | 다음 실험 |
|---|---|---|---|---|
| (a)(b) 빠름 | 겹침 = 자기 확인. N_max 2~3. 스텝당 3표 가능 → (a) 합의가 주 역할 | D-줌 그대로 | 확정 길이 짧음 | E0.5 → E1 → E2a 그대로 |
| (c) 보통 | 겹침 = 지연 숨기기 + 가능한 만큼 자기 확인. N_max 3~5 | D-줌 가능, `mag_fine`은 (a) 방식(한 스텝 늦음) | `d_p95` 만큼 확정 | 그대로 |
| (d) 느림 | 가확정 실행 비율↑ 예상 | D-줌 불리 경고 [결정 필요] | 확정 구간 김 | E2a에서 G/H 해석 비중↑ |
| (e) 매우 느림/불안정 | — | — | — | 사용자 논의 먼저 |

### 2.9 비용·시간
- 토큰: 약 17만 요청 × 평균 약 2k 토큰 ≈ 3.4억 토큰 × $0.042/1M ≈ **$14**(X8 포함 여유 $18). usage가 질문별로 state를 다시 세는지는 모름 → 실제 `usage.input_tokens`로 첫 세션 뒤 재계산.
- 시간: 스크립트 작성 0.5일 + 자동 세션 4일(사람 개입 없음) + 분석 0.5일.

### 2.10 위험
- 속도 제한이 "동적으로 조정 중"(공식 경고) → 세션마다 한도가 다를 수 있다. 429 비율을 칸마다 보고하고, 설계값 계산에서 429가 1%를 넘은 세션은 표시.
- 업체 측 캐시: 같은 페이로드 반복이 빨라질 수 있다 → 지연 측정 페이로드는 매 요청 상태 한 줄(`t_state`, 프레임 번호)을 바꾼다. 결정성 측정만 완전히 같은 페이로드.
- 파드 네트워크 출구(egress)가 느리거나 프록시를 거칠 수 있다 → 네트워크 분해로 확인.
- 이 측정은 2026-09 말 시점이다. 모델·인프라가 바뀌면(별칭 이동, GPU 증설 예고) 다시 잰다. M4는 실행 중 최근 50회 p95로 `d_p95`를 갱신하므로 초기값 오차의 영향은 제한적.

---

## 2A. E0.5 — 오프라인 표 재생 (M4 (a) 합의에 거를 불일치가 있나) [D4 W2, 00 §13]

### 2A.1 목적
M4 새로움의 절반((a) 합의)은 "시간차로 받은 Jev 표들 사이에 정보가 있는 불일치가 있다"에 기댄다. Jev가 같은 입력에 결정적이고 0.33 s 사이 상태 변화가 작으면 (a)는 자명하게 일치하고, 주장은 (b) 코드 잔차 감시로 줄어든다(D4 F2). 이것을 **폐루프 E-M4(수천 에피소드) 전에, 기록된 궤적을 재생해 싸게** 잰다. 겹침 자체는 결과와 무관하게 끄지 않는다(00 §9 C1 [사용자]).

### 2A.2 가설 (사전)
- H05-a: 성공 궤적에서 연속 스냅샷(0.33 s 간격) 사이 최빈 보기 불일치율(flip rate)은 5% 이상이다(모름, 방향만).
- H05-b: 합의 규칙(LA-2 / γ=0.67)이 newest-wins보다 결과 기반 정답률이 높다.
- H05-c: 섭동 직후 창의 불일치가 성공 궤적 창보다 크다(`C_flip` 조기 경고의 전제).

### 2A.3 설정
- 자료: 스냅샷 풀 POOL 에피소드(§1.5)에 **0.33 s 간격 연속 스냅샷 열**(시뮬 전체 상태 포함, 짧은 롤아웃 재시작용)을 함께 저장한다. 궤적은 오라클 스크립트 플래너가 몬다(재생이라 표가 궤적을 바꾸지 않는다).
- 재생 방식: 결정 스텝 ds마다 H=1 "같은 스텝을 시각을 당겨 2~3회 묻기"(M4 §4.1)를 흉내 낸다. 스텝 시작 시각 t_s에서 `d_p95`(E0 값) 앞의 최근 연속 스냅샷 3개(t_s − d_p95 − {0, 0.33, 0.66} s)의 텍스트 상태로 같은 질문(같은 `question_id`)을 묻는다. 각 표 = 그 시각 상태에서의 답.
- 재시험: 같은 스냅샷을 한 번 더 물어 재시험 flip(E0 판정 6과 같은 정의)을 같이 잰다.
- 표현: M3 D-줌(분할 방향 질문 `dir_xy`·`dir_z` + `mag_coarse`)과 H안(`target`·`phase`, 접촉 근처 `fine_dir`). 확률 게이트 끔(00 §6).
- 비교 규칙(같은 표 묶음에서 사후 계산): **newest-wins**(C2) / **LA-2·γ=0.67**(C3의 합의 부분, 합의 안 되면 가확정 = 첫 표) / **감쇠 융합**(C2', 표 나이에 지수 감쇠 가중을 둔 최빈, 반감기 0.33 s 시작값).
- 결과 기반 라벨(§4 E2a-off와 같은 라벨러): 스냅샷 상태를 복원해 보기마다 그 보기를 한 스텝 실행한 뒤 고정 후속 정책(오라클 플래너)으로 최대 10 s(시뮬 시간) 굴려, 성공·진행 점수가 최선인 보기 **집합**을 정답으로 둔다. 플래너와 다르지만 똑같이 좋은 보기는 정답이다(D4 F5).
- `C_flip` 두 계산식을 같이 계산: (i) 표 분포 거리(같은 스텝 표 분포 대 직전 창 분포의 총변동 거리, 00 §12) / (ii) 한 답 대 한 답 뒤집힘 비율(첫 판 식).

### 2A.4 표본과 근거
- POOL 120 에피소드 × 에피소드당 결정 스텝 약 10~30개(D-줌이 많음) × 3표 + 재시험 1 → 표현 두 개 합쳐 약 1만~1.5만 요청. 짝(같은 스텝 같은 표 묶음) 비교라 규칙 사이 정답률 차 2%p를 약 1,500 스텝에서 가를 수 있다(불일치 5% 가정 [우리 계산]). 에피소드 군집 부트스트랩.
- 성공 궤적(P0 무섭동)과 섭동 궤적(P1~P2, 보류 섭동 P3·P4는 쓰지 않는다 — E2a TEST 보호)을 나눠 보고.

### 2A.5 지표
재시험 flip 비율 / 연속 스냅샷 사이 최빈 불일치율(flip rate, 성공·섭동 궤적 따로) / 규칙별 결과 기반 정답률 / LA-2 미확정 비율 / `C_flip` 두 식의 섭동 창 대 성공 창 판별 AUROC / 두 식의 성공 궤적 1−α 분위(FLIP_TH 초기값 후보).

### 2A.6 판정 기준 (사전 등록)
1. **M4 주장 좁힘**: 성공 궤적 flip rate < 5%(≈0)이고 LA-2·감쇠 융합 둘 다 newest 대비 정답률 +2%p 미만이면, **E-M4 전에 M4 컨트리뷰션 문장을 (b)(코드 잔차 감시 + 전제 epoch 무효화) 중심으로 좁히고 (a)는 "안정화 장치"로 적는다**(M4 §5 판정 2를 앞당김). 겹침은 끄지 않는다.
2. **(a) 유지**: flip rate ≥ 5%이고 LA-2 또는 감쇠 융합이 newest보다 +2%p 이상(하한 > 0) → (a) 주장 유지, E-M4 그대로.
3. **불일치는 있으나 잡음**: flip rate ≥ 5%인데 정답률 이득 < +2%p → (a)는 "흔들림 억제"로만 주장, 번복률 지표를 E-M4 주 지표에 올린다.
4. **조기 경고**: 섭동 창 flip이 성공 창의 2배 이상이고 AUROC ≥ 0.7이면 `C_flip`을 M7 소프트 채널로 유지. 두 계산식 중 AUROC가 0.03 이상 높은 쪽을 잠정 기본으로 보고([결정 필요] 4 승인 전), 차이가 0.03 미만이면 00 §12 식(표 분포 거리)을 유지.
5. 재시험 flip > 0이면 그 값을 flip rate 해석의 기준선으로 뺀 값도 함께 보고.
- 한계(결과 문장에 명시): 재생은 개루프다. 표가 궤적을 바꾸지 못하므로 "불일치에 정보가 있는가"만 재고, 폐루프 이득은 E-M4가 잰다.

### 2A.7 비용·시간
- Jev: 약 1.5만 요청 × 약 1~1.5k 토큰 ≈ 2,000만 토큰 ≈ **$1**. 표현·반복을 늘려도 **$1~5**.
- 롤아웃 라벨: 보기 수(≤ 17) × 라벨 스텝 수 × 10 s 시뮬 → GPU 수 시간(벽시계 동기 불필요, 실시간보다 빠르게 돌려도 됨). E2a-off와 라벨러 공유.
- 벽시계 약 1일(풀·라벨러 준비 뒤).

---

## 3. E1 — 보정 (질문 유형·보기 이름·보기 수·위치)

### 3.1 목적
00 §6에 따라 지금은 모든 확률 게이트가 꺼져 있다. E1은 **어느 질문군에서, 어떤 형식으로, 어느 임계값까지 확률 게이트를 켜도 되는지**를 정한다. 부수로 보기 이름 형식·보기 순서 규칙(E2a 템플릿 고정)을 정한다. E-M3-1(보기 수·형식 오프라인 시험)과 같은 자료·같은 호출로 돈다.

### 3.2 가설 (사전)
- H1-a: 공식은 "보정됨"이라 하지만 기준이 정답이 아니라 최상위 LLM 판단에 가깝다(plan §1). **시뮬 오라클 정답 기준 ECE는 0.05를 넘을 것**이고, 온도 조정 뒤 줄어든다.
- H1-b: 보기 수 K가 늘면 정확도·보정이 나빠진다(2604.14634 방향, Jev 아님).
- H1-c: 의미 있는 보기 이름 > 중립(A/B/C) > 무작위 문자열 순으로 정확도가 높다.
- H1-d: 위치 편향이 있다(앞쪽 보기 선호). 크기는 모름.
- H1-e: Noul 긍정/부정 문구의 확률 합은 1에서 벗어난다(공식 #8 예: 1.19).

### 3.3 설정
- 자료: 스냅샷 풀 1,200개(§1.5), 에피소드 단위로 적합 600 / 시험 600.
- 질문군(모두 설계 문서에서 실제로 쓰는 질문, 영어 고정 문구):

| 질문군 | 유형 | 보기/수준 | 정답(오라클) | 쓰는 곳 |
|---|---|---|---|---|
| QC-dir | Choice | 분할 10(`dir_xy`) + 4(`dir_z`) 기본, 비교 조건 28(`D-dir28`) | 오라클 플래너 방향(최근접 방향). 보정용 라벨이며 지표 이름은 "플래너 일치율"(D4 F5). 룰 대 Jev 비교에는 쓰지 않는다(§4.4b) | M3 D |
| QC-mag | Choice | 5(로그 구간) | 오라클 거리 구간 | M3 D |
| QC-target | Choice | 관련 물체 + 방해물 | 올바른 목표 ID | M3 G/H |
| QC-phase | Choice | {continue, next, hold, NONE_ESCALATE} | 오라클 단계 전이 | M3 G/H, M4 |
| QC-progress | Choice | {valid_progress, allowed_change, failure, recovering, NONE_ESCALATE}(`PROGRESS_OPTIONS`, 00 §11.2) | 코드 진행 라벨 | M7 S5 |
| QS-dist | Score | 4단계(far/mid/near/contact, M3 `dist_cat`) | 오라클 거리 범주 | 순서형 비교용 |
| QN-pred | Noul | 술어 5종(above, near, holding, aligned_xy, on) | 오라클 술어 참값 | M7, M9 |
| QN-t3b | Noul | "should have been completed by now?" | 오라클 경과 > 예상 여부 | M8 T3b |
| QC-accept | Choice | M9 복구 제안 목록({`continue_lower_layer`, `resume_ckpt_<k>`…, `reset_skill`, `safe_wait`}) + `NONE_ESCALATE`. 예/아니오로 묻지 않는다(거절 보기 없음, M7 FAIL 뒤에만, 00 §11.2 B10) | 오라클이 고른 제안 | M9, M6 `dp.critic_accept` |

- 형식 요인(본 운영 형식 = 굵게):
  - 보기 이름: **의미 있는 이름**(`move_left_small`) / 중립(`A`,`B`,`C`… + 설명은 `criteria`에) / 무작위(`k7qz` 같은 4자 + 설명).
  - 보기 수 K(QC-dir·QC-target에서): {3, 6, 12, 28}. 작은 K는 정답 + 오라클 비의존 무작위 방해 보기.
  - 위치: 각 항목을 정답 위치 앞·가운데·뒤 3회전(순환)으로. `criteria` 맵의 키 순서가 모델에 그대로 가는지는 **[가정]**(JSON 객체 순서 보존) — 순서 보존 여부를 확인할 수 없으므로, 위치 효과가 0이면 "순서가 전달되지 않았을 가능성"도 함께 적는다.
  - Noul: 긍정/부정 문구, `criteria` 있음/없음.
- **묶음(packing) 확인** [제안]: 한 스냅샷의 여러 변형 질문을 한 요청에 묶으면 비용·시간이 크게 준다. 먼저 50항목을 (단독 질문 요청) 대 (묶음 요청)으로 보내 비교: 최빈 일치율 ≥ 98%이고 평균 |Δp| ≤ 0.02면 묶음 허용. 아니면 실제 `JevCall` 구성(결정 + 감시 질문만)으로 한 요청씩.
- 반복: 시험 세트 전체를 2회(서로 다른 날) → 결정성 재확인. 분석은 첫 회.

### 3.4 표본과 근거
- 본 운영 형식 칸: 질문군별 시험 600항목. ECE(15개 동일 질량 구간) 부트스트랩 표준오차 약 0.01~0.015 **[가정: 일반적 규모, 실제 구간은 부트스트랩으로 보고]**. AUROC는 오답 수가 좌우한다: 오답 ≥ 60이면 AUROC 표준오차 약 0.03. **시험 세트 오답이 30개 미만인 질문군은 "AUROC 판정 불가"로 두고 게이트를 켜지 않는다**(검증 못 한 게이트는 켜지 않는다). 경계 과표집(§1.5)이 오답 수를 확보하는 장치.
- 형식 요인 칸: 시험 세트 중 300항목 하위집합에서 이름 3 × K 4 × 위치 3 = 36변형(QC-dir·QC-target). 300 짝 항목이면 정확도 차이 약 5%p를 짝 비교로 가를 수 있다(불일치 10% 가정 시 약 310항목 필요, M1 E3와 같은 계산).

### 3.5 지표
- 정확도(최빈 보기), 우연 보정 정확도(NA, E-M3-1과 같음).
- 보정: **최상위 확률 ECE**(Choice·Score), **이진 ECE**(Noul p(yes)), 클래스별 ECE(K ≤ 6), Brier, NLL, 신뢰도 도표.
- 선택적 예측: 정오답 판별 AUROC(점수 = 최상위 확률, 그리고 응답의 `confidence` 필드 둘 다), AURC, 임계 θ별 (적용률, 적용 구간 정확도).
- 위치 편향: 정답 위치별 정확도 기울기, 오답이 고른 위치 분포(균등 대비 χ², 최대 위치 몫/균등 몫), 회전 사이 최빈 불변 비율.
- Noul 극성: p(긍정) + p(부정)의 1과의 차이 분포(보고만, #8 규칙상 설계에 쓰지 않음).
- Score: **최빈 수준의 정답 여부로만** 보정을 잰다. `score` 기댓값은 보정 대상이 아니다(#2(c) 보간 금지).

### 3.6 온도 적합
- 로짓 = log(p)(p는 1e-6에서 자름; 응답 확률이 소수 2자리로 반올림돼 오면 꼬리 해상도가 낮다 → **[가정] 반올림 여부 확인**, 반올림이면 0.0 확률 보기는 1e-3으로 두고 보고).
- 질문군별 단일 온도 T를 적합 세트 NLL 최소화로 적합. 비교: 히스토그램 구간화·등위 회귀. **기본은 온도**, 등위 회귀는 시험 ECE가 0.02 이상 더 낮을 때만.
- 원 확률 ECE ≤ 0.03이고 T ∈ [0.8, 1.25]면 보정 없이 원 확률 사용.

### 3.7 판정 기준 (사전 등록)
질문군 Q(본 운영 형식 고정, `question_id` 계열 단위)마다, 시험 세트에서:
1. **게이트 켬 조건(모두 만족)**: (i) 보정 뒤 ECE ≤ 0.05이고 부트스트랩 95% 상한 ≤ 0.08, (ii) AUROC ≥ 0.75(하한 ≥ 0.70), (iii) 후보 임계 θ(모듈 설정 표 후보값: M5 0.8, M7 0.8, M8 0.7, M9 0.6)에서 "보정 확률 ≥ θ" 구간의 실제 정확도 ≥ θ − 0.03(하한 ≥ θ − 0.08), (iv) 그 구간 적용률 ≥ 20%. 통과하면 그 질문군에 **보정 확률 기준 θ 게이트**를 허용(원 확률에 거는 것 아님).
2. **확률을 양으로 쓰는 설계**(가중 평균·가중 합의, M3 "봉우리 검사 후 평균", M5 AAC 확정 길이 게이트 등): 1번 + ECE ≤ 0.03 + K·이름 형식을 바꿔도 ECE 차이 ≤ 0.03일 때만. 아니면 계속 끈다.
3. **보기 이름 형식**: 의미 있는 이름이 기본. 중립 또는 무작위가 정확도에서 −2%p 이내이면서 AUROC가 0.03 이상 높으면 그 형식으로 바꾼다(이름이 답을 흘리는 효과를 줄이는 쪽). 무작위 이름 정확도가 의미 있는 이름보다 10%p 이상 낮으면 "Jev는 보기 이름을 크게 읽는다"로 기록 → M3 보기 이름 규칙을 엄격히(이름과 설명을 일치시키는 검사 추가).
4. **보기 수**: K별 정확도·ECE를 E-M3-1 판정 1과 함께 보고. K=28(`D-dir28`)에서 QC-dir의 ECE가 K=6보다 0.05 이상 나쁘면 한 질문 28개 형식은 게이트 영구 금지(최빈만)로 기록. 분할 형식(10 + 4)의 방향 정확도(두 답의 결합 기준)가 `D-dir28`보다 5%p 이상 낮으면 [결정 필요]로 되돌림 여부를 올린다(M3 §4.2).
5. **위치 편향**: |기울기| > 0.02(M3 E-M3-1과 같은 값) 또는 오답 위치 최대 몫 > 균등의 1.5배(χ² p < 0.01)면 보기 순서를 **오라클 비의존 고정 순서(ID 정렬)**로 두고 편향 크기를 결과에 적는다. 회전 사이 최빈 불변 비율 < 90%면 M4 합의에서 "같은 `question_id`"의 정의에 보기 순서까지 포함(순서가 다르면 다른 질문).
6. **Noul 대 Choice**: 같은 판단을 Noul과 2보기 Choice로 물었을 때 임계값을 옮기지 않는다(공식 #8). 각각 따로 보정한 값만 쓴다.
7. **재보정 조건**: 모델 버전, 질문 문구, 보기 목록, 상태 형식(M1 후보) 중 하나라도 바뀌면 해당 질문군은 게이트를 끄고 E1 축소판(시험 300항목)을 다시 돌린다.

### 3.8 결과별 설계 분기
- 통과한 질문군만 00 §6의 후보 게이트를 켠다(예: QN-t3b 통과 → M8 T3b "예 확률 ≥ 0.7" 변형 허용). 통과 못 한 질문군은 최빈 선택만 유지(현재 설계 그대로라 설계가 깨지지 않는다).
- 전부 실패: 확률은 기록용으로만 남고, M4 교체 게이트 θ·M5 확정 길이 게이트는 영구 끔. **설계 전체는 최빈 선택만으로 이미 서 있으므로 방향 변경은 아니다.**
- QC-progress·QN-pred 정확도 자체가 낮으면(< 80%, **[가정] 문턱**) E2a 전에 M7 진행 질문·M1 형식을 먼저 손본다(E-M4의 감시 질문 잡음이 결과를 흐리지 않도록).

### 3.9 비용·시간
- 호출 수: 묶음 허용 시 1,200 스냅샷 × 약 3요청 × 2회 ≈ 7,200요청 + 형식 하위집합 300 × 36변형(묶음 약 6요청) ≈ 1,800 + 묶음 확인 100. 요청당 약 3~6k 토큰 → 약 4,000만 토큰 ≈ **$2**. 묶음 불허 시 질문 하나당 한 요청 → 약 5~10배 → **$10 이하**.
- 시간: 20 요청/초 한도 안에서 수 시간. 템플릿·분석 1~2일. 스냅샷 풀 생성(환경 구축 뒤) 약 0.5일.

### 3.10 위험
- **오라클 정답이 "사람이 볼 정답"과 다를 수 있다**: 문턱 근처 술어, 방향 26개의 최근접 선택은 경계에서 애매하다. `ambiguous` 항목은 본 ECE에서 빼고 따로 보고.
- Jev 보정 기준이 LLM 판단이라 오라클 기준 ECE가 나빠도 모델 결함이 아닐 수 있다 → 결론은 "우리 과제·우리 정답 기준에서"로 한정.
- 오라클 상태 기준 보정은 인식 잡음이 있는 상태에서 옮겨지지 않을 수 있다 → 인식 조건을 쓰는 단계에서 축소판 재측정.
- 확률 반올림이 꼬리(0.95 이상) 해상도를 깎으면 θ=0.9 이상 게이트는 판정 불가로 둔다.

---

## 4. E2a — 마차 시험 (같은 텍스트 상태에서 룰 대 Jev C1, M4 없이)

> 개정(D4 F3·F4·F5·D-1, 00 §13): 첫 판 E2를 나눴다. **E2a** = 룰 대 Jev C1(세 표현), M4 없이 — 마차 판정은 여기서. 첫 판의 C5 조건(JD-C5·JG-C5·JH-C5)과 "M4 첫 신호" 판정은 **E-M4의 첫 칸으로 옮겼다**(M4 §5). 오프라인 비교는 결과 기반 라벨(E2a-off)로, Astra를 켠 대응 조건을 보조 쌍으로 더했다. 방향 재검토는 95% 상한 < +5%p일 때만 발동하고, 그 밖의 비유의는 "결론 유보"다.

### 4.1 목적
plan §0의 "가장 약한 고리": **Jev가 코드가 만든 술어 위에서 내리는 결정이 같은 술어를 쓰는 룰보다 나은가.** 낫지 않으면 M3·M4 이후 설계의 전제가 사라지므로 방향을 다시 본다(plan §5). M4 구현(C5, CAL 보정) 없이 돌 수 있어야 "먼저 싸게 재는 마차 시험"이 된다(D4 F4). 부수로 M3 세 안(D-줌/G/H)의 첫 신호를 얻는다(최종 판정은 E-M3-2).

### 4.2 가설 (사전)
- H2-a(주): 최선 Jev 조건의 성공률이 R0보다 5%p 이상 높다.
- H2-b: 그 우위는 **룰 작성자가 보지 못한 보류 섭동**(P3·P4)에서 더 크다(LLM 일반화 주장, plan §0 [사용자]).
- H2-c: R0 대 R-lag 차이(지연만의 비용)는 Jev 조건 사이 차이보다 작다.
- H2-e: 결과 기반 라벨로 채점한 오프라인 정답률에서도 최선 Jev ≥ 룰이다(플래너 일치율과 방향이 다를 수 있음).
- H2-f(보조): Astra 실패 호출을 룰·Jev 양쪽에 켜면 둘의 차이가 줄어든다(방향 모름, D4 D-1).
- (첫 판 H2-d "M4 켬 > 끔"은 E-M4로 옮김.)

### 4.3 설정
- 환경: §1.4 단일 팔 자작 장면 pick-and-place(00 §13: E0~E3는 이 장면, RoboDojo 본 평가와 섞지 않는다), **오라클 상태**, 벽시계 동기 실시간, 제어기·스킬·IK·M5(L2 RTC식 감쇠 블렌딩 + L3 Ruckig, 00 §9 C3 기본)는 모든 조건에서 같다.
- 상태: §1.4의 측정용 고정 형식(등록부 술어 줄 표). **룰과 Jev는 같은 텍스트 상태(같은 술어 값)를 받는다.** 룰은 파싱된 술어 값을, Jev는 그 텍스트를 받는다. 룰은 원시 좌표를 보지 않는다(보면 마차 시험이 아니라 플래너 비교가 된다).
- 행동 공간: 조건 안에서 룰과 Jev의 **보기 집합이 같다**(룰도 같은 보기 목록에서 고른다). 연속 값은 모두 코드가 만든다(M3 §1).
- Astra: 세션 계약(M2)은 **과제 템플릿당 1회 Astra가 작성하고 모든 조건·시드에서 같은 계약을 재사용**. **effort = high**(잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5, 00 §16. low·medium은 쓰지 않는다). 주 조건에서는 **실행 중 Astra 개입(실패 시 호출)을 끈다.** 이는 [사용자] "실패할 때마다 Astra 개입" 원칙에서 **측정 목적상 벗어나는 것**이며 E2a 주 조건 한정이다. 이유: 결정 층만 분리해 재기 위해. 그래서 **Astra를 켠 대응 조건 쌍(R0+A / J*+A)**을 보조로 둔다(§4.4). 주 조건에서 M7은 신호를 기록만 하고 개입하지 않는다. 종료 = 성공 / 60 s 시간 초과 / 낙하.
- JevCall: H=1 고정(M3 원안, H는 E-M4에서 정함). 확률 게이트 전부 끔(00 §6, E1 결과와 무관하게 E2a에서는 끔 — 조건 사이 공정성). 보기 이름·순서는 E1 판정 3·5 결과로 고정. 방향 질문은 두 질문 분할(M3 §4.2). `mag_fine`은 (a) 방식(직전 스텝 거친 구간 기준)만.
- M4: **쓰지 않는다.** Jev 조건은 모두 C1(단일 in-flight + 직전 행동 유지)이다. C1은 00 §9 C1에 따라 설계 후보가 아니라 비교 조건이며, 사용자 필수 요구인 겹침은 E-M4에서 켠다. 따라서 E2a 결과는 "M4 없는 Jev 결정 층"에 대한 결론으로 한정한다.
- 병렬: 환경 4개 동시(총 약 12 요청/초 ≤ 한도의 60%). 초과 시 3개로.

### 4.4 조건
| 조건 | 결정 주체 | M3 표현 | 실행 중 Astra | 뜻 |
|---|---|---|---|---|
| **R0** | 룰 | 각 표현의 보기 집합(D용·G용 두 벌) | 끔 | 마차 기준 |
| R-lag | 룰 | 같음 | 끔 | C1 파이프라인 + E0 지연 분포 재생. 지연만의 비용 분리 |
| JD-C1 | Jev | D-줌 | 끔 | 사용자 원안 |
| JG-C1 | Jev | G(목표 지정) | 끔 | |
| JH-C1 | Jev | H(혼합) | 끔 | M3 제안 1순위 |
| R0+A (보조) | 룰 | R0와 같음 | **켬** | 사용자 구조 안의 룰 |
| J*+A (보조) | Jev | 주 비교 최선 Jev 조건의 표현 | **켬** | 사용자 구조 안의 Jev |
| (참고) SKILL | 기존 스킬 스크립트(원시 기하 사용) | — | 끔 | plan §3 "룰베이스(스킬 다발만)" 참고치, 판정에 안 씀 |

- R0는 표현별 두 벌(R0-D: 방향·크기 규칙, R0-G: 목표·접근·단계 규칙)을 만들고, 판정에는 **두 벌 중 TEST 성공률이 높은 쪽**을 R0로 쓴다(룰에 유리하게 = 보수적 판정).
- **Astra 켠 대응 쌍(R0+A / J*+A)**: M7 FAIL → M8 T_fail(비동기 Astra, effort high) → M9 최소판(L1 파라미터 재시도 + L3 대기, D4 §3.2)을 **룰과 Jev에 똑같이** 붙인다. J*는 주 비교 TEST가 끝난 뒤 정한다(사전 규칙: 주 비교 점추정이 가장 높은 Jev 조건). 표본은 TEST 중 보류 섭동 P3·P4 60쌍(실패가 나오는 곳). 시간 제한은 Astra high 첫 토큰 약 73 s(plan §1, 2026-09-23 값, 날마다 변동)가 60 s보다 길므로 **180 s로 돌리고, 같은 기록에서 60 s 절단 성공률도 계산해 둘 다 보고**한다(M9 §7 [결정 필요] 사용자 원칙 충돌의 자료).
- 룰 작성 공정성: 룰과 Jev 질문 템플릿은 **같은 사람이 같은 시간 예산(각 1일)으로** DEV 시드와 **DEV 섭동(P0~P2)만** 보고 만든다. 둘 다 TEST 실행 전에 동결(해시 기록).

### 4.4b E2a-off — 오프라인 룰 대 Jev (결과 기반 라벨)
- 자료: 스냅샷 풀 시험 분할 600 스냅샷(E1 적합 에피소드 제외).
- 라벨(D4 F5): 스냅샷 상태를 복원해 **보기마다 짧게 굴려**(그 보기를 한 스텝 실행 → 고정 후속 정책(오라클 플래너)으로 최대 10 s 시뮬) 성공·진행 점수가 최선인 보기 **집합**을 정답으로 둔다. 오라클 플래너 선택과의 일치는 **"플래너 일치율"**로 이름을 바꿔 따로 보고한다. 후속 정책이 플래너라서 남는 치우침(플래너가 잘 이어 가는 보기에 유리)은 결과에 적는다.
- 조건: R0-D·R0-G, JD-C1·JG-C1·JH-C1의 질문(같은 상태, 같은 보기).
- 지표: 결과 기반 정답률, 플래너 일치율, 둘의 차이(Jev가 "플래너와 다르지만 좋은 답"을 내는 비율).
- 비용: Jev 약 3,000 요청 ≈ **$1 미만**, 롤아웃 GPU 수 시간(E0.5와 라벨러 공유).

### 4.5 섭동 (시드로 결정, 모든 조건 같은 시각·크기)
| 코드 | 섭동 | 발동 | 룰 작성자 공개 |
|---|---|---|---|
| P0 | 없음(초기 배치 무작위만) | — | DEV |
| P1 | 대상 물체 수평 2 cm 이동(방향 무작위) | 그리퍼가 대상에 `near` 처음 참일 때 | DEV |
| P2 | 방해물이 경로 옆에 등장 | 운반 단계 시작 뒤 0.5 s | DEV |
| P3 | 잡기 미끄러짐(들어 올린 뒤 물체 이탈) | `lifted` 처음 참 뒤 0.3 s | **보류(TEST에서만)** |
| P4 | 짧은 가림(대상의 술어가 0.8 s 동안 `unknown`, 오라클 상태에 마스크) | 놓기 단계 진입 시 | **보류(TEST에서만)** |
- 보류 섭동은 **Jev 템플릿 작성자에게도 보류**다. 둘 다 못 본 상황에서의 일반화를 비교한다.
- E2a는 오라클 상태·섭동만 쓰고 **환경 변화 축(조명·배경·재질)은 재지 않는다**(D4 F1). 그 축의 LLM 몫은 EVAL S1 직후 H4 선행 측정과 random 스냅샷 오프라인 룰 대 Jev에서 먼저 본다(EVAL §5).

### 4.6 표본과 근거
- TEST 150 시드 = 섭동 5종 × 30시드. 주 조건(R0, R-lag, Jev 3조건)이 같은 150개를 짝으로 돈다.
- 주 비교(최선 Jev − R0, 합산 성공률): 짝 McNemar로 차이 15%p를 검정력 0.8·α 0.05로 잡으려면 불일치 비율 25% 가정 시 약 85쌍 필요 → 150쌍이면 약 12%p까지. 차이 추정의 95% 구간 반폭 약 ±7~8%p. **그래서 "유의하지 않음"만으로는 "우위 없음"을 말할 수 없다**(D4 F3) — 방향 재검토는 상한으로만 판정한다(§4.8).
- 보류 섭동 부분집합(P3+P4, 60쌍): 20%p 차이 정도만 가를 수 있다 → **보조 판정**으로만 쓴다. Astra 켠 대응 쌍도 이 60쌍.
- 섭동별(30쌍)은 기술 통계만.
- **1회 사전 등록 연장**: 어느 Jev 조건이든 95% 구간이 +5%p를 걸치면(우위도 우위 없음도 보이지 않음) TEST2(시드 1150~1299, 150개)를 주 조건 모두에 추가하고 300쌍으로 한 번만 다시 판정. 그 외 연장 없음.
- 실행 순서: 시드 블록(10시드)마다 모든 조건을 무작위 순서로 돈다(시간대 효과 제거, E0 판정 1).

### 4.7 지표
- 주: 성공률(TEST 합산).
- 보조: 보류 섭동 성공률, 섭동별 성공률, 완료 시간, 최종 놓기 오차(mm), 결정 수, `NONE_ESCALATE` 비율, 섭동 반응 시간(섭동 → 새 선택이 실행에 반영), 정지 시간(00 §4상 주 조건에서 0이어야 함, Astra 켠 쌍은 L3 대기 시간을 따로), jerk, Jev 호출 수·지연 p50/p95·토큰, (Astra 켠 쌍) Astra 호출 수·첫 토큰 지연·비용.
- 오류 분해(실패 에피소드 전수): 결정 오류(**결과 기반 라벨러**로 그 스텝의 보기가 최선 집합 밖이었나) / 실행 오류(보기는 최선 집합 안인데 스킬·제어 실패) / 지연 오류(맞는 결정이 늦게 반영). 첫 판의 "오라클이 다른 보기를 택했을 스텝" 기준은 플래너 일치율로만 보조 보고.

### 4.8 판정 기준 (사전 등록)
1. **마차 통과**: Jev 3조건 중 하나 이상이 R0 대비 성공률 +5%p 이상이고 Holm 보정(3개) 짝 부트스트랩 하한 > 0.
2. **방향 재검토 발동(마차 실패)**: **모든 Jev 조건에서 (Jev − R0)의 95% 상한 < +5%p**(우위 없음이 **보인** 경우, 연장 뒤)일 때만 **프로젝트 방향 재검토 보고서**를 사용자에게 올린다(설계 진행 멈춤, E-M4 보류).
   - 보고서에 담을 선택지(결정은 사용자): Jev를 결정 지점(G) 전용으로 축소 / Jev를 감시·확인(M7 진행, M4 (b) 보조) 역할로 옮김 / 촘촘한 결정은 룰, 실패 판단은 Astra 중심 / 상태 형식(M1)을 먼저 바꿔 재시험.
   - 보류 섭동 부분집합의 결과는 보고서에 함께 적되 발동 조건에 쓰지 않는다(60쌍은 가를 힘이 없다).
2′. **결론 유보**: 1도 2도 아니면(구간이 +5%p를 걸치고 연장 뒤에도 그대로) **"결론 유보"**로 사용자에게 보고한다. 멈춤이 아니다 — E-M4로 진행하되, 마차 판정은 E-M4의 C1 대 룰 대조와 EVAL 결과로 다시 본다.
3. **강한 반대 증거**: 최선 Jev 조건이 R0보다 유의하게 낮으면(상한 < 0) 2와 같이 보고하되 "Jev 결정이 해롭다"로 명시.
4. **일반화 쪽으로 좁힘**: 1은 실패, 보류 섭동에서 최선 Jev − R0 ≥ +10%p(하한 > 0) → 방향 재검토가 아니라 **주장을 "처음 보는 섭동에서의 강건성"으로 좁혀** 진행(plan §3 주장과 같은 방향). 보고.
5. **지연이 병목인가**: R0 − R-lag ≥ 10%p이고 최선 Jev ≈ R-lag(±5%p) → "결정 품질은 같고 지연이 비용" → M4·G/H(결정 수 적음)로 방향 보고, E0 판정과 함께 해석.
6. **M3 표현 첫 신호**(E-M3-2 전 예비, 자동 탈락 없음): 표현별(C1 기준) 성공률을 보고. **D-줌이 최선 표현보다 15%p 이상 낮고 상한 < 0이면 [결정 필요]로 사용자에게 올린다**(사용자 원안이므로 자동으로 빼지 않는다). E-M4는 M4 문서대로 D-줌과 H 두 안으로 간다.
7. **오프라인 대조(E2a-off)**: 결과 기반 정답률에서 최선 Jev − R0 ≥ +5%p인데 플래너 일치율에서는 Jev가 낮으면 "플래너와 다르지만 좋은 답"으로 보고한다. 폐루프 판정 1·2를 뒤집지 않고 해석에만 쓴다.
8. **Astra 켠 대응 쌍(보조, 판정 1·2를 뒤집지 않는다)**: (J*+A) − (R0+A)와 주 비교의 (J* − R0)을 나란히 보고. 결과 문장은 **"Astra 없는 결정 층 비교"(주)**와 **"사용자 구조 안 비교"(보조)**로 나눠 적는다. 60 s 절단과 180 s 결과를 둘 다 적는다.
9. 주 조건에서 정지 시간이 0이 아닌 조건이 있으면(00 §4 위반) 버그로 보고 그 조건 재실행.
- (첫 판 판정 7 "M4 첫 신호: C5 대 C1"은 E-M4 판정으로 옮김, M4 §5.)

### 4.9 결과별 설계 분기
| 결과 | 의미 | 다음 |
|---|---|---|
| 1 통과, 보류 섭동에서도 우위 | 마차 통과, 주장 그대로 | E-M4(C0~C6, C5 포함, H=1·3) → E-M3-2 |
| 4 (보류만 우위) | 결정 층 가치는 일반화에서 | 주장 문구 좁힘, E-M4 진행 |
| 5 (지연 병목) | Jev 결정은 룰 수준, 지연이 손해 | G/H 비중↑, M4 확정 규칙의 역할 재평가 |
| 2′ (결론 유보) | 가를 힘 부족 | 사용자에게 "결론 유보" 보고, E-M4 진행(멈춤 아님) |
| 2·3 (마차 실패) | 전제 흔들림 | 방향 재검토 보고(사용자 결정) |
| 사용자가 E3 뒤 M1 후보를 A 외로 결정 | 상태 형식 변경 | R0·최선 Jev만 새 형식으로 TEST 150 재실행(E1 재보정 축소판 선행, E1 판정 7) |

### 4.10 비용·시간
- Jev(주): Jev 조건 3 × (TEST 150 + DEV 약 20) ≈ 510 에피소드. 에피소드 약 40 s × 3 호출/초 ≈ 120 호출. S1 약 1.3k, G1 약 0.8k 토큰 → 에피소드당 약 0.1~0.16M 토큰 → 합계 약 0.7억 토큰 ≈ **$3**. 연장 발동 시 약 **$6**. E2a-off **$1 미만**. Astra 켠 쌍의 J*+A 60 에피소드 추가 약 **$0.5**.
- Astra(계약): 과제 템플릿 수(1~3) × 조정 수회. 요청당 입력 약 20k(이미지 포함)·출력 약 5~20k(추론 포함) **[가정]** → 10회면 **약 $5~12**.
- Astra(켠 쌍): 120 에피소드 × 실패당 1~3회 호출 **[가정]** ≈ 120~360 요청 → 입력 2.4~7.2M × $10 = $24~72, 출력(high) 0.6~7.2M × $50 = $30~360 → **약 $55~430**(첫 10요청으로 재추정, §7-7). 비용을 줄여야 하면 effort가 아니라 표본을 줄인다.
- 시간: 주 에피소드 약 900(R0 두 벌·R-lag 포함, 리셋 포함 50 s) ≈ 12.5 시간 직렬 → 환경 4개 병렬 약 **3~4 시간**. Astra 켠 쌍 120 × 약 200 s ≈ 6.7 시간 직렬 → 병렬 약 2 시간. 준비: 룰·템플릿 각 1일, 롤아웃 라벨러 0.5일(E0.5와 공유), 분석 1일 → 총 약 2~3일. **M4 C5 구현·CAL 보정은 E2a에 필요 없다**(E-M4로 옮김). **[가정] 환경·술어 계산기·스킬·M5 구현은 이 추정 밖.**

### 4.11 위험
- **룰이 약하게 만들어지면 마차 통과가 거짓 양성.** 대책: 시간 예산 동일, R0 두 벌 중 좋은 쪽, SKILL 참고치 보고, 룰 코드 공개.
- **오라클 상태라 Jev의 이점이 과소/과대평가될 수 있다.** 오라클 술어는 깨끗하므로 룰에 유리할 수 있다(룰은 잡음에 취약). 인식 조건 재시험 전까지 결론은 "오라클 상태에서"로 한정.
- **M4 없는 Jev(C1)는 사용자가 만들려는 시스템이 아니다.** E2a 결론은 "M4 없는 결정 층"으로 한정하고, 겹침(M4)을 켠 비교는 E-M4에서 한다.
- **결과 기반 라벨의 후속 정책 치우침**: 후속 정책이 오라클 플래너라 플래너가 잘 이어 가는 보기에 유리하다. 두 번째 후속 정책(R0)으로 100 스냅샷 하위집합을 다시 라벨해 라벨 일치율을 보고한다.
- 단일 pick-and-place는 룰이 쉽게 잘하는 과제다 → 천장 효과(R0 > 90%)면 판정 1은 사실상 불가능. **R0 DEV 성공률이 90%를 넘으면 TEST 전에 섭동 크기를 한 단계 키운다(P1 2 cm → 4 cm)** — 이 규칙 자체를 사전 등록.
- 주 조건에서 Astra 개입을 끈 것은 사용자 원칙에서 벗어난 측정 설정이다. 결과를 "Astra 없는 결정 층 비교"로 명시하고, 켠 대응 쌍을 함께 보고.
- 속도 제한 변동으로 429가 나면 해당 에피소드는 지연 오류로 남는다 → 429 발생 에피소드 비율을 조건별 보고, 5%를 넘는 조건은 재실행.

### 4.12 E-M4로 옮긴 것 (첫 판 E2에서)
- 조건 JD-C5·JG-C5·JH-C5(겹침 + (a)+(b) + `C_flip`), C5 설정(`d_p95`·N_max = E0 값, γ=0.67, W=1, τ=1(접촉 근처 0), α=0.01, STALE_MAX=1.5 s), CAL 시드 conformal·FLIP_TH 적합(표현별 ≥ 30회, E0.5 초기값에서 시작), 가설 H2-d, 판정 "C5가 C1보다 섭동 성공률 +5%p 이상이면 진행 근거 / 5%p 이상 낮고 상한 < 0이면 M4 설계 점검". E-M4의 첫 칸 = E2a와 같은 TEST 시드·섭동에서 C1 대 C5(표현별), 그 뒤 C0~C6 전체(M4 §5).
- (00 §14·§15) E-M4의 새 조건 **C3'**(Adaptive-Consistency식 베이즈 정지 규칙, 표 수만 사용. 원문 기본 C_thresh 0.95면 만장일치 4표가 필요해 스텝당 약 3표로는 확정되지 않으므로 임계값을 조건에 명시, 예: 0.9)와 **C3''**(같은 문구·같은 보기 집합에서 보기 순서만 돌리기)는 E-M4에 속한다. E2a·E3에는 넣지 않는다.

---

## 5. E3 — M1 상태 표현 비교 (오프라인, 오라클 정답)

### 5.1 목적
사용자가 M1 변환 방법(후보 A/B/C)을 고를 근거 자료를 만든다. **결정은 사용자**(00 §11.3). 판정 기준은 M1 §5를 그대로 가져오고, 이 문서는 실행 절차와 표본·비용을 채운다.

### 5.2 가설 (사전, M1 §5·§6에서)
- H3-a: 관련 부분만(A)이 전체(A-full)보다 정확도에서 2%p 넘게 떨어지지 않으면서 토큰을 50% 이상 줄인다(공식 jaggedness #5 방향).
- H3-b: 숫자 좌표(N-num)는 A보다 Q1·Q3에서 5%p 이상 낮다(공식 #2 방향).
- H3-c: 단순 pick-and-place에서 B ≈ A(M1 §4.2 추론).
- H3-d: 인식 잡음 주입 하락폭이 표현마다 다르다(모름).
- H3-e(00 §14, 방향 모름): 줄 순서만 바꾼 A-QPR이 A와 정답률이 같거나 높다. 원문 근거: TabGR(2601.08444, 무학회, LOW) **부록 Table 12** — QG-PPR을 빼고 행·열을 섞으면 WikiTQ 하락 5.8~7.1% 대 넣은 판 0.1~1.3%(TabFact에서는 1.0~1.5% 대 0.5~0.7%로 차이 작음). 원문은 행·열 격자 표 QA이고 우리 상태는 격자가 아니다(접목안).

### 5.3 설정
- **E3a(지금, pick-and-place)**: 스냅샷 풀(§1.5)에서 600 스냅샷(시험 분할과 같은 에피소드 쪽을 쓰되 E1 적합에 쓴 에피소드는 제외), 에피소드 ≥ 60개.
- **E3b(뒤, 관절 물체·뚜껑 과제)**: M1 판정 4(B)·5(C)는 부품·의미 상태가 있는 과제가 있어야 판정 가능 → 서랍 열고 넣기, 뚜껑 있는 용기 과제 2종을 추가한 뒤 같은 절차. **E3a에서는 판정 4·5를 "판정 보류"로 둔다.**
- 질문(M1 §5 그대로, 오라클 정답): Q1 목표 선택(`Q_target`), Q2 단계 완료(`exit_k`), Q3 다음 보기(`Q_fine_dir`, 정답 = 오라클 방향 — 표현 사이 비교용이며 지표 이름은 "플래너 일치율", D4 F5), Q4 계약 위반(금지 술어). 질문 문구·보기·순서(ID 정렬)는 표현 사이에 같다(#8 규칙).
- 표현 조건: A / B / C / A-full / N-num / D-only / V-free(M1 §4.1~4.4).
  - C의 `attrs`: 오라클 설정에서는 시뮬 메타데이터(재질·내용물)로 채운다(영역 캡셔너 오차 제외, 상한 측정). `astra_note`: Astra가 스냅샷 기준 1 s 전 프레임 이미지로 작성(effort high 고정).
  - V-free: Astra가 스냅샷 이미지(카메라 1대, `detail` 명시)로 자유 서술 → 그 텍스트를 state로(effort high). 오프라인 정확도 비교에만(M1 §4.4).
  - **A-QPR**(00 §14, [제안]): A와 내용은 같고 **줄 순서만** 다르다. 고정 순서(ID 정렬) 대신 질문 앵커(목표 물체 ID, 그리퍼, 결정 지점이 참조하는 술어)에서 장면 관계 그래프 위 개인화 PageRank 점수 순. 한 `JevCall`의 질문들은 상태를 공유하므로(00 §2) 앵커 = 그 호출의 결정 질문 + 감시 질문이 참조하는 ID의 합집합. 계산은 코드. 보기 순서(ID 정렬)는 바꾸지 않는다.
- 인식 잡음 주입(오라클 상태를 변형, A·B·C·N-num에만): 깨끗 / ID 교체 5% / 물체 누락 5% / 깊이 +2 cm 편향(술어 재계산). 실제 인식 앞단 조건(E3-P)은 인식 스택이 붙은 뒤.
- 반복: 스냅샷 × 표현마다 3회(결정성; E0 판정 6에서 결정적이면 1회로 줄이고 이유 기록).
- 묶음: E1 묶음 확인 결과를 따른다(허용 시 스냅샷당 Q1~Q4를 한 요청).

### 5.4 표본과 근거
- 질문 유형별 600 짝 항목(같은 스냅샷·같은 질문, 표현만 다름). 짝 비교에서 불일치 10% 가정 시 5%p 차이에 약 310 필요 → 600이면 약 3.5~4%p까지, 2%p 비열등 판정(판정 1)의 구간 반폭은 약 ±2~2.5%p → **판정 1의 "2%p" 비열등은 경계에 걸릴 수 있다.** 구간이 겹치면 M1 판정 7대로 "차이 없음"으로 적는다.
- 에피소드 군집 부트스트랩(같은 에피소드 스냅샷 상관 반영).

### 5.5 지표 (M1 §5 그대로)
질문 유형별 정답률(최빈 보기), `NONE_ESCALATE` 비율, 입력 토큰, Jev 지연 p50/p95, M1 직렬화 지연 p50/p95, 오류 분해(술어 오류 전파 / Jev 추론 오류: 오라클 술어로 바꿔 넣으면 맞는지로 판별), 잡음 조건별 하락폭.

### 5.6 판정 기준 (사전 등록, M1 §5 판정 1~7을 그대로 인용)
1. 필터 효과: A가 A-full보다 평균 정답률 2%p 이상 낮지 않고 토큰 50% 이상 적으면 "관련 부분만" 유지. A-full이 3%p 이상 높으면 [결정 필요].
2. 숫자 대조: N-num이 A보다 Q1·Q3에서 5%p 이상 낮으면 "Jev에 숫자 금지" 확정 근거로 기록.
3. diff 단독 금지: D-only가 A보다 Q2에서 10%p 이상 낮으면 규칙 유지. 3%p 미만이면 변화 절의 가치를 따로 본다(A 대 A−변화 절).
4. B 채택 조건(E3b에서): 관절 물체 하위집합 B − A ≥ 5%p, 단순 과제 −2%p 이내 → 과제 유형별 B 제안.
5. C 채택 조건(E3b에서): T3 필요 질문에서 C − A ≥ 5%p, 나머지 −2%p 이내 → C 제안. 나머지에서 떨어지면 C'(`attrs`만) 재시험.
6. 인식 민감도: 깨끗 → 잡음 하락폭이 표현 사이 5%p 이상 차이 → 하락폭이 작은 표현을 권고 1순위로(환경 일반화 주장과 직결).
7. 모든 비교는 부트스트랩 95% 구간, 겹치면 "차이 없음".
- 추가(이 문서) — **V-free 해석**: V-free가 A보다 높아도 다른 모듈 전제(코드 술어)를 깨므로 채택 판정에 쓰지 않고, M1 §7-2 [결정 필요]의 자료로만 보고.
- 추가(00 §14) — **A-QPR**: A-QPR이 A보다 평균 정답률 +2%p 이상이고 구간이 0을 넘으면 "줄 순서 규칙"을 M1 결정 자료에 넣는다. 근거가 LOW라 기본값으로 올리지 않고, 변환 방법 선택은 사용자 몫(00 §11.3).

### 5.7 결과별 설계 분기
- 판정 1·2·3은 A 형식 세부 규칙(필터, 숫자 금지, 변화 절)을 확정 또는 수정 → E2a 측정용 형식에도 반영(바뀌면 §4.9 재실행 규칙).
- 판정 6에서 하락폭 차이가 크면 M1 결정 자료의 1순위 근거로 사용자에게 제시.
- 사용자가 후보를 정하면: 다른 모듈은 등록부 인터페이스에만 의존하므로 설계 변경 없음. E1 해당 질문군 재보정(E1 판정 7).

### 5.8 비용·시간
- Jev: 7 표현 × 600 × 3회 ≈ 12,600 요청 + 잡음 3조건 × 4표현 × 600 × 3회 ≈ 21,600 요청, 요청당 약 0.7~3k 토큰(A-full이 가장 큼) → 약 4,000~6,000만 토큰 ≈ **$2~3**(3회 반복이 1회로 줄면 1/3).
- A-QPR 추가분: 600 × 3회 ≈ 1,800 Jev 요청, 약 $0.3~0.5(A와 같은 크기).
- Astra(V-free 600 + C `astra_note` 600 = 1,200 요청): 입력 = 이미지 1장(640×480이면 32px 패치 20×15=300 × 1.2 ≈ 360 토큰, plan §1 공식) + 텍스트 약 1.5k ≈ 2k → 2.4M × $10 = $24. 출력(effort high, 추론 포함) 요청당 1k~5k **[가정]** → 1.2~6M × $50 = $60~300. **합계 약 $85~325.** 비용을 줄여야 하면 effort가 아니라 **표본을 줄인다**(V-free·C를 300 스냅샷으로, 구간이 넓어짐을 기록) — effort를 낮추지 않는다(잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5, 00 §16).
- 시간: Jev 부분 수 시간. Astra high는 첫 토큰 약 73 s(plan §1, 날마다 변동) → 1,200 요청을 동시 20개로 약 1.5~2 시간. 분석 1일. 합계 1~2일.

### 5.9 위험
- 풀이 pick-and-place 하나뿐이라 B·C의 강점이 드러날 자리가 없다 → E3b 전에는 B·C에 대해 결론 내리지 않는다.
- C의 `attrs`를 메타데이터로 채우면 상한이다. 실제 캡셔너 오류는 E3-P에서.
- V-free는 Astra 결과이지 "VLM 일반" 결과가 아니다(plan §3 RoboDojo 교훈과 같은 한정).
- E1과 같은 풀을 쓰므로 형식 과적합 위험 → E1 적합 에피소드는 E3에서 뺀다(§5.3).

---

## 6. 일정·비용 합계 [가정: 구현 완료 시점 기준]

| 주 | 할 일 |
|---|---|
| 0주(준비) | 환경·술어 계산기·오라클 플래너·섭동 훅·기록기. E0 스크립트 먼저 완성해 자동 세션 시작(환경과 무관) |
| 1주 | E0 4일 자동 실행 / 스냅샷 풀 생성(연속 스냅샷 열 포함) / 롤아웃 라벨러 / **E0.5** / E1(+E-M3-1) / E3a |
| 2주 | 룰·템플릿 작성(DEV), E2a TEST 실행(주 3~4시간 × 필요 시 연장, Astra 켠 쌍 약 2시간), E2a-off, 분석 |
| 이후 | 마차 통과 또는 "결론 유보" 시 E-M4(C5 구현·CAL 포함, 00 §13) → E-M3-2 → E-M5-1 → E-M8a → E-M9 → E-M6·E-M10, E3b는 과제 추가 뒤 |

| 항목 | Jev | Astra |
|---|---|---|
| E0 | ~$14~18 | 0 |
| E0.5 | ~$1~5 | 0 |
| E1 | ~$2~10 | 0 |
| E2a | ~$4~7 | ~$60~440(계약 $5~12 + Astra 켠 쌍 $55~430) |
| E3a | ~$2~3 | ~$85~325 |
| 합계 | **~$23~43** | **~$145~765** |
Jev 비용은 무시할 만하고, 비용과 시간을 좌우하는 것은 **Astra 출력 토큰(effort high의 추론량)**과 **E2a 실시간 실행**이다. Astra 켠 쌍의 상한이 크므로 첫 10요청으로 재추정하고, 줄여야 하면 effort가 아니라 표본을 줄인다.

---

## 7. 가정 목록 (실행 전 확인할 것)
1. 폐루프 실행 기계(클러스터 파드)가 한국에 있고 외부 HTTPS 출구가 직접 연결이다.
2. 크기 등급별 입력 토큰 수(§1.3)는 추정이다. `usage.input_tokens`가 질문마다 state를 다시 세는지 모른다.
3. Choice `criteria` 맵의 키 순서가 모델 입력 순서로 전달된다(위치 편향 측정의 전제).
4. 응답 확률의 소수 자릿수(반올림 여부) — 공식 예시는 소수 2자리(0.88, 0.12, 0.0).
5. 환경 엔진은 Isaac Sim 자작 장면(00 §13: E0~E3 전용, 본 평가 RoboDojo와 분리).
6. 크기 구간 cm 값, 섭동 크기·시각, 과제 시간 제한 60 s(Astra 켠 쌍 180 s)는 우리 초기값이다.
9. 결과 기반 라벨의 후속 정책(오라클 플래너)과 롤아웃 길이 10 s는 우리 초기값이다(§4.11에서 두 번째 후속 정책으로 점검).
7. Astra effort high의 출력 토큰량(요청당 1k~20k)은 모른다. 첫 10요청으로 재추정.
8. 한국–미국 서부 왕복 약 0.13~0.16 s는 일반값이다(E0이 잰다).

## 8. [결정 필요] (이 문서에서 새로 생기거나 다시 확인할 것)
1. E2a 주 조건에서 **실행 중 Astra 개입을 끄는 것**(측정 목적상 [사용자] 원칙에서 벗어남, E2a 주 조건 한정)을 허용할지. 보조로 Astra 켠 대응 쌍(R0+A / J*+A)을 둔다(§4.4).
2. (정리됨, 00 §13) E0~E3 환경 = 단일 팔 자작 장면, 본 평가 = RoboDojo. 둘을 섞지 않는다.
2b. 사용자 원칙 세 개의 수치 충돌(실패마다 Astra + 웬만하면 안 멈춤 + effort 잠정 기본 high([결정 필요] 5); high 첫 토큰 약 73 s·xhigh 약 198 s가 60 s 시간 제한보다 김) — M9 §7에 묶어 올림. E2a의 Astra 켠 쌍(60 s 절단 대 180 s)과 E0 날의 effort별 첫 토큰 측정이 자료다.
3. E0 판정 3 (a)(d)(e)에 해당하면 올라가는 결정(겹침 목적 보고, D-줌 결정 빈도, 방향 논의).
4. E2a 판정 2 발동 시 방향 재검토 선택지 중 무엇. "결론 유보"(판정 2′)일 때 E-M4로 진행하는 것에 동의하는지.
5. E2a 판정 6: D-줌이 크게 뒤질 때 사용자 원안을 유지할지.
6. E3 뒤 M1 후보 선택(plan §5-2, 00 §11.3).

## 9. 확인 못 한 것
- Jev 한국발 지연, 보정 곡선: 아직 아무도 공개 측정 없음(v3/01) — 이 문서의 E0·E1이 그것을 잰다.
- TypeSafe 문서에서 동시 요청 수 제한(동시성 한도) 항목은 찾지 못했다(읽은 문서: models, api, retries, fan-out). 요청/분·토큰/초 한도만 있다 → E0 동시성 스윕이 사실상의 한도를 잰다.
- 질문 수가 지연에 주는 영향의 공식 수치 없음(fan-out 문서는 "보통 작다"는 정성 서술, parallel_questions 쿡북은 13질문 묶음 10.0배 빠름·12.2배 싸다를 보고하나 코드 주석상 jev-1.12 가격 기준).
- 시뮬 엔진·과제의 구체 선택과 그 구현 비용.
