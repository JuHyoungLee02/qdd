# D11 최종 일관성 점검 (dev, 2026-09-24)

읽은 문서는 user-log, CLAUDE.md, plan v5.8, 00-interfaces §1~§23, SUMMARY v3, M1~M10, E-first, EVAL, paper/sec/*.tex입니다. 파일은 하나도 고치지 않았고 커밋도 하지 않았습니다. "파일:줄" 번호는 현재 dev 기준입니다.

## 범주별 건수
| 범주 | 건수 | 높음 / 중간 / 낮음 |
|---|---|---|
| 1. 사용자 의도 위반·오표기 (tex 밖) | 13 | 5 / 4 / 4 |
| 2. 문서 사이 모순·옛 정의 잔존 | 15 | 0 / 8 / 7 |
| 3. 정본 절 참조 오류 | 4 | 0 / 2 / 2 |
| 4. 출처 규칙 (기간 밖 표시, 수치 출처) | 7 | 0 / 3 / 4 |
| 5. Overleaf (`\intent` 원문 대조, 누락, 옛 방향) | 21 | 2 / 10 / 9 |
| **합계** | **60** | 일부 항목은 범주 사이에 교차 표시 |

- FAIL·정지·확정의 소유 모듈은 위반이 없습니다. FAIL은 M7만, 확정·교체는 M4만, 정지는 T0와 M9 L3만 한다는 규칙을 M3~M6·M10이 모두 지킵니다.
- 절 참조는 grep 표본으로 확인했습니다. 3.1~3.4 말고 틀린 절 번호는 찾지 못했습니다.

---

## 1. 사용자 의도 (tex 밖)

**I-1 [높음] M8에 effort 규칙이 [사용자]로 붙어 있음**
- 위치: `docs/design/M8-astra-invocation.md:24`
- 원문: "[사용자] (CLAUDE.md) Astra effort를 낮추는 것을 기본안으로 쓰지 않는다."
- 문제: 25행에 주석은 있지만 표지가 여전히 [사용자]입니다. 정본 §16과 어긋납니다.
- 고칠 것: 표지를 "[다른 작업 발언, CLAUDE.md 경유 — 잠정 기본 high, 이 프로젝트 적용은 [결정 필요] 5]"로 바꿉니다.

**I-2 [높음] 정본 §13에 "사용자 원칙"이 남아 있음**
- 위치: `00-interfaces.md:117` (§13)
- 원문: "Astra effort 기본 high(사용자 원칙)."
- 문제: §16이 덮는다는 규칙은 있지만, 정본 본문에 틀린 표지가 그대로 있습니다.
- 고칠 것: "(→ §16: 잠정 기본 high(다른 작업 발언 근거), [결정 필요] 5)"로 고칩니다.

**I-3 [높음] effort를 "사용자 원칙"으로 묶어 부름**
- 위치: `CLAUDE.md:85`, `E-first-experiments.md:140`, `:364`, `:566`, `EVAL-evaluation-design.md:232`, `:260`, `plan.md:231`(제목)
- 문제:
  - CLAUDE.md:85는 effort 규칙을 잠정 표시 없이 고정 제약으로 적었습니다.
  - E-first:140은 "(사용자 원칙 세 개의 충돌)"로 셋째 원칙인 effort까지 사용자 원칙으로 셉니다.
  - plan:231은 제목이 "사용자 원칙 세 개"입니다.
- 고칠 것:
  - 이름을 "원칙 세 개(사용자 원칙 2 + 잠정 기본 1)"로 바꿉니다. SUMMARY W4 행이 이미 이렇게 되어 있습니다.
  - CLAUDE.md:85 끝에 "이 프로젝트 적용은 [결정 필요] 5, 그 전까지 잠정 기본"을 덧붙입니다.

**I-4 [높음] Claude 제안인 "≤255"와 D-줌이 "사용자 원안"으로 적힘**
- 위치:
  - `M3-action-representation.md:205` "사용자 원안의 '≤255'를 바꾸는 제안이다"
  - `SUMMARY.md:621` (D6b, 같은 문장)
- 문제 1: 출처인 `plan.md:76`은 "**[제안]** 원안: … 차원당 ≤255 보기 1회"입니다. user-log에는 "촘촘하게 가도 된다"만 있습니다.
- 문제 2: D-줌(로그 간격 5구간 + 세밀 줌)도 Claude가 "촘촘하게"를 구현한 안인데, 여러 곳에서 "사용자 원안"이나 "[사용자 방향 그대로]"로 불립니다.
  - `M3:23`, `:103`, `:184`, `:187`
  - `SUMMARY:206`, `:620`, `:645`
  - `plan:206`
  - `E-first:356`, `:410`, `:569`
- 영향: M3:184의 "D-줌 기본(사용자 의도 우선)" 판정 규칙이 이 오표기에 기대고 있습니다.
- 고칠 것:
  - "촘촘한 원안(D-줌 — 사용자 '촘촘하게'를 Claude가 구현한 안)"으로 바꿉니다.
  - D6b 이유는 "plan [제안] '≤255'를 바꾸는 것"으로 고칩니다.
  - D6b는 사용자 [결정 필요]에서 빼고, 메인 세션 기술 결정(SUMMARY §5.2)으로 옮기는 것을 검토합니다.

**I-5 [높음, 출처 불확실] "변환 방법은 사용자가 정한다"에 user-log 근거가 없음**
- 이 문장이 [사용자]로 붙은 곳: `plan.md:63`, `M1-state-representation.md:9`, `SUMMARY.md:152`, `CLAUDE.md:29`
- 문제:
  - M1:10은 출처를 "CLAUDE.md·plan 63행의 사용자 지시"라고 적는데, 두 파일 모두 Claude가 쓴 문서라 근거가 순환합니다.
  - user-log 머리말은 사용자 발언을 "빠짐없이" 옮겼다고 하지만, 이 줄은 없습니다.
  - D5 4-4의 제안도 아직 반영되지 않았습니다(SUMMARY:725).
- 고칠 것: 둘 중 하나로 정합니다.
  - 사용자에게 확인한 뒤 user-log에 추가합니다.
  - 확인 전까지는 "[CLAUDE.md 규칙 — user-log 출처 없음]"으로 표지를 바꿉니다.

**I-6 [중간] 스무딩 문구를 바꿔 인용함**
- user-log:47 원문: "액션 스무딩**이나** VLA에서 쓰는 부드럽게 하는 방법으로 아주 작은 움직임을 처리한다"
- 다르게 적은 곳:
  - `00-interfaces.md:145` (§16): "user-log 원문대로 '액션 스무딩 **등** VLA에서 쓰는 방법'으로 읽는다" — 원문이 아닙니다.
  - `00-interfaces.md:10` (§1): "VLA에서 쓰는 방법으로" — "액션 스무딩이나"가 빠졌습니다.
  - `CLAUDE.md:38`, `plan.md:96`, `M5-smoothing.md:17`: 모두 "등"을 씁니다.
  - `M5:19`: "[사용자] 'VLA 방법으로'"라는 부분 인용을 L2 기본 유지의 근거로 씁니다.
  - `M5:90`: "[사용자] '아주 작은 움직임**만** 부드럽게'" — "만"이 더해졌습니다.
- 영향: "이나"는 둘 중 하나라는 뜻입니다. 액션 스무딩도 사용자 문장에 직접 들어갑니다. 반면 "등"은 액션 스무딩을 VLA 방법의 하위로 만듭니다. D21(L2 빼기)의 근거가 달라집니다.
- 고칠 것: 원문 그대로 인용합니다. `SUMMARY:493`은 이미 원문과 같습니다.

**I-7 [중간] plan M8 [사용자] 줄에서 실패 예외가 빠짐**
- 위치: `plan.md:113`
- 원문: "처음 계획할 때만 멈추고 이후는 웬만하면 멈추지 않는다."
- 문제: "실패로 생각해야 하는 경우만 어쩔 수 없다"가 빠졌습니다. D4 때 고친 것과 같은 종류의 누락입니다.
- 고칠 것: 원문대로 덧붙입니다.

**I-8 [중간] CLAUDE.md가 "정해 둔 순간"을 "사용자가 정한 순간"으로 바꿈**
- 위치: `CLAUDE.md:41`
- 원문: "사용자가 정한 순간마다"
- user-log:56: "정해 둔 필요한 순간마다"
- 영향: 누가 정하는지는 D23(T2 목록을 사용자가 정할지, Astra가 정할지)에서 열려 있는 문제인데, 이 문장이 답을 미리 정해 버립니다.
- 고칠 것: 원문대로 적습니다.

**I-9 [중간] "기존 방법"이 [사용자] 인용으로 남아 있음**
- 위치:
  - `M6-skills-jev.md:212` "[사용자] 문제 설정: '물체를 보고 스킬을 생성해서 잡는 기존 방법'"
  - `plan.md:103` 같은 문구
- 문제: "기존 방법"은 user-log 3에 없습니다. §16이 풀어 쓰기로 정정한 표현입니다. M6:221은 정정되어 있지만 212행의 인용이 남았습니다.
- 고칠 것: user-log 3 원문으로 바꿉니다.

**I-10 [낮음] plan·모듈의 [사용자] 줄이 조금씩 줄거나 바뀜**
- `plan.md:82`, `M4-overlap-commit.md:21`: "가장 중요" — 원문은 "굉장히 중요"라 뜻이 세졌습니다.
- `plan.md:75`, `M3:21`: "할 만하다고 본다"가 빠졌습니다.
- `plan.md:70`: M2 줄이 "M1과 같은 문제로 본다"만 남았습니다.

**I-11 [낮음] "안전 지표는 두지 않는다 [사용자]"**
- 위치: `EVAL-evaluation-design.md:246`, `SUMMARY.md:602`
- 문제: 원문은 "크게 다루지 않는다"입니다. "지표를 두지 않는다"는 우리 해석입니다.
- 고칠 것: "[사용자] 크게 다루지 않음 → 우리 해석: 안전 지표 없음"으로 나눠 적습니다.

**I-12 [낮음] 정본 §1이 사용자 원칙 일부만 담음**
- 위치: `00-interfaces.md:6-11`
- 빠진 것: 실패 시 연속 프레임(한 이미지에 10장), 호출 방식 비교 세 가지, VLA 일반화 부각, 비교 대상 6범주, 경험 축적, "촘촘하게", 안전
- 문제: "바꾸지 않는다"는 목록이 불완전합니다.
- 고칠 것: 추가하거나 "전체 목록은 SUMMARY §1.2"라고 적습니다.

**I-13 [낮음, 불확실] 정지 규칙이 사용자 허용보다 엄격함**
- 위치: 정본 §4 "정지가 허용되는 곳은 두 군데뿐"과 D13
- 내용: 사용자는 "실패로 생각해야 하는 경우"에 정지를 허용했습니다. 설계는 그보다 엄격하게 L3 안에서만 정지하고, 그것도 [결정 필요]로 둡니다.
- 판단: 위반은 아닙니다. 다만 "사용자 원칙"이 아니라 "설계 선택(사용자 허용 범위보다 엄격)"으로 표시하는 편이 정확합니다.

## 2. 문서 사이 모순

**C-1 [중간] "적시 재현율" 이름이 두 가지 뜻으로 쓰임**
- M7 판정 기준 1~8(`M7-progress.md:149-157`)이 "적시 재현율"을 씁니다. 그런데 같은 절 `:146`에서는 "적시 재현율" = (b) CheckVLA 원문 정의이고, (a)는 "고정 창 재현율"입니다. §23은 기준 1~8을 (a)로 읽으라고 합니다. 이름과 뜻이 반대로 붙어 있습니다.
- 같은 문제가 있는 곳:
  - `M8-astra-invocation.md:150` — "적시 재현율(… τ=2초 안 호출)", 즉 고정 창판을 그 이름으로 정의
  - `M8:154`, `EVAL:243`, `plan:121`, `plan:164`
  - `SUMMARY:346`, `:374`, `:575`
- 고칠 것: 판정문과 지표 목록의 이름을 "고정 창 재현율(τ=2 s)"로 바꿉니다.

**C-2 [중간] Astra effort max 첫 토큰 값이 다름**
- "352 s": `plan:36`, `00:202`, `M7:146`, `SUMMARY:575`
- "max 미측정": `M8:99`, `M8:148`, `SUMMARY:357`
- 고칠 것: "max 352 s(Artificial Analysis 2026-09-23 읽음)"로 통일합니다.

**C-3 [중간] Artificial Analysis 읽은 값을 "측정값·실측"이라 적음**
- 위치:
  - `plan.md:231` "(2026-09-23 측정값)"
  - `M7:146` "(… max 352 s, 2026-09-23 실측)"
- 문제: plan:36이 밝히듯 둘 다 Artificial Analysis에서 읽은 값입니다. 우리가 잰 값이 아닙니다.
- 고칠 것: "AA 기준 읽음"으로 바꿉니다.

**C-4 [중간] 정본 안에서 C_pred 지속 시간이 다름**
- `00:84` (§11.2): "T2 술어 위반 또는 unknown이 1초 넘게 지속"
- `00:52` (§7): "T2 위반 0.5 s / unknown 1.0 s"
- M7:108과 SUMMARY는 §7 값을 따릅니다.
- 문제: "뒤 절이 앞 절을 덮는다" 규칙대로 읽으면 옛 값인 §11.2가 이깁니다.
- 고칠 것: §11.2에 "(지속 시간은 §7: 0.5 s / 1.0 s, D5 반영)"을 덧붙입니다.

**C-5 [중간] plan에 옛 C2' 정의와 옛 핵심 비교가 남음**
- 위치:
  - `plan:92` "C2' 겹침 + 감쇠 융합 / … 핵심 비교 C5 대 C2"
  - `plan:167` "핵심 비교 C5 대 C2"
  - `plan:177`, `plan:230`: E0.5 "최신 답 대 LA-2 대 감쇠 융합"
- 정본 기준:
  - §20: C2' = Probability Fusion 원문판, 옛 판은 C2''
  - §20 추가문: E0.5 판정 1·2는 C2''와 비교
  - M4 판정 1: C5를 C2·C2' **둘 다**와 비교
  - §18: C5 대 C5-A3 추가
- 고칠 것: 조건 목록에 C2'' / C2'-S / C5-A3 / C-FIX를 넣고, 핵심 비교를 "C5 대 C2·C2' + C5 대 C5-A3"로 바꿉니다.

**C-6 [중간] plan §4의 M4 새로움 문장이 옛 판**
- 위치: `plan:167`
- 원문: "확정 규칙은 선행 못 찾음(v3/18…)"
- 문제: §18·§19에 따르면 A3가 "합의 → 앞 구간 확정"을 먼저 했습니다. 새로움은 결합이고, (b)가 합의 원장의 전제 무효화·확정에 쓰인다는 점입니다.
- 고칠 것: 이 문장을 §19의 차별화 문장으로 바꿉니다.

**C-7 [중간] "처음 잰다" 표현이 남음**
- 위치: `plan:168` "…를 처음 잰다"
- 문제: §18이 금지한 표현입니다.
- 고칠 것: "같은 인식·같은 실행기 위에서 결정 층만 바꿔 짝지은 낙폭 비교는 없다(2026-09-22 색인 기준)"로 바꿉니다. tex 쪽 같은 문제는 O-12에 있습니다.

**C-8 [중간] 실험 순서 목록이 §17 반영 전 판**
- 위치: `plan:175`, `plan:215`, `M3:178`, `M4:286`, `M9:127`, `M7:142`
- 빠진 것: E-M4-gen, E-M4-lat, "E0.5는 E0와 같은 날 가장 먼저"
- 고칠 것: SUMMARY §3.8 순서로 맞춥니다.

**C-9 [낮음] 정본 §7의 T_c 비고**
- 위치: `00:39`
- 원문: "사용자 예시 '1초 3번'"
- 문제: user-log는 "3번 정도"입니다. D5 4-6이 아직 반영되지 않았습니다(SUMMARY:694, :726).

**C-10 [낮음] SUMMARY 머리 정보가 낡음**
- `SUMMARY:5`: "plan.md v5.7" — 지금은 v5.8입니다.
- `SUMMARY:727`: "D9·D10 반영 절은 plan에 없다" — plan §11이 이제 있습니다.
- `SUMMARY:729`: Overleaf 반영 여부 미확인.

**C-11 [낮음] E-first 안에서 비용 추정이 다름**
- E1: `E-first:26` "$3~10" / `:542` "$2~10"
- E3: `:28` "Jev $2~5 / Astra $85~300" / `:544`, `:518` "$2~3 / $85~325"

**C-12 [낮음] 이름 D1·D11이 여러 뜻으로 겹침**
- "D11"이 셋입니다:
  - E-M7 조건 (`M7:145`, `00:185`)
  - SUMMARY [결정 필요] D11 (H = 1 대 3)
  - 이번 보고서 파일 이름 D11
- "D1-CiL"도 [결정 필요] D1(주장 문구), `D1-verification.md`와 겹칩니다.
- 고칠 것: E-M7 조건 앞에 다른 접두어를 붙이는 방안(예: "CD1-CiL", "CD11")을 권합니다.

**C-13 [낮음] plan에 RPent 92.63% 인용 조건이 빠짐**
- 위치: `plan:37`
- 원문: "Harness VLA Astra low LIBERO-PRO 92.63%"
- 문제: §22 인용 규칙(저장소 리더보드 값, T+S 8칸, 메모리 두 묶음, 에피소드당 412 s 정지형)을 적지 않았습니다. 같은 문서 plan:259는 조건을 적었습니다.

**C-14 [낮음] EVAL에 "high(기본)" 표기**
- 위치: `EVAL:232`
- 고칠 것: "잠정 기본"으로 바꿉니다. EVAL:11과 E-first:11의 개정 줄 "Astra effort 기본 high"는 기록 줄이라 선택 사항입니다.

**C-15 [낮음, 불확실] 빈 집기 복구의 층 배정이 서로 다름**
- §22와 `M9:66`: Harness "빈 집기 → 재위치·re-stage"를 L1 재시도 사례로 인용합니다.
- §20과 `M9:69`: Show-Harness 빈 집기 되돌림을 L2 `resume_ckpt_<k>`의 근거로 인용합니다.
- 고칠 것: 같은 실패 유형이므로 "파라미터만 바꿔 재시도하면 L1, 잡기 하위 단계로 되돌리면 L2"라는 구분 문장을 넣습니다.

## 3. 정본 절 참조 오류

**R-1 [중간] M9가 이미 정정된 §20 표기를 인용함**
- 위치: `M9-recovery.md:5`, `M9-recovery.md:76`
- 원문: "00 §20은 이것을 'M9 L1 `resume_ckpt`'로 적었으나…"
- 문제: 정본 §20(00:179)은 00:40에 "L2 보기"로 이미 정정되었습니다.
- 고칠 것: 괄호 설명을 지웁니다.

**R-2 [중간] 정본 §16의 user-log 인용이 틀림**
- 위치: `00-interfaces.md:145`
- 문제: "user-log 원문대로 '액션 스무딩 등…'"이라고 했지만 원문 문구가 아닙니다(I-6).

**R-3 [낮음] M5 개정 줄의 user-log 인용이 틀림**
- 위치: `M5-smoothing.md:4`
- 원문: "user-log 원문('액션 스무딩 등 VLA 방법')으로 고침"
- 문제: 인용 문구가 user-log와 다릅니다.

**R-4 [낮음] 기간 예외의 출처를 README로 적음**
- 위치: `M9-recovery.md:164`
- 원문: "README '큰 주제의 기초 문헌은 예외'"
- 고칠 것: 출처를 "user-log 20 / CLAUDE.md:49"로 바꿉니다.

## 4. 출처 규칙

**S-1 [중간] 정본 §12의 기간 밖 문헌에 표시가 없음**
- 위치: `00-interfaces.md:108` (§12)
- 표시 없는 것: Shi 외(ICML 2023), 적응형 검색(ACL 2023), Lost in the Middle(TACL, arXiv 2023), Xiong 외(ICLR 2024, arXiv 2023), BT 백체이닝(ICRA 2019)
- 참고: SUMMARY §6.2는 이들을 기간 밖으로 분류합니다.
- 고칠 것: "기간 밖, 기초 문헌"을 붙입니다.

**S-2 [중간] tex의 기간 밖 문헌에 표시가 없음**
- `3_method.tex:71`: GVL~\cite{gvl2024} (ICLR 2025, arXiv 2024)
- `3_method.tex:21`: Set-of-Mark (arXiv 2023)

**S-3 [중간] 기준 방법 π0·OpenVLA·RDT에 표시가 없음**
- 위치: `plan.md:163`, `EVAL:106`, `EVAL:118`
- 내용: π0(2410.24164), OpenVLA(2406.09246), RDT(2410.07864)는 모두 기간 밖인데 표시가 없습니다. 같은 줄의 VoxPoser만 표시되어 있습니다. 셋 다 plan §5-4의 예외 요청 목록에는 있습니다.

**S-4 [낮음] 정본 §10의 RALCP에 표시가 없음**
- 위치: `00:71`
- 내용: RALCP(2309.06706, ALTA 2024). M4와 SUMMARY에는 표시가 있습니다.

**S-5 [낮음] REACH의 첫 공개일이 아직 확인되지 않음**
- 위치: `00:66` (§9 C4), `SUMMARY:404`
- 내용: 공개일을 확인하지 못해 기간 안인지 판정할 수 없습니다.

**S-6 [낮음, 불확실] 출처 문서가 없는 수치·인용**
- `CLAUDE.md:82` "high 45~73초": 하한 45의 출처 문서가 없습니다. plan:36에는 73만 있습니다.
- `plan:38` 인용의 둘째 문장 "옳은 추론을 하면 더 빨리 성공한다": 근거가 Claude 메모리뿐입니다.

**S-7 [낮음] M9 안에서 BT 문헌의 쓰임이 엇갈림**
- 위치: `M9:52`, `:71`, `:80`
- 내용: 행동 트리 문헌을 52행은 "사용 여부 [결정 필요]", 71행은 "근거로 인용하지 않는다", 80행은 "근거가 올라갔다"로 적었습니다.
- 고칠 것: "잠정 기본 근거(D4 승인 전)" 하나로 통일합니다.

## 5. Overleaf (paper/sec)

### 5.1 `\intent` 줄이 user-log 원문과 다름
CLAUDE.md:22 규칙은 빨간 줄을 요약하거나 줄이지 말라고 합니다. 아래는 모두 그 규칙 위반입니다.

**O-1 [높음] M1**
- 위치: `3_method.tex:12`
- 지금: "AI 전 분야에서 가장 효율적인 변환을 가져옴"
- 원문: "LLM/VLM/VLA 논문뿐 아니라 텍스트와 이미지를 제대로 변환하는 다른 분야 연구에서 가장 효율적인 방법을 찾는다"

**O-2 [높음] M8 호출 방식**
- 위치: `3_method.tex:81`
- 지금: "비교: 일정 주기 / 정한 순간 / '이때쯤 나왔어야 하는데'"
- 원문: "호출 방식은 비교 평가한다: Hz 단위 주기 / 정해 둔 필요한 순간마다 / '이때쯤 결과가 나왔어야 하는데 안 나오네'라는 자체 판단"

**O-3 [중간] M8 정지 예외**
- 위치: `3_method.tex:82`
- 지금: "실패해서 다시 생각해야 할 때만 예외"
- 원문: "실패로 생각해야 하는 경우만 어쩔 수 없다"

**O-4 [중간] M5**
- 위치: `3_method.tex:51`
- 지금: "액션 스무딩 등 VLA에서 쓰는 방법"
- 원문: "먼저 RTC와 유사한 방법, 다음으로 액션 스무딩이나 VLA에서 쓰는 부드럽게 하는 방법으로 아주 작은 움직임을 처리한다"

**O-5 [중간] M6**
- 위치: `3_method.tex:57`
- 문제: 문장 순서를 바꾸면서 "스타 상위 5개 중에서 … 잘게 엮어 놓은 논문을 참고해"라는 한정 관계가 사라졌습니다.

**O-6 [낮음] M3**
- 위치: `3_method.tex:27`
- 문제: "할 만하다고"가 빠졌습니다.

**O-7 [낮음] M10**
- 위치: `3_method.tex:105`
- 문제: "어떻게 하는지"가 빠졌습니다.

**O-8 [중간] 계층 구조 줄에 Claude 요약어가 섞임**
- 위치: `1_intro.tex:6`
- 지금: "상위 Astra(느림, 계획) / 하위 Jev(빠른 객관식 판단)"
- 문제: 괄호 안이 Claude 요약입니다. 빨간 줄에 검정 내용을 섞지 말라는 규칙 위반입니다.
- 원문: "예전 VLA에서 상위에 큰 LLM, 하위에 작은 VLM을 둔 것처럼, 상위에 Astra, 하위에 Jev를 둔다"

**O-9 [낮음] 기타 두 줄**
- `4_experiments.tex:5`: "LLM+스킬을 **연결한** 기존 논문", "Astra를 쓰는"에서 조사·어미가 빠졌습니다.
- `3_method.tex:38`: 빨간 줄 안에 `\cite`가 들어 있습니다.

### 5.2 누락
**O-10 [낮음] 빠진 사용자 발언**
- 모듈 M1~M10은 모두 있습니다.
- 빠진 `\intent`:
  - user-log 3 "컨트리뷰션은 아직 없고 점차 잡아간다"
  - user-log 12 "마차를 이어서는 기차를 만들 수 없다" (1_intro:14는 검정 줄로만 씁니다)

### 5.3 정본 §17~§23과 어긋나는 옛 방향
**O-11 [중간] 1_intro.tex:12**
- 원문: "우리 자리는 겹친 답을 확정하는 규칙(M4)"
- 문제: §18에 따르면 A3가 확정 규칙을 먼저 했습니다. §19의 결합 문장으로 바꿉니다.

**O-12 [중간] 1_intro.tex:19–20**
- 19행: M4 기여에 A3 선행 언급이 없습니다.
- 20행: "(처음 잼)"은 §18 금지 표현입니다.

**O-13 [중간] 1_intro.tex:13**
- 원문: "대규모 로봇 데이터·학습 없이 덜 무너짐"
- 문제: §17 문구 규칙("과제 데이터로 미세조정한 학습 정책(VLA 포함)", "같은 인식 앞단 위에서") 반영 전 판입니다.

**O-14 [중간] 3_method.tex:44**
- 문제: E-M4 조건 목록이 옛 판입니다. C2''·C2' 원문판·C5-A3·C-FIX·E-M4-lat가 없고, "핵심은 (a)+(b) 대 겹침+최신 답"도 §18·§20과 다릅니다.

**O-15 [낮음] Slow Brain 설명이 절마다 다름**
- `3_method.tex:43`: "최신 답만 씀"
- `2_related.tex:10`: "최신 답 감쇠 융합"
- 고칠 것: §20 기준으로 "VLM Stream = 최신 답만, Fusion = 최신 답 + 빠른 층 점수 감쇠 융합"으로 통일합니다.

**O-16 [낮음] 3_method.tex:29**
- 원문: "보기 255개 한 번이면 충분할 수 있음"
- 문제: 지금 M3 설계(질문당 뚜렷한 보기 ≤ 약 17개, D255는 비교 조건)와 같은 파일 117행의 방향과 반대입니다.

**O-17 [낮음] 3_method.tex:86**
- 원문: "Astra low로 강한 결과도 있음(Harness VLA)"
- 문제: §22 인용 조건(저장소 리더보드 값)이 빠졌습니다. 같은 파일 109행에는 있습니다.

**O-18 [낮음] 3_method.tex:74**
- 문제: 판정 기준은 고정 창판(τ=2 s)으로 읽는다는 §23 결정이 적혀 있지 않습니다.

**O-19 [낮음] 4_experiments.tex:8**
- 원문: "VLA가 장면 바뀌면 무너지는 건…"
- 문제: 검정 주장 줄인데 "VLA"를 그대로 씁니다. §17에 따라 "과제 데이터로 미세조정한 학습 정책(VLA 포함)"으로 바꿉니다. 사용자 빨간 줄은 그대로 둡니다.

**O-20 [낮음] 판 표기가 낡음**
- `0_abstract.tex:2`: "마지막 갱신 23:45"
- `main.tex:15`: 제목의 "(v3)"
- 문제: plan v5.8·정본 §23과 맞지 않습니다.

**O-21** S-2와 같은 항목입니다(3_method:21, :71의 기간 밖 표시). 여기서는 건수만 셉니다.

---

## 메인 세션 우선 적용 순서 (제안)
1. **의도 표지부터 고칩니다**: I-1~I-5와 O-1·O-2.
   - 특히 I-4("사용자 원안" 오표기)는 M3 판정 규칙에 영향을 줍니다.
   - I-5(M1 줄의 출처)는 사용자 확인이 필요할 수 있습니다.
2. **정본 안의 자기 모순을 고칩니다**: I-2(§13), R-2(§16 인용), C-4(§11.2 대 §7), C-9(§7 T_c 비고), S-1·S-4.
3. **"적시 재현율" 이름을 통일합니다**: C-1. M7·M8·EVAL·plan·SUMMARY를 한 번에 바꿉니다.
4. **plan §2·§4·§5를 §17~§20에 맞춥니다**: C-5~C-8.
5. **Overleaf 옛 방향을 고칩니다**: O-11~O-19.
