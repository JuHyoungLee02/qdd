# 모듈 사이 통합 인터페이스 (정본)

작성: 2026-09-23 22:00 UTC, 메인 세션. 근거: `D1-verification.md` Part B(모순 12건)·Part C(사용자 의도 5건).
**개별 모듈 문서(M1~M10)와 이 문서가 다르면 이 문서가 우선한다.** 개별 문서는 다음 라운드에서 이 문서에 맞춰 고친다.
> 개정 2026-09-24 (정본 §25, D13 반영): §7 L2 블렌딩 줄(τ_b 삭제, T_f·schedule = RTC 식 5)과 §11.4 AgentSpec 문장을 그 자리에서 고치고 "(§25 D13 정정)"으로 표시했다.
> 개정 2026-09-24 (D11 일관성 점검 반영, `D11-final-consistency.md` 범주 1~4): 앞 절의 틀린 곳을 그 자리에서 고치고 "(D11 정정)"으로 표시했다 — §1 사용자 원칙을 user-log 원문으로 되돌리고 전체 목록 위치 명시, §4 정지 규칙은 설계 선택임을 표시, §7 T_c 비고 "3번 정도", §9 C4 REACH 공개일 [미확인], §10 RALCP 기간 밖, §11.2 `C_pred` 지속 시간을 §7 값으로, §11.3 user-log 3 원문 인용·M1 줄 출처, §12 기간 밖 표시, §13 effort "사용자 원칙" → 잠정 기본, §16 스무딩 인용을 원문으로, §21·§23 E-M7 조건 이름 CD1-CiL·CD11, 재현율 이름, Astra 352 s 출처, §22 빈 집기 L1/L2 구분.

## 1. 사용자 원칙 (바꾸지 않는다) [사용자]
- 실패할 때마다 Astra가 개입해서 진행하는 방식을 원한다(user-log 3). 적어도 실패 판정이 났을 때는 Astra를 부른다(user-log 11 (6)).
- Astra가 처음 생각할 때는 멈추는 게 맞지만, 그 이후는 웬만하면 멈추지 않는다. 실패로 생각해야 하는 경우만 어쩔 수 없다(user-log 11 정지).
- 1초에 Jev를 한 번이 아니라 3번 정도 계단식으로 겹쳐 쓴다. 겹치는 부분의 움직임이 제대로 반영됐는지 스스로 감지하고 업데이트하는 시스템이 자동으로 있어야 한다. 이게 굉장히 중요하다(user-log 11 RTC 적용).
- 두 단계: 먼저 RTC와 유사한 방법, 다음으로 액션 스무딩이나 VLA에서 쓰는 부드럽게 하는 방법으로 아주 작은 움직임을 처리한다(user-log 11).
- 자료는 무조건 1년 반 이내. 단, 아주 큰 주제를 포괄하는 기초적인 것은 1년 반이 아니어도 된다(user-log 20). 예외로 쓸 때는 "기간 밖, 기초 문헌"으로 표시(CLAUDE.md 조사 규칙).
- (D11 정정) 위 다섯 줄은 user-log 원문으로 되돌렸다(첫 판은 줄이거나 바꿔 적었다). 이 절은 정본 규칙에 직접 걸리는 원칙만 담는다. 사용자 원칙 전체(실패 시 연속 프레임, 호출 방식 비교 세 가지, VLA 일반화 부각, 비교 대상 6범주, 경험 축적, "촘촘하게", 안전 등)는 `SUMMARY.md` §1.2에 있다.

## 2. 한 번의 Jev 호출 (`JevCall`) — B1, B8
- 한 호출에 싣는 것: 결정 질문 H개(미래 결정 스텝 k..k+H-1) + 감시 질문(M7 진행 범주 1개, 필요 시 M8 T3b 1개).
- H는 **1과 3을 비교**한다(E-M4). M3 D-줌의 "한 요청 = 한 스텝"은 H=1 조건이다. H=1일 때 M4 (a)의 여러 표는 "같은 스텝을 시각을 당겨 여러 번 묻기"로 얻는다.
- 입력 길이 상한을 설정 표에 둔다. E0 지연 측정은 실제 `JevCall` 크기(결정 H개 + 감시 질문)로 잰다.

## 3. 예상 상태는 한 곳에서 — B2, B12
- M5가 계획 궤적 `ref(t)`(Ruckig 출력)를 공개한다. **M4 (b)의 예상 상태는 `ref(t)`다.** 추종 지연으로 인한 가짜 LAG를 막는다.
- M3가 보기마다 붙이는 "예상 결과 술어"는 `expected_after`로 M4와 **M7 모두**에 전달한다.
- M4 (b)는 (1) `ref(t)` 대비 연속 잔차와 (2) `expected_after` 술어 일치 여부를 따로 계산한다.

## 4. 실패 판정과 정지는 한 곳에서 — B3, B4, B10, C2
- **M4는 신호만 낸다.** CONTRADICT → M7 하드 채널. DEVIATE → M7 소프트 채널 `C_m4`. 호출 사이 불일치 급증(`flip_score`, TIDE 이산판) → 새 소프트 채널 `C_flip`.
- **FAIL은 M7만 낸다.** AND 결합(소프트 채널 2개 이상이면 FAIL, 1개면 WARN)은 M7에만 둔다.
- 정지가 허용되는 곳은 두 군데뿐: T0(첫 계획)과 M9의 안전 대기(L3). M4의 hold, M5의 NONE_ESCALATE 감속 정지는 **정지가 아니라 "직전 확정 행동 유지 + 감속"**으로 바꾼다. 완전 정지가 필요하면 M7 FAIL을 거친다.
- (D11 정정) "정지는 두 군데뿐"은 사용자 원칙이 아니라 **설계 선택**이다. 사용자 허용 범위("실패로 생각해야 하는 경우만 어쩔 수 없다")보다 엄격하게, 실패 뒤 정지를 L3 안으로 좁혔다.
- [결정 필요] 정밀 접촉 구간의 짧은 정지(동기 실행, WAM 72.5 증거)와 M9 안전 대기(L3)를 사용자 예외로 인정할지.

## 5. 확정·교체 규칙은 M4 한 곳에서 — B5, B6
- 고정 구간 길이 = 실측 Jev p95 지연(`d_p95`). M5의 AAC식 확정 길이 게이트와 M3의 `commit_window` 필드는 이 값을 읽기만 한다.
- 보기 교체(도전 선택 수용) 규칙은 M4의 유예 창(W)과 합의 규칙만 쓴다. M5 L1의 "연속 2표"는 M4 규칙의 W=2 설정으로 흡수한다.

## 6. 확률 게이트 — B7
- **E1(보정 측정) 전에는 모든 모듈에서 확률 게이트를 끄고 최빈 선택만 쓴다.** M5 0.8, M7 0.8, M8 0.7, M9 0.6 값은 E1 뒤에 켤 후보 값으로 설정 표에만 둔다.

## 7. 설정 표 (시간 값 통일) — B9
| 이름 | 값(초기) | 쓰는 곳 | 비고 |
|---|---|---|---|
| T_c 호출 간격 | 0.33 s | M4 | 사용자 예시 "1초에 … 3번 정도"(고정 요구 아님, E0·E-M4로 조정 가능) (D11 정정, D5 4-6) |
| d_p95 고정 구간 | E0 측정값 | M4, M5, M3 | |
| grasp/release 전환 보류 | 1.0 s | M7, M9 | M9의 0.5 s는 폐기 |
| near/contact 구간 정의 | 목표까지 ≤ 5 cm / 접촉 술어 참 | M3, M5, M7 | M7에 추가 |
| 정체 N / M | 10 / 20 틱 | M7 | |
| 복구 뒤 cooldown | 2 s | M7, M8 | |
| 확률 게이트 | E1 전 끔 | 전체 | §6 |
| H (결정 스텝 수/호출) | 1, 3 비교 | M3, M4 | §2 |
| `JevCall` 입력 길이 상한 | E0 뒤 확정 | M3, M4 | §2. E0는 실제 `JevCall` 크기로 잰다 (D5 반영) |
| `h_lift` (`lifted` 문턱) | 3 cm | M1, M2, M6 | M1 등록부 (D5 반영) |
| `tilt_max` (`tilt_ok`·`upright` 문턱) | 30° | M1, M2 | M1 등록부 (D5 반영) |
| `contact_stable` 창 | 미정(개발 시드로) | M1, M6 | M6 `reentry` (D5 반영) |
| `C_assume` 지속 | 0.5 s 연속 거짓 | M2, M7 | `contract_patch_rejected`는 사건 즉시 발동(§15) (D5 반영) |
| `C_pred` 지속 | T2 위반 0.5 s / `unknown` 1.0 s | M7 | §11.2 (D5 반영) |
| 마감 `D_k` / H6 | conformal(α_ep 0.1), 표본 < 20이면 1.5×`T_exp_s` / H6 = 2×`D_k` | M7, M8 | §11.2 (D5 반영) |
| STALE_MAX 표 폐기 나이 | 1.5 s | M4 | jev-drone (D5 반영) |
| L2 블렌딩 T_b / T_f / schedule | 0.3 s / 0 s([가정]) / RTC 식 5(exp) | M5 | RTC 식 5 시간판, 구간 끝 정확히 0. τ_b 삭제, E-M5-2 스윕 schedule {exp, linear} × T_b {0.2, 0.3, 0.5} s (D5 반영) (§25 D13 정정: schedule = RTC 식 5, T_f = 0) |
| 관성화 감쇠 T_i | 0.3 s | M5 | 잠정 기본(§16), E-M5-2 (D5 반영) |

## 8. 실험 순서 — B11
E0 지연(실제 JevCall 크기) → E1 보정 → E2 마차 시험 → E-M4(C0~C6, H=1·3) → E-M3-2(행동 표현) → E-M5-1(스무딩). 오프라인 E-M7(기록 궤적 재생)·E-M8b(기록 실패 사건 묶음)는 병행.
- 폐루프 실패 흐름: E-M8a(호출 방식 × effort, 호출 수 맞춤) → E-M9(복구) 순서로 E-M5-1 뒤에 둔다(M3·M4·M5가 고정된 뒤에야 실패 흐름을 공정하게 잴 수 있다). E-M6·E-M10은 E-M9 뒤. E1·E3(M1 형식)는 오프라인이라 언제든 병행.

## 9. 사용자 의도 복원 — C1~C5
- C1: M4의 "겹침이 필요 없으면 끈다"는 판정은 사용자 필수 요구("계단식 겹침 호출")를 뒤집을 수 있다 → **겹침은 끄지 않는다.** E0에서 지연이 결정 간격보다 짧게 나오면, 겹침의 목적을 "지연 숨기기"에서 "같은 스텝에 여러 표 받기(자기 확인)"로 옮겨 유지한다. 끄는 안은 [결정 필요].
- C2: §4.
- C3: M5는 **L2(같은 보기끼리 RTC식 감쇠 블렌딩, VLA 방법)를 기본으로 유지**한다. "L1 + Ruckig만"은 비교 조건이고, 기본값 변경은 [결정 필요].
- C4: M9의 행동 트리 근거는 "기간 밖, 기초 문헌"으로 표시. FLARE 공개일 2026-08(2608.26645) 기입. REACH 공개일 [미확인] — 기간 안인지 아직 판정하지 못했다(D11 정정).
- C5: M8 effort 실험 축은 low / medium / high / xhigh / max 전부를 후보로 두고, 비용 때문에 뺄 때는 이유를 적는다.

## 10. 검증 정정 반영 메모 (D1-verification Part A)
- T2SGrid 격자 대응 확인: 1×1 53.5/32.9, 3×3 63.7/41.1, 4×3 64.5/41.2, 4×4 61.6/35.9(Table 6, 학습 설정일 가능성 높음, 5×2는 원문에 없음).
- RALCP = ALTA 2024(2309.06706, **기간 밖, 기초 문헌**, D11 정정). FIPER NeurIPS 2025 확인(메인 세션은 arXiv 주석으로 확인 못 했으나 검증 에이전트가 학회 목록으로 확인).
- 2604.14634 "Full"은 "비슷한 보기"가 아니라 난도 1~4 전체에서 뽑은 방해 보기. 2605.24660 93.1 대 87.1은 K≈2.2 목록.
- OpenAI 이미지 토큰: low 1장 231. 640×480에서 격자 5×2 original ≈ 개별 10장 high ≈ 3,600 토큰.

## 11. D2 반영 (2026-09-23 22:15 UTC, `D2-verification.md` Part B·C)
### 11.1 공통 어휘와 이름
- **결정 지점 id는 스킬에 고정된 id(M6, 예: `dp.approach_dir`)가 정본이다.** Astra(M2 세션 계약)는 이 id를 **골라 쓰고 인자만 채운다**. 새 id를 만들지 않는다. M10 Jev 규칙 키도 이 id로 맞춘다. (B1)
- 단계 필드 이름은 M6 이름으로 통일: `phases`, `entry`, `exit`, `invariants`. M2 계약의 `pre/done/inv/substeps`는 각각 `entry/exit/invariants/phases`로 바꾼다. (B2)
- **술어 사전은 하나(M1 술어 등록부)다.** M2·M6·M7·M10의 모든 술어는 등록부에 있어야 하고, 예시에 쓰인 `lifted`, `gripper_open`, `touch`, `top_clear` 등은 등록부에 추가한다. (B3)
- M1 후보 이름은 "후보 A / B / C"로 통일한다(M6·M7·M10의 "후보 1"은 "후보 A"). (B13)
### 11.2 채널과 판정
- M7 하드 채널(즉시 FAIL)은 **T1 술어(코드 기하, 결정적)**만 쓴다. T2(임계값 민감)·`unknown` 술어는 소프트 채널로만 들어간다. (B4)
- M2 "가정 술어 거짓"은 새 소프트 채널 `C_assume`으로 M7에 들어가고, 동시에 M8에 재호출 요청 신호를 보낸다. (B5)
- 소프트 채널 `C_pred`: T2 술어 위반이 0.5초 연속 또는 `unknown`이 1.0초 넘게 지속(D11 정정: 지속 시간은 §7 설정 표 값, D5 반영. 첫 판은 "1초 넘게" 하나였다). 하드 채널 H5: M2 `forbidden` 중 `enforce: fail`(T1 술어만 허용). (M7 개정에서 제안, 메인 세션 채택 22:25 UTC)
- M7 채널 전체: 하드 H1·H2(스킬 오류 코드)·H3·H4·H5·H6(단계 경과 > 2×`D_k`) — 술어를 쓰는 H1·H3·H4·H5는 T1만, H2·H6은 결정적 코드 신호 / 소프트 정체·마감·C_jev·C_m4·C_flip·C_assume·C_pred(두 개 이상이면 FAIL, 1개면 WARN, 예외 없음). (D5 1-4 반영: 첫 판 목록에서 H2가 빠져 있었다.)
- (D5 1-3 해소) M7 첫 판의 "`C_dead`가 2×`D_k`를 넘으면 소프트 채널 단독 FAIL" 예외는 **하드 채널 H6으로 옮긴다.** 이유: 소프트 층에 예외를 두면 §4의 AND 규칙이 깨진다. 경과 시간은 시계로 재는 결정적 양이라 하드 층의 조건(결정적, T1 등급과 같은 성질)에 맞고, `D_k` 보정의 임계 민감도는 2배 여유로 흡수한다. 규칙을 지우면 한 채널만 계속 뜰 때 FAIL이 나지 않아 "실패할 때마다 Astra"가 막힌다.
- **세션 계약 버전이 바뀌면 M4 `premise_epoch`를 올린다.** (B6)
- M6 `default_on_timeout`은 "직전 확정 행동 유지 + 감속"이다. 정지는 §4대로 M7 FAIL을 거친다. (B8, C6)
- M6 문구의 확률 게이트는 §6을 따른다: E1 전에는 끄고 최빈 선택만. (B9)
- M6 `dp.critic_accept`는 **M9 복구 제안 중 하나를 고르는 질문**으로만 쓴다. M7의 FAIL 판정을 뒤집을 수 없다. (B10)
- `dp.grasp_result` 보기 집합은 M7 S5 보기와 같게 한다(한 목록을 공유). (B11)
- M10 L1 라벨의 LAG 판정은 `ref(t)` 대비로 계산한다. (B14)
### 11.3 사용자 의도 (C1, C2, C5)
- **M6 "기존 스킬"**: 사용자 문장(user-log 3 원문)은 "물체를 보고 스킬을 생성해서 잡는 방법들이 많지만 실패할 때가 있다"이다(D11 정정, §16: 첫 판의 "기존 방법들을 기본으로 사용"은 초안의 풀어 쓰기). 설계의 1순위를 (a') 보유 라이브러리로 두지 않는다. **(b) 생성형(CaP-X식)과 (a') 보유 라이브러리를 동등한 조건**으로 두고, 구현 순서를 정해야 하면 사용자 문장에 가까운 (b)를 먼저 한다. 최종은 [결정 필요].
- **M1 변환 방법**: "임시 기본 A"를 쓰지 않는다. 다른 모듈은 특정 후보가 아니라 **"술어 등록부 인터페이스"**에만 의존한다(A·B·C 모두 등록부 술어를 낸다). 변환 방법 선택은 사용자 몫이고, E3는 비교 자료만 만든다. (D11 정정) 이 규칙의 출처: [사용자 — CLAUDE.md '연구 주제에서 확인된 제약'(첫 세션부터), user-log에는 원문 없음].
- **신뢰도**: BATON(심사 전, 인용 1)·PlanAhead(인용 0)는 설계 필드나 1순위를 **좌우하는 근거로 쓰지 않는다**. 보조 참고로 내리고, 필드는 HIGH 근거(Agent Skills 명세, MCP 스키마, ICSE AgentSpec, ICML ACON 등)나 우리 실험으로 정한다.
### 11.4 D2 원문 정정 (개별 문서에 반영할 것)
- ViPlan = EMNLP 2026 Findings. FocusAgent 청크 = 200 토큰, 값 42.4~45.8, "거의 유지"는 5-mini 검색기일 때만(qwen3-8b 43.9).
- PlanAhead "32.5~84.0"은 Table 5(STC). Table 4(AR)는 1.20~4.79.
- GPSFSM이 BTGenBot보다 나은 것은 GPT 모델에서만(90 대 54). 로컬 모델에서는 BTGenBot이 낫다.
- PragmaBot 22%는 다른 방법(COME)의 값. 장기 메모리 100개 중 96개는 교시 경험.
- Read More "+14~17%p"는 두 모델만, o3-mini는 −7.6. AgentSpec에서 LLM(o1)이 만든 규칙은 처음 보는 위험 사례 집행률(원문 "precision") 95.56%, 안전 짝 사례로 낸 "recall" 70.96%(원문 정의 모호, 놓침과 과차단을 함께 설명. "위험의 약 3할을 놓친다"로 읽지 않는다)(§25 D13 정정; M2에 더 가까운 근거). MCP 출력 검증은 SHOULD.
- Proactive Memory Agent: Sonnet 4.5 +8.3/+6.8, Opus 4.6 +2.4/+2.5, macro에서만 우세, 과제 안 메모리.
- GEPA Pareto는 "인스턴스별 최고 후보 유지"(다양성 장치). 2505.16067 삭제 규칙은 "최소 n회 꺼낸 뒤". SkillsBench 사람이 다듬은 스킬 +18.2~+24.8pp도 함께 적는다.

## 12. D3 반영 (2026-09-23 22:30 UTC, `D3-evidence-upgrade.md`)
- **`C_flip` 계산식 변경(기술 결정, 메인 세션 채택)**: 호출 사이 "한 답 대 한 답" 뒤집힘 수가 아니라, **같은 스텝에 쌓인 표들의 분포와 직전 창 표 분포 사이 거리**(총변동 거리 등)를 쓰고, 임계값은 성공 실행으로 conformal 보정한다. 근거: Sentinel/STAC(CoRL 2024, 기간 밖 기초 문헌) 절제 그림 5 — 한 샘플끼리 비교하면 기준 방법보다도 나빴다. M4 §(a) 원장이 이미 스텝별 표를 모으므로 추가 호출 없이 계산된다.
- 근거 상향(설계 유지): M9 "사전조건이 참인 가장 늦은 지점부터 재개" = PLANEX 삼각표(Fikes·Hart·Nilsson 1972, 기간 밖 기초 문헌) + BT 백체이닝(ICRA 2019, 기간 밖, 기초 문헌) + BacktrackAgent(EMNLP 2025) / M5 "모드가 다르면 평균하지 않는다" = RTC(NeurIPS 2025) + BID(ICLR 2025, 기간 밖) / M8 이벤트 대 주기 = 이벤트 트리거 제어 이론(Tabuada 2007, Heemels 2012, Åström 2002, 기간 밖 기초 문헌) / M10 "골라 넣은 소수 > 통째 주입" 원리 = Shi 외(ICML 2023, 기간 밖, 기초 문헌), 적응형 검색(ACL 2023, 기간 밖, 기초 문헌), Lost in the Middle(TACL, arXiv 2023, 기간 밖, 기초 문헌) / M8 T3b 과신 = Xiong 외(ICLR 2024, arXiv 2023, 기간 밖, 기초 문헌). (기간 밖 표시는 D11 정정)
- 여전히 잠정: M9의 "반성 유지"(로봇 반대 증거 있음), M10 "0~2개" 개수(E-M10 스윕), 호출 수를 맞춘 LLM 호출 방식 비교(학회 통과 문헌 없음 → E-M8a).
- 서지 정정: 2505.16067 = **ACL 2026**(Anthology 2026.acl-long.27 확인; S2의 ACL 2025 표기는 틀림). Rewind-IL "RA-L 2026"은 S2 표기뿐 → 학회 미확인.

## 13. D4 반영 (2026-09-23 22:45 UTC, `D4-devils-advocate.md`)
- 우선순위: CONTRADICT → 하드 채널은 **T1 술어일 때만**(§11.2가 §4보다 우선). T2/unknown이면 C_m4.
- 실험 순서 개정: E0 → **E0.5 표 재생(오프라인)** → E1 → **E2a(룰 대 Jev C1, M4 없이, 결과 기반 라벨, Astra 켠 대응 조건 포함)** → E-M4(C0~C6, C5 포함) → E-M3-2 → E-M5-1 → E-M8a → E-M9 → E-M6·E-M10. 본 평가 S1 직후 **EVAL H4(인식 술어 standard 대 random) + random 스냅샷 룰 대 Jev 오프라인**을 당긴다.
- E2 방향 재검토 조건: Jev 최선 조건의 R0 대비 차이의 **95% 상한 < +5%p**일 때만. 그 밖의 비유의는 "결론 유보".
- 환경 역할: E0~E3는 단일 팔 자작 장면(싸고 빠름). 본 평가는 RoboDojo(양팔). 둘을 섞지 않는다.
- Astra effort 잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5(→ §16. D11 정정: 첫 판의 "사용자 원칙" 표기는 틀림). RoboDojo 원문 재현(medium)은 재현용 조건으로만. (§26으로 폐기: 기본 low, low·high 비교)
- RoboDojo 루프 동기 여부를 S0 통과 조건으로. 공식 동기 트랙과 벽시계 트랙을 따로 보고.
- 기간 밖 문헌이 정한 기본값(LocalAgreement, Sentinel식 C_flip, Ruckig)은 사용자 승인([결정 필요] 4) 전까지 "잠정 기본".

## 14. D4 로봇 밖 분야 2차 반영 (2026-09-23 22:50 UTC, `D4-cross-field.md`)
에이전트가 올린 [결정 필요] 5건은 사용자 의도가 아니라 기술 선택이라 메인 세션이 **잠정 결정**하고 실험으로 검증한다(사용자 [결정 필요] 목록을 부풀리지 않는다).
1. M3 보기 순서 돌리기(순열 자기 일관성, NAACL 2024 기간 밖 + RecSys 2026): 같은 문구·같은 보기 집합에서 **순서만** 바꾸는 것이라 Jev #8("다른 질문끼리 확률 비교 금지")과 충돌하지 않는다고 본다(확률이 아니라 최빈 선택만 비교). **기본값 아님, E-M4 조건 C3''로 비교.**
2. M7 `C_order`(프로세스 마이닝 정렬 적합성, 기간 밖): 새 채널을 늘리지 않는다(D4: 채널 과다). **기존 마일스톤 원장 계산에 "건너뜀 / 계획 밖 사건" 구분을 넣고, 마지막 일치 위치를 M9 재개 지점 계산과 공유**한다. 채널 추가는 E-M7 결과로 판단.
3. M6 스킬 단계 `effect`(가역 + 보상 스킬 id / 비가역) — 사가(1987, 기초) + SagaLLM(2025-03-15, 기간 8일 밖) + Atomix(2602.14849): **채택.** M9 재개는 보상 행동을 역순으로 실행하고 비가역 경계를 넘어 되돌리지 않는다. M4는 비가역 보기의 확정 조건을 한 단계 엄격하게(W+1).
4. M2 계약 수리 왕복(형식 검증 오류를 Astra에 되돌림): **T0(첫 계획, 정지 허용)에서만 왕복.** 실행 중 재계획 patch는 검사만 하고, 실패하면 거부 + M7 신호(로봇을 멈추지 않는다). `forbidden`·순서 제약은 DFA로 컴파일해 H5·C_assume 감시를 하나로 통일.
5. M5 관성화(모드 전환 시 새 궤적에 위치·속도 오프셋만 감쇠, 게임 애니메이션 기법, 기간 밖 + 2602.21702 LOW): **채택하되 접촉 구간(near/contact)에서는 끈다.** 오프셋은 `ref(t)`에 포함(M4 (b) 기준과 일치).
- 실험 조건 추가: E3에 질문 앵커 PageRank 상태 줄 정렬(TabGR, LOW), E-M8a에 **정적 규칙 조건**(ICLR 2026 2602.09902 "정적 규칙이 대개 최적" 반대 근거), E-M10에 역량 보존 사례 삭제(Smyth·Keane 1995, 기초), E-M4에 C3'(Adaptive-Consistency식 베이즈 정지 규칙, 표 수만 사용).
- M6 생성형(b)에는 **typed hole 사후 검사 게이트**(PLDI 2025 타입 제약 생성의 API판: 타입·철저성·id·술어 검사 후 오류 되먹임).
- M8 T3b 게이트는 E1 뒤 conformal risk control(RouteNLP, ACL 2026 Industry)로 임계를 정한다. 실패 트리거는 항상 호출(불변).

## 15. D4 2차 원문 확인 반영 (2026-09-23 23:00 UTC, `D4-cross-field-verification.md`: 맞음 12 / 정정 4 / 보완 3)
- **2602.09902(ICLR 2026) 근거 표기 정정**: 원문은 "모델 2개 + 이탈하는 사용자" 게임에서 **공급자 비용 기준** 최적 라우팅이 거의 항상 캐스케이드 없는 정적 정책이라는 결과다. 트리거 방식 비교(E-M8a)의 근거가 아니다. E-M8a의 "정적 규칙 조건"은 유지하되 근거에서 이 논문을 뺀다. 이 논문은 오히려 "실패하면 항상 상위 모델 호출"에 대한 **비용 측면 반대 근거**로 기록한다. 사용자 원칙("실패할 때마다 Astra 개입")은 바꾸지 않는다(성공률 기준이 아니라 공급자 비용 기준의 결과다).
- M3 순서 돌리기: 기간 안 직접 근거가 있다 — RecSys 2026 Short("Position Bias Induces Inconsistent Rankings in Listwise LLM-based Recommendation") Table 1, 3회 섞기 + Borda 집계로 PPI 0.4775→0.2992. 단 "노출 평평화는 반드시 복원하지는 않는다(does not necessarily restore)"로 표현한다. 등급 MED(Short).
- E-M4 C3'(Adaptive-Consistency 베이즈 정지): 원문 기본값(Beta, C_thresh 0.95)이면 만장일치 4표가 필요하다(3표 0.9375). 스텝당 약 3표이므로 **임계값을 0.9로 명시**하거나 표 수를 늘린다. 실험 조건에 임계값을 적는다.
- M6 typed hole: PLDI 2025 원문은 **디코딩 중 제약**만 평가했다. "사후 타입 검사 + 오류 되먹임"은 **우리 접목안**으로 표기한다.
- M5 관성화: 반대 근거 추가 — Half Pound Filter §1이 관성화의 과평활·overshoot 한계를 적는다. "약 60% 절감"은 SIGGRAPH 2017 Talk §2(12µs 대 30µs, 원본 애니메이션 1개 조건)이고 GDC 소개문에는 수치가 없다.
- 보완: RouteNLP는 OpenReview상 ACL 2026 Industry Poster(Anthology 목록 미등재), 보장은 작업·단계당 보정 약 500개일 때(n=100 위반 7.2%) → E1 뒤 T3b 게이트 보정 표본은 500 이상. Inter-Cascade 임계값은 logprob 필요(Astra 불가) → 개념만. TabGR 순서 강건성 근거는 부록 Table 12. Agentproof 저자 구성·형식 위치 정정. SagaLLM = PVLDB 18(12)(기간 8일 밖).

- (23:10 UTC) 실행 중 거부된 계약 patch(`contract_patch_rejected`)는 새 채널을 만들지 않고 **`C_assume`로** 보낸다(계약 관련 소프트 신호로 묶음, 채널 수 억제).

## 16. D5 사용자 의도 정정 (2026-09-23 23:25 UTC, `D5-consistency.md` 범주 4, 메인 세션)
- **§11.3의 인용 정정**: user-log 3 원문은 "물체를 보고 스킬을 생성해서 잡는 방법들이 많지만 실패할 때가 있다. 실패할 때마다 Astra가 개입해서 진행하는 방식을 원한다"이다. "기존 방법들을 기본으로 사용"은 첫 세션 초안의 풀어 쓰기였다. M6에서 (b) 생성형을 먼저 구현한다는 판단은 **이 원문 뜻(스킬 생성형 잡기가 실패하는 곳에 Astra가 개입)에 대한 Claude의 해석**이고, (b)·(a') 대칭 + 최종 [결정 필요]는 그대로다.
- **effort high의 출처**: user-log(이 프로젝트)에는 없다. 출처는 사용자가 **다른 작업**(2026-09-23)에서 한 "추론 강도를 낮추는 건 옳지 않다" 발언(Claude 메모리, CLAUDE.md 모델 제약 절). 모든 문서에서 "사용자 원칙"이 아니라 **"잠정 기본 high(다른 작업 발언 근거) — 이 프로젝트 적용은 [결정 필요] 5"**로 적는다. (§26으로 폐기: 기본 low, low·high 비교)
- 정본 §1·§9 C3의 스무딩 문구는 user-log 원문대로 "먼저 RTC와 유사한 방법, 다음으로 액션 스무딩이나 VLA에서 쓰는 부드럽게 하는 방법으로 아주 작은 움직임을 처리한다"로 읽는다(D11 정정: 첫 판의 "액션 스무딩 등 VLA에서 쓰는 방법"은 원문 문구가 아니었다. "이나"라서 액션 스무딩도 사용자 문장에 직접 든다).
- 확률 규칙 문구 통일: "E1 전에는 확률 게이트를 모두 끄고 최빈 선택만 쓴다"(§6). plan §1의 "게이트로만 쓴다"는 틀린 문구.
- 정지 규칙 재확인: 정지는 T0와 M9 L3뿐. **M9의 "Astra 재계획이면 정지"는 L3(안전 대기) 안에서만 허용**되고 그 자체가 [결정 필요] 13에 묶인다.
- 기간 밖 근거에 기댄 새 기본값도 "잠정 기본"에 추가: 사가(M6·M9), 관성화(M5), DFA 형식 검증(M2), PLANEX/BT식 재개(M9), Ruckig(M5).

## 17. D6 모의 심사 반영 (2026-09-23 23:45 UTC, `D6-mock-review.md`)
- **실험 추가(메인 세션 채택, 기술 결정)**:
  - E-M4-lat: E-M4 장면에서 인공 지연 주입 스윕(p95 0.3/0.8/2/5초), C1·C2·C2'·C5 성공률 곡선(Slow Brain과 같은 축).
  - E-M4 모델 일반성: 같은 확정기를 B8 logprob 선택기(GPT-6 Sol/Luna `none`)에 붙인 C2 대 C5.
  - E-M4 지표 보강: C5 이득의 (a)/(b) 분해 그림, P0(섭동 없음) 무손해, 잘못 확정 비율을 결과 기반 라벨로도 계산, epoch 교체 빈도·미확정 실행 비율·번복률 분포(성공/실패 분리).
  - EVAL: 2×2(결정 층 {LLM, 규칙, B3c} × 상태 {정답, 인식}) S3 파일럿 규모, B3c에 사전학습 VLA 기반 변형(동결 π0.5 표현 + 헤드), π0.5 few-shot 적응 곡선(random 시연 k=5, 20), 바닥 과제 뺀 7과제 부분집합 사전 등록 분석.
  - E-link(조건부): RoboDojo Gen 4과제 random 벽시계 트랙에서 C2' 대 C5 — 사용자가 "한 논문(2안)"을 고를 때 필수.
  - E-real 최소판(조건부): 실물 단일 팔 pick-and-place, 섭동 2종 × C2' 대 C5 × 약 20회 — 로봇 학회 제출이면 필수, 하드웨어 필요([결정 필요]).
- 문구: 평가 주장에서 "VLA" 대신 "과제 데이터로 미세조정한 학습 정책(VLA 포함)"을 쓰고 "같은 인식 앞단 위에서" 한정어를 항상 붙인다. 사용자의 "VLA는 일반화가 안 된다"는 빨간 줄은 그대로 두고, 검정 줄(주장 문구)만 방어 가능한 형태로.
- E0.5를 실제로 가장 먼저(E0와 같은 날) 돌리고, flip≈0이면 M4 주장을 **미리** "(b) 되먹임 + 전제 epoch"로 좁힌다.

## 18. D7 선점 재검사 반영 (2026-09-23 23:55 UTC, `D7-preemption-rescan.md`)
- **M4 새로움 문장(수정)**: "합의가 되면 앞 구간을 확정한다"만으로는 새롭지 않다(A3 2605.11567). 새로움은 (1) **시간차로 겹쳐 부른 블랙박스 typed 결정 호출 사이의 합의**와 (2) **실행 뒤 코드 예상 상태 대 측정 상태 비교**를 **함께** 쓰는 확정 규칙, (3) 그 결과로 keep/replace/repair를 고르는 것.
- E-M4 조건 추가: **C5-A3**(A3식: 같은 시각 다중 샘플 합의만으로 앞 구간 확정, (b) 없음). 핵심 비교에 C5 대 C5-A3를 넣는다.
- **평가 주장 문구(수정)**: "처음 잰다" 금지. "**같은 인식·같은 실행기 위에서 결정 층만 바꿔**, 두 조건의 깨끗한 장면 → 바뀐 환경 낙폭을 짝지어 잰 연구는 없다(2026-09-22 색인 기준)." RoboDawn(C2R: Astra 무학습 53.2%·1-shot 73.6% 대 π0.5 46.0%)과 RoboDojo Astra(−11.1% 대 −72.2%, 우리 계산)는 **이 주장을 뒷받침하는 선행**으로 인용한다.
- 평가 축 후보 추가: RoboTwin 2.0 C2R(RoboDawn과 직접 비교 가능). [결정 필요] 3 실험 환경에 선택지로 추가.
- E2a: RoboJEV·embodied-jev의 "Jev ≤ 룰" 관측을 알려진 반례로 인용하고, 룰이 못 푸는 조건(모호함·보류 섭동)을 반드시 포함.

## 19. D8 정독 반영 (2026-09-24 00:20 UTC, `D8-robodawn-a3-deepread.md`)
- **M4 차별화 문장(확정)**: "A3는 학습된 VLA 한 모델이 같은 관측에서 뽑은 여러 청크의 합의와 조건부 재디코딩으로 실행 전에 확정 길이를 정하며, 외부 증거(세계 모델·상태 전이 예측)는 향후 과제로 남겼다(A3 §6, 원문 확인). 우리는 시간차로 겹쳐 부른 블랙박스 typed 결정 호출 사이의 합의에 실행 뒤 코드 예상 상태 대 측정 상태 비교를 결합해, 전제가 깨진 표를 무효화하고 keep/replace/repair를 고른다." 실행 뒤 되먹임 자체는 RoboDawn에도 있으므로(reached/partial/failed), 차별점은 "(b)가 합의 원장의 전제 무효화·확정에 쓰인다"는 점.
- M4 추가(기술 결정): 증분·순서형 보기의 합의는 **코드가 계산한 누적 예상 상태 기준**으로도 판정(보기 ID가 달라도 누적 결과가 같으면 일치, 허용 폭은 데이터 척도에서 시작) / 시간 정렬 ±1 스텝 허용 후보 / 기준선에 "격자 탐색한 최적 고정 확정 길이" / 지표에 평균 확정 길이·호출 수 / C5-A3 공정성을 위해 E0.5에서 같은 시각 반복 호출의 뒤집힘도 잰다.
- M3 추가: E-M3-1 비교 조건에 "6축 한 명령 + 한 JevCall에 명령 여러 개(RoboDawn식)"와 "회전 15° 단위·자세 프리셋". 해석: RoboDawn에서 성능을 가장 움직인 것은 공간 근거(격자 제거 −14.6%p)와 결정 횟수(예산 60→240 +16%p) → 사용자 "촘촘하게"를 결정 빈도·공간 근거로 구현하는 방향을 지지.
- 평가: RoboDawn은 같은 인식 비교가 아니다(하네스 주석 카메라 2대, cm 상태, 탁자 높이, 명령당 계획기 실행, 판정 요령 프로파일). 0-shot은 재현 구성 미제공 보고치, clean 점수 없음, RoboDojo는 `_random` 미평가. 인용 문장에 이 차이를 적는다. Astra RoboDojo 22.58%(자체 평가) 대 35.67%(RoboDawn 하네스)는 "같은 앞단" 요구의 근거.

## 20. D9 정독 반영 (2026-09-24 00:35 UTC, `D9-slowbrain-showharness-gptpolicy.md`)
- **E-M4 기준 조건 정정(원문 충실)**: C2 = Slow Brain "VLM Stream"(요청 시각 기준 가장 새 유효 응답 하나 적용, in-flight 상한 없음·실측 보고, 5 s 타임아웃). **C2' = Probability Fusion + 스트리밍**: p = (1−α)·softmax(S1) + α·softmax(Sim), α = λ/(λ+1)·exp(−Δt/τ), λ=3, τ=5 s, T_vlm=1, 5 s 타임아웃, Sim = 늦게 온 선택 보기의 코드 예상 결과 궤적과 현재 보기들의 (진행분 잘라낸) 거리/d_scale, **S1 = 빠른 층 코드 규칙 점수**(Jev logprob 아님). C2'-S = Score Fusion(λ=1). 선택 C2-match. 이전의 "표 나이 감쇠 가중 최빈(반감기 0.33 s)"은 우리 변형 **C2''**로 개명.
- M4 차별 문장 보강: 실행 뒤 명령 대 측정 되먹임 자체는 Show-Harness("lowered X of Y cm")와 RoboDawn(reached/partial)에도 있다 → 우리 차별점은 그것을 **합의 원장의 전제 무효화와 확정 판정**에 쓰는 것.
- EVAL: B6b(GPT-as-Policy 원문판: 같은 π0.5 체크포인트, Astra xhigh, 청크 경계 동기 게이트, gate 원문) + **B6b-wall**(Astra 생각 중에도 시뮬 진행). GPT-as-Policy 10과제는 표준 41 + 무작위 9(무작위 3과제) — 해결.
- M3: Show-Harness 기준선 원문판(단위 목록, 2/4 cm, 청크 3, WRIST 신호, 정지형 루프, 50 스텝, 빈 집기 5 mm), 1 cm 수치 본문 82 / 그림 80 병기.
- M9·M7: GPT-as-Policy gate 규칙("불확실만으로는 개입 금지", 직전 결과/다음 의도 분리)을 M7 FAIL·M9 L2의 원문 근거로 인용. Show-Harness 빈 집기 되돌림을 M9 L2 보기 `resume_ckpt_<k>` 구조의 원문 근거로 인용(정정 00:40: L1은 파라미터를 바꾼 재시도, `resume_ckpt_<k>`는 L2 보기).

- (00:40) E0.5 판정 1·2는 "여러 표 사이 합의에 정보가 있는가"를 묻는 판정이라 가장 새 표 하나만 쓰는 원문판 C2'로는 답할 수 없다 → 판정 1·2의 비교 대상은 C2''(감쇠 가중 최빈), 원문판 C2'는 E0.5 판정 7(보고용)과 E-M4 본 판정에 쓴다(편집 에이전트 제안 채택).

## 21. D10b 정독 반영 (2026-09-24 00:44 UTC, 처음 00:55로 잘못 적음, `D10b-critic-checkvla.md`)
- **M7 지표 정정**: "적시 재현율(CheckVLA 방식, τ = 2 s)"은 원문 정의가 아니다. 원문 = τ_on ≤ t* 그리고 t* + d_lat < τ_irrev(τ_irrev = 시험한 수리 규칙으로 더는 못 살리는 가장 이른 시각). E-M7은 (a) 우리 고정 창판(이름 "고정 창 재현율")과 (b) 원문판(d_lat = 실측 Astra 지연)을 병기. 오경보는 **에피소드 FWER**로 보고.
- M7 C_stag 원문 충실 조건 **CD1-CiL**(진행값 최고점 엄격 미갱신 지속, N_stag = 180 @ ~20 Hz ≈ 9 s) + M ∈ {2, 4.5, 9} s 스윕. **복구·재계획 뒤 C_stag 최고점·카운터 초기화**(원문 따름, 채택). functional CP(sup 점수) **CD11** 후보(D11 정정: 이름 바꿈, §23). E-M7 데이터에 무해 편차 롤아웃.
- M8 E-M8a 조건 추가: **A2-CV**(CheckVLA식 주기: 간격은 검증 분할에서만 호출 수를 맞춰 선택, 같은 감시기 shadow, 수리 규칙 동일, 호출 수·벽시계 보고), **A1-cal**(M7 FAIL 문턱을 에피소드 FWER 5%로 보정한 사건 트리거), **A1-wait**(실패 트리거를 다음 주기 호출까지 미룸 — 사용자 원칙 "실패하면 부른다"의 이득을 재는 절제, 원칙 불변), **A6-CiL-sync**(Critic in the Loop 충실판: 정체 시 버퍼 비움 + 리셋 + 동기 호출). 지표에 rescue/harm(같은 스냅샷 분기). 맞춤 정밀도 원문 약 1% 대 우리 ±10% 병기.
- M8 반대 증거: CheckVLA Table S6 — d_lat = 10(1 s)에서 청크 안 수리 규칙 전부(23.4~31.4%)가 "다음 경계까지 기다림"(34.0%)보다 낮다(우리 읽기, 원문 확인). Astra 지연은 몇 자릿수 길다 → A1-wait가 이길 수도 있다. 그래도 사용자 원칙은 바꾸지 않고 결과로 보고. Critic in the Loop Dual-System(청크마다 재계획) 진동.
- 차별화: "멈추지 않는다" 자체는 CheckVLA에도 있다 → 우리 M8 평가 기여의 차별점은 "느린 **별도** API 모델 + 수 초 이상 지연 + effort 축 + 계획 쪽 마감(T3a)·모델 자체 판단(T3b)을 관측 쪽 정체와 같은 호출 수에서 비교".

## 22. D10a 정독 반영 (2026-09-24 00:48 UTC, 처음 00:50으로 잘못 적음, `D10a-harness-capx.md`)
- **M6 스킬 출력**: 출구 술어 + 오류 코드에 더해 **원자료 진단 필드**(예: 최고 들어올림 높이, 최소 그리퍼 간격, 사용 청크 수, 종료 신호)를 필수로 둔다. 근거: RPent `pi0_pick` 반환 dict, 전역 메모리 교훈 "`pi0_pick.success`=false여도 영상상 들고 있으면 계속" = 스킬 불리언 하나는 틀린다 → M7 "코드 술어가 정답, 스킬 자기 보고는 보조"의 원문 사례.
- **M6 typed 결정 지점 후보**(Harness 탐색 레버에서 도출, [접목] — 원문은 자유 JSON 수치 인자): `dp.stage_pose`, `dp.invoke_now`, `dp.chunk_budget ∈ {short(≤8), mid(12–16), long(24)}`([가정], 값은 RPent 교훈 수치), `dp.retry_prompt`(grasp-only / full-task), `dp.grasp_verify`(스킬 불리언 / 영상 증거). 결정 지점 id 등록은 §11 규칙 그대로(스킬 고정 id, Astra는 새 id를 만들지 않음). (00:59 정정: 처음엔 "§7 표"로 잘못 적음, §7은 설정 표)
- **M6 (b) 생성 스킬**: Harness VLA는 (a)/(a') 근거(고정 어휘, "cannot invent"), (b) 근거 아님. CaP-X 스킬 라이브러리 = 성공 롤아웃 → 정규식 추출 → 2회 이상 + 이름 필터 → LLM 큐레이션, 결과는 **기하 유틸 9개, 사전·사후조건 없음, 스크립트에 자동 검증 없음**. 우리 (b)는 같은 추출 + 계약(entry/exit/effect) + typed hole + **실행 기반 검증**(재생 + 새 seed 비열화, M10 게이트와 같은 장치). E-M6-3에 "생성 기반 API 단계" 축(우리 스킬 위 = 높은 단계 / M1 술어 + IK만 = 저수준) 추가.
- **M9**: 매 프리미티브 뒤 {progress, recoverable failure, unrecoverable failure} 3분류(Harness 부록 A) ↔ M7 정상 / WARN·FAIL / 복구 불가(L3) 대응 표를 M9에 둔다. L1 재시도 원문 사례(빈 집기 → 재위치·re-stage, full prompt 금지, 한 레버만 변경). (D11 정정) 같은 빈 집기라도 파라미터만 바꿔 재시도하면 L1, 잡기 하위 단계로 되돌리면 L2(§20 Show-Harness 사례)다. **반성 유지(D3 잠정)의 반대 증거 추가**: CaP-X H.4 검증·디버깅 강화 프롬프트 68.29 → 65.43(7과제 × 100회, 3M 앙상블).
- **M10**: 교훈 항목에 `applies_when`, `symptom`, `evidence.cells`, `confidence`(cells ≥ 3이고 과제 ≥ 2 → verified, cells ≥ 2 → probable, 그 밖 single-shot — RPent `manager.py`), **`falsify`(반증 조건)** 필드 추가. Astra 주입은 probable 이상만([제안]). RPent는 효용 추적·삭제 없음, 플래너가 grep으로 스스로 검색, 평가 프롬프트에 seed-0 과제별 수치 하드코딩 → 우리 "추상 교훈만, 키 정확 일치 0~2개"와 다름을 차별 문장에. E-M10에 "과제별 trace 주입" 상한 조건([제안]; 근거 Goal-S 메모리 없음 31.0 대 있음 87.0, Flash 72.63%).
- **M8 입력**: CaP-X M2(원시 RGB) < M1(stdout) < M3(텍스트 시각 차분) → 기존 "변화 텍스트" 비교 조건의 근거 보강. 기본 조건(사용자 예시: 격자 한 장 10프레임)은 그대로. Astra는 이미지 강점이라 결과가 다를 수 있음([가정]).
- **EVAL**: §2.3 "RPent Astra low 92.63% 섭동 축 확인 필요" → **해소**: LIBERO-PRO Task(T) + Swap(S) × Spatial/Object/Goal/Long 8칸, seed 1~10, 메모리 스냅샷 두 묶음(Long 157/200 + 나머지 584/600), **논문이 아니라 저장소 리더보드 값**, 에피소드당 평균 412.14 s 정지형. 논문 본문 플래너는 Codex(리더보드상 GPT-5.5 xhigh)와 Claude Code(Opus-4.7 max). 인용 시 이 조건을 반드시 병기. B6c-0(메모리 없음, Table 5 조건), B6c-S(RPent 루프에 우리 스킬 등록, 같은 스킬·같은 인식), B6c-S-wall, B5a(CaP-X 공개 코드, 구성은 "우리 가정" 표기), B5b-H / B5b-L(우리 스킬 위 / 술어 + IK만), 앙상블 끔·켬 따로 보고. RPent 평가 프롬프트의 "PROVEN LEVERS"는 과제별 정보 → 우리에게 안 주면 "입력 정보 다름" 각주.
- 인용 정정: Harness VLA RoboCasa365 향상은 본문 Table 4 기준 **+27.1pp**(57.1 − 30.0), abs 페이지 초록은 +25.4pp → 불일치 병기. CaP-X LIBERO-PRO "12~26%"는 과제당 50회 기준(Harness Table 3의 Cap-X 행은 이 값을 옮긴 것, 프로토콜 다름).
- 차별 문장: D10a §1.2·§2.2 "차별 문장 초안" 4개를 M6·M9·M10·EVAL 관련 연구 절 후보로.

## 23. 메인 세션 정리 (2026-09-24 00:49 UTC, 처음 00:55로 잘못 적음)
- M7 판정 기준 1~8의 재현율 = **고정 창 재현율(τ = 2 s)**. CheckVLA 원문판(d_lat = 실측 Astra 지연)은 병기 보고만 한다. 이유: Astra 지연이 날마다 크게 달라(첫 토큰 약 3~352 s, Artificial Analysis 기준 읽음, 2026-09-23) 사전 등록 기준으로 불안정. (D11 정정) 판정문과 지표 목록의 이름은 "고정 창 재현율(τ=2 s)"로 쓰고, "적시 재현율"은 CheckVLA 원문 정의에만 쓴다.
- M7 대안 "A6 functional CP"는 **A-FCP**로 이름을 바꾼다(M8 E-M8a 조건 A6와 겹침). E-M7 조건 이름 D11은 그대로(→ 아래 D11 정정 줄에서 CD11로 바꿈).
- (D11 정정, 2026-09-24) E-M7 조건 이름 **D1-CiL → CD1-CiL**, **D11(functional CP) → CD11**로 바꾼다. [결정 필요] D1(주장 문구)·D11(H = 1 대 3), `D1-verification.md`, `D11-final-consistency.md`와 이름이 겹쳤기 때문이다. 설계 문서(plan, SUMMARY, M1~M10, E-first, EVAL)는 새 이름을 쓴다. 옛 D 보고서 본문은 기록이라 그대로 둔다.

## 24. D12 선점 재검사 반영 (2026-09-24 01:42 UTC, 처음 01:52로 잘못 적음 — 짐작 시각, `D12-preemption-0901-0924.md`)
- **C1·C2 강한 선점 없음**(2026-09-23 제출분까지). C2 문구의 색인 기준일을 "2026-09-23 제출분 기준"으로 갱신.
- **계층은 새로움이 아니다**: JEV-Star(2609.27331)가 "fast JEV action selection" + GPT-6 Astra(medium effort) 비동기 계획을 StarCraft II에서 먼저 했다(게임은 요청 중에도 진행, 계획 실패·지연 시 이전 계획 유지, 계획기는 60 게임초 주기 + 사건 호출, JEV는 초당 1회 제한). **반드시 인용**하고, 우리 차이는 "로봇 실행 중 **실패 판정이 불러내는** Astra 호출 + 겹침 호출 합의와 실행 뒤 확인(M4)"으로만 쓴다. 사용자 원칙("실패할 때마다 Astra 개입")은 그대로.
- **M4 차별화 문장 보강**: A3 옆에 WCD(2609.02159: 실행 뒤 예측-실제 불일치를 **다음 후보 선택 예측기 학습**에 씀, 같은 시각 샘플)와 SMC(2609.03236: 두 모델 일치로 확정, 비로봇)를 인용. 우리는 실행 뒤 불일치를 합의 원장의 **전제 무효화·확정 판정**에 쓴다. 전제 epoch 설명에 RegenHarness(2609.27612) stale/version-bound 선례 인용. "실행 뒤 확인 자체"의 선례 목록에 EmbodiedSkills(2609.01281) 추가.
- **(a)만으로는 부족 → (b)가 필요하다는 근거**(반대 증거를 동기로): Type-Safe Is Not Error-Free(2609.26758) — 보기 이름을 0/1 → no/yes로 바꾸면 100개당 70.4개 답이 바뀌고 AUC .94 → .23, hosted 모델도 .8146 → .5806, type-error 0% 유지. 보기 이름에서 오는 체계적 오류는 반복 호출 합의로 걸러지지 않는다. PACT(2609.01662, 08-31 제출 → 기간 안): "Repeated inference over one observation can improve predictions without adding an evidential origin" → 우리 합의는 시간차 관측·실행 뒤 측정이라 증거 출처가 늘어나는 합의라는 차별점.
- **E0.5 추가**: (i) 보기 이름 치환 대조(의미 있는 이름 대 중립 식별자, 같은 루브릭), (ii) 같은 입력 반복 test-retest flip 바닥. 판정: 치환 flip이 test-retest 바닥보다 크면 M3/M6 보기 이름 규칙을 바꿀 근거.
- 새 [결정 필요](사용자): (가) Jev 보기를 중립 식별자로 바꾸고 뜻은 루브릭에 적는 규칙을 기본으로 둘지(E0.5 (i) 결과 전/후), (나) M8 비교 조건에 JEV-Star식 주기 + 사건 호출과 REFLEX(2609.26532)식 확신 기반 올려 보내기를 넣을지, (다) this-that-model-1.0(2609.23886, 2B 오픈 typed 결정 모델, [초록만])을 Jev 로컬 기준선 후보로 둘지.
- 한계 문장: RoboTwin-Phys(2609.26292, [초록만]) "degrade markedly under changes in physical conditions" → 우리 random 조건(시각·배치)만으로는 일반화를 다 못 잰다는 한계를 EVAL에 적는다.
- 관련 연구 한 줄 후보: VLM-MPPI(2609.18451, 비동기 VLM 후보 선택 + 20 Hz MPPI 비정지), 2609.26084(비동기 VLM copilot, [초록만]), C²Nav(2609.15142, 비교형 질문 > 절댓값형, [초록만]), RoboFind(2609.20330, 검증 다중 에이전트 10/12 대 Astra 단독 5/12, [초록만]), GTA-2(2609.09808, [초록만]).

## 25. D13 정독 반영 (2026-09-24 02:12 UTC, `D13-m1-m2-m5-deepread.md`)
- **M5 L2 가중식 교체(§7 덮어씀)**: RTC 원문 식 5는 순수 지수가 아니다(코드 `w·expm1(w)/(e−1)`, 구간 끝 정확히 0, 앞에 가중 1 고정 구간). 우리 exp(−t/0.1)은 T_b = 0.3 s 끝에서 e^−3 ≈ 0.050이 남아 약 5% 계단(우리 계산). → w = 1 (t < t0 + T_f), w = c(e^c − 1)/(e − 1), c = (t0 + T_b − t)/(T_b − T_f) (t0 + T_f ≤ t < t0 + T_b), 그 뒤 0. T_b = 0.3 s 유지, **T_f 초기값 0**([가정]), τ_b 삭제. E-M5-2 스윕은 schedule {exp, linear}(원문 절제: 지수·선형 거의 같음) × T_b {0.2, 0.3, 0.5} s. L2 기본 유지(사용자 방향, §9 C3)는 그대로 — 모양만 바뀜.
- **M5 조건 추가(기본 아님)**: S4-epoch = M4 `premise_epoch` 교체 직후·M4 (b) 불일치 WARN 중에는 같은 보기라도 블렌딩하지 않음(근거: WAM Fig. 6 갑작스러운 변화에서 prefix 제약 해로움, SEAM "RTC can lock into a failed alignment"). E-M5-1 지표에 "잘못된 접근 유지 시간".
- **M5 인용 정정**: WAM 사후 블렌딩은 끼우기 40 → 37.5뿐, 컨베이어 20 → 40, 전자레인지 44 → 80, 저자 결론 "workable floor" → "L1+L3만" 대안의 지지 근거에서 뺌(혼재). WAM 등급 LOW-MED로 통일(저자 "Motubrain Team"). `d_p95` 근거에 WAM "지연 과소 추정 = 되돌릴 수 없는 위치 점프, 과대 = 경미" 추가. FluxVLA Ruckig 1.17 ms 원문 확인(조건: 12-DoF·50스텝 합성·Xeon 8336C·관절 분리만, 과제 효과 미측정) → L3 1순위는 관절 공간([가정], 직교 공간은 원문 평가 없음).
- **M2 근거 정정**: COPE의 큰 → 작은 이득(Llama-3B 42.8 → 53.0)은 **절차형(guideline) 계획**이었다. 목표형 우위(30.2 대 23.2)는 1B 자기 계획뿐이고, 본 방법 Stage 2도 큰 모델 절차형. → COPE를 v1(목표 중심 조각)의 근거에서 뺀다. v1 기본값은 설계 선택으로 유지(M4·M7·M9가 술어를 읽음). E-M2-1에 **B-COPE**(v1 + Astra 절차 1~2문장) 조건 추가, 판정 2를 B-COPE에도 적용. 근거 약화는 사용자에게 알림.
- **AgentSpec 정정(§11.4 덮어씀)**: o1 생성 규칙 = 처음 보는 위험 사례 집행률(원문 "precision") 95.56%, 안전 짝 사례로 낸 "recall" 70.96%(원문 정의 모호, 놓침과 과차단을 함께 설명). "위험의 약 3할을 놓친다"는 원문에서 바로 나오지 않음. 사람 규칙도 안전 과제 수행 58.62 → 54.26%. 설계 결론(M7 하드 층 독립)은 유지, 이유에 "과차단" 추가. E-M2-3 지표에 `forbidden` **과차단률**(정상 성공 에피소드 중 거부 상태 진입 비율, 에피소드 단위).
- **AgileThinker 등급**: HIGH(ICLR 2026) → **MED(학회 [미확인], abs 코멘트 "30 pages"만)**. Table 10 네 열 그대로(반응 생각 끔 0.12 열 포함). 16k에서 결합 0.70 < 반응(생각 켬) 0.76 → 압박 약할 때 결합이 손해일 수 있음 → E-M2-2에 지연 구간별 L+A 대 L 분리 보고.
- **M1**: lmgame-Bench "복잡 그래픽 게임에서 효과 작음"은 §3.1 본문(Table 2 아님), Sokoban 상자 1.0~1.3 → 5.3~6.0개(3회), 2048에서 강한 모델 이득 없음·음수(gemini 120.5 → 117.4), **텍스트 전용 모델이 기호 표만으로 참가·상위권**(grok-3-mini Sokoban 5.7) — Jev에 가장 직접적인 기간 안 근거, 저자 "preliminary". ViPlan: 가장 강한 모델(GPT-5.2)도 HH nextto 0.52~0.73·open 0.53~0.59·reachable 0.42~0.63, 파생 술어 `clear`는 우연 수준 근처 → 등록부 술어는 조작적 정의 + 파생 술어는 코드 계산(M1 #10)의 원문 근거. E3에 정답 yes/no 균형, yes·no 정확도 분리, 물체 수별 곡선.

## 26. 사용자 결정 반영 (2026-09-24 03:17 UTC, user-log 25) [사용자]
- **D29 논문 틀 = 한 편**(user-log 25 "논문을 일단 한 편으로"). 모의 심사 2안(방법 + 평가 한 논문)을 따른다 → 두 기여를 잇는 **E-link**(RoboDojo Gen random 벽시계 트랙에서 C2' 대 C5)와 **E-M4-lat**(지연 스윕)이 필수 실험이 된다(D6 조건). M8 비교·M9·M10은 최소판.
- **Astra effort 기본 = low**(user-log 25 D14 답: high는 첫 토큰 약 73 s라 "너무 느린 것 같아" → "로우로"). **실험에서는 high와 low를 둘 다 비교**(user-log 25 "둘다 비교"). 따라서 D5·D14 해소: 잠정 기본 high는 폐기, 실패 호출·첫 계획 모두 기본 low, E-M8a·E0·E2a의 effort 축은 최소 {low, high}(medium·xhigh·max는 여력 있으면). 다른 작업의 "effort 낮추지 말 것" 발언은 이 프로젝트에 적용하지 않는다(사용자 결정). §13·§16·§23의 "잠정 기본 high" 서술은 이 절이 덮는다.
- **D32 보기 이름 규칙**: "일반적으로는 어떻게 하는지 찾아봐서 적용"(user-log 25) → 조사 D14(진행 중) 결과의 일반 관행을 기본 규칙으로 적용하고, E0.5 (i) 치환 대조는 확인 실험으로 유지.
- **M1 "변환 방법은 사용자가 정한다" 삭제**: 사용자가 한 말이 아니다(user-log 25). 변환 방법은 Claude가 정해 진행한다 → **기본 = 후보 A(ID 술어 표 + 변화 절, 근거 수 1위)**, 후보 B·C는 E3 비교 조건. CLAUDE.md·plan·M1·SUMMARY·§11.3의 해당 줄과 D2(사용자 결정 항목)는 이 절로 해소.

## 27. D32 보기 이름 규칙 (2026-09-24 03:29 UTC, `D14-option-naming.md`, user-log 25 "일반적으로는 어떻게 하는지 한번 찾아봐서 적용")
- 조사 결론: "모든 보기를 중립 식별자로"는 일반 관행이 아니다. Type-Safe(2609.26758)의 중립 식별자 권고는 **2지선다** 근거이고, 같은 논문 §4.5(3지선다 이상, n = 683)는 중립 문자로 바꾸면 정답률 .5637 → .2782, 답 52.42% 변경, 설명이 거의 무시되고 위치를 따라간다(저자는 "directional support"로만 둠, 호스트 Jev 측정 아님으로 추정). 업체 지침(Pydantic TypeSafe 문서): "name Literal and Enum options for what they mean", "reordering them can move the answer. If a classification matters, test it with the options in more than one order."
- **지금 기본으로 적용(호출 추가 없음)**:
  - R1 모든 보기에 설명(Choices 매핑·docstring) 필수. 이름은 설명 앞머리를 줄인 것이고 설명에 없는 뜻을 담지 않는다. 이름 금지어: 극성·판정어(yes/no/true/false/ok/fail/accept/reject/good/bad/safe/correct 등), 조건을 품은 이름(`descend_if_aligned` 등 — 조건은 설명에만).
  - R2 **2지선다 Choice는 중립 ID**(`A`/`B` 또는 5자 무작위 문자열), 뜻은 설명에만(호스트 Jev: 이름 맞바꿈 32.50% 대 중립 2.08%·1.67%, test-retest 바닥 1.33%). Noul은 기준 문장을 부정 없이 긍정형으로.
  - R4 순서: `question_id`별 고정 표준 순서, `NONE_ESCALATE`는 항상 마지막. 순서 순환(C3'')은 E-M4 조건 그대로.
  - R5 원장·M4 합의·결과 라벨은 고정 `option_key`로 센다. 표시 이름·표시 순서·`perm_id`·`display_id → option_key` 대응표는 호출마다 기록. 확률 평균 없이 최빈 보기만.
  - R6 이름 불변성 flip은 오프라인(E0.5·E1)에서만 잰다.
- **E0.5 (i) 결과로 확정**: R3 3지선다 이상 행동 보기(방향·목표 물체·크기, ≤ 약 17개)는 내용을 그대로 적은 이름 + 설명을 유지(중립 문자로 바꾸지 않음). E0.5 (i) 판: A0 지금 이름 / A1 중립 문자 / A2 무작위 5자 / A3 이름을 설명과 한 칸 어긋나게(진단) / A4 순서만 한 칸 순환 / (ii) A0 반복. 층: 2지선다·Noul / k = 3~6 / k = 7~17. 지표: `option_key` 기준 flip, 결과 라벨 정답률, A3 이름 추종률, 첫 자리 선택률 대 정답 첫 자리 비율. 판정(층별): 중립 전환 = A1 정답률 ≥ A0 − 2pt(짝 부트스트랩 하한) 그리고 A3 이름 추종률이 (ii) 바닥보다 유의하게 큼 / 이름 유지 = A1이 A0보다 낮고 차의 95% 하한 > 0 / A4 flip ≥ 5%면 C3''를 필수 조건으로. 약 9천 요청, 약 $1 미만(우리 계산).
- D32는 이 절로 해소(사용자 지시대로 일반 관행 적용, 남은 부분은 E0.5가 확정).
- (메인 판단) 보기 수 k는 `NONE_ESCALATE`를 빼고 센다 → 두 갈래 결정 지점은 R2(중립 ID + 설명), `NONE_ESCALATE`는 이름 그대로 항상 마지막. R1 금지어에 걸리는 기존 보기 이름(`failure`·`valid_progress`·`place_safe_now`·`safe_wait`)은 M7·M9 목록과 함께 E0 전에 바꾼다(이름만 바꾸고 뜻은 설명으로, 진행 중 과제).

## 28. API 일관성 규약 (2026-09-24 04:41 UTC, `D17-api-consistency.md`, user-log 30 "일관성을 어떻게 유지할지에 대한 … 프롬프팅 엔지니어링 … 1년 반 있는 논문에서 찾아서 적용")
- **전제**: API 모델은 temperature 0·seed로 결정성을 얻을 수 없다(추론 모델은 greedy에서도 정확도 최대 9% 변동 — 2506.09501, NeurIPS 2025 Oral). 효과 크기가 작은 비교는 반복 실행이 필요하다(2%p 차이 검출에 약 9회 — 2602.07150, MED). effort를 올려도 분산이 준다는 보장은 없다(2512.07795, MED) → 사용자 결정 "effort 기본 low, low·high 비교"는 그대로 두고 비교 때 분산도 보고한다.
- **Astra 규약**
  - A1 템플릿 고정: `astra_prompt_id` = 해시(시스템 문장 + 계약 스키마 + 스킬 카드 + 술어 어휘 + 예시 계약 + 격자 렌더러 판본). 실험 도중 불변, 판본은 실험 사이에만 올림. 모델은 날짜 박힌 스냅샷 ID(가능하면), 응답의 모델 식별 필드를 기록([가정]: 필드 유무 미확인).
  - A2 입력 정규화: 물체 id 순, 술어는 등록부 순, 숫자 고정 자릿수, 시각은 상대 범주. 격자 이미지는 배치·해상도·패딩·덧그림 글꼴을 바이트 단위로 고정, 프레임 시간순. 정적 부분(시스템·스키마·카드)을 앞, 동적 부분(상태·프레임·실패 문맥)을 뒤에(근거: 형식 민감도 FormatSpread 기간 밖 기초 문헌, 라벨 앞 공백만으로 최대 11% — 2509.15020 EMNLP 2025).
  - A3 출력: 계약 JSON + 검사기 1–6 유지. "자유 계획 → 2턴 스키마 변환"은 E-M2-3 첫 시도 통과율 < 80%일 때만 비교 조건(Format Tax 2604.03616, MED: 최근 폐쇄 모델은 형식 손실이 거의 없음).
  - A4 재계획 고정(**잠정 기본**, M2 R6 승격): T0 이후 재계획은 활성 계약 + `based_on` 이후 변한 사실 목록 + 이미 VERIFIED·EXECUTING 단계를 입력으로 받고, 기본 출력은 `patch`. 바꾸는 단계마다 근거 변화 사실 id(`change_reason`), 이미 실행한 단계는 변경 금지, 편집 거리 기록. 근거: 체화 과제의 계획 지역성(AgenticCache 2604.24039, MLSys 2026), 계획 템플릿 재사용(2506.14852, NeurIPS 2025). 사용자 원칙("실패할 때마다 Astra 개입")은 그대로 — 부르는 방식이 아니라 받는 형식의 규칙이다.
  - A5 투표(**잠정 기본**): T0(정지 허용)에서만 effort low로 K = 3 병렬 호출, 계획 서명(단계 스킬 열 + 결정 지점 id + 물체 역할) 다수결, 셋 다 다르면 검사기 통과 첫 번째 + `plan_unstable` 기록. 비정지 호출(T_fail·T3)은 K = 1(지연을 늘리지 않음, 오류는 검사기·M4 (b)가 잡음). effort high 조건에서는 투표 안 함. K = 3은 설계 선택([가정]), E0 Astra 반복 결과로 확정. 같은 관측 반복은 분산만 줄이고 증거를 늘리지 않는다(PACT, §24).
  - A6 재현 기록: 요청 원문(이미지 바이트 해시 + 원본), `astra_prompt_id`, 모델 ID, effort, 최대 토큰, 사용량, 첫 토큰·완료 시각, 원응답, 검사기 결과, 계획 서명, patch 편집 거리.
- **Jev 규약**
  - J1 `question_id@vN` = 해시(문구 + `option_key` 표준 순서 + 보기 설명 + 표시 문자열(공백 포함) + legend + 상태 직렬화기 판본). §27 R5 기록은 그대로.
  - J2 상태 직렬화 정규화: 같은 세계 상태 → 바이트까지 같은 텍스트(M1 후보 A 표, id·등록부 순, 고정 구간 숫자, 나이 범주).
  - J3 M4 합의 표는 시간차 상태에서만 모은다. 같은 입력 되묻기는 측정용이며 합의 표로 세지 않는다. `FLIP_TH`와 LA-2 불일치 해석은 E0.5 (ii)에서 잰 바닥 위에서 한다(두 갈래에서 바닥 p면 잡음만으로 두 호출이 어긋날 확률 약 2p(1−p), 우리 계산).
  - J4 확률 게이트와 감시는 `question_id@vN`별로만(질문 사이 확률 비교 금지, Jev 공식 약점).
- **측정 추가**
  - E0: Astra 같은 입력 반복(실제 T_fail 입력 3개 × effort {low, high}); 지표 = 계획 서명 일치율, 필드 flip, patch 편집 거리, 지연. 판정: low 서명 불일치가 high보다 유의하게 클 때만(짝 부트스트랩 하한 > 0) 비정지 호출에도 투표를 검토, 아니면 A5 유지.
  - E0.5 (ii): test-retest 바닥을 층별(2지선다·Noul / k 3–6 / k 7–17) × 시간 블록별로 잰다. 블록 차가 유의하면 폐루프 조건을 같은 시간 블록 안에 교차 배치.
  - 매일 카나리(E0 이후 모든 실험일): Jev는 고정 스냅샷 × 고정 `question_id@vN` 소수 세트의 최빈 보기·확률 분포를 기준일과 비교(로그 확률 추적 2512.03816, ICLR 2026); Astra는 고정 입력 1–2개를 low로 돌려 계획 서명 비교. 기준 대비 불일치가 그날 바닥보다 유의하게 크면(하한 > 0, Holm) "표류 의심" → 그날 결과 분리 보고, E1 보정 재실행 뒤 게이트 재사용. 모델 식별 필드가 바뀌어도 같은 처리.
  - E1: 보정값은 `question_id@vN` × 모델 ID에 묶는다. 판본이 바뀌면 그 질문 게이트를 끄고 재보정.
  - E-M8a·E2a의 Astra 조건: 반복 실행 수와 검정력 계산을 사전 등록(2%p 검출에 약 9회 기준). 단일 실행 비교로 결론 내지 않는다.
- 제외(신뢰도 낮음, user-log 14): D17 §2의 12편은 근거로 쓰지 않는다.

## 29. 사용자 의견 반영 (2026-09-24 04:49 UTC, user-log 31)
- §28 A5(T0에서 K = 3 병렬 다수결)는 **잠정 기본에서 내리고 보류**한다. 사용자: "세번 동시에 다수결로 부르는 방식 자체가 너무 오리지널 예전 방식 같은 느낌". 대신 1년 반 안의 LLM·VLM·VLA 논문에서 "완벽하지 않은 출력의 신뢰도를 재고 보장하는 방법"을 조사해(D18) 대체안을 정한다.
- §28 A4(재계획 patch 기본)도 조사 결과(D18: 요즘 최신 방식인지) 전까지 잠정 유지하되 확정하지 않는다.

## 30. D18 반영 제안 (2026-09-24 05:03 UTC, `D18-confidence-patch.md`, user-log 31) — 사용자 결정 대기
- 조사 결론: 같은 모델 병렬 다수결은 2025–26 문헌에서 **비교 기준선**이다(사용자 직감과 일치). 최신 흐름은 (가) 확신 가중 투표(logprob 필요), (나) 별도 검증기로 고르기, (다) 보정 집합으로 문턱을 맞춘 conformal 집합·기권·에스컬레이션(커버리지·위험 상한 보장). 보장이 붙는 방법은 모두 확률 점수 + 보정 집합이 필요 → **Jev에는 맞고(E1 뒤), logprob가 없는 Astra에는 거의 안 맞음**.
- **[결정 필요] (가) A5 → A5′ "검사 → 수리"**(제안): Astra는 1회 호출 → 검사기 1–6 + 실행 기반 전제·효과 검사(전제 술어를 측정 상태·앞 단계 `expected_after`로 코드 평가) → 실패하면 위반 목록을 붙여 순차 수리 재호출(최대 N회, N = 2 [가정]; 참고 2509.02761 "≤3 iterations 96.5%"는 오프라인 조건). 비정지 호출은 검사 실패 시 이전 유효 계약 유지 + 백그라운드 수리(JEV-Star "previous valid plan available"과 같은 모양). 다른 계열 검증은 선택(2512.02304 "verification across model families is more effective", MED). Astra 말로 한 확신은 기록만(게이트 아님; 2505.14489 NeurIPS 2025는 개선 방향).
- **[결정 필요] (나) A4 → 적응형 `patch | replace`**(제안): 판정 — patch 전용은 체화·에이전트 계획의 표준이 아님. 강한 논문은 이전 계획을 입력에 넣고 다시 생성(Plan-and-Act ICML 2025, π0.5 매 스텝 하위 과제 재추론). 코드 편집은 적응형 선택이 최신(SWE-Edit 2604.26102 "adaptive find-replace/whole-file-rewrite policy improves edit success by 12.5 pp"). → 기본 patch, (i) 깨진 전제에 기대는 미실행 단계가 절반 초과 (ii) 목표·물체 역할 변경 (iii) patch가 검사기 불통과 때 replace [가정 규칙]; replace도 이전 계약을 입력에 넣고 실행한 단계는 불변. E-M2에 {patch 전용, 이전 계약 넣고 전체 재생성, 적응형} 비교.
- **[결정 필요] (다) J5 Jev conformal 게이트(E1 뒤 후보)**: `question_id@vN`별 보정으로 예측 집합(KnowNo 기간 밖 기초 문헌·CoFineLLM 2511.06575 방식). 원소 하나 → M4 확정 후보, 집합 > 1 또는 `NONE_ESCALATE` 포함 → 확정 보류(직전 확정 행동 유지 + 감속), 반복되면 Astra로 에스컬레이션(Trust-or-Escalate·JRA 두 문턱 라우팅 모양). 보장은 단계별 주변 커버리지(에피소드 수준 아님). 교환 가능성 전제 → §28 버전 묶기·매일 카나리가 조건을 지킨다. E1 전에는 §6(게이트 끔) 유지.
- M7: 소프트 채널 문턱을 성공 에피소드로 conformal 보정(FIPER NeurIPS 2025식, 오경보 ≤ δ) [접목] — 기존 CD11(functional CP)과 같은 방향.
- 신뢰도 주의: JRA(COLM 2026)·Trust-or-Escalate(ICLR 2025)·RoboMonkey(CoRL 2025)의 학회 표기는 arXiv abs에서 확인되지 않았다(인용 시 표기). LOW 10편은 근거에서 제외(D18 §5).
- 결정 전까지: A5는 보류(§29, K = 1), A4는 잠정 patch, J5는 꺼짐.

## 31. §30 결정 (2026-09-24 05:06 UTC, user-log 32 "너가 옳다고 생각하는 것대로 … 근거가 있어야 돼") — Claude 결정, 근거는 `D18-confidence-patch.md`
형식: "선행은 이렇게 했다 → 우리도 이렇게 한다". §29로 보류한 A5는 아래 A5′로 대체한다.

- **A5′ Astra 계획은 "1회 호출 → 코드 검사 → 수리 재호출"로 확인한다(다수결 삭제).**
  - 선행: LLM 체화 계획 검증 연구(2509.02761, UIUC, MED)는 Judge가 계획을 비평하고 Planner가 고치는 반복 수리로 "up to 90% recall and 100% precision", "96.5% of sequences requiring at most three iterations"를 보고했다(오프라인 TEACh 정제 조건). VerifyLLM(2507.05118, IROS 2025, MED)은 실행 전에 계획의 빠진 전제 조건을 형식 검사했다. 다른 계열 검증이 자기 검증보다 낫다는 결과도 있다(2512.02304, NYU, ICLR 2026 워크숍, MED: "verification across model families is more effective than either self-verification or verification within the same family"). JEV-Star(§24)는 계획 요청이 실패하거나 늦으면 "the previous valid plan available"로 이전 계획을 유지했다.
  - 우리: Astra를 effort low로 1회 부른다 → 검사기 1–6에 **실행 기반 전제·효과 검사**를 더한다(단계 i의 전제 술어를 현재 측정 상태 또는 앞 단계 `expected_after`로 코드 평가, 연쇄 일관성, 금지 술어) → 실패하면 위반 목록을 붙여 **순차 수리 재호출**(최대 2회 [가정], 선행의 "≤3회 96.5%"보다 보수적). T0(정지 허용)에서는 통과할 때까지 기다린다. 비정지 호출(T_fail·T3)에서는 검사 실패 시 **이전 유효 계약을 유지**하고 수리는 백그라운드로 한다(로봇은 계속 — 사용자 원칙 불변). 다른 계열 검증(Jev typed 질문)은 E-M2 비교 조건으로만 둔다(Jev와 Astra가 다른 계열인지 미확인).
  - 왜 다수결이 아닌가: 같은 모델 병렬 다수결은 2025–26 문헌에서 비교 기준선이다(CISC·DeepConf가 majority vote를 기준선으로 둠). 같은 관측 반복은 증거를 늘리지 않는다(PACT, §24). 확신 가중 투표는 logprob가 필요해 Astra에 쓸 수 없다.
  - Astra가 말로 한 확신은 계약에 **기록만** 한다(추론 모델의 말로 한 확신이 더 잘 보정된다는 결과 2505.14489 NeurIPS 2025 HIGH; 에이전트 수준에서는 이득이 없다는 반대 결과는 MED-LOW라 참고만). 게이트로 쓰지 않는다.

- **A4′ 재계획 형식은 적응형 `patch | replace`로 한다.**
  - 선행: Plan-and-Act(2503.09572, ICML 2025, UC Berkeley)는 매 스텝 "the Planner takes in the current state as well as the previous plans and actions and generates a new plan"으로 이전 계획을 입력에 넣고 다시 생성해, 동적 재계획으로 WebArena-Lite 29.63% → 53.94%를 얻었다(요약 도구 경유 수치). π0.5(2504.16054)는 추론 스텝마다 하위 과제를 다시 예측한다. 코드 편집에서는 SWE-Edit(2604.26102, Microsoft)이 "adaptive find-replace/whole-file-rewrite policy improves edit success by 12.5 pp"를, AdaEdit(2604.27296, Findings ACL 2026)가 전체 생성과 같은 정확도에 지연·비용 30% 이상 감소를 보고했다. patch 전용을 표준으로 쓰는 강한 로봇·에이전트 논문은 찾지 못했다(D18 §3).
  - 우리: 출력 `mode ∈ {patch, replace}`. 기본은 `patch`(변경이 국소적일 때 지연·토큰 이득과 약속 보존 — AgenticCache MLSys 2026의 "plan locality"). 다음이면 `replace`로 강제한다 [가정 규칙]: (i) 실패가 깨뜨린 전제 술어에 기대는 미실행 단계가 절반 초과, (ii) 목표·물체 역할 변경, (iii) patch가 A5′ 검사를 통과하지 못함. `replace`도 이전 계약을 입력에 넣는다(Plan-and-Act식). 두 경우 모두 이미 실행한 단계는 바꾸지 않고, `change_reason`·편집 거리를 기록한다. E-M2에 {patch 전용, 이전 계약 넣고 전체 재생성, 적응형} 비교를 둔다(지표: 성공률, 출력 토큰, 첫 토큰~완료 지연, 약속 보존율).

- **J5 Jev conformal 게이트를 E1 뒤 켤 후보로 채택한다.**
  - 선행: KnowNo(2307.01928, CoRL 2023 Oral, 기간 밖 기초 문헌)는 LLM 객관식 계획을 conformal 예측 집합으로 바꿔, 집합이 하나면 실행하고 아니면 도움을 요청했다. CoFineLLM(2511.06575, WashU, MED)은 같은 틀에서 "wrapping LLM outputs into prediction sets that contain the correct action with a user-defined confidence. When the prediction set is a singleton, the planner executes that action; otherwise, it requests help"로 계획 전체 커버리지를 단계 집합의 곱으로 보장했다. Trust or Escalate(2407.18370, 기간 밖 기초 문헌, 학회 표기 abs 미확인)는 싼 판정기를 먼저 쓰고 필요할 때만 강한 모델로 올려 "over 80% human agreement with almost 80% test coverage"를 보장했다.
  - 우리: E1에서 `question_id@vN`별 보정 집합으로 문턱을 잡는다. 예측 집합이 원소 하나면 M4 확정 후보, 집합이 둘 이상이거나 `NONE_ESCALATE`를 포함하면 확정을 보류(직전 확정 행동 유지 + 감속, §4)하고, 같은 결정 지점에서 반복되면 Astra로 올린다(싼 Jev → 비싼 Astra 계단은 Trust or Escalate와 같은 모양). 보장은 단계별 주변 커버리지다(에피소드 수준 아님). conformal은 교환 가능성을 전제하므로 §28의 버전 묶기(`question_id@vN` × 모델 ID)와 매일 카나리로 그 조건을 지킨다. E1 전에는 §6대로 게이트를 끄고 최빈 선택만 쓴다. 보정 집합 크기는 [가정](선행: CoFineLLM 400개).
- M7 소프트 채널 문턱은 성공 에피소드로 conformal 보정한다(FIPER 2510.09459 NeurIPS 2025 HIGH: 성공 롤아웃만으로 오경보 확률 ≤ δ) — 기존 CD11과 같은 방향.
- (덮는 규칙) A5′의 비정지 호출 백그라운드 수리는 §14-4 "수리 왕복은 T0에서만"을 덮는다. 비정지 중에는 이전 유효 계약으로 로봇이 계속 움직이므로 정지 원칙과 충돌하지 않는다. 편집자가 §31 반영 때 둔 세부 [가정](J5 비순응 점수 = 1 − 정답 보기 보정 확률, J5 켠 질문군에서 θ 게이트 미중복, E1 판정 8 원소 하나 비율 ≥ 50%, E-M2-4 문턱, M7 δ_c 시작 0.05, T_j5 반복 = 2회 연속)은 E1·E-M2 결과로 확정한다.

## 32. 학습 실행기 B 채택 (2026-09-24 06:12 UTC, user-log 33·34) [사용자 결정]
- 사용자 제안(user-log 33): action expert 같은 학습 모듈, 스킬 실행으로 만든 GT로 학습, "스킬들의 모든 실행이 거기들어가 있어야" 함, "소규모엔드투엔드 학습 중 올바른거". 사용자 결정(user-log 34): **B**.
- B의 뜻: 모든 스킬 실행을 하나의 학습 실행기가 맡고, 인접 모듈 일부(인식 일부·스무딩·예상 상태 출력)를 흡수하는 소규모 end-to-end. **결정 층(Astra 계획, Jev 선택)은 학습하지 않고 바깥에 둔다.** 스크립트 스킬은 GT 생성기이자 백업 실행기가 된다.
- 유지할 것: M1 술어 등록부(Jev 입력과 M7 T1 판정은 코드 계산 그대로), M4 (b)와 M7이 돌 수 있도록 실행기가 예상 상태(`expected_after`·`ref(t)`)를 함께 낸다, 평가 공정성 — 같은 실행기를 모든 결정 층 조건에 똑같이 붙인다(C2 유지).
- 로컬 학습 모델 허용 여부(옛 결정 항목 D9)는 이 결정으로 **허용**.
- 세부 설계(입력·출력·흡수 범위·데이터·크기·지연·백업 규칙·실험)는 D19 조사 근거로 채운다(§33 예정).

## 33. 학습 실행기 B 세부 설계 (2026-09-24 06:30 UTC, `D19-action-expert.md`, §32)
- **근거 요약**: 물체 crop과 스킬 문장을 조건으로 받는 DiT 행동 익스퍼트(HiVLA 2604.14125, MED)는 스킬 조건을 빼면 Hard 과제에서 8.8% 떨어졌다. 스크립트 스킬 조합으로 만든 성공 궤적만으로 학습한 모델이 공식 π0와 맞먹었다(InternData-A1 2511.16651, MED: "18 skills, 70 tasks", "matches the official π0 across 49 simulation tasks"). 반대로 깨끗한 시연만으로 학습하면 무작위 장면에서 크게 떨어진다(RoboTwin 2.0 2506.18088, ICML 2026: π0 46.4 → 16.3, "data without domain randomization does not help"). 교사 궤적만 모방하면 스스로 실수를 못 고친다(VIRAL 2511.15200: "fails to correct its own mistakes", 학생 롤아웃 섞기로 개선).
- **위치**: M6 스킬 FSM의 "phase 실행" 자리. FSM 전이는 계속 코드 술어 사건이 결정한다(익스퍼트는 전이를 정하지 않는다).
- **입력**: `skill_id`·`phase_id`, 확정된 Jev `option_key`, M3 코드 `target`, 공유 M1 앞단이 만든 대상·관련 물체의 crop + 마스크(손목·머리 카메라; 전체 장면 원본은 넣지 않음 [가정]), 고유수용 상태, 짧은 행동 이력.
- **출력**: 행동 청크(0.5 s 분량을 100 Hz로 보간, 약 10 Hz로 생성 [가정]) + 보조 머리 셋 — (i) phase 진행도 → M7 `C_stag` 보조 소프트 채널, (ii) phase 끝 술어 예측 → 보조 채널만, (iii) flow 샘플 분산 → OOD 신호(conformal 보정).
- **흡수하는 것**: M5 L2 블렌딩 → RTC 인페인팅(RTC는 학습형 flow 정책용, 재학습 없이 적용), M1의 접촉 근처 세밀 기하 일부(crop). **코드로 남는 것**: M1 술어 등록부(Jev 입력·M7 T1), M3 `expected_after`(실행기와 무관한 목표 술어 — S·B 조건이 같음), M4 규칙 전부(`ref(t)` = 확정된 행동 청크), M5 L1·L3, M6 계약·진단 필드(센서로 코드 계산), M7 FAIL 판정, Astra·Jev.
- **폴백**: 샘플 분산이 보정 문턱 초과 또는 M4 (b) DEVIATE 2회 연속 [가정]이면 그 phase를 스크립트 스킬로 넘긴다(청크 경계, M5 관성화). 폴백은 FAIL이 아니며 Astra 호출 규칙은 그대로, 폴백 비율은 모든 표에 기록.
- **데이터(스킬 × phase × 보기마다)**: ① standard 장면에서 스크립트 스킬 실행, 성공 에피소드만(phase 경계·id·`option_key`·target 기록) ② 섭동 주입(물체 자세·시작 자세·실행 중 밀기) ③ DAgger — 익스퍼트 롤아웃 상태에서 스크립트 스킬이 라벨(α 0.5 시작, VIRAL 값 [가정]) ④ 복구 구간은 섞어서만. random 장면은 시험 전용이므로 학습 데이터는 standard에서만 만든다. 도메인 무작위화를 쓰면 B3c 등 모든 학습 조건에 같은 데이터를 준다.
- **커버리지 지표**: (skill, phase, option_key) 칸 중 성공 에피소드 ≥ N이고 칸별 폐루프 성공률이 스크립트 대비 −2%p 이내인 비율 [제안].
- **크기·지연 [가정]**: 동결 소형 시각 인코더(Knowledge Insulating 방향) + 약 50–100M flow DiT, 청크당 ≤ 30 ms 목표(참고: π0.5 익스퍼트 300M, SmolVLA 전체 0.45B).
- **평가 공정성(C2)**: 결정 층 비교 표 안에서는 실행기를 모든 조건에 똑같이 둔다. **사전 등록 주 표는 스크립트 실행기 S로 유지**하고 B 실행기 표를 병기한다(Claude 결정 [잠정] — 학습 실행기가 random에서 무너질 위험(RoboTwin 2.0·LIBERO-Plus) 때문에 핵심 주장을 실행기 효과와 섞지 않기 위함). 실행기 단독 낙폭(오라클 결정 + 실행기)을 따로 잰다.
- **반대 증거(기록)**: π0.5는 학습된 저수준 위에 zero-shot GPT-4를 상위로 두면 "attains the worst performance"(새 집 환경), Hi Robot도 GPT-4o보다 지시 정확도가 40% 이상 높았다. 우리 typed 인터페이스(보기 목록 + 코드 목표)가 이 차이를 줄이는지 E-AE-2·E-AE-4로 확인한다. "증류 정책이 원래 스크립트 스킬을 넘었다"는 강한 근거는 찾지 못했다 → B의 이득(세밀 보정·연속성·지연 흡수)은 가설.
- **실험**: E-AE-0 오프라인 모방 오차·커버리지·데이터 규모 곡선(스킬당 {100, 300, 1k, 3k} [가정], BC / +DAgger / +섭동) / E-AE-1 보기 준수(`option_key`만 바꿨을 때 궤적이 기대 방향으로 바뀌는 비율 — LIBERO-Plus "ignore language" 위험) / E-AE-2 결정 층 고정, 실행기 {S, A, B, B−폴백} × {standard, random} / E-AE-3 실행기 단독 낙폭 / E-AE-4 결정 층 {LLM, 규칙, B3c} × 실행기 {S, B} 상호작용(부호가 같을 때만 "실행기와 무관") / E-AE-5 M5 L2 대 RTC 인페인팅.

## 34. D35·D36 결정과 expert 역할 조사 (2026-09-24 07:03 UTC, user-log 35) [사용자 결정]
- **D35 = 도메인 무작위화 넣는다.** 학습 실행기 데이터에 도메인 무작위화를 넣고, 같은 무작위화 데이터를 모든 학습 조건(B 실행기, B3c 등)에 똑같이 준다(§33 공정성). random 시험 장면은 여전히 학습에 쓰지 않는다.
- **D36 = 주 표 실행기는 스크립트 스킬 S.** 단, 사용자 요청대로 action expert가 주 시스템 안에서 역할을 더할 방법을 찾는다 → 후보: (가) 스크립트 스킬 위 잔차 보정, (나) 실패 뒤 아래 층 복구(L1·L2) 담당, (다) 예상 상태·진행도 예측기로 M4 (b)·M7 보조, (라) Jev 보기 후보 생성. 근거 조사(D20) 뒤 §35에서 정한다.

## 35. action expert 역할 = 구간 제한 잔차 R (2026-09-24 07:16 UTC, `D20-expert-role.md`, user-log 35·36)
- **사용자 제약**: Jev는 "앞으로, 뒤로 좌우 몇 센티를 판단하는 역할"(user-log 36) — 방향과 크기 구간의 권위는 Jev에 있다. 주 표 실행기는 스크립트 스킬 S(§34).
- **주 역할 = 구간 제한 잔차 R** (Claude 결정, 근거 D20):
  - 선행: 잔차 학습은 베이스가 무엇이든 붙는다 — 손으로 설계한 제어기 위(Residual Policy Learning, 기간 밖 기초 문헌: "RPL consistently and substantially improves on the initial controllers"), BC 베이스 위(ResFiT 2509.19301, MED-HIGH: 실물 휴머노이드 "boosted the performance of the base model from 14% to 64%"), 물체 상태 입력 잔차(Object-Centric Residual RL 2606.18953, MED: sim→real "improves the success rate from 42% to 76% zero-shot", 이미지 입력 47/100 대 물체 자세 76/100, "the visual domain gap is the dominant sim-to-real barrier"), 힘 입력 잔차(CR-DAgger 2506.16685, NeurIPS 2025: "both the delta correction data and the force data are crucial"). 잔차는 제한해야 한다(Policy Decorator, 기간 밖 기초 문헌·ICLR 2025: "Bounded residual action is essential"; PLD 2511.00091, ICLR 2026: "bounded within a range of [−ξ, ξ]").
  - 우리: S가 명령을 내고, R은 접근·접촉 근처(목표 5 cm 이내 또는 contact 술어 참)에서만 작은 병진 잔차를 더한다. 입력 = `skill_id`·`phase_id`, Jev 확정 방향 d̂·크기 구간 [m_lo, m_hi], S 명령, M1 대상 자세·불확실도, 관절·그리퍼·힘/토크, 짧은 이력(**이미지 없음** — 외관 변화는 M1을 거쳐서만 들어가 S와 같은 경로). **코드 투영으로 Jev 권위를 지킨다**: 누적 이동의 d̂ 성분은 [m_lo, m_hi] 안, 방향 반전 불가, 수직 성분 |r_⊥| ≤ ε_⊥(M1 위치 오차의 conformal 분위 [가정]), 마지막은 M5 L3 Ruckig. 원문의 제한은 학습 안정성용이고, Jev 권위 보호에 쓰는 것은 우리 접목이다.
  - 데이터: 라벨 = 특권 시뮬 상태로 돌린 S 행동 − M1 추정 상태로 돌린 S 행동(투영 후) [접목]; standard + 도메인 무작위화(§34) + 섭동 + DAgger; 선택 2단계로 T1 phase-exit 술어를 희소 보상으로 한 잔차 RL(ResFiT·PLD식) [제안].
  - 기록: `jev_choice`(권위값), ∫r, 포화 비율, R on/off. M4 (b)의 `ref(t)` = 투영 뒤 실제 명령, `expected_after`는 그대로.
- **보조 역할 = 그림자 예측기**: 익스퍼트를 실행하지 않고 옆에서 돌려 진행도·phase 끝 확률·OOD 점수를 M7 소프트 채널 **후보**로 넣는다(SAFE·FIPER, NeurIPS 2025식 conformal 보정). 실행하지 않는 모델의 특징이 S 실패를 예측한다는 직접 선례는 없다 → [가정], E-M7 조건 D-AE로 **우리 시스템 내부 절제로만** 평가(Claude 결정: 교차 비교 표에는 넣지 않음). M4 (b)에는 넣지 않는다.
- **흡수·제외**: 학습 복구 실행기(나)는 단독 채택하지 않는다 — "학습 복구가 스크립트 재시도를 이긴다"는 강한 근거가 없고(FAR 참고: 단순 재시도 +12.1%p, 학습 추가분 +4.3%p, MED-LOW), 복구의 "무엇을"은 Jev·Astra·M9가 정한다. 복구 동작 실행 때도 R이 같은 제한으로 켜진다. 후보 생성(라)은 Jev 역할을 바꾸므로 제외. 학습 추종 실행기(마)는 §33 B와 같고, 선행이 보인 "명령의 맥락 재해석"(Steerable Policies 2602.13193: "servos left towards an object, rather than moving left unconditionally")이 Jev 크기 권위를 넘을 위험이 있어 주 표에 넣지 않는다(E-AE-2의 B 표로만).
- **공정성**: R은 실행기 부품이라 모든 결정 층 조건에 똑같이 붙인다. B3c가 연속 행동을 내면 같은 방향·크기 격자로 양자화해 R을 붙인다(Claude 결정). 주 표는 S, S+R은 병기 표·절제.
- **실험**: E-R0 오프라인 라벨 포화율 / E-R1 결정 층 고정, 실행기 {S, S+R-state, S+R-crop, S+R-무제한} × {standard, random} 짝(성공률, 최종 정렬 오차 mm, 접촉 phase 성공률, Jev 권위 위반율, 포화율, jerk, FAIL·Astra 호출 수) / E-R2 오라클 결정 + {S, S+R} 낙폭 / E-R3 결정 층 {LLM, 규칙, B3c(양자화)} × {S, S+R} 상호작용 / E-R4 R 끔·켬 짝과 phase별 절제. S+R을 병기 표로 올리는 조건: standard 성공률 짝 차 95% 하한 > 0 그리고 RD(S+R) − RD(S) 95% 상한 ≤ +δ [가정].
- 확인 못 한 것: 스크립트 베이스 위 2025–26 잔차 결과(기초 문헌뿐), 그림자 예측기 직접 선례, R 지연 실측.

## 36. 로봇·카메라 결정 (2026-09-24 07:19 UTC, user-log 37) [사용자 결정]
- **로봇 = ROBOTIS AI Worker 기준. 카메라 = ZED 스테레오 카메라(AI Worker가 쓰는 것과 같게).** 스테레오라 깊이 추정이 가능하고 이미지도 그대로 쓴다.
- 해소: 카메라 구성(D2c) = 스테레오. M1 인식 앞단은 스테레오 깊이 경로(원래 후보 "스테레오면 스테레오 깊이 추정")로 간다. 실물 기준 플랫폼(D30)은 AI Worker.
- 확인할 것(D21 조사): AI Worker 사양(팔 자유도·그리퍼·손목 카메라·제어 주기·ROS 2·LeRobot 연동), 쓰는 ZED 모델과 깊이 방식, 시뮬 모델 유무(Isaac Sim·MuJoCo 등), 본 평가 벤치마크(RoboDojo)와 로봇이 다를 때의 정합.

## 37. AI Worker·ZED 반영 (2026-09-24 07:28 UTC, `D21-aiworker-zed.md`, §36)
- **확인된 사양**(공식 사양 페이지·ai_worker 저장소): 팔 7자유도 × 2, 그리퍼 RH-P12-RN(1자유도 평행, 0–107.6 mm), 머리 2자유도(pitch −50°~30°, yaw −20°~20°), 리프트 0–500 mm, 머리 ZED Mini(102°×57°, 기선 63 mm), 손목 RealSense D405 × 2(7–50 cm), Jetson AGX Orin 32GB, ROS 2 Jazzy, 팔마다 100 Hz JointTrajectoryController(위치 명령), 상태에 관절 `effort`(전류 기반). **손목 힘/토크 센서 없음.** 기본 ZED 설정은 **깊이 끔**(`depth_mode: 'NONE'`), VGA 30 fps.
- **M1 인식 앞단** [Claude 결정]: 기본 설정의 깊이 끔을 바꾼다. 머리 깊이 1순위 = ZED SDK NEURAL(Orin 공식 30 FPS 수치는 ZED X 기준, ZED Mini 실측 필요), 비교 조건 = Fast-FoundationStereo(CVPR 2026, NVIDIA, 1,497★; ZED SDK `CUSTOM` 모드로 같은 경로 주입). 손목은 D405 센서 깊이(근거리, 접촉 근처 담당). 기록은 SVO(원시 스테레오)로 남겨 깊이 모드를 나중에 바꿔 비교한다. 오차 추정(우리 계산): VGA에서 0.6 m 약 5 mm, 1.0 m 약 15 mm → T1 술어 문턱(cm 단위)에 대체로 충분.
- **지금 로봇 없이 할 수 있는 시험** [제안]: ROBOTIS 공개 데이터(HF `ROBOTIS/Task_0001` 718편·`Task_0002` 857편)에 머리 좌우 스테레오 쌍이 있다 → Fast-FS → SAM 3.1 → 3D 중심 → 술어의 프레임 간 **안정성**을 잰다(정답 자세가 없어 정확도는 못 잼, 보정값 [미확인]).
- **Astra 격자**: ZED 왼쪽 영상만 격자로(카메라별 한 장, M8 규칙 그대로), 깊이 컬러맵은 절제 조건. 손목 격자는 3×2 이하 또는 현재 프레임 1장. 머리가 움직이므로 칸 덧그림에 머리 각을 표시하고 M6에 `look_at` 스킬을 둔다.
- **M5 한계**: 100 Hz 위치 명령 → DYNAMIXEL 내부 프로파일(서보가 한 번 더 평활화할 수 있음 [추정]). 실물에서 계단 응답을 재고, 내부 프로파일을 끄거나 그 지연을 `ref(t)`에 넣는다(M4 (b) 가짜 LAG 방지). URDF 속도 한계(4.8 rad/s 일괄)는 자리표시로 보여 쓰지 않고 실측·서보 사양의 50%로 시작 [가정]. 로봇별 한계표를 두 벌(ARX X5, AI Worker) 둔다.
- **스킬(양팔)**: 기존 pick/place FSM·`arms: left|right|both` 결정 지점 유지, 추가 `look_at`(머리 2축), `set_lift`, `handover`, `bimanual_hold`. 이동 베이스는 범위 밖.
- **잔차 R의 힘 입력**: 손목 F/T가 없으므로 "힘/토크" → **관절 `effort`(전류)와 그리퍼 전류**로 바꾼다. CR-DAgger 근거는 손목 F/T 기준이라 근거가 한 단계 약해짐을 표기하고, 절제 조건 R-noforce를 추가한다.
- **GT 생성 시뮬** [Claude 결정]: 공식 Isaac 모델(cyclo_lab, Isaac Sim 5.1 / Isaac Lab 2.3 — RoboDojo와 같은 판본)이 있다 → **E0–E3, E-M4, E-R, E-AE의 "단일 팔 자작 장면"을 FFW-BG2 한 팔(7자유도 + RH-P12-RN)로 고정**한다. 카메라는 Stereolabs ZED Isaac Sim 확장의 `ZED_M` 디지털 쌍둥이(오라클 = 렌더러 GT 깊이, 인식 조건 = 스트리밍 경로의 ZED SDK 깊이 또는 Fast-FS). 데이터 경로 cyclo_lab Mimic → `isaaclab2lerobot.py` → LeRobot, 도메인 무작위화는 우리가 추가(§34). 액추에이터 게인은 실물 계단 응답으로 다시 맞춘다 [가정].
- **RoboDojo와의 정합** [Claude 결정, 3층]: (1) 결정 층 비교(H2·H3, 낙폭 RD)는 **RoboDojo-Sim ARX X5 그대로**(공개 수치와 같은 열). 보조로 "트랙 D-stereo"(머리 카메라 옆 63 mm 가상 카메라로 스테레오 쌍을 렌더해 AI Worker와 같은 Fast-FS/M1 경로, "입력 다름" 표기). (2) 개발·GT·모듈 실험 = cyclo_lab FFW-BG2 + ZED_M 쌍둥이. (3) E-real = 실물 AI Worker(최소판, 제출 2026-11-16까지 약 7주). 결정 층·술어 등록부·Jev/Astra 계약은 두 로봇이 같은 코드, 스킬·M5 한계·R은 로봇별.
- **[결정 필요] (사용자, 하드웨어)**: (c) 실물을 FFW-BG2(고정 베이스, 국내 판매)로 할지 FFW-SG2(이동 베이스를 고정해 사용)로 할지. (d) R의 힘 입력 — 관절 전류로 갈지(기본값, 추가 장비 없음) 손목 F/T 센서를 추가 장착할지(기계 인터페이스 [미확인]).

## 38. 실물 = FFW-SG2 (2026-09-24 07:34 UTC, user-log 38) [사용자 결정]
- §37 (c) 해소: 실물 로봇은 **FFW-SG2**(스워브 이동 베이스, 총 25자유도). 이동 베이스는 범위 밖으로 두고 **베이스를 고정**해 팔 7자유도 × 2, 그리퍼, 머리 2자유도, 리프트만 쓴다.
- 시뮬도 가능하면 SG2 모델(cyclo_lab `FFW_SG2.usd`, `Cyclo-Real-Pick-Place-FFW-SG2-v0` Sim2Real 과제)로 맞춘다. 개발 장면은 한 팔 설정을 유지한다.
- 남은 결정: (d) 잔차 R의 힘 입력 — 관절 전류(기본안) 대 손목 F/T 추가.

## 39. 힘 입력 = AI Worker 기본 구성 (2026-09-24 07:34 UTC, user-log 39) [사용자 결정]
- §37 (d) 해소: 손목 F/T 센서를 추가하지 않는다. 잔차 R의 힘 입력은 **관절 `effort`(전류 기반)와 그리퍼 전류**. 절제 조건 R-noforce 유지. 남은 하드웨어 결정 없음.

## 40. Inspect Robots 판정 (2026-09-24 07:46 UTC, `D22-inspect-robots.md`, user-log 40)
- 정체: Robocurve의 오픈소스 로봇 평가 하네스 `robocurve/inspect-robots`(MIT, 600★, 2026-06-26 생성, 논문 없음). UK AISI Inspect의 구조를 로봇으로 옮긴 것("this is that for robotics"). LLM 정책(`agent`, 도구 호출로 `move_to`/`move_by` 등)·CaP-X식·VLA(XPolicyLab)를 여러 로봇·Isaac Lab에서 같은 로그로 돌린다. 루프는 **동기**(시뮬이 LLM을 기다림, "no wall-clock pacing of its own").
- **선행 겹침 없음**: 빠른 typed 결정(Jev), 겹침 호출 확정(M4), 실패가 부르는 비동기 Astra, 같은 인식 비교가 없다 → C1·C2 판단 그대로(§24). 관련 연구에 소프트웨어로 한 줄 인용한다.
- **근거로 쓰지 않는 것**: Robocurve의 Astra 실물 수치(보고서 조건: medium effort·20호출·25% 속도, 과제당 20회, 모델을 아는 사람 채점 — 원문이 편향 가능성을 밝힘)는 신뢰도 규칙(user-log 14)상 LOW → 본문 근거로 쓰지 않는다.
- **주 평가(RoboDojo)에는 쓰지 않는다**: RoboDojo 몸체가 없고, 동기 루프라 우리 비동기·벽시계 조건을 표현할 수 없고, 공개 수치와의 같은 기준이 깨진다.
- **도구로서**: E-real 로그 필드 설계(설정·git 리비전·판본·transcript·사람 판정 출처, Rerun 기록)를 참고한다(의존성 추가는 안 함).
- **[결정 필요] (사용자)**: E-real(AI Worker SG2)에 범주 1 기준선 "B1a-real = Inspect Robots `agent` + Astra"를 넣을지. 넣으려면 ROS 2 몸체(`joint_pos`만 지원)와 AI Worker 관절 매핑을 먼저 점검해야 하고, 행동 공간이 Robocurve 보고서(eef_pos)와 달라진다.

## 41. 1차 평가 = Inspect Robots (2026-09-24 07:51 UTC, user-log 41) [사용자 결정]
- **1차 평가는 Inspect Robots에서 한다**(빠르고 쉽게 돌릴 수 있게 잘 만들어져 있음). 향후 더 큰 평가(RoboDojo 등)로 넓힌다. §40의 "주 평가 부적합" 판단은 이 결정으로 **1차에 한해 바뀐다**(RoboDojo는 2차 확장).
- 이점: 같은 하네스·같은 로그로 기준선을 바로 붙일 수 있다 — 범주 1 Astra만(`agent`), 범주 5 CaP-X식(`capx`), VLA(`xpolicylab`, π0.5·GR00T 등 40여 종).
- 풀어야 할 점: 하네스 루프는 동기(시뮬이 정책을 기다림)이고 우리 시스템은 비동기·비정지다. 설계 문서의 "an embodiment that needs real-time cadence paces itself inside step()"을 이용해 몸체 쪽에서 벽시계 박자를 맞추고, 정책 안에서 Astra를 비동기로 돌리는 방식을 검토한다(D23 코드 조사).
- 몸체: 1차는 Isaac Lab(하네스 기본 Franka 7자유도 → AI Worker SG2 한 팔로 매핑 훅), 실물은 ROS 2 몸체로 AI Worker SG2(베이스 고정).
- §40의 [결정 필요] B1a-real은 이 결정으로 해소 — Inspect Robots `agent` + Astra를 1차 평가의 범주 1 기준선으로 쓴다.

## 42. Inspect Robots 통합 설계 (2026-09-24 08:05 UTC, `D23-inspect-robots-integration.md`, §41)
- **코드 확인**(메인 재확인, v0.59.0 / 커밋 `7e506e3`): 롤아웃은 단일 스레드 동기 루프("no wall-clock pacing of its own", `rollout.py:276-280`). Policy = `info, config, reset(scene), act(observation) -> ActionChunk`. `derive_seed(eval_seed, scene_seed, epoch)`는 scene.id를 쓰지 않는다 → 같은 `init_seed`로 std/rnd 짝 장면이 된다. 기본 isaacsim 몸체는 행동 Box에 경계가 없고(`embodiment.py:215`), agent 기준선은 "finite low and high bounds"를 요구한다(`_tools.py:632`) → **그대로 못 쓴다**.
- **시계 세 가지, 같은 정책 코드에 시계만 주입**: sync(세계가 정책을 기다림), simlat(시뮬 시각 t에 보낸 응답을 t + 실측 지연에 전달), wall(몸체가 self_paced로 벽시계에 맞춤 — 실물 또는 RTF ≥ 1일 때만). 스모크(`D:/tools/audit_d23/smoke/smoke_async.py`, 장난감 몸체)에서 세 모드 동작, simlat ≈ wall, 하네스 오버헤드 약 80 µs/스텝. §41의 "몸체 쪽 벽시계 박자"는 ZED_M 렌더로 RTF < 1이면 LLM이 빨라 보이게 되므로 **시뮬에서는 simlat로 대체**하고 RTF를 기록한다.
- **주 표 = 지연 충실(latency-faithful) 트랙** [Claude 결정]: 우리 = simlat, 동기 기준선(agent·capx) = `LatencyChargingController`(act() 시간 L만큼 "직전 명령 유지" 행동 ceil(L×hz)개를 앞에 붙임 — 실물에서 생각하는 동안 팔이 서 있는 것과 같음; agent README "the arm stands still while the model thinks"). 근거: 우리 주장(비정지·실시간 지연 흡수)은 지연이 공짜인 조건에서는 정의되지 않는다. **sync-fair 트랙을 같은 표에 병기**해 "생각 시간 공짜" 조건에서의 결과도 숨기지 않는다.
- **정보 동등** [Claude 결정]: agent는 `obs.state`의 모든 키를 프롬프트에 넣는다(`policy.py:1201-1219`). → 오라클 물체 자세는 우리 M1이 오라클일 때(E0 오라클 조건)에만 state에 넣고, 인식 조건에서는 두 쪽 모두 넣지 않는다(C2 "같은 인식" 원칙).
- **`aiworker` 몸체**(새로 작성): FFW-SG2 한 팔 joint_pos 8-D(7 + 그리퍼 연속), 유한 경계·`dim_labels`, decimation 1, 카메라는 새 프레임일 때만, 깊이·내부행렬은 extra callable, `extra["sim_time"]`, 성공 술어, RTF 기록, 실물은 `ChainApprover(Clamp, DeltaLimit)` 직접 연결(Python eval 기본은 AutoApprover). 실물 100 Hz 경로는 rclpy 네이티브, rosbridge는 30–50 Hz 기준선용 [가정].
- **`OursPolicy` 어댑터**: act()는 비블로킹(현재 확정 행동 + 스킬 100 Hz 한 틱, 청크 길이 1), Jev 겹침 호출·Astra T_fail은 스레드 풀, M4 원장·M7은 act() 안에서 O(ms), T0 계획만 블로킹 허용, `DefaultController(replan_interval=1)`. 정책 핵심은 하네스 중립 모듈 + 얇은 어댑터 둘(Inspect Robots Policy, 2차 RoboDojo용 XPolicyLab 서버).
- **기준선**: B1a-IR = agent + Astra(충실판 effort=medium·max_llm_calls=20·max_speed_frac=0.25 [대응 미확인] / 공정판 effort low·high, 호출 예산 우리와 같게), B5-IR = capx(SAM3·Contact-GraspNet·Pyroki 서버, 컨테이너 격리), B3b-IR = xpolicylab π0.5(`action_type=joint arms=1 arm_dim=7 ee_dim=1`, AI Worker 한 팔 미세조정 필요 — cyclo_lab Mimic → LeRobot, std+DR).
- **로그**: 설정 dataclass를 `policy.config`로(astra_prompt_id, question_id@vN, 모델 ID, effort, T_c, 시계 모드, M4 파라미터), 트라이얼별 부가 JSONL(M4 표·option_key·epoch·요청 해시·카나리 id) 경로를 `record.metadata` → `trial_metadata`.
- **병렬**: 하네스가 순차이므로 환경 동시 실행 대신 프로세스·파드 분할.
- **단계** [가정: 공수]: P0 설치·스모크(0.5–1일, 태그 고정) → P1 aiworker 몸체(4–7일) → P2 OursPolicy + LatencyChargingController + 로그(5–8일) → P3 기준선(3–6일, π0.5 학습 별도) → P4 짝 파일럿 layout 0–4 × {std, rnd} × epoch 3 × 두 시계 트랙(3–5일).
- **위험**: alpha API(마이너마다 깨질 수 있음 → 판본 고정·사본 보관), 관리자 1인 의존(커밋 355/515), Isaac Lab 2.3 호환 [미확인], H200 RT 코어 없음(렌더), 프레임 메모리 누적, 외부 라이선스 [미확인].

## 43. 카메라 = AI Worker 기본 카메라 그대로 (2026-09-24 09:02 UTC, user-log 44) [사용자 결정]
- **시뮬·실물 모두 AI Worker에 기본으로 달린 카메라(머리캠, 손목캠)를 그대로 쓴다. 카메라를 추가·교체·이동하지 않는다.** §37의 "Stereolabs ZED Isaac Sim 확장 `ZED_M` 디지털 쌍둥이를 붙인다"와 D-stereo 보조 트랙의 "가상 카메라 추가"는 이 결정으로 **폐기**한다.
- 시뮬(cyclo_lab 모델): 모델에 정의된 기본 카메라 prim만 쓴다. 오라클 깊이가 필요하면 그 카메라의 렌더러 깊이 출력만 켠다(카메라 prim은 바꾸지 않음). 기본 카메라의 목록·해상도·화각·머리 좌우 쌍 여부는 T11에서 cyclo_lab 설정을 읽어 `docs/stage3/results/scene_bringup.md`에 적는다.
- 실물: §36·§39대로 기본 구성(머리 ZED Mini, 손목 D405 × 2). 깊이 방식(§37: ZED NEURAL 1순위, Fast-FS 비교)은 기본 카메라 위의 처리라 유지한다. 단, 시뮬 기본 머리캠이 단안이면 시뮬 인식 조건의 깊이는 그 기본 출력에서 얻을 수 있는 것으로 한정하고, 실물과 입력이 달라지는 점을 결과에 표기한다.
- 영향: 계획 T11 Step 2 취소·대체, E-first §1.4 장면 줄의 `ZED_M` 표기, EVAL D-stereo 보조 트랙은 편집 때 폐기 표시.

## 44. Jev 대체 = 로컬 VLM typed 선택기 (2026-09-24 09:55 UTC, `D24-jev-replacement.md`, user-log 46) [Claude 결정, 사전 시험으로 확정]
- **사용자 판단**(user-log 46): Jev는 쓸 수 없고, 텍스트 전용 API는 우리 구조에 부족할 수 있다 → 대체한다.
- **1순위 [Claude 결정, 근거 D24]**: "Jev-L" — H200에서 vLLM(92.6k★, `VLLM_BATCH_INVARIANT=1` 배치 불변 모드·접두 캐시)으로 **Qwen3-VL-8B-Instruct**(2025-10, HF 좋아요 1.1k·다운로드 1,970만, 기술 보고서 2511.21631)를 돌려 typed 선택기로 쓴다. 입력 = M1 텍스트 상태 + 머리캠 1장, 질문 형식은 기존 `JevCall`을 그대로(이름만 `DecCall`). 생성 없이(max_tokens 1) 보기 토큰의 로그 확률을 읽어 **보기 집합 안에서 재정규화** → `p_chosen`/`p_second`. 보기 이름 첫 토큰이 겹치면 보기 문자열 전체 로그 확률.
- **바뀌지 않는 것**: M4 겹침 호출·확정 규칙((a) 합의 + (b) 예상 대 측정), E1 보정, J5 conformal, M7 단일 FAIL, Astra 위층, 스킬 + 잔차 R 아래층. 새로움 주장(M4·평가)도 그대로(호출 대상만 바뀜).
- **2순위(하이브리드)**: 3 Hz 결정은 Jev-L, GPT-6 Luna(effort none, `top_logprobs`, 이미지)는 J5 보류 반복 때 Astra 전 한 단계·저빈도 감시·"빠른 선택기만" 기준선. Luna 단독을 3 Hz에 쓰지 않는 근거 = 메인 실측(한국, 연결 재사용 중앙 1.17 s, 최대 2.91 s; `docs/stage3/results/sol_luna_probe.md`) — E0 판정 3 기준 (d) 구간. Claude API는 logprobs "Ignored"/"Always empty"(공식 호환 문서)라 제외.
- **학습 결정 헤드(C)**는 π0.5 상위 단·Hi Robot·HiVLA와 같은 학습 정책이 되어 사용자 핵심 주장("LLM이 일반화")과 부딪치므로 상한·절제 조건으로만 둔다.
- **확정 절차(사전 시험, D24 §5)**: (1) 지연·결정성 — Qwen3-VL-8B/4B를 GPU 3에서 vLLM으로, 텍스트만 대 텍스트+머리캠, 동시성 1–8, p50/p95, 같은 입력 반복 flip(배치 불변 켬/끔). (2) 정답률·보정 — 스냅샷 풀(T13) 뒤 약 1,500 스냅샷, 결과 기반 라벨, ECE·AUROC, 순서 flip, Luna 300개 부분 표본. 판정 기준은 실행 전 `docs/stage3/prereg.json`에 새 절로 고정.
- **[결정 필요] (사용자)**: (a) cyclo_lab SG2 시뮬 모델에는 손목캠이 없다(D24·T11 확인 중). 실물 기본 구성에는 D405 손목캠이 있으므로, 시뮬에 실물과 같은 위치의 손목캠을 둘지(§43 "추가 금지"의 예외) / 시뮬은 머리캠만 쓸지. (b) 실물 추론 위치 — LAN GPU 서버(권장) / Orin 단독 / 클러스터 H200.
- 영향: 계획(`docs/superpowers/plans/2026-09-24-stage3-experiments.md`)의 E0·E0.5·E1 과제는 Jev-L 기준으로 다시 쓴다(DC0부터, user-log 42 되돌아가기 규칙). T0–T7 도구는 그대로 쓴다(클라이언트만 추가).

## 45. Astra 호출 주기 = 하트비트 + 단계 경계 + 사건 (2026-09-24 10:25 UTC, `D25-planner-cadence.md`, user-log 48) [Claude 결정, 주기 N은 실험으로 확정]
- **사용자 의문**(user-log 48): "첫 계획 + 실패 때만" 부르는 것은 이상하다, 어느 정도 주기가 있어야 한다 → Gemini 방식 조사.
- **원문(메인 재확인, ai.google.dev robotics-streaming)**: Gemini Robotics-ER은 하트비트로 "periodically send the latest camera frame followed by a short text prompt that forces the model to inspect the scene and make an explicit decision", "opportunistically targets a 1 Hz cadence … while waiting for each turn to complete", 하트비트 선택지 = 진행 중이면 `ack`, 단계 완료면 `run_instruction`(다음 단계), 목표 달성이면 `reset`. 타이머만 믿고 보내면 "cancellation loop" 경고. 저수준 VLA는 기다리지 않고 계속 실행("The robot continues executing until you call run_instruction again", 공식 예제). ER 2는 Gemini 3 계열 기반(모델 카드). GR 1.5 보고서: 오케스트레이터가 "success detection to decide when to switch to the next step". 비교: π0.5 매 스텝 하위 과제 예측, Hi Robot 1 s 또는 사용자 개입 때(기간 밖 기초 문헌) — 배포형 시스템은 주기 확인 + 단계 경계 + 사건 + 비동기 실행을 섞는다. "첫 계획 + 실패 때만"은 선행 설계와 맞지 않는다.
- **결정 [Claude, 근거 D25] — H-cadence**: 사용자 원칙(T0만 정지 허용, 실패하면 항상 Astra, effort low)은 그대로 두고 두 호출을 더한다.
  - **T_hb 하트비트**: 직전 Astra 응답 도착 후 N초 뒤 최신 머리캠 프레임 + 원장 요약으로 닫힌 선택 `ack` / `patch` / `replace`를 묻는다. in-flight 1개(겹쳐 보내지 않음, Gemini 경고), 15 s 무응답이면 기록 후 재송신. **잠정 N = 5 s**(Gemini·Hi Robot의 "주기 ≈ 상위 모델 한 번 응답 시간" 원칙 + Astra low 첫 토큰 실측 2.975 s 1회 → 실효 주기 약 8–9 s) [가정].
  - **T_sub 단계 경계**: M7이 단계 완료를 확정하면 "다음 단계 계약이 지금 장면에도 맞나"를 비동기로 묻는다. 다음 단계는 기다리지 않고 시작한다.
  - 기존 사건 호출(C_assume·T3a·T_stag·T_j5·사용자 새 지시)은 다음 하트비트를 앞당긴다. T_fail이 나가면 복구 끝 + 2 s까지 하트비트를 멈춘다.
  - **합치기**: 새 규칙 없음. M2 R5(관측 시각이 더 새 답만), `ack`는 `premise_epoch`를 올리지 않는다(M4 합의 표 보호), `patch`/`replace`는 A5′ 검사 → M2 R3 경계에서 교체 + epoch +1, 실패 시 이전 계약 유지(R7) + 백그라운드 수리. [가정] 같은 단계에서 서로 되돌리는 patch가 30 s 안에 두 번이면 적용 보류, 에피소드당 하트비트 예산 상한.
- **N 확정 실험 E-M8c(사전 등록)**: K0 현행 / K1 + 단계 경계 / K2-N(N ∈ {0, 5, 10, 20} s) / K3 Gemini 하트비트 원문 충실판 / K4 호출 수 맞춘 사건 전용 대조. **M7이 FAIL을 내지 않는 감시기 밖 실패 섭동을 반드시 포함**. 판정: K2-N − K1 ≥ +3%p이고 95% 하한 > 0이면 하트비트 채택, 최고 대비 −2%p 안의 가장 큰 N을 기본으로. 반복·검정력은 §28.
- 이 절은 M8 [결정 필요] D33("주기 + 사건을 넣을지")을 해소한다(사용자 제기 → Claude가 근거로 결정). M8·M9·E-first·EVAL·논문 방법 절은 다음 편집 묶음에서 반영한다.

## 46. 인식 프롬프트 = Astra가 장면을 보고 정한 물체 이름 (2026-09-24 10:51 UTC, user-log 49, E3-ST SAM 3.1 결과) [사용자 결정]
- **근거(E3-ST §7)**: 분할기를 바꿔도(GDINO+SAM 2.1 ↔ SAM 3.1) 정지 흔들림은 거의 같고(T1 1.25 ↔ 1.24 mm 중앙값), 성패는 검출 프롬프트가 가른다 — 과제 문장의 "box"는 흰 바구니를 0/20 검출, "basket"은 19/20. "yellow roller"는 0건, "roller"는 실제 롤러.
- **결정**: M1 인식 앞단(SAM 3.1 문장 프롬프트)의 물체 이름은 과제 문장에서 기계적으로 뽑지 않고, **Astra가 계획(T0·하트비트 `replace`·T_fail)할 때 장면 이미지를 보고 M2 계약 `objects`에 적어 준 검출용 이름(`detect_phrase`)**을 쓴다. 물체 ID(o3 등)와 역할은 지금처럼 계약이 정한다.
- 운영: `detect_phrase` 검출이 0건이거나 추적이 끊기면 M7 소프트 채널(`id_uncertain`)로 올리고 다음 하트비트(§45)를 앞당겨 Astra가 이름을 고치게 한다(정지 없음). 첫 계획(T0, 정지 허용) 때는 검출 성공을 확인한 뒤 실행을 시작한다.
- 영향: M2 계약 `objects` 필드에 `detect_phrase` 추가(A1–A6 검사기에 "각 물체 detect_phrase 비어 있지 않음" 추가), M1 §4 인식 앞단 입력, E3 인식 조건. 다음 편집 묶음에서 반영.

## 47. 시뮬 로봇·카메라 설정 = humanoid-challenge-env 복사 (2026-09-24 11:03 UTC, user-log 50) [사용자 결정]
- **결정**: 시뮬 로봇과 카메라 설정은 `kairobahq/humanoid-challenge-env`(Apache-2.0)의 설정을 **그대로 복사**해 쓴다(`third_party/humanoid_challenge_env/`, 원본 레포는 수정하지 않음). 그 레포의 원칙: 로봇은 시뮬 설정(`FFW_SG2_MOBILE_CFG` — USD·게인·질량·관절 한계) 그대로, **카메라만 실물 사양**(장착 위치·화각·해상도, 출처 ROBOTIS ai_worker URDF·드라이버 설정·제조사 사양).
- 카메라(원문 `scripts/FFW_SG2_REAL_cameras.py`): 머리 = ZED Mini **왼쪽 정류 영상** 672×376, VGA 모드 수평 **85°**(fx 367, 실측 camera_info fx 364.0); 손목 = RealSense D405 컬러 424×240, 수평 87°(fx 223.4, 실측 218.4/217.6), 좌·우 손목(우손목 있음 — user-log 50). §43 "기본 카메라만"과 일치(이 설정이 실물 기본 카메라의 시뮬 복제).
- **정정**: §37·M1의 "ZED Mini 102°×57°"는 센서 최대 화각이며 VGA(672×376) 모드가 아니다(원문: "a maximum-sensor figure that is not the WVGA mode"). E3-ST가 쓴 공칭 fx 272.1(102°)은 틀렸다 → **fx 367로 깊이·3D 중심을 다시 계산**한다(시차 캐시 `/data/juhyoung_qdd/out/e3st_disp` 재사용). 깊이는 fx에 비례하므로 거리·흔들림 mm 값이 약 1.35배 커질 것으로 예상.
- 영향: T11 장면 v2(이식), T13 풀은 v2 장면으로 생성(v1으로는 만들지 않음), E3-ST 재집계, M1·D21 수치 정정은 다음 편집 묶음.

## 48. 시뮬 물리 = CPU PhysX, 라벨 점수식은 사전 등록 규칙으로 선택 (2026-09-24 11:46 UTC, T13·T14 중간 보고) [Claude 결정]
- **물리**: 풀·라벨·폐루프 시뮬 실험은 **CPU PhysX**로 돌린다. 근거(옛 장면 DEV 0–4 P0 시험): 1.3–3.9배 실시간 대 GPU 물리 0.18–0.56배(5–10배 빠름), 5/5 성공, 프로세스당 약 2.5코어. 라벨 롤아웃이 GPU 물리로 스냅샷 1,200개에 프로세스당 약 100 h라 GPU 물리로는 일정 안에 불가. 조건: v2 장면(§47)에서 CPU 물리로 T12 DEV 성공률(P0 ≥ 90 %)을 다시 재서 통과해야 한다. 같은 실험 안에서는 물리 장치를 섞지 않는다(풀·라벨·E0.5·E-M4 모두 같은 장치).
- **라벨 점수식**: 계획서 정의(10 s 안 성공 = 1)는 오라클이 한 스텝 어긋남을 회복해 전 보기 동점 → 판별력 없음(옛 장면 스모크 11/11 질문). 후보 D-plan / D-short(h) / D-time(τ) 중 **v2 DEV 자체 점검 전 사전 등록한 규칙**(`docs/stage3/prereg_labeler.md`: 오라클 ∈ best ≥ 90 % 적격, 적격 중 판별력 최대)으로 고른다.
- 공유 rootfs `/tmp` 항목 증가는 옆 파드(x2)의 다른 프로젝트(`juhyoung_pi05` Task C) Isaac 실행이 원인 — 우리 실행은 `ir_run.sh` 덮어씌우기로 깨끗함(프로세스 mountinfo 확인). 다른 프로젝트 파일은 건드리지 않는다.

## 49. 실물 추론 = 로봇 옆 GPU 서버(RTX PRO 6000) (2026-09-24 13:01 UTC, user-log 52) [사용자 결정]
- §44 [결정 필요] (b) 해소: 실물(E-real)에서 빠른 typed 선택기(Jev-L, Qwen3-VL-4B)와 인식 앞단(SAM 3.1·깊이)은 **로봇 옆 GPU 서버의 RTX PRO 6000**에서 돌린다(LAN). Astra는 지금처럼 API.
- 할 일: 그 서버에서 Jev-L 지연 시험(사전 시험 (1)과 같은 절차·판정, 사전 등록)을 다시 한다 — H200 수치는 그대로 옮길 수 없다(GPU·네트워크 다름). 로봇 ↔ 서버 LAN 왕복 지연을 E-real 지연 예산에 넣는다.

## 50. Jev-L 모델 = Qwen3-VL-4B 유지, 그리고 상태 표현 결함 발견 (2026-09-24 13:56 UTC, `docs/stage3/results/jevl_model_select.md`, user-log 53)
- **선정(사전 등록 규칙 그대로)**: Qwen3-VL-4B 유지. Gemma 4 E4B는 텍스트+머리캠 N=4 p95 0.380 s로 허용선(0.33 s) 초과 → 부적격. 정확도도 Qwen이 높다(DEV 0–29 × P0–P2, 2,387 스냅샷 × 6질문 = 14,322 항목: Qwen 0.483 대 Gemma 0.425, 짝 차 −0.057 [−0.065, −0.050]). 두 모델 결정성 flip 0. 사후 변경 2건(Gemma만 TRITON_ATTN 백엔드, 측정 중 CPU 부하 겹침)은 결과 문서에 두 판 모두 기록 — 판정은 바뀌지 않는다.
- **발견(방향 검사 5번, 메인 재계산으로 확인)**: 두 모델 모두 **질문별 다수 답 기준선 0.669보다 낮다.** Qwen dir_xy 0.088(91 %를 `plus_x`로), mag 0.282, target은 99 %를 o3로. 원인: 측정용 최소 직렬화(`ser-A-min-1`, E §1.4)는 술어 참거짓만 싣고 **그리퍼↔물체 상대 위치·거리가 없다**. 이미지에도 로봇 좌표계(+x·+y) 규약이 없다. → 방향·크기 질문은 입력에 답의 근거가 없다(모델 능력 문제로 읽으면 안 됨).
- **결정 [Claude]**: E0.5·E1로 가기 전에 **상태 표현 진단 E3-lite**를 사전 등록해 돌린다(E3 M1 A/B/C 비교의 앞당김): (S0) 지금 최소 직렬화 / (S1) + 로봇 기준 상대 위치 줄(그리퍼→각 물체 Δx 앞(+)/뒤, Δy 왼(+)/오, Δz 위(+)/아래, cm 반올림, 좌표계 한 줄 설명) / (S2) S1 + 크기 구간 이름 / 각각 텍스트만·텍스트+머리캠, Qwen3-VL-4B·8B, 보기 순서 순환(C3'') 유무. **상한 기준선**: S1 상태만 보는 코드 규칙의 정확도(질문이 잘 정의됐는지 확인). 판정은 결과 문서에 실행 전 고정.
- 영향: M1 후보 A(술어만)가 방향·크기 결정에 충분하다는 가정이 흔들린다 — E3 결과로 M1 기본 후보를 다시 정할 수 있다(정본 §26 "M1 변환은 Claude가 정함").

## 51. VLM 사용·학습 경로 = A→B 단계적 (2026-09-24 14:06 UTC, user-log 54) [사용자 결정]
- **단계 C(지금, 학습 없음)**: Jev-L = Qwen3-VL-4B zero-shot typed 선택기(보기 확률 읽기). E3-lite(§50)가 이 단계의 한계와 입력 형식을 정한다. 이후에는 학습 단계들의 **zero-shot 기준선**으로 남는다.
- **단계 A(다음)**: 같은 백본을 **결정층만** 학습한다 — 입력 = 머리캠(+우손목캠) 이미지 + 과제 문장 + Astra 계약 요약 + 짧은 기하 요약, 출력 = 지금과 같은 typed 보기 토큰(한 결정 = 보기 토큰 하나). 학습 = 오라클·결과 기반 라벨(풀, T13·T14)로 SFT → 시뮬 롤아웃 보상으로 RL. 출력이 보기 확률이므로 **M4 확정 규칙·E1 보정·J5 게이트를 그대로** 쓴다. 실행은 스크립트 스킬 S + 잔차 R(§34·§35) 유지.
- **단계 B(확장)**: 같은 백본에 action expert를 붙여 결정 토큰 + 연속 행동을 함께 학습(π0.5형)한다. 학습 실행기 B(§32·§33)와 결정층을 한 모델로 합치는 것. 사용자 핵심 주장(LLM 일반화)과의 관계는 평가에서 짝 비교로 드러낸다(주 표 실행기는 지금대로 S, §34).
- **고정할 것(지금부터)**: 입출력 형식을 학습 가능한 모양으로 둔다 — 보기 토큰 하나 = 결정 하나(첫 토큰 겹침 없게 보기 이름 설계, R1 유지), 이미지 입력 기본, 질문 문구·보기 순서는 `question_id@vN`으로 버전 관리(학습·평가 일치). 백본 = Qwen3-VL-4B(§50), 실물 = RTX PRO 6000 서버(§49).
- 다음 조사: D26 — VLM을 이산 행동·결정 토큰으로 SFT·RL 하는 레시피, 학습 뒤 확률 보정 유지, 계층형 VLA의 결정 토큰 학습(1.5년·신뢰도 규칙).

## 52. 단계 A 학습 레시피·보정 계획 (2026-09-24 14:20 UTC, `D26-vlm-training-recipe.md`, §51) [Claude 결정, 근거 D26]
- **메인 재확인**: Chu 외 "SFT Memorizes, RL Generalizes"(2501.17161, 기간 밖 기초 문헌) 원문 — SFT 확대 시 V-IRL-L "80.8% → 1.3%", RL은 "80.8% → 91.8%"; starVLA 3,723★ 확인.
- **SFT**: Qwen3-VL-4B, LoRA r32(LLM 전 선형층), 비전 타워 동결, 손실 = **보기 집합 안 재정규화 확률의 NLL**(추론 때 읽는 값과 같은 양), lr 1e-4, 1–2 에폭, standard 검증 NLL 조기 종료, TRL/transformers+PEFT 맞춤 스크립트. 영점(0.483 < 최빈 0.669)이라 SFT를 건너뛰지 않는다.
- **정답(§50 발견 반영)**: SFT 정답은 풀의 `oracle` 필드(계획기 명령·타이머 의존)가 아니라 **결과 기반 라벨**(사전 등록 점수식, `prereg_labeler.md`)과, 관측 상태로 정의한 라벨(E3-lite 뒤 정의)만 쓴다.
- **RL**: 스냅샷 복원으로 한 상태에서 모든 보기를 분기 실행 → 기대 보상을 정확히 계산 + KL(π‖π_SFT) 닻(최적해 ∝ π_SFT·exp(R/β), 확률 붕괴 방지). on-policy(학습된 선택기가 M4를 켠 채 간 상태), 라운드당 150판·2k 스냅샷 분기 라벨, 3라운드 [가정: 시간 약 12 h]. 절제 = 표집형 GRPO(EasyR1).
- **보정**: CAL 시드를 판 단위 둘로 나눠 한쪽 = 질문별 온도 6개, 다른 쪽 = J5 conformal 문턱. 모델이 바뀔 때마다(A-SFT, RL 각 라운드, B) 다시 맞춘다. 라벨 스무딩은 절제로만.
- **잠정 결정 3건 [Claude, D26 결정 필요 항목]**:
  1. `NONE_ESCALATE` 정답 규칙: 결과 기반 라벨에서 **모든 보기가 지평 안에 실패(점수 0)**인 스냅샷만 정답 = `NONE_ESCALATE`. SFT 뒤 NE 확률이 0으로 가면 J5 집합 크기(>1)를 상위 호출 신호로 쓴다.
  2. random 5축 무작위화를 우리 Isaac 장면에 구현한다(§34 D35 "무작위화 넣음"과 일치, RD 측정에 필요).
  3. 보정 판정 초기값: ECE ≤ 0.05, AUROC ≥ 0.75(질문군별). 실행 전 사전 등록 파일에 고정.
- **단계 B 개요**: 같은 백본 + flow-matching expert(구간 제한 잔차 R 출력), expert→백본 기울기 차단(KI, NeurIPS 2025), 백본은 결정 토큰 + 일반 VQA 공동 학습(섞는 비율 1:1:0.25 시작, 보존 탐침으로 조정 [가정]). 코드 후보 starVLA(3.7k★, MED).
- **논문 주장**: "LLM이 일반화한다"는 영점 C 조건에 건다. A·B는 학습량에 따른 standard→random 낙폭(RD) 변화와 일반 VLM 보존 탐침으로 보고(어느 방향이든 결과). M4 새로움 영향 없음.

## 53. E3-lite 판정: 영점 Jev-L은 이 과제에 부족 — 단계 A(학습)가 필수, 결정 질문 정답 재정의 (2026-09-24 15:00 UTC, `docs/stage3/results/e3lite.md`)
- **사전 등록 판정(13:59 UTC 고정)**: (1) 상태 = **S1**(로봇 기준 상대 위치 줄 추가, S1−S0 +0.049 [+0.043, +0.056]) / (2) 모델 = **4B 유지**(8B −0.058) / (3) **이미지는 이 상태에서 아무것도 더하지 않는다**(+0.0001 [−0.003, +0.004]) / (4) 보기 순서 회전 필수 아님(판정 조건에서 −0.008) — 단 판정 밖 조건에서 순서 효과 2.4–6.4 pt, 고정 순서 첫 자리 선택률 0.48–0.75 대 정답 0.41 / (5) **"Jev-L insufficient on this task"**: 최고 0.532 [0.527, 0.537] < 최빈 기준선 0.669(민감도 분석 target·phase·progress만: 0.714 < 0.740).
- **질문 정의 결함**: 코드 규칙 상한 dir_xy 0.920·dir_z 0.803·mag 0.730 < 0.95 → 사전 규칙대로 ill-posed. 오라클 = 계획기 '명령' TCP의 다음 0.33 s 이동(명령–실제 차 90백분위 최대 1.7 cm, close·open 타이머, 스텝 안 단계 전환).
- **모델 행동(판정 밖)**: S1 이득은 좌표 해석이 아니라 최빈 보기 쏠림 — dir_z 100 % `down`, mag 92 % `medium`, target 99.8 % `o3`(쥔 뒤 단계 S2에서도). target은 상태만으로 거의 자명한데 0.43–0.54 → 질문 문구 해석 문제 가능 [미확인].
- **결정 [Claude, §51·§52 승인 범위 안]**:
  1. 폐루프 실험(E0.5·E-M4 등)에 **영점 Jev-L을 쓰지 않는다.** 영점 C는 기준선 행으로만 남고, 결정층은 **단계 A SFT를 거친 뒤** 쓴다(§51 경로가 선택이 아니라 필수가 됨).
  2. **정답 재정의(SFT 전 필수)**: 방향·크기 정답을 관측 상태에서 결정되는 양으로 바꾼다 — "현재 실제 그리퍼(손가락 중점)에서 **단계 목표점까지 남은 변위**"의 부호·구간(데드밴드 1 cm), target·phase는 원장·술어로 결정. 새 정의의 코드 규칙 상한 ≥ 0.95를 먼저 확인한 뒤(사전 등록) 라벨을 만든다. 결과 기반 라벨(§48)과 함께 SFT 정답으로 쓴다.
  3. target 질문 문구를 다시 쓴다("which object must the gripper move toward / relative to next") — E3-lite 재측정은 SFT 전후 비교에서 한다.
  4. 이미지 입력은 SFT 조건에서 다시 판정한다(영점에서 무효라는 결과가 학습 뒤에도 성립하는지 — 단계 B·실물 전이를 위해 입력 경로는 유지).
- **논문 영향**: 핵심 주장 "LLM이 일반화한다"는 **Astra(위층, 영점)**에 걸린다. 빠른 결정층은 학습 모듈로 서술하고, 영점 C 대 A-SFT 대 A-SFT+RL의 standard→random 낙폭을 보고한다(§52). M4 새로움은 영향 없음.

## 54. 결정 질문 정답 v2 채택 + S1 정밀도 1 mm (2026-09-24 15:14 UTC, `docs/stage3/results/labels_v2.md`, §53 결정 2) [Claude 결정]
- **정의(사전 등록 15:06 UTC)**: g = 실제 손가락 중점, 운동 소단계 M(approach/grasp/lift/carry/place/retreat/wait)을 관측(단계·그리퍼 상태·수평 거리 ALIGN 1.5 cm)으로 고르고 G = 그 소단계 목표점(실제 물체 자세에서 계산), Δ = G − g. dir_xy·dir_z = 성분 부호(데드밴드 1 cm), mag = |Δ|의 MAG 구간(경계 = 기하 중점 0.707/1.414/2.828/5.657 cm), target = 단계(S1 → o3, S2 → o5), phase = hold/next/continue 명시 규칙, progress 그대로.
- **게이트**: S1 텍스트만 읽는 코드 규칙 — 1 cm 직렬화는 dir_xy 0.944·dir_z 0.856·mag 0.932로 미달, **1 mm 직렬화(재실행 1회) dir_xy 0.995·dir_z 0.991·mag 0.988·target 1.0·phase 0.999 통과**. 잔차는 전부 반올림(데드밴드·구간 경계). → **S1 직렬화 기본 = `step_cm=0.1`(1 mm)**. 사후 기록 2건(progress 게이트 제외, 0.5 cm 대신 1 mm로 한 번에 재실행)은 결과 문서에 시각과 함께.
- **분포**: 최빈 기준선 5질문 평균 0.641(6질문 0.688). 옛 오라클과 일치: dir_xy 0.816, dir_z 0.894, mag 0.269(옛 = 0.33 s 한 스텝 이동량, 새 = 목표까지 남은 거리 — 뜻이 다른 양), target 0.818(lift 구간), phase 0.678.
- **주의(정의 유지, 기록)**: carry 중 실제 그리퍼가 명령 높이보다 0.4–1.1 cm 낮아 carry dir_z의 41 %가 데드밴드 경계(Δz 0.8–1.2 cm)에 붙는다 → mm 흔들림에 민감. 결과 기반 라벨과의 일관성(91 스냅샷, 참고)은 mag에서 새 라벨이 확실히 낫고(time0.33: 0.879 대 0.692) dir_z·target·phase는 옛보다 약간 낮다.
- **SFT 정답(§52)**: labels_v2(관측 정의) = 1차 정답, 결과 기반 라벨(사전 등록 점수식) = 교차 확인·보조. 풀의 `oracle` 필드는 쓰지 않는다.

## 55. 단계 A SFT 합격 (2026-09-24 17:08 UTC, `docs/stage3/results/stageA_sft.md`, §52–§54)
- **사전 등록(15:51 UTC) 기준 (a)–(d) 모두 합격**, 한 번 학습(시드 0), DEV를 보고 바꾼 것 없음. 메인이 `report.json`에서 재확인(최빈 0.6405, SFT−최빈 +0.2892).
- DEV 2,387 스냅샷 × 5질문(학습에 안 쓴 장면): **SFT A 0.930 [0.926, 0.933]** 대 영점 0.426 대 최빈 0.641. ECE 0.013, AUROC 0.954(보정 전). 질문별 SFT: dir_xy 0.992·dir_z 0.939·**mag 0.730**·target 1.000·phase 0.987. 섭동별 P0 0.933·P1 0.919·P2 0.937.
- 지연·결정성: 병합 BF16 vLLM text+image N=4 p95 **0.307 s**(판정 조건), flip 0/20·Δp 0. **판정 밖 주의**: 실제 DEV S1 1 mm 프롬프트(입력 토큰 중앙 824)로는 p95 **0.347 s** — 문턱 0.33 s를 약간 넘는다(부하 56–61).
- 약점: mag(xlarge 과다, 상한 0.988보다 한참 낮음), phase `hold` 19건 중 0건 선택, `NONE_ESCALATE` 평균 확률 7e-5(§52 결정 1대로 J5 집합 크기를 상위 호출 신호로), 학습 안 한 progress가 표류(`failure` 17→124회) → M7 progress 게이트·보정 재측정 필요.
- **해석 한계**: labels_v2는 S1 텍스트로 거의 결정되는 정답이라 "좌표를 읽는 법"을 배운 것이지 이미지 이해 향상이 아니다. DEV = 풀과 같은 분포(같은 장면, P0–P2); random 5축 일반화는 아직 안 쟀다.
- **다음(순서) [Claude 결정]**: (1) 일반화 — random·dr 장면 DEV 스냅샷에서 영점 대 SFT 낙폭(RD, 사용자 핵심 주장 축) / (2) 이미지 기여 절제 — SFT를 텍스트만·텍스트+이미지로 / (3) mag 개선(구간 경계 표기·보기 설명) / (4) CAL 시드로 온도·J5 conformal 보정 / (5) 지연 여유 — 접두 캐시·질문 묶음 최적화로 실제 프롬프트 p95 ≤ 0.33 s / 그 뒤 E0.5 재생·E-M4 폐루프. 각 단계 판정은 실행 전 사전 등록.

## 56. §55는 파이프라인 확인용 소규모 시험이다 (2026-09-25 17:13 UTC, user-log 56) [사용자 확인]
- 단계 A SFT(§55)는 **실제 실험이 아니라 "파이프라인이 끝까지 동작하는지"를 본 소규모 시험**이다. 확인된 것: 데이터 생성 → 라벨 → 학습 → 병합 → vLLM 서빙 → 평가가 이어져 돈다, 학습 확률 = 추론 확률, 지연·결정성이 문턱 근처, 형식이 맞으면 모델이 배운다.
- **결과로 쓰지 않는다**: DEV 0.930 등 수치는 논문 결과·근거로 인용하지 않는다. 이유: 입력 좌표가 시뮬 참값(실제는 M1 추정), 과제 1개(머그→트레이), 120편·3,000항목, 무작위화 없음, 정답이 좌표에서 거의 기계적으로 나옴.
- 제대로 된 실험의 데이터 조건(설계 대상): M1 추정 상태(인식 잡음 포함) 입력, 여러 과제·물체, 학습용 DR(§34), 규모 확대, 결과 기반 라벨과의 교차, 실물 데이터 여부 — 사용자와 확정한 뒤 진행한다.

## 57. 결정층 이미지 입력 = 머리캠 + 활성 팔 손목캠 (2026-09-25 17:22 UTC, user-log 57) [사용자 제안 + Claude 설계]
- **사용자 제안**: 손목캠을 넣되, 실제로 상호작용하는(활성화된) 팔의 손목캠을 보게 한다.
- **기본 규칙**: 머리캠은 항상. **스킬을 실행 중인 팔의 손목캠(D405, 424×240)**을 넣는다 — 활성 팔은 Astra 계약의 `arms`와 현재 스킬에서 결정(추정 불필요). 양팔 스킬(`handover`, `bimanual_hold`)일 때만 두 손목. 쉬는 팔의 손목은 넣지 않는다(지연·잡음).
- **근거**: 머리캠은 약 0.77 m 거리라 파지·접촉의 cm 이하 정렬이 잘 안 보이고, D405는 7–50 cm 근거리용; 인식 추정 좌표가 틀릴 때 이미지로 바로잡을 곳이 손목; 주요 VLA(π0·π0.5)도 머리 + 손목 입력. 이미지 1장 추가 ≈ +0.15 s(N=4, 머리캠 실측) → 두 손목 상시는 0.33 s 문턱에 불리.
- **절충안(조건)**: 지연 여유가 없으면 활성 손목캠을 근접·접촉 구간(M3 near/contact, 잔차 R 활성 구간과 같음)에만.
- **결정 실험(사전 등록 후)**: {머리만 / 머리 + 활성 손목 상시 / 머리 + 활성 손목 근접·접촉만 / 머리 + 두 손목} × 입력 좌표 {참값 / M1 추정 잡음} — 정확도(특히 mag·접촉 구간)와 p95 지연. 참값 좌표에서는 이미지 효과가 가려지므로(E3-lite 판정 3) 잡음 조건이 주 판정. 우손목캠은 풀에 이미 저장돼 있다.
- 영향: 본 실험 데이터 설계(§56)의 입력 조건, DecCall 이미지 슬롯(질문 id 판본에 카메라 구성 포함), 실물 서버 지연 예산(§49).

## 58. 융합 원칙: 런타임은 하나의 모델로 (2026-09-25 17:58 UTC, user-log 60) [사용자 원칙 + Claude 설계]
- **원칙(사용자)**: 모듈마다 따로 돌며 각 성능에 기대는 파이프라인보다 최대한 하나로 융합된 것이 좋은 연구다.
- **런타임 구성(바뀜)**: **하나의 VLA = Qwen3-VL-4B 백본**이 이미지(머리 + 활성 손목, §57)를 직접 보고 (1) typed 결정 토큰(방향·cm 구간·대상·단계 — 보기 확률, M4가 소비) + (2) action expert의 연속 행동 청크를 함께 낸다(단계 B를 주 시스템으로). 모델 밖에 남는 것: Astra(느린 API 계획기 — 분리가 설계 의도), **M4 확정 규칙(추론 때 규칙, 논문의 새로움)**, 안전 투영.
- **M1 명시적 인식은 런타임 의존에서 뺀다**: 시뮬 특권 상태로 만든 기하(물체 상대 위치·술어)를 **보조 손실(auxiliary head)과 결정 토큰 정답**으로만 쓴다 — 모델이 기하를 이미지에서 배운다. 명시적 인식 파이프라인(R1)은 **모듈형 기준선 행**과 진단용으로 남긴다.
- **스크립트 스킬 S·잔차 R**: S = 데이터 교사(시연·결과 라벨)와 기준선, R은 융합 모델의 action expert로 흡수. §34의 "주 표 실행기 = S"는 [Claude 제안: 주 표 = 융합 모델, S는 기준선 행] — 사용자 확인 전까지 두 행 모두 유지.
- **논문 주장 정리**: 새로움 = M4(융합 모델의 결정 토큰 확률을 시간차 호출로 확정) + 평가. "LLM 일반화" = Astra 위층 + 융합 모델의 영점·학습량별 RD.
- 영향: 계획 e2e-ready의 R1(보조 라벨·기준선으로 범위 축소), R3·R4(백본 하나에 결정 토큰 + 보조 기하 헤드 + expert), R5(런타임 = 모델 한 번 호출로 결정 + 행동).
- (§58 보충, 17:58 UTC, user-log 60 둘째 발언) 융합할 수 없는 부분은 그대로 만든다: Astra(API 계획기), M4 확정 규칙, 안전 투영, 비교용 모듈형 기준선(명시적 인식 → 결정 → 스크립트 스킬), 교사 데이터용 스크립트 스킬 S. 융합 가능한 인식·결정·연속 행동은 한 백본으로.

## 59. 다중 이미지 입력 배치와 호출 방식 (2026-09-24 18:07 UTC, `D27-multi-image-input.md`, user-log 58) [Claude 결정, 근거 D27]
- **배치**: 타일 합성이 아니라 Qwen3-VL 기본 다중 이미지. 순서 = system → "head camera:" + 머리 672×376 원본(252 토큰) → "right/left wrist camera (active arm):" + 활성 손목 424×240 원본(104 토큰) → 상태 → 질문. 이미지 토큰 약 360(양팔 스킬 약 466). 타일 한 장(672×616)은 여백 때문에 399 토큰으로 더 많고 빠르지도 않았다. 토큰 가지치기(VisionZip 등)는 모델 내부 수정이 필요하고 병목이 아니라 쓰지 않는다.
- **호출 방식 = lead**: 한 호출의 트라이 시퀀스 중 첫 시퀀스를 먼저 보내 이미지·접두 캐시를 채운 뒤 나머지를 병렬로. 병목은 이미지 토큰 수가 아니라 같은 이미지를 든 시퀀스 8개의 동시 진입이었다. 메인 재확인(원자료 `/data/harvest/d27`): 머리+손목 N=4 p95 = base 0.29/0.53 s 대 **lead 0.28/0.18 s**(두 판); 캐시를 끄면 0.63 s → **접두 캐시 + 멀티모달 캐시 필수(실물 서버 포함)**. 설계값은 최악 판 0.28 s로 둔다(문턱 0.33 s). leadref(뒤 시퀀스는 이미지 uuid만) 대 lead는 다음 사전 등록 판에서 정한다.
- **학습 일치**: 단계 A′·B의 SFT는 같은 순서·표지·원본 해상도로 학습하고, `question_id@vN` 해시에 카메라 구성을 넣는다.
- 손목캠 효과의 기간 안 절제 근거는 찾지 못했다(OpenVLA-OFT·Hsu 외는 기간 밖, 손목 단독 효과 미분리) → §57 결정 실험이 우리 근거.
- 남은 일: RTX PRO 6000 실물 서버에서 같은 벤치(H·HW × base·lead × N=1·4).

## 60. 주 표 병기 유지 + M4 (b)·critic 확인 신호는 필수 해결 과제 (2026-09-25 18:47 UTC, user-log 65) [사용자 결정]
- 주 결과 표: **지금은 융합 모델과 모듈형 스택을 나란히** 두고, 결과로 정리가 확실해지면 융합으로 정리한다(§58 제안의 확정 시점 = 결과 뒤).
- **M4 (b)(예상 대 측정)와 M7 critic(실패 판정)이 융합 런타임에서 무엇을 "측정"으로 쓸지 반드시 해결한다.** 배경: R1 명시적 인식은 3D 오차 중앙값 35 mm·손목 근거리 머그 검출 누락 87.6 %·추정 좌표로 라벨 정확도 0.612 < 최빈 0.634 → 명시적 인식을 측정원으로 쓰기 어렵다. 조사 D28(1.5년 이내 논문 + GitHub 전수, 신뢰도 규칙)로 후보를 찾고, 사전 등록 시험으로 고른다.

## 61. M4 (b)·critic 측정원 = 고유 감각 코드(로봇 쪽) + 같은 백본의 확인 헤드(세계 쪽) (2026-09-24 19:03 UTC, `D28-m4-critic-measurement.md`, §60) [Claude 결정, E-M4b-meas 통과 조건부]
- **메인 재확인**: Self-Improving EFM(2509.15155, NeurIPS 2025) "significantly more reliable than explicitly including a success detection binary classification objective"(같은 모델의 steps-to-go 예측을 성공 판정기로), TA-VLA(2509.07962, CoRL 2025) 절 제목 "Torques are Good Indicators for End-effector Status", SAFE(2506.09937, NeurIPS 2025) 제목 확인.
- **측정원 분리**: (1) **로봇 쪽 술어**(그리퍼 열림, 쥠/빈 집기, 쥔 채 들림, 접촉 멈춤) = 그리퍼 폭·전류·관절 effort 잔차로 코드 판정 → **T1, 하드 채널 허용**. (2) **세계 쪽 술어**(on, in_contact, near 등) = 같은 백본의 **확인 헤드**(R4 보조 기하 헤드를 확장: 실행 뒤 다음 관측으로 `expected_after` 판정, 시뮬 특권 라벨로 학습, 술어별 온도 + conformal) → **T2, 소프트 채널만**; conformal 집합에 참·거짓이 모두 남으면 `unknown`. M4 (b)(1) ref(t) 잔차는 원래 고유 감각이라 그대로. 확인 헤드는 매 호출 문맥 순전파에 붙어 추가 호출 없음(지연 수 ms).
- **쓰지 않는 것**: 토큰 엔트로피·결정 확률을 측정으로(SAFE 토큰 불확실성 기준선 AUROC 50–55, CheckVLA "확신하며 틀림" 25 %). Astra 성공 판정은 단계 경계 느린 감사로만(GR 1.5 "stale" 경고). 잠재 일치(FLARE식)는 절제용 소프트 채널.
- **선례 공백**: "결정 스텝마다 expected_after 술어를 같은 VLA 헤드로 재서 원장 갱신"의 직접 선례는 없다 → [가정], 논문 주장은 시험 통과 뒤.
- **판정 시험 E-M4b-meas(사전 등록, D28 §4)**: 풀 fit 60(학습)·eval + R1-DEV(V0 명시 인식 기준선) + 새 FI-DEV(DEV 0–29, 실패 주입 5종·무해 편차 3종·명목 ≈270편, 보정 0–14 / 평가 15–29, 팔 applied_torque 기록 추가). 조건 V0/V1z/V1h(주)/V1q/P/V1h+P/L(A는 유료라 사용자 승인 뒤). PC1 세계 술어 균형 정확도 ≥ 0.90·V0와 최빈보다 +0.15·ECE ≤ 0.05·커버리지 0.88–0.95 / PC2 로봇 술어 ≥ 0.97이면 T1 / PC3 FWER 5 %에서 재현율 ≥ 0.80·단독보다 +0.05 / PC4 추가 지연 ≤ 20 ms / PC5 잠재 +3 %p면 [결정 필요] / PC6 PC1 미달이면 세계 술어 unknown + Astra 단계 경계 확인.
- 통과 뒤 반영: M4 `holds()` → 술어별 출처 표 `measure()`, M7 C_vq 채널(하드 = 고유 감각 T1만), E-M7 조건 추가, 논문 방법·관련 연구·한계.

## 62. 행동 주기: S-E2E는 10 Hz 그대로, 우리 수집 데이터는 30 Hz (2026-09-25 19:18 UTC, user-log 66) [사용자 결정]
- S-E2E(ROBOTIS AI Worker 공개 데이터, Task_0001 5.4 GB + Task_0002 3.6 GB ≈ 9.0 GB, 1,575편, 10 fps)는 **원래 10 Hz를 그대로** 쓴다(리샘플링 없음). 행동 청크 길이는 시간 기준(0.5 s = 5스텝)으로 맞추고, 결정 라벨 창(0.33 s)은 가장 가까운 스텝 수로.
- **우리가 직접 모으는 데이터(시뮬 풀·실물)는 30 Hz로 기록**한다(§51·§52, R4 데이터 계약 hz = 30).
- 코드: 단계 B 로더는 데이터셋별 hz를 받는다(30 전용 거부 규칙을 데이터셋 설정으로 바꿈).

## 63. S-E2E 데이터 확정과 열린 문제 처리 (2026-09-24 19:48 UTC, `docs/stage3/results/se2e_data.md`, §62) [Claude 결정, 소규모 시험 범위]
- 데이터: ROBOTIS AI Worker 실물(RB1 `Task_0001_CoffeeClassification_lerobot` 718편 5.40 GB·HF 60565d02 / RB2 `Task_0002_OrderPicking_lerobot` 857편 3.55 GB·7e54be90), 1,575편·211,220프레임·10 fps, 무결성 전수 확인. 학습 표본 39,283(train 37,484 / val 1,799, 에피소드 해시 5 %). RB2 617–816은 오른쪽 머리·손목 영상 없음 → 기본 제외, RB2 ep 46 제외, RB1 손목은 90° 회전 저장 → 회전해 정렬(프레임 확인). FK = `ROBOTIS-GIT/ai_worker` 897ef342 BG2 URDF.
- 결정 라벨 = 휴리스틱 `se2e_heur_ee033@v1`(활성 팔 말단의 다음 0.30 s 실제 변위, 1 cm 데드밴드·MAG 구간; target·phase 없음) — labels_v2와 뜻이 달라 단계 A 수치와 비교하지 않는다.
- **열린 문제 처리 [Claude 결정]**: (1) 정규화 = **팔별(왼·오) 통계**(활성 팔 8-D 설계 유지, 16-D 양팔 expert는 절제) / (2) 그리퍼 = 데이터셋별 관절값을 [0, 1] 열림 정도로 선형 사상(시뮬 폭 m도 같은 [0, 1]로), 범위 밖은 자름 / (3) 토크 없음 → `tau` 마스크 0(로스·입력에서 제외) / (4) Astra 계약 문장 없음 → 데이터의 과제 지시문을 계약 요약 자리에 넣고 표기 / (5) 라벨 창·데드밴드·학습 판정은 S-E2E 학습 전에 사전 등록 / (6) RB1은 카드·라이선스 미표기 → **내부 학습 시험만**, 논문·공개 자료에 RB1 프레임·모델을 싣기 전 ROBOTIS 확인 필요(RB2는 apache-2.0).
- 이 시험의 성격(§56): 실물 데이터로 E2E 파이프라인이 끝까지 도는지 확인 — 결과 수치로 쓰지 않는다. 학습 시작은 R7 순회 2회 통과 뒤(user-log 61).

## 64. E-M4b-meas 판정: 세계 쪽 = 확인 헤드 V1h 채택, 로봇 쪽 T1 = 그리퍼 열림·쥠·쥔 채 들림, critic = V1h (2026-09-24 20:59 UTC, `docs/stage3/results/e_m4b_meas.md`, §61) [사전 등록 판정 + Claude 결정]
- 데이터: FI-DEV 270편(DEV 0–29 × 명목·무해 편차 3·실패 주입 5), 평가 스냅샷 4,400(시드 15–29), 보정 0–14. 주입 강도 변경 4건은 데이터 생성 전 고정·기록. 메인이 `results.json` 재확인.
- **PC1 통과**: V1h(단계 A 백본 동결 + 확인 헤드) 세계 쪽 4술어 평균 균형 정확도 0.949 [0.927, 0.965], ECE 0.004, 커버리지 0.905; V0(명시 인식) 0.596, V1z(영점) 0.750 → **세계 쪽 측정 = V1h**. V1q(질문 SFT)는 0.959지만 +0.03 교체 조건 미달.
- **PC2 통과**: 고유 감각 코드 규칙 gripper_open 0.998·holding 0.992·쥔 채 들림 0.992 → **T1(하드 채널)**. `contact_stall`(P 0.845, V1h 0.944)은 T2.
- **PC3 불통과**: V1h+P 재현율 0.786(FWER 0.033) < 0.80, V1h 단독 0.871(FWER 0.00)보다 낮음 — P의 관절 토크 충돌 규칙이 명목 운동 토크에 걸려 문턱을 끌어올림. → **[Claude 결정] critic 경보 = V1h 단독**(T1 고유 감각 3술어는 하드 판정에만, 경보 합산에서 토크 충돌 규칙 제외). 유형별 재현율: 빈 집기 1.0·들다 미끄러짐 1.0·운반 중 충돌 0.6·놓기 이탈 0.87·멈춤 0.8.
- **PC4 통과**: 헤드 추가 지연 p95 0.44 ms. PC5(잠재 일치) 미실행, PC6 해당 없음, 조건 A(Astra) 미실행(유료, 사용자 승인 필요).
- **[Claude 결정] conformal 빈 집합(9 %) = `unknown`**(보수적: 판정 보류, 소프트 채널에 올리지 않음).
- 한계: 백본 동결 헤드만 학습, 같은 장면·같은 정책 분포의 특권 라벨 → 실물 전이는 미검증. 파이프라인 확인 성격(§56)이며 논문 수치는 본 데이터로 다시 잰다.
- 반영: M4 `measure()` 출처 표(세계 = V1h, 로봇 = P T1), M7 critic = V1h 단독 경보 + T1 하드, 단계 B 학습에 확인 헤드 손실 포함(공동 학습판은 다음 판).

## 65. 결과 기반 라벨 점수식 판정 = plan(사전 등록 규칙), 그러나 판별력 없음 → 거부권 용도로만 (2026-09-24 21:33 UTC, `docs/stage3/prereg_labeler.md`, DEV 자체 점검 90편·900 스냅샷)
- 사전 등록 규칙 적용(메인이 `cli_label summarize` 실행): 오라클∈best ≥ 0.90 적격은 **plan 하나**(0.986, 판별력 0.025) → 선택 = plan. 나머지: time0.66 0.872/0.167, time0.33 0.796/0.353, short3 0.745/0.469, short2 0.696/0.556, short1 0.578/0.677.
- **해석**: 선택된 plan은 거의 모든 보기를 best로 둬(판별력 0.025) **학습 신호가 되지 못한다**. 적격 기준도 풀의 `oracle` 필드(계획기 명령 의존, §50에서 부적합 판정)와의 일치로 정의돼 있어 기준 자체가 약하다 — 사전 등록 규칙은 그대로 적용하고 이 한계를 함께 적는다.
- **결정 [Claude]**: 결정층 학습 정답 = labels_v2(§54) 유지. 결과 기반 라벨은 **실패로 이어지는 보기의 거부권(점수 0 보기 제외)** 용도로만 쓴다. 풀 라벨(label4)이 끝나면 `finalize --rule plan`으로 `best`를 채운다.

## 66. R2 데이터 생성기 확정과 생성 전 결정 (2026-09-24 22:13 UTC, `docs/stage3/results/r2_datagen.md`) [Claude 결정]
- 생성기: 30 Hz 기록(물리 dt 0.01, 틱 k를 서브스텝 (10k+1)//3에 두어 3·4·3 서브스텝, 누적 오차 없음), 머리 + 활성 우손목 원본 해상도, q·qd·tau(팔 applied_torque)·그리퍼, action_exec/script, labels_v2, aux, §61 확인 헤드 목표, R4 계약(hz 30, H 15), LeRobot v2.1 내보내기(lerobot 0.3.3으로 적재 확인), 파일 잠금 큐 병렬·재개. DEV 36편 구조 검사 36/36, 성공 32/36, 체인 재생 0.000 mm, 프레임 확인.
- 과제: mug_tray 12/12, mug_marker(자홍 시각 표적 o11) 12/12, bottle_tray 8/12(쥔 채 20–24° 기울어 놓은 뒤 전도), box_marker는 lift 실패로 제외(실험용 등록만).
- **결정**: (1) **R2_TRAIN 시드 = 10000–59999**를 정본 시드 표에 추가(DEV 0–29 / CAL 500–549 / TEST 1000–1149 / TEST-P5 1300–1329 / POOL 2000–2119와 겹치지 않음), 생성은 `--confirm-train`이 있어야 한다. (2) 병 파지 수율(약 67 %)은 지금은 감수하고 실패 편은 제외(구조 검사로 걸러짐); 파지 개선은 DEV에서만 조정. (3) 결과 기반 라벨은 거부권 용도(§65)라 이 데이터에 필수 아님 — 붙일 때는 체인 재생. (4) 텍스트 상태 `f<N>`은 기존 20 Hz 번호 체계를 유지(단계 A 프롬프트 바이트 일치), 시간은 `t=` 초로 이미 표기.
- 본 생성 계획(아직 실행 안 함): R2_TRAIN 10000–10999 × 과제 3 × {standard, dr} ≈ 6,000편(유효 약 5,300), GPU 0·1 4프로세스 약 7–8 h, 원본 약 103 GB + LeRobot 약 26 GB. S-E2E 이후 본 단계 B 학습 준비 때 실행.
- 운영: pytest 임시 폴더를 프로세스별(`D:/tools/scratch_qdd/pytest_tmp/p<PID>`)로 — 에이전트 동시 실행 충돌(setup ERROR) 해소.

## 67. R7 1회차 문서 정정과 범위 결정 (2026-09-24 23:32 UTC, `docs/stage3/results/r7_cycle1.md`) [Claude 결정]
- **날짜 정정(C1)**: §56–§66 제목의 "2026-09-25 … UTC"는 KST 날짜에 UTC 시각을 붙인 오류였다(실제 UTC 날짜 2026-09-24, 파드·로컬 `date -u` 기준). Claude 결정 절(§59·§61·§63–§66) 제목은 2026-09-24로 고쳤고, **[사용자] 표시가 붙은 절(§56·§57·§58·§60·§62) 제목은 '[사용자] 줄 수정 금지' 규칙에 따라 그대로 두었다 — 이 다섯 절의 '2026-09-25'는 2026-09-24 UTC로 읽는다.** 계획 파일 이름 `2026-09-25-e2e-ready.md`는 참조가 많아 그대로 두되 KST 작성일로 읽는다.
- **호출 구조 정정(C4)**: §58의 "모델 한 번 호출"은 **스텝마다 두 호출(decide → chunk)**로 구현했다 — decide = 공유 접두 순전파 한 번으로 결정 확률 + 문맥 은닉 + 확인 헤드, chunk = 캐시된 문맥에 확정 결정을 조건으로 expert(CUDA 그래프). vLLM이 은닉 상태를 내주지 않고 expert가 확정 결정(M4 뒤)을 조건으로 받기 때문(R5·pre-R7). 백본 순전파는 스텝당 1회로 융합 원칙은 유지.
- **E2E-ready 범위 밖(SCOPED)으로 확정(C8)**: M9 복구와 T_fail 뒤 하트비트 정지, patch/replace의 A5′ 검사·M2 R3 합치기, 모듈형 스택의 세계 쪽 측정(기준선이라 unknown 유지), §52 VQA 보존 공동학습(기본 끔 — 본 단계 B 학습 사전 등록 때 결정), 융합 런타임의 토크 입력·확인 헤드 보정 파일·동시 부하 지연(본 학습 체크포인트가 생긴 뒤 calib·RTX PRO 6000 조건에서). 근거: 완료 정의 1–5는 "학습 직전까지의 배관"이고 위 항목은 학습된 체크포인트나 폐루프 본 실험이 있어야 의미가 있다.
- **R1–R6 관문 방향 질문(C3)**: 관문별 6문항 기록이 direction-log에 빠져 있었다 → 사후 일괄 기록(아래 direction-log 행)으로 보완, 앞으로는 관문마다 기록.
- (§67 보충, 2026-09-25 00:08 UTC) R7 1회차 코드 수정 반영: §63 (1)–(3) 구현(팔별 통계, 그리퍼 `open01@v1` [0,1] 사상, tau 마스크 채널), 단계 B `train --data r2|pool|se2e`(hz 전달), §42 로그에 요청 해시·카나리 id(`harvest.eval.canary`, Astra 카나리 id = "none" 명시 — 유료라 미실행). 그리퍼 선형 사상은 시뮬 패드 간격과 실물 관절값의 비선형 차이가 있어 **두 데이터를 한 모델에 섞을 때 재검토**(S-E2E는 공개 데이터 단독).

## 68. R7 2회차 문서 정정과 범위 추가 (2026-09-25 00:33 UTC, `docs/stage3/results/r7_cycle2.md`) [Claude 결정]
- **K1 날짜**: R2·R3·R4·R6 결과 문서와 D28·E-M4b-meas 머리말의 '2026-09-25'를 2026-09-24 UTC로 고쳤다. '시계 차'라는 설명은 틀렸다(실제 원인은 KST 날짜 오기, §67 C1).
- **K2·K3·K5**: 계획 개정 줄에 §67 두 호출 정정 표시, `harvest/datagen/gen.py` 주석의 R2_TRAIN '[proposal]'·옛 시험 범위 500–699 정정(시험 시드 3000–3199), `se2e_data.md` §9에 b4a58ce 해결 표시, `random5.md`의 '예약되지 않은 500–699'(CAL과 겹침) 정정.
- **K6 → SCOPED 추가**: D28의 CONTRADICT-soft(같은 T2 술어가 연속 2회 conformal {거짓})는 E2E-ready 범위 밖이다. 근거 — (1) conformal {거짓} 판정에는 확인 헤드 보정 파일이 있어야 하는데 이것 자체가 §67 SCOPED(본 학습 체크포인트 뒤 CAL에서)이고, (2) CONTRADICT-soft의 결과 범주는 DEVIATE와 같은 `C_m4`(하드 아님, §11.2)라 지금의 런타임 동작은 달라지지 않는다. 확인 헤드 보정 때 함께 구현한다(`harvest/runtime/measure.py`, 지금은 매번 단일 DEVIATE).
- **NOTE 처리**: 카나리 id는 날짜별이라 S-E2E·본 실험 시작 때 그날 카나리를 새로 만든다(지금 실행은 `stale: true`로 전날 id를 싣는다 — 파이프라인 확인용이라 허용). 파드 `/data/harvest/ir/ir_run.sh` 사본의 옛 'GPU2 금지' 주석은 저장소 판으로 다시 동기화했다.

## 69. R7 3회차 문서 정정 (2026-09-25 00:55 UTC, `docs/stage3/results/r7_cycle3.md`) [Claude 결정]
- L1–L4 정정: `se2e_data.md` 작성일(2026-09-24 UTC), `r2_datagen.md`의 R2_TRAIN '[제안]'·500–699 서술, 런타임 주석(`harvest/runtime/core.py` backend, `models.py` FusedModel)과 `r4_stageB.md`의 '한 번 호출'에 §67 두 호출·HF 백본 표시, `handoff.md` 정본 범위(§1~§69, 읽을 범위 §43–끝). §68 K1 목록에 `se2e_data.md`를 더한다.
- **정정 절차(재발 방지)**: 사실 하나를 고칠 때는 저장소 전체(코드 주석 포함)를 grep해 같은 사실을 적은 곳을 모두 고치고, 원래 절의 문장은 지우지 않고 정정 표시를 붙인다.
- **§68 K6 근거 보정(N1)**: 보정 파일이 없어도 런타임은 argmax 단일 원소 기본값으로 {거짓}을 내고 DEVIATE를 올린다 — '판정이 성립하지 않는다'는 과장이었다. CONTRADICT-soft를 M4 CONTRADICT로 보내면 정지와 Astra 사건 호출이 더해져 동작이 달라진다. 그래도 연속 2회 규칙의 문턱은 보정된 conformal 집합 위에서 정해야 의미가 있으므로 보정 때 함께 구현한다는 결정은 유지한다.
- **S-E2E 사전 등록에 넣을 것(N2·N3)**: `--max-val`은 앞 N개를 잘라 RB1만 보게 되므로 층화(RB1·RB2) 무작위 부분집합을 쓴다; 학습 중간 체크포인트·재개와 평가 간격·크기를 정한다(기본값은 평가가 학습을 덮는다).

## 70. R7 4회차 문서 정정 (2026-09-25 01:14 UTC, `docs/stage3/results/r7_cycle4.md`) [Claude 결정]
- M1·M2 정정: fused decide가 vLLM에서 돈다는 서술(`harvest/runtime/fused_action.py` 머리 설명, `r5_closed_loop.md` 1순위·열린 문제 7, 검토에서 빠졌던 `r6_eval.md` 열린 문제 1)에 §67 C4 정정 표시. 실제 = decide·chunk 모두 HF 백본, vLLM은 모듈형 기준선 Jev-L 전용. `fused_action.py`의 '30 Hz, H = 15' 고정 서술은 체크포인트의 hz·H(R2 30 Hz·H 15, S-E2E 10 Hz·H 5)로.
- §69 정정 절차의 grep 목록에 구현 이름(vLLM·HF·StageBFused 등) 검색을 더한다 — 문구('한 번 호출')만 찾으면 같은 사실을 다른 말로 적은 곳을 놓친다.
- 논문 `3_method.tex`·`4_setup.tex`의 vLLM 결정 서빙 서술은 실물 서빙 선택(RTX PRO 6000, §67 C8 SCOPED)이 열려 있어 지금은 두고, 논문 갱신 때 §67에 맞춘다.

## 71. R7 5회차 문서 정정과 문서 전수 정리 sweep6 (2026-09-25 01:48 UTC, `docs/stage3/results/r7_cycle5.md`, `docs/stage3/results/r7_sweep6.md`) [Claude 결정]
- P1·P2 정정: `handoff.md` 머리 상태 줄은 회차 수를 싣지 않고 §2.8 마지막 줄을 가리키게 했다. 코드 무결 연속 횟수 오기(4회 → 3회, 2·3·4회차)를 `handoff.md`·`direction-log.md`와 `r7_cycle4.md` 끝 정정 줄에 표시했다. `r6_eval.md` 열린 문제 1과 `r5_closed_loop.md` 열린 문제 7의 '단계 B 실체크포인트 어댑터 없음'에 pre-R7(519de26) 해결 표시.
- 전수 정리(§69 절차 확장): `docs/stage3/results/*.md`와 D2x의 열린 문제·[결정 필요] 목록을 항목마다 코드·후속 문서와 대조해 해결(커밋·문서 포인터)·범위 밖(§67 C8·§68 K6 SCOPED) 표시를 붙였다. 남은 열린 항목은 `r7_sweep6.md` §3에 모았다.
- §43·§47 후속(§47이 약속한 편집 묶음): 'ZED Mini 102°×57°'·fx ≈ 272·`ZED_M` 쌍둥이·트랙 D-stereo 서술(M1·D21·SUMMARY·E-first·EVAL·plan v6.8·D23·p0 결과)에 정정 표시. 단계 2 설계 문서(M1–M10·E-first·EVAL·SUMMARY·plan·design README) 머리에 Jev → Jev-L(§44)·융합(§58·§60)·카메라(§43·§47) 안내 줄. 본문은 당시 기록이라 고치지 않는다.
- user-log 62 풀이: 계획 `2026-09-25-e2e-ready.md` 39줄과 `r5_closed_loop.md` §5의 '같은 vLLM 설정·캐시·lead'는 원문에 없는 Claude의 풀이로 표시했다(원문 인용·사용자 줄은 그대로). 실물 서빙 방식은 §67 C8 SCOPED 그대로.
- 코드 주석·문구(동작 불변): `stagea_train` 사용법의 `load` 줄, `datagen/gen.py` 시드 거부 문구, `runtime/m4.py`(J5는 R6에서 `core.py`에 구현, θ는 오프라인 판정만)·`eval/closed.py`(단계 B 체크포인트 = HF 서버) 머리 설명, Jev 옛 도구(`clients/jev*.py`·`cli_e0.py`·`config.py`·`canary.py`)에 '쓸 수 없음(user-log 46)' 표시.
- 정정 절차 grep 목록(§69·§70)에 더한다: 상태 줄(`마지막 갱신`, `회차까지`, `연속`), 해결된 열린 문제(`없음`, `없다`, `훅이 없다`, `mock_fused`, `not implemented`, `[결정 필요]`를 후속 정본 절과 대조), 카메라 수치(`102`, `272`, `ZED_M`, `D-stereo`).
- 주석도 고치지 않는 파일: `harvest/train/stageb_data.py`·`stageb_model.py`(`stageb_train.PROMPT_FILES_B`)와 `stagea_train.PROMPT_FILES`(`clients/jevl.py`·`deccall_snap.py`·`jevcall.py`·`options.py`·`e3lite.py`·`serialize.py`·`train/stagea_data.py`·`stagea_loss.py`·`prefix_share.py`)는 바이트 해시가 체크포인트 `prompt_config.files_sha`와 대조된다(`runtime/fused_model.check_prompt`는 어긋나면 거부). 주석 한 줄만 바꿔도 기존 체크포인트가 거부되므로 문서 정리에서는 이 파일들을 건드리지 않는다(sweep6에서 `stageb_data.py` 머리 설명에 'Jev-authority' 안내를 넣었다가 되돌렸다 — HEAD 블롭과 같음 확인).
- 아직 열린 것(결함 아님): §27 끝의 R1 금지어 보기 이름 교체(`jevcall.PROGRESS`의 `failure`·`valid_progress` — progress 질문은 지금 호출·학습에서 빠져 있음), T0 첫 계획 호출·`detect_phrase` 런타임 반영(§46), 잔차 모드 체크포인트 런타임, θ 게이트 런타임 적용 등 — `r7_sweep6.md` §3.
- (§71 보충, 2026-09-25 01:59 UTC, 메인) **SCOPED 추가**: `harvest/runtime/latency_ctrl.py`의 `LatencyChargingController`(동기 기준선 agent·capx의 지연 충실 트랙, §42)는 스텁으로 둔다 — 기준선 비교는 본 실험 단계에서 쓰며 E2E 학습 준비(완료 정의 1–5)와 무관하다. 시험 1개 건너뜀(`tests/runtime/test_latency_ctrl.py`)은 이 결정에 따른 것이다.

## 72. R7 7회차 결함 E1 수정과 평가 통계·M4 W의 사전 등록 대조 (2026-09-25 03:08 UTC, `docs/stage3/results/r7_cycle7.md`, `docs/stage3/results/r7c7_fixes.md`) [Claude 결정]
- 원칙: 사전 등록은 구속한다. 코드는 사전 등록에 적힌 값·방식을 그대로 구현하고, 사전 등록이 정하지 않은 값은 이 절에 적는다. 아래 각 항목에 따르는 사전 등록 줄을 적었다.
- **E1(DEFECT) 부트스트랩 횟수 = 10,000**: 사전 등록 `E-first-experiments.md` §1.7 통계 공통(:130, "에피소드(또는 스냅샷의 에피소드) 군집 부트스트랩 10,000회")·`EVAL-evaluation-design.md` §4.2(:184, 짝 층화 부트스트랩 10,000회). 평가 한 명령 네 개(`harvest.eval.e05`·`rd`·`calib`·`closed`의 `--n-boot`)와 라이브러리 기본값(`e05.mean_ci`·`diff_ci`·`block_diff_lo`·`analyze`, `rd.majority`·`offline_rd`·`drd`, `runtime/calibration.evaluate`, `eval/closed.aggregate`, `harvest/canary.canary_compare`)을 모두 `analysis.stats.N_BOOT = 10000`으로 바꿨다(이전 2,000은 어떤 기록에도 근거가 없었다). 출력 `meta.bootstrap`(`e05.json`·`rd.json`·`calib.json`·`closed.json`, `closed.json`은 `result.bootstrap`에도)에 `n_boot`·seed·수준·백분위 구간·재표집 단위를 싣는다. 카나리 비교 결과에도 `n_boot`·`boot_seed`·`boot_unit`. 사전 등록이 정하지 않은 값: 부트스트랩 난수 seed = 0(모든 구간 공통, `analysis.stats` 기본값), 구간 = 95% 백분위.
  - 예외(그대로 둠): `harvest/m4b/*`의 2,000회는 자기 사전 등록(D28 §4, `e_m4b_meas.md:94`)을 따른 것이라 바꾸지 않는다. `tools/*` 보고 스크립트는 지난 결과 재현용이라 그대로.
- **E0.5 판정 10 = Holm 단계 하강**(N3): 사전 등록 E-first §2A.6-10(:265, 해시 절) "짝 부트스트랩 95% 하한 > 0, 블록 여러 개면 Holm", §1.7(:131) "한 판정에 여러 조건을 걸면 Holm 보정". 구현 `analysis.stats.holm_ci` = 구간 역산 p값의 Holm: m쌍 중 단계 s에서 남은 쌍의 양측 구간을 수준 1 − α/(m − s)(α = 0.05)로 보고 0을 빼는 쌍 하나를 기각, 없으면 멈춘다(같은 뽑기의 백분위 구간은 겹쳐 들어가므로 p ≤ α/(m − s) ⇔ 그 구간이 0 제외). `e05.block_diff_holm`이 블록 쌍마다 기각 여부를 `floor.block_pairs_holm`·`judgments.j10_block_pairs_holm`에 낸다. 판정 10의 참·거짓(`time_block_effect`)은 `block_diff_lo > 0` 그대로 — Holm의 첫 단계가 Bonferroni 수준 1 − α/m이므로 "한 쌍이라도 유의"는 두 방법에서 정확히 같다(7회차 N3의 '더 보수적'은 쌍별 판정에만 해당). 기본 `--blocks 2`(쌍 1개)에서는 모두 "95% 하한 > 0"과 같다.
- **E1 ECE = 15개 동일 질량 구간**(N4): 사전 등록 E-first §3.4(:320) "ECE(15개 동일 질량 구간)"; 해시 절 §3.7-1(:339) "보정 뒤 ECE ≤ 0.05이고 부트스트랩 95% 상한 ≤ 0.08"과 §3.6(:335) "원 확률 ECE ≤ 0.03"은 구간 방식을 따로 적지 않으므로 §3.4의 정의를 쓴다. `runtime/calibration.judge_question`은 `ece_cal_mass`와 그 군집 부트스트랩 구간 `ece_cal_mass_ci`로 판정하고, `fit_question`의 원 확률 규칙(`fit_raw_ece`)도 등질량이다. 등폭 `ece_raw`·`ece_cal`은 보고만 한다(이전 `ece_cal_ci`(등폭)는 없앴다). 사전 등록이 정하지 않은 값: 등질량 구간 = 신뢰도로 안정 정렬한 항목을 min(15, n)개 묶음으로 `numpy.array_split`(앞의 n mod 15개 묶음이 1개 더 큼, 같은 값은 정렬 순서대로 경계에서 나뉨), 묶음 크기 가중 |정확도 − 평균 신뢰도|. `analysis.stats.ece`(등폭)는 m4b(D28)·지난 보고 도구용으로 그대로 둔다.
- **폐루프 RD 재표집 단위 = 레이아웃 시드**(N5): 사전 등록 EVAL §4.2(:184 "과제 안 layout 짝 재표집, 시스템 간에도 같은 layout 인덱스로 같이 재표집", :179 1차판은 "이 절의 규칙(RD, 짝 부트스트랩)을 그대로 옮겨")·E-first §1.7(:130 "같은 시드 짝 비교"). `eval/closed.aggregate`의 `rd_ci`는 (시드, epoch) 짝이 아니라 레이아웃 시드를 뽑고 그 시드의 모든 epoch 짝을 함께 넣는다(RD* = 1 − Σ변형 성공 / Σ표준 성공; `n_seeds` 출력). 성공률·조건 차·`sr_diff`는 원래 시드 군집이었다. 사전 등록이 정하지 않은 값: epoch(같은 레이아웃 반복)는 그 시드 군집 안에 둔다(같은 레이아웃의 반복은 독립 표본이 아니다). 과제 층화는 폐루프 과제가 하나라 해당 없음.
- **M4 유예 창 W의 정의**(N2): 정의하는 곳은 M4 §3 #3(:148 "W개 호출 동안 가수용 대기, 창 안에서 도전 선택이 다시 나오면 교체")과 §4.2 의사코드(:212–214 첫 도전 표에 `defer_left = W`, 같은 도전 표마다 1 빼고 0 이하면 교체)다. 둘 다 **W = 첫 도전 표 뒤에 더 필요한 같은 도전 표 수**: W = 1이면 도전 표 2개(= M5 L1 첫 판의 "겹친 호출 2개가 연속으로 같은 새 보기")에서, W = 2면 3개에서 교체. 따라서 §5(:35)·M4 §4.4 W 행(:281)·M5 §4 L1(:86)·SUMMARY의 "M5 L1의 '연속 2표'는 **W=2** 설정으로 흡수"는 이 정의와 맞지 않는 옮김이고, **W=1**로 정정한다(§5 원문은 지우지 않고 이 절이 뒤 절로 우선). 이 정의라면 사전 등록된 C5 설정(E-first §4.12 :487 "W=1")과 M4 기본값(§4.4 "1, 경계 직후 2")·비가역 W+1(§4.6 "기본 1 → 2")의 뜻이 그대로이고, **기본 조건 C0–C6(`conditions.py`는 W를 덮어쓰지 않음, 기본 W = 1)의 동작은 비트 단위로 같다**(이번 코드 변경은 W = 0 경로와 음수 거부뿐). 코드 수정: 의사코드대로면 W = 0도 두 번째 도전 표에서 교체되어 W = 1과 같았다 → E-M4 절제 범위 {0, 1, 2}(M4 §4.4)가 세 수준이 되도록 **W = 0 = 첫 도전 표에서 즉시 교체**(FLy의 유예 없음, 비가역이면 W+1 = 1 → 도전 표 2개), W < 0은 `M4Params`가 거부. '연속'의 뜻(사전 등록이 정하지 않음, 의사코드 그대로): 사이에 현 선택과 같은 표가 와도 도전은 이어지고, 다른 도전 선택이 오면 그 선택으로 새로 센다.
- **융합 폐루프 `meta.prompt_config`**(N7): 단계 B 체크포인트 판은 체크포인트 `stageb.json`의 학습 prompt_config(+ `source`)를 적는다(`eval/closed.run_prompt_config`). 융합 서버의 `check_prompt`가 런타임 입력과 다르면 거부하므로 이 값이 실제로 쓴 구성이다. 모듈형·모의 판은 그대로 `prompt_config_eval(layout)`.
- 정정 절차 grep 목록(§69–§71)에 더한다: 사전 등록 수치·방식과 구현 기본값의 대조(`n_boot`·`10,000`·`Holm`·`Bonferroni`·`동일 질량`·`등폭`·재표집 단위·`W=`). 명령이 끝까지 도는지뿐 아니라 사전 등록의 숫자와 코드의 숫자를 나란히 본다.

## 73. R7 8회차 결함 D1–D5 수정과 사전 등록이 열어 둔 값 (2026-09-25 04:16 UTC, `docs/stage3/results/r7_cycle8.md`, `docs/stage3/results/r7c8_fixes.md`) [Claude 결정]
- 원칙(§72와 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`)은 구속한다. 코드는 사전 등록에 적힌 값·방식을 그대로 구현하고, 사전 등록이 열어 둔 값은 이 절에 적는다(값마다 한 줄 근거). 코드 줄 번호는 이 수정 뒤 작업 트리 기준.
- **D1 E0.5 판정 2 = Holm**: 사전 등록 E §1.7(:131) "한 판정에 여러 조건을 걸면 Holm 보정", §2A.6-2(:257) "LA-2 또는 C2''가 newest보다 +2%p 이상(하한 > 0)". `e05.gain_holm`이 두 이득(LA-2 − newest, C2'' − newest; 스텝 짝 차, 에피소드 군집 부트스트랩, 같은 뽑기)에 `stats.holm_ci`(α 0.05, 첫 단계 수준 1 − 0.05/2)를 적용하고, `replay.judge_e05`는 "점추정 이득 ≥ 0.02이고 Holm 기각이며 하한 > 0"인 규칙이 하나라도 있을 때만 판정 2로 본다(음의 방향 기각은 판정 2가 아님). 가설별 결과 = `judgments.j2_gain_holm`(`judge_input.gain_holm`). 이전 입력 `gain_la2_lo`·`gain_c2pp_lo`(보정 없는 95% 하한)는 판정 입력에서 뺐다(95% 구간은 `rules.pooled`에 보고로 남음). 판정 1·3은 사전 등록 문장대로 점추정(+2%p 미만)을 본다 — 그래서 "점추정 ≥ 2%p이지만 Holm 불성립"은 판정 1–3 어디에도 들지 않아 `undecided`로 그대로 보고한다(새 규칙을 만들지 않음, 기존 처리).
- **D2 E1 `ambiguous` 제외**: 사전 등록 E §3.10(:359) "`ambiguous` 항목은 본 ECE에서 빼고 따로 보고". `calib.items_from`이 스냅샷의 `ambiguous` 표시를 싣고, `calibration.evaluate`의 모든 ECE(판정 ECE `ece_cal_mass`·그 구간, 보고용 등폭 `ece_raw`·`ece_cal`)는 애매하지 않은 항목만(`n_ece`), 애매한 항목은 `heldout.ambiguous` {n, ece_cal_mass, ece_cal, acc}로 따로 보고한다. §3.6 원 확률 규칙의 ECE(`fit_question.fit_raw_ece`)도 같은 제외(`n_fit_ambiguous`). 정확도·AUROC·θ 구간·J5·온도 적합은 사전 등록이 ECE만 적었으므로 모든 항목. 열린 값: `ambiguous`의 정의 = 풀 표시(사전 등록 E §1.5 :121 "문턱 근처(히스테리시스 띠 안) 술어는 `ambiguous` 표시" = `sim/snapshot.ambiguous_predicates`, near 5–6 cm 띠) — §3.10이 예로 든 "방향 26개 최근접 선택의 경계"(labels_v2 데드밴드·구간 경계)는 표시가 없어 빼지 않는다(정의를 넓히려면 표시를 다시 만들어야 함).
- **D3 매일 카나리 표류 판정 = 정본 §28 그대로**: 정본 §28(:264)·E §1.8(:139) "그날 바닥보다 유의하게 크면(하한 > 0, Holm)". `harvest/canary.canary_compare`: 질문마다 (기준 최빈과의 불일치 − 그날 바닥)의 에피소드 군집 부트스트랩 구간(E §1.7 :130 "스냅샷의 에피소드"; 군집 = 스냅샷 id `<kind>_ep<seed>_k<k>`에서 `_k<k>`를 뗀 편, `episode_of_key`), 질문들에 Holm 단계 하강, **표류 의심 = 하한 > 0으로 기각된 질문이 하나라도 있음**. 이전의 "평균 불일치 > 바닥의 2배" 조건은 어떤 사전 등록·정본 줄에도 없어 뺐다(정본·설계 문서 `grep` 0건, 옛 계획서 한 줄만 — 정정 표시함). 결과에 `per_question`(불일치·기각·구간·수준)·`n_clusters`·`boot_unit`. 부트스트랩 횟수 = `stats.N_BOOT`(10,000). 열린 값: 바닥 = 그날 반복 1..의 반복 0 대비 불일치(세트 전체 합친 값)를 주어진 상수로 둔다(사전 등록은 바닥의 추정 방법을 적지 않음; 바닥 자체의 표집 변동은 넣지 않음). "모델 식별 필드가 바뀌어도 같은 처리"는 보정 파일이 모델 지문에 묶여 지문이 바뀌면 읽기를 거부(게이트 꺼짐)하고 카나리 기준일도 지문별이라, E1 재보정 전 게이트 재사용이 막히는 것으로 충족한다고 읽는다.
- **D4 C5 설정 τ = 1(접촉 근처 0)**: 사전 등록 E §4.12(:487) "τ=1(접촉 근처 0)", M4 §4.4(:277)·§4.6(:307). `runtime/core.near_contact(raw, phase)` = §7 near/contact 구간 정의("목표까지 ≤ 5 cm / 접촉 술어 참"): 단계의 목표(approach·descend·close·lift = o3, 그 뒤 = o5)와 손가락 중점 거리 ≤ `CFG.near_in_m` 0.05 m, 또는 그리퍼–목표 접촉(목표가 o5면 o3–o5 접촉도). `OursRuntime.act`가 매 틱 배달 직전에 계산해 `CommitLedger.on_vote(..., near=)`와 `try_commit_prefix(..., near=)`(새 인자) 모두에 넘기고, 원장은 순서형 질문(`mag_coarse`)의 합의 허용을 near면 0칸으로 센다. 적용 범위 = 원장의 (a) 합의 규칙을 쓰는 모든 조건(C3·C5·C6 — C3는 C5의 (a), C6는 C5에서 겹침만 끈 것, M4 §5 조건 표 :337·:345); newest 조건(C0·C1·C2·C4)은 합의를 쓰지 않아 동작이 aad30d6과 비트 단위로 같다(가짜 세계 폐루프 대조; near를 끄면 C3·C5·C6도 비트 동일 — `r7c8_fixes.md`). §72의 "기본 조건 C0–C6 동작 비트 동일"은 §72의 W 변경에 대한 말이고, 이 절의 τ 변경으로 C3·C5·C6은 접촉 근처에서 달라진다. 열린 값: 슬롯(미래 스텝)의 near 여부 = 표를 처리하는 틱의 M1 관측으로 판단(미래 상태는 모름); 잔차 R 게이트(`_execute`, R 미학습 0 출력)는 거리 조건 그대로 둠.
- **D5 M4 설계 확장(SCOPED)**: 사전 등록된 실험 조건은 평탄한 C5 설정(E §4.12 :487 "γ=0.67, W=1")을 쓴다. M4 설계의 **경계 직후 엄격화**(W 2·γ 1.0 — M4 §3 #5 :150, §4.2 :214·:221 `W(s.zone_dist)`·`gamma(s.zone_dist)`, §4.4 W 행 :281 "1 (경계 직후 2)"·W_irrev :291 "경계 직후 2 → 3"), **큐 임계 조기 호출** g 0.5(§4.3 :259, §4.4 :289, §3 #11)·**장면 변화 조기 호출**(§4.3 :261 "P90 두 번 연속"), 그리고 같은 부류로 대조 중 찾은 **확정 못 한 채 실행한 스텝의 (b) 문턱 한 단계 엄격화**(§4.2 :225)는 사전 등록에 없는 M4 설계 확장이다 → 구현하지 않고 SCOPED(뒤의 절제 후보, 위 줄 번호). `runtime/m4.py` 머리 "Not implemented" 목록에 적었다. **§72 정정**: §72(:608)의 "M4 기본값(§4.4 '1, 경계 직후 2')… 의 뜻이 그대로"는 W의 정의(도전 표 수)에 대한 말이고, "경계 직후 2"가 구현된 기본값이라는 뜻이 아니다 — 코드는 평탄 W = 1(이 절).
- **E0.5·E1이 열어 둔 값(DOC D-2)** — 모두 판정 입력:
  - E0.5 **섭동 창 = 사건 뒤 1 s**(`e05.in_perturb_window` :159–161, `--perturb-window 1.0` :466): 사전 등록은 "섭동 직후 창"(:232)만 적음. 근거: 1 s = 결정 스텝 3개(T_c 0.33 s)로 재생 표(t_s − d_p95 − {0, .33, .66})가 사건을 걸치는 폭이고, M4 §3 #7 `C_flip` 직전 창의 예시 규모("직전 1초")와 같다. 판정 4(섭동 창 flip ≥ 성공 창 2배·AUROC)와 성공/섭동 스텝 분리가 이 값을 쓴다.
  - E0.5 **시간 블록 = 한 실행 안에서 바닥 부분집합을 차례로 두 번씩 묻는 통과 B개**(`e05` 머리 :11–13, `--blocks 2` :454, `--floor-n 0` = 전부 :455): 사전 등록은 "E0 시간대 슬롯과 같게 [가정]"·반복 횟수 [가정](:244–245). 근거: 우리 결정 모델은 로컬 서버라 E0(API) 시간대 슬롯이 없다 — 같은 실행 안의 앞뒤 통과가 서버 상태 변화를 재는 가장 가까운 대응이다. 판정 10의 입력. 시간대를 넓히려면 `--blocks`를 늘리거나 실행을 나눠 합친다.
  - E0.5 **`C_flip` (i)의 "직전 창"** = 직전 결정 스텝의 표 3개(`e05.c_flip_tv` :74–77); 런타임 `flip_score` = 그 질문의 최근 표 4개 대 그 앞 4개(`M4Params.flip_win` 4, `m4._update_flip`). 사전 등록·M4 §3 #7은 "예: 직전 1초"만 적음. 근거: 둘 다 약 1 s 규모(스텝 3개 = 0.99 s, 표 4개 ≈ 1.3 s), FLIP_TH는 E0.5 전 꺼져 있어 런타임 값은 기록만.
  - E1 **스냅샷 모집단 = 적합·보류 편의 연속 스냅샷 전부**(`calib.items_from` :34–53, `--decision-only` 기본 끔): 사전 등록 §3.3(:296)의 결정 스냅샷 풀 1,200은 정본 §52가 보정 자료를 CAL 편 반분으로 바꾸기 전 설계. 근거: 연속 스냅샷은 자연 분포 그대로(E §1.5 자연 가중치)이고 편당 항목이 많아 적합 400(§3.7-8) 조건에 가깝다; 과표집 모집단은 `--decision-only`로 남긴다.
  - E1 **반분 방법 = 편 id(`(kind, seed)`) sha256 정렬 뒤 교대**(`calib.halves` :28–31): §52는 "판 단위 둘로"만 정함. 근거: 시드·폴더 순서와 무관하게 결정적이고 두 반쪽 편 수가 ±1로 같다.
  - E1 **AUROC 부트스트랩에서 한 부류가 빠진 뽑기 = 0.5**(`calibration.evaluate._au` :155–157): 사전 등록은 뽑기 처리를 적지 않음. 근거: 한 부류뿐인 뽑기는 AUROC가 정의되지 않는다 — 버리면(조건부 구간) 하한이 부풀 수 있어, 우연 수준 0.5로 두는 쪽이 판정 1(ii) 하한 ≥ 0.70에 보수적이다(오답 < 30이면 어차피 판정 불가).
- **E §1.7 실행 기록의 해시 줄**(대조 중 찾은 것): 사전 등록 E §1.7(:133) "판정 절을 해시(SHA-256)로 남기고 해시와 시각을 실행 기록 첫 줄에". 평가 명령의 `meta`(e05·rd·calib·closed, `eval/common.run_meta`) 머리(`utc` 다음)에 `prereg` {written_utc, hashes(= `docs/stage3/prereg.json`), check("OK" | "CHANGED [...]", `tools/prereg_hash.py`로 지금 E-first 본문을 다시 해시)}를 싣는다. 문서가 없는 사본이면 "unavailable".
- **E1 범위(N5)**: `calib`가 내는 판정은 §3.7의 판정 1(θ 게이트)과 8(J5)이다. 판정 7(재보정 조건)은 보정 파일의 모델 지문 × question_id 결속으로 구현(불일치면 읽기 거부). 판정 2(확률을 양으로 쓰는 설계 — 지금 설계에 없음)·3–5(보기 이름·수·위치 형식 요인 — 형식은 정본 §27·§54로 고정, 요인 실험은 이 명령 밖)·6(Noul — 결정 질문에 Noul 없음), §3.5의 AURC·부류별 ECE·위치 편향·신뢰도 도표·`confidence` 필드 AUROC, §3.3의 2일 반복은 `calib`에 없다(SCOPED; E1 결과 문서에 "하지 않음"으로 적는다). §3.6의 "등위 회귀는 시험 ECE가 0.02 이상 낮을 때만" 비교: 정본 §52가 보정기를 **질문별 온도**(+ J5 문턱)로 정했고 런타임 보정 파일(`r6-calib-v1`)은 온도만 담는다 → 등위 회귀 비교는 하지 않는다(§3.6의 "기본은 온도"와 같은 쪽). §52 본문은 등위 회귀를 직접 언급하지 않으므로 이것은 이 절의 결정이다.
- **E-M4 H 인자(N8)**: `python -m harvest.eval.closed --m4-h {3|1}`(기본 3 = `M4Params.H`, 기본 출력 불변; H < 1 거부), 사전 등록 E §4.12·M4 §4.4 "H 1과 3 비교". 스펙·`meta.m4_H`에 기록.
- 정정 grep 목록(§69–§72)에 더한다: 판정 한 줄에 조건이 둘 이상이면 Holm 여부(`또는`·`or`), `ambiguous`·표시 제외 규칙, 카나리 규칙 문구, 설정 표의 괄호 값(예: "τ=1(접촉 근처 0)")이 호출 경로 끝까지 전달되는지.
- (§73 보충, 2026-09-25 04:37 UTC, 메인 결정) D4 τ 적용 범위: τ는 합의 원장 단계 규칙이므로 M4 §5 정의상 C5의 (a) 부분인 C3와 겹침을 끈 C5인 C6에도 같이 적용한다(C0·C1·C2·C4는 aad30d6과 비트 단위 동일, C3·C5·C6은 접촉 근처에서만 다름 — `r7c8_fixes.md`). 접촉 근처 구역은 정본 §7 문구대로(5 cm 이내 또는 접촉)라 성공 판에서 틱의 약 60–74 %를 덮는다; 정밀 단계만으로 좁히는 해석은 E-M4 절제 후보로 둔다(사전 등록 문구를 바꾸지 않음).

## 74. R7 9회차 결함 D1·D2 수정, Holm 방향, E-M4-lat의 STALE_MAX, 사전 등록 수치 경계 대조 (2026-09-25 06:10 UTC, `docs/stage3/results/r7_cycle9.md`, `docs/stage3/results/r7c9_fixes.md`) [메인 결정 D1·D2·N1, Claude 결정 N4·경계]
- 원칙(§72·§73과 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`, 조건 정의 `M4-overlap-commit.md` §4.4·§5)은 구속한다. 코드는 사전 등록 문장을 그대로 구현하고, 문장이 열어 둔 값·문장 사이의 충돌은 이 절에 적는다. 사전 등록 문서는 바꾸지 않았다(`tools/prereg_hash.py --check` OK). 코드 줄 번호는 이 수정 뒤 작업 트리 기준.
- **D1 γ = 0.67은 정확히 2/3("3표 중 2")**: 사전 등록 E §4.12(:487) "γ=0.67", §2A.3(:240) "LA-2·γ=0.67(C3의 합의 부분…)", M4 §4.4(:276) "0.67 (3표 중 2)", §3 #2(:147) "3표면 2표 = 0.67". 사전 등록 문구 "0.67"은 그대로 두고 뜻을 **0.67 = 2/3 정확히**로 정한다. 구현: `runtime/m4.GAMMA = "2/3"`(기록되는 M4 파라미터가 JSON으로 남도록 문자열), `m4.share_at_least(n, v, γ)` = 정수 교차 곱 `n·분모 ≥ 분자·v`(엡실론 없음, `Fraction`이 받는 값). 런타임 원장 `CommitLedger.try_commit_prefix`(:252)와 E0.5 재생 `analysis/replay.la2`(:20)·`eval/e05.apply_rules`(:62)가 **같은 함수**를 쓴다(이전: 런타임 `len(live)/len(votes) >= 0.67` → 3표 중 2표가 확정되지 않음, 재생 `≥ 2/3 − 1e-3`). 명시적 소수 0.67을 주면 문자 그대로(> 2/3)이므로 절제 수준 {0.5, 0.67, 1.0}(M4 §4.4)의 0.67도 "2/3"로 준다. 100표 미만에서는 [2/3, 0.67) 사이에 다른 분수가 없어 바뀌는 경우는 정확히 2/3(3표 중 2, 6표 중 4 …)뿐이다. 무작위 대조(가짜 세계 200판 × C0–C6, 지연 0.15–2.0 s·흔들림): C3·C5 23/200, C6 118/200만 a9b56e3과 원장 통계가 같고 나머지는 확정 상태가 달라짐(행동 바이트는 C3·C5 1판만 다름), **HEAD에 γ = 0.67(옛 문자 값)을 주면 C3·C5·C6 200/200 비트 동일**이고, 달라진 판(C3·C5 177, C6 82)은 γ 규칙이 정확히 2/3 공유를 본 판(계수 도구, 같은 집합)과 일치한다 → 차이는 2/3 경계에서만 온다. 절제 γ 1.0은 그대로(만장일치).
- **D2 C2 = 사전 등록 VLM Stream 그대로**: 사전 등록 E §4.12 D9(:495) "C2 = Slow Brain VLM Stream(T_c 고정 주기, in-flight 상한 없음·실측 보고, 요청 시각 기준 가장 새 유효 응답 하나 적용, 무효 무시, 5 s 타임아웃이면 기본 행동)", 정본 §20(:179), 정의 정본 M4 §5(:332) "동시 요청 상한 없음(… 실측 in-flight 수 보고), **매 틱** 요청(관측) 시각 기준 가장 새 유효 응답 하나의 보기를 그대로 적용, 무효 응답 무시, 마지막 유효 응답이 5 s보다 오래되면 기본 행동(타임아웃)". 구현: `conditions.py` C2 = `{"agree": "stream", "feedback_b": False, "stale_max": 5.0, "n_max_cap": False}`(:22). 원장 `agree == "stream"`(`m4.py` :184–190, :264–268)은 슬롯을 쓰지 않는다 — 질문마다 요청 시각(`t_state`)이 가장 새 유효 응답 하나를 들고, 그 응답이 5 s보다 오래되기 전까지 모든 스텝의 결정이다(대상 스텝이 이미 시작했어도 적용, FROZEN `log_only` 없음, 더 오래된 요청 시각의 응답은 "older"로 무시). `n_max()`는 C2에서 무한(`m4.py` :146–148). `OursRuntime.act`는 매 틱(100 Hz `act()` — §73 D4의 틱과 같은 뜻) 스텝 경계 뒤에 C2의 현재 결정을 다시 읽어 달라졌으면 실행 중 스텝에 `PickPlaceSkill.redecide`로 적용한다(`core.py` :451–456, `skills.py` :115–121). 다른 조건(C0·C1·C3–C6)의 동작은 a9b56e3과 같다(`agree "newest"`의 C0·C1·C4는 그대로). 열린 값(사전 등록이 정하지 않음): (i) 5 s 나이 = 지금 − 요청(관측) 시각(C2' Δt와 같은 정의, M4 :334); 도착 때 이미 5 s보다 오래된 응답은 `dropped_stale`로 센다. (ii) **기본 행동** = 결정이 없는 스텝과 같은 스킬 동작(결정된 움직임 없음, 제자리) — 모든 조건의 빈 스텝과 같다. (iii) 스텝 안에서 새 응답을 적용할 때 그 스텝의 움직임 예산 = 새 보기 크기(`MAG_CAP`) − 이 스텝에서 이미 움직인 양(스텝당 한 보기 크기를 넘지 않게; 사전 등록 "보기를 그대로 적용"을 스텝 단위 보기 뜻과 맞춤). (iv) 실측 in-flight = 결정 호출을 보낼 때 날아가 있는 수(그 호출 포함), `summary.dec_inflight` {max, mean, n_sends} — 모든 조건에 기록(보고만). (v) premise epoch 폐기는 공통 유효성 규칙으로 둔다(C2는 (b) 되먹임이 꺼져 있어 epoch는 Astra 결정에서만 바뀌고, E-M4 주 조건은 Astra 개입을 끈다 — E :483). (vi) H는 C2에서 적용에 영향 없음(응답 하나 = 질문마다 보기 하나). (vii) 융합 백엔드의 C2는 청크를 요청할 때의 가장 새 응답을 쓰고 스텝 안 청크 재요청은 하지 않는다(E-M4 주 비교는 모듈형 런타임). 무작위 대조: C2 200/200이 달라짐(의도), 지연 2.0 s에서 a9b56e3은 표 15,825/15,825 `dropped_stale`·성공 0/18 → 이 수정 뒤 0 폐기·8/18; 0.45–1.2 s와 흔들림 판의 `log_only` 310–1,005개 → 0.
- **N1 Holm은 사전 등록 방향으로만 기각을 센다**: 판정 2(E §2A.6-2 :257 "LA-2 또는 C2''가 newest보다 +2%p 이상(하한 > 0)")와 매일 카나리(E §1.8 :139·정본 §28 :264 "바닥보다 유의하게 크면(하한 > 0, Holm)")는 방향이 있는 가설이다. `stats.holm_ci(..., direction="greater")`(:87–101): 각 단계 수준 1 − α/(m − s)의 같은 양측 백분위 구간을 쓰되 **하한 > 0만 기각**이고, 0보다 완전히 아래인 구간은 기각이 아니며 다음 단계의 수준을 풀지 않는다(이전 양측: 음의 방향 유의가 다음 가설을 95%로 풀었음 — 9회차 N1). `e05.gain_holm`(:222)·`harvest/canary.canary_compare`(:48)이 이것을 쓴다. 판정 10(E §2A.6-10 :265 "시간 블록 사이 차이가 유의하면")은 블록 쌍의 순서가 임의이고 `block_diff_lo`가 max(lo, −hi)로 두 방향을 보므로 **양측 그대로**(`direction` 기본값). §72의 "양측 구간"은 구간 모양에 대한 말로 그대로 맞다.
- **N4 E-M4-lat 지연 점과 C5 STALE_MAX 1.5 s(사전 등록 안의 충돌)** — 방향은 사전 등록 문장으로 정해지고 척도 규칙은 열림, 코드 없음: C5 설정 STALE_MAX 1.5 s(E §4.12 :487, M4 §4.4 :288 "jev-drone", 근거는 차용값뿐)를 E-M4-lat 지연 p95 2 s·5 s(E §4.12 :490, M4 §5.1 (1) :374–392)에 그대로 쓰면 모든 표가 도착 때 이미 1.5 s보다 오래되어 버려진다(9회차 `c2_stale.py`: 2.0 s C5 1,230/1,230) — C5뿐 아니라 같은 원장 규칙을 쓰는 C1도 결정을 하나도 받지 못한다. 사전 등록 문장으로 정해지는 것: E-M4-lat가 재려는 것은 "지연이 커질 때 C1·C2·C2'·C5의 섭동 성공률이 어떻게 떨어지는가"(M4 :375), 판정 11 "지연이 클수록 **확정 규칙** 이득"(:381), 판정 11e "5 s 점에서 C5의 **미확정 실행 스텝 비율** 중앙값이 50%를 넘으면 '(a)는 이 지연에서 사실상 꺼짐'"(:385 — 5 s에서 C5가 표를 받아 가확정으로 실행한다는 전제), 구현 문장 "요청 발사 시각·d̂·N_max는 **목표 분포의 값으로 다시 잡는다**(스케줄러가 지연을 아는 조건)"·"d*는 E0 실측 지연 분포를 p95가 목표값이 되도록 **척도만 바꿔**"(:378). 표를 모두 버리는 읽기로는 판정 11·11e가 확정 규칙이 아니라 "모델 없음"을 재게 되어 사전 등록 목적과 어긋난다 → **E-M4-lat에서는 STALE_MAX도 지연에 묶인 원장 값으로서 목표 분포에 맞춰 다시 잡는다**(주 지연 E-M4에서는 1.5 s 그대로). 척도 규칙은 사전 등록이 적지 않았다 — 두 읽기: (가) 같은 척도 배율 STALE_MAX × d*_p95 / d_p95(E0)(분포 모양이 같다는 [가정] 아래 폐기 비율이 주 지연과 같게 유지 — "척도만 바꿔"와 같은 방식), (나) 1.5 s + (d*_p95 − d_p95(E0))(p95 뒤 여유를 초로 고정). 사전 등록 문장은 방향(다시 잡는다)까지는 정하지만 (가)·(나) 중 하나를 정하지 않는다 → **어느 쪽도 고르지 않고 메인 세션 결정으로 올린다**((가)가 "척도만 바꿔"와 같은 방식이라는 점만 적어 둔다). 둘 다 주 지연(d*_p95 = d_p95(E0))에서는 1.5 s다. 지연 큐 구현(E-M4-lat, 아직 없음) 전에 정하고 E-M4-lat 결과 문서에 두 읽기와 선택을 적는다. C2·C2'의 5 s 타임아웃은 원문 고정값이라 지연 점마다 그대로(Slow Brain Fig. 6도 고정 타임아웃으로 지연을 스윕). **지금 코드는 바꾸지 않는다**(지연 큐가 없어 E-M4-lat 경로가 없음; 주 지연의 동작 불변). [결정 필요: 메인 세션] (가)·(나).
- **사전 등록 수치 문턱의 경계 대조**(9회차 지시): 판정 입력은 **반올림하지 않은 통계**로, 문턱은 사전 등록 문구의 방향(≥·≤·<·>) 그대로, 부동소수 잡음 허용 `stats.CMP_EPS = 1e-12`(`at_least`·`at_most`·`below`·`above`, :9–29)로 비교한다(정확히 문턱과 같은 유리수가 부동소수 연산으로 반대쪽에 떨어지지 않게; 판정이 보는 개수 비율 사이의 간격보다 훨씬 작음). 표시용 반올림(4–5자리)은 출력에만. 고친 것: (1) E0.5 `judge_input`이 4자리 반올림 값이었다(예: flip 500/10001 = 4.9995% → 0.05로 판정 1 대신 3) → `e05._mean_x`·`_lo_x`·`_diff_lo_x`(:184–196)로 반올림 없이, 판정 7·8도 같음, `gain_holm`의 lo·hi도 반올림 없음(하한 1e-5가 0.0이 되던 것); (2) 판정 4 "AUROC 0.03 이상 높은 쪽": 0.83 − 0.80 = 0.0299999… → `at_least`; (3) 판정 9 "A1 ≥ A0 − 2pt" 하한 −0.02 경계 → `at_least`; (4) E1 판정 1 (iii) θ − 0.08(0.8 − 0.08 = 0.7200000000000001)·θ − 0.03, 판정 8 1 − α − 0.03 → `at_least`; (5) E1 §3.6 "원 확률 ECE ≤ 0.03": |78/100 − 0.75|가 0.030000000000000027로 계산되던 것 → `at_most`; (6) E1 판정 1·8이 반올림된 ECE(5자리)·AUROC·정확도·적용률·단일 집합 비율(4자리)로 판정하던 것(예: AUROC 12824.5/17100 = 0.749971이 0.75로 통과) → `calibration.evaluate`가 `judge_values`(반올림 없는 값)를 함께 내고 `judge_question`이 그것으로 판정(:206, :210–234); (7) M1 `near` 들어가기 "거리 ≤ 5 cm"(M1 :140·정본 §7 :45)가 `<`였던 것 → `predicates.py:59`·`stereo/pipeline.near_update`·`sim/planner.py:272`·`cli_pool.py:100`·`datagen/gen.py:81`·`sim/labeler.py:200`을 `≤`로, 그에 맞춰 애매 띠(`snapshot.ambiguous_predicates`)는 이력에 따라 값이 갈리는 (5, 6] cm(5 cm 정확히는 이력과 무관하게 참). 연속 물리량이 정확히 5 cm가 될 일은 사실상 없어 기존 풀·R2 자료의 값은 바뀌지 않는다(재생성 불필요). 기록만(코드 무변경): E-M4b-meas PC3 "+0.05"·V1q "+0.03"(`m4b/analyze.py:465`, :443)의 부동소수 덧셈 경계와 D28 :176 "지연 증가 ≤ 50 ms" 미구현 — §64로 끝난 판정이고 여유가 커서(0.786 대 0.871) 결과 불변, 다시 쓸 때 `at_least`로; `e05 --d-p95` 기본 0.307은 정본 §55의 판정 값(자리값) — 본 실행은 E0 측정 d_p95를 넘긴다(E §2A.8 재실행 규칙은 수동). 대조 표와 시험하지 못한 문턱(E2a·EVAL H1–H3 판정 코드 없음 등)은 `r7c9_fixes.md` §4.
- **DOC D-1**: `docs/stage3/results/r7_fixes.md:45` "한 모델·하루 한 검정이라 Holm 없음"에 [해결 R7 8회차, §73 D3] 표시.
- **N6 (SCOPED)**: `SUMMARY.md:287`·`:589`의 "경계 직후 W=2·γ=1.0"에 [SCOPED R7 9회차, §73 D5·§74] 표시(사전 등록 C5는 평탄 W 1·γ 0.67 = 2/3). **논문 갱신 항목(지금 `paper/` 수정 안 함, §70의 논문 보류와 같은 처리)**: `paper/sec/3_method.tex:103`의 경계 적응형 서술("경계 직후 $W=2,\gamma=1.0$ …")을 평탄 C5로, γ 0.67 = 2/3 정의, C2 = VLM Stream 원문 충실판(5 s 타임아웃·상한 없음·매 틱 가장 새 응답), Holm 방향(하한 > 0 방향만), E-M4-lat STALE_MAX 척도 규칙(N4) — E-M4 결과를 쓰기 전에 반영.
- **§73 정정**: §73 D4의 "newest 조건(C0·C1·C2·C4)은 … aad30d6과 비트 단위로 같다"는 그때의 사실이고, 이 절부터 C2는 `agree "stream"`(newest 조건은 C0·C1·C4).
- 정정 grep 목록(§69–§73)에 더한다: 비교 연산의 경계(`>=` 대 `>`, 0.67 대 2/3, 반올림 뒤 비교, 부동소수 문턱 산술 `θ - 0.08`), 기준선 조건 정의(C0–C6·C2 계열)를 설정 **값**이 아니라 사전 등록 조건 문장 전체(상한·타임아웃·적용 시점)와 대조, 한 사전 등록 안의 다른 실험(E-M4-lat)이 같은 설정 값을 다른 지연에서 쓸 때의 뜻.
- (§74 보충, 2026-09-25 06:20 UTC, 메인 결정) **N4 → (가) 비례 규칙 채택**: E-M4-lat의 지연 점 d*에서 STALE_MAX = 1.5 s × d*_p95 / d_p95(E0). 근거: M4 :378 "척도만 바꿔"는 곱셈 척도 변환을 말하며, d̂·N_max를 "목표 분포의 값으로 다시 잡는다"와 같은 방식(비율)으로 맞춘다. 주 지연에서는 1.5 s 그대로라 지금 동작은 바뀌지 않는다. 구현은 지연 큐와 함께 E-M4-lat 준비 때(테스트: d*=d_p95(E0)면 1.5 s, 2배면 3.0 s). 판정 10의 Holm은 양측 유지(블록 쌍 순서가 임의) — 수정 에이전트 판단 채택.

## 75. R7 10회차 결함 D1 수정(H = 1 = 같은 스텝을 앞당겨 묻기), 나이 비교 경계, 문서 정리 (2026-09-25 07:26 UTC, `docs/stage3/results/r7_cycle10.md`, `docs/stage3/results/r7c10_fixes.md`) [Claude 결정, 열린 값 1건 메인 확인 요청]
- 원칙(§72–§74와 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`, 조건 정의 `M4-overlap-commit.md`)은 구속한다. 코드는 사전 등록 문장을 그대로 구현하고, 문장이 열어 둔 값은 이 절에 적는다. 사전 등록 문서는 바꾸지 않았다(`tools/prereg_hash.py --check` OK). 코드 줄 번호는 이 수정 뒤 작업 트리 기준.
- **D1 H = 1 = "같은 스텝을 시각을 당겨 여러 번 묻기"**: 사전 등록·정본 문장 — 정본 §2(:18) "H=1일 때 M4 (a)의 여러 표는 '같은 스텝을 시각을 당겨 여러 번 묻기'로 얻는다", M4 §4.1(:188) "이때 같은 스텝의 여러 표는 **호출 시각을 스텝 시작보다 앞당겨 같은 스텝을 2~3회 묻기**(질문 문구 동일)로 얻는다", (:189) "H = 3이면 각 스텝이 받는 표 수 ≈ … 약 3표. H=1이면 앞당김 횟수만큼", §4.4 H 행(:273) "H=1은 호출 시각 앞당기기로 표 수 확보", E §2.6(:183) "스텝 시작 전 `lead_max` ∈ {1.0, 1.5} s부터 `d_p95` 전까지 T_c 간격으로 같은 스텝을 묻는 H=1 방식(M4 §4.1)에서 고정 구간 경계 전에 도착한 답의 수", E §2.7-4(:197) "T_c=0.33, lead_max=1.5 s일 때 스텝당 표 수 중앙값 ≥ 2이면 M4 (a)의 LA-2 확정이 성립 가능", E §2A.3(:236) 재생 "H=1 '같은 스텝을 시각을 당겨 2~3회 묻기'(M4 §4.1)를 흉내 … t_s − d_p95 − {0, 0.33, 0.66} s", M4 §5 판정 7(:365) "C5(H=3)가 C5(H=1)보다 주 지표 하나 이상에서 CI 하한 > 0이면 H=3, 아니면 입력이 짧은 H=1". 구현: `runtime/m4.early_ask_steps(t_send, d̂, T_c, lead_max)` — H = 1 호출은 시작 시각이 [t_send + d̂, t_send + lead_max](양 끝 포함)인 **모든 스텝의 앞당긴 표**다(E §2.6 스케줄 "스텝마다 lead_max 전부터 d̂ 전까지 T_c마다 묻기"를 호출 쪽에서 본 것; 오프라인 `analysis/latency.votes_per_step`과 같은 규칙). `CommitLedger.target_slots`가 H = 1이면 이것을 쓰고 H > 1은 그대로다. 앞당김은 사전 등록 문장이 모두 "H=1일 때"로만 적었고 H = 3은 "H가 약 3표를 준다"(:189)이므로 **H = 3 동작은 6522fda와 비트 동일**(무작위 대조 — 가짜 세계 폐루프 300판 × C0–C6, 지연 0.2–2.5 s·흔들림, H ∈ {1, 3}, W ∈ {0, 1, 2}, 잡음 선택기: **H = 3 판 155/155가 7개 조건 모두 비트 동일**(나이 경계 N1 포함); H = 1 판 145개는 의도대로 달라짐 — 확정이 한 번이라도 일어난 판 C5·C3 32 → 88, C6 9 → 45; C2는 행동 바이트 145/145 동일(표 계수만 다름, §74 (vi))).
  - 열린 값 (i) **`lead_max` 기본 1.0 s**(`M4Params.lead_max`, `closed --m4-lead-max`, 스펙·`meta.m4_lead_max`, `RuntimeConfig.m4`): 사전 등록 후보 {1.0, 1.5}(E §2.6), 확정은 E0 뒤. 1.0을 고른 근거: 정본 §2가 가리키는 정의(M4 §4.1) "2~3회"와 E0.5 재생(t_s − d_p95 − {0, .33, .66}, 3표)이 현재 d_p95 0.307 s에서 1.0이면 그대로 나온다(격자 위 호출의 앞당김 0.33·0.66·0.99 s, 재생과 요청 시각 일치 — 시험 `test_runtime_h1_asks_match_e05_replay_and_e0_count_on_a_grid_schedule`), 1.5면 4회(0.33–1.32 s)로 "2~3회"를 넘는다. E0 판정 4는 1.5 s에서 "중앙값 ≥ 2"를 보므로, **E0 d_p95가 약 0.67 s를 넘으면 1.0에서는 스텝당 1표**(가짜 세계 0.9 s: 중앙값 1, 확정 5 대 1.5 s 249)라 1.5로 바꿔야 한다 — E0 결과를 받을 때 메인 세션이 {1.0, 1.5} 중 하나로 확정(**[결정 필요: 메인 세션, E0 뒤]**). (ii) 창 안에 스텝이 없으면(lead_max < d̂ + 격자 간격) d̂ 뒤 첫 스텝 하나(M3 원안 "한 요청 = 한 결정 스텝")에 표를 준다. (iii) 조건 범위: H는 호출 단위 속성이라 슬롯을 쓰는 모든 조건(C0·C1·C3–C6)에 같이 적용, C2(stream)는 영향 없음(§74 (vi)). (iv) 확정 뒤 도착한 앞당긴 표는 확정 불변 규칙대로 `log_only`(3회 묻기 중 LA-2가 두 번째 표에서 확정하면 세 번째는 기록만 — 스텝당 쌓인 표 중앙값 2).
  - **알려 둘 한계(SCOPED, 판정 7 해석)**: 단계 A DecCall은 결정 계열마다 질문 하나(스텝 번호에 묶이지 않은 답)라 한 호출의 답이 여러 슬롯의 표가 된다 — H = 3도 "답 하나 = H 슬롯의 표"(기존 구현, `m4.py` 머리 설명)이고 H = 1도 같은 방식으로 창 안 슬롯의 표가 된다. 그래서 이 런타임에서 H = 1(lead 1.0)과 H = 3은 **호출이 스텝 격자에 맞고 지연이 T_c보다 작으면 같은 슬롯에 표를 준다**(가짜 세계 지연 0.15·0.3 s에서 원장·행동 동일, 0.45 s 이상·조기 호출에서만 H = 1 표가 적다). 사전 등록이 H로 비교하려는 입력 길이·질문 내용 차이(M4 §4.1 "H=3이면 D-줌 세밀 질문은 첫 스텝에만", E §2.7-5 크기 효과, 판정 7 "입력이 짧은 H=1")는 이 런타임에 없다(요청은 H와 무관). 다중 스텝 DecCall(스텝마다 질문 하나, H개)은 단계 A 학습 형식(한 질문)을 바꾸는 일이라 이 수정 범위 밖이다(§71 학습 파일 보호). → **[결정 필요: 메인 세션]** E-M4 판정 7 전에 H = 3 다중 스텝 DecCall을 만들지, 판정 7을 "이 런타임의 H 차이 = 표 창 차이뿐" 단서로 낼지.
  - `lead_max`는 §59의 "호출 방식 lead"(vLLM 첫 시퀀스로 캐시 채우기, `RuntimeConfig.call_mode`)와 **다른 것**이다(이름만 겹침).
- **N1 나이 비교의 경계**: `m4.older_than(age, limit)` = age > limit + `T_EPS`(1e-9 s — 원장이 슬롯 시작 경계에 이미 쓰는 시각 허용, 10 ms 틱보다 훨씬 작음)를 `on_vote`의 STALE_MAX 폐기(C5 1.5 s, C2 5 s)와 C2 `decision`의 5 s 타임아웃에 쓴다. 정확히 1.50 s·5.00 s인 나이는 모든 틱 위치에서 유지, 한 틱 더(1.51·5.01 s)면 폐기·기본 행동. 이전 원시 비교는 100 Hz 틱 6,000 위치 중 1.5 s 174곳·5 s 372곳에서 한 틱 일찍 폐기했다(검증자 계수와 같음). 비율 판정용 `stats.CMP_EPS`(1e-12) 대신 1e-9를 쓴 이유: 시뮬 시각은 누적 부동소수 합일 수 있어 오차가 1e-12를 넘을 수 있다. §74 보충의 E-M4-lat 비례 STALE_MAX도 구현 때 같은 `older_than`을 쓴다. 범위 밖(기록): `core.py`의 T2 확인 대기 2 s·V1h 나이 비교(측정 창, 사전 등록 문턱 아님).
- **DOC D-1**: `handoff.md` 9회차 줄·`r7c9_fixes.md:15`의 N4 "[결정 필요: 메인 세션]"에 [해결 R7 10회차 D-1: §74 보충 (가) 비례 규칙] 표시.
- **N4**: near "< 5 cm" 옛 서술에 [정정 R7 10회차 N4, §74: 들어가기 ≤ 5 cm] 표시 — `e3st.md:66`, `planner_dev.md:102`, 옛 계획서 `2026-09-24-stage3-experiments.md:400`(`:314`는 0.049 m라 참, 표시 안 함; `tests/test_predicates.py:22` 주석도 참).
- **N7 논문 갱신 항목(§74 목록에 더함, 지금 `paper/` 수정 안 함)**: H = 1 = 같은 스텝을 앞당겨 묻기(`lead_max` ∈ {1.0, 1.5} s, 한 호출의 답이 창 안 스텝들의 표, 이 런타임에서 H = 1과 H = 3의 차이는 표 창뿐이라는 단서), 나이 비교 경계(정확히 STALE_MAX인 표는 유지).
- 관련 문서 표시: `r5_closed_loop.md:33`(H = 3 규칙, H = 1 앞당김), `r7c8_fixes.md:20`·`:93`(N8 "일치" → 인자만 전달), `r7_cycle9.md:124`(행 51).
- **대조표 점검 방식(9회차 N8 교훈을 넘어)**: 조건 **변수 행**(H·W·γ·τ·N_max·조기 호출·FLIP_TH)은 값 전달이 아니라 그 변수가 사전 등록에서 만들어야 하는 **결과**를 원장·폐루프에서 센다 — 이번에 행동 시험으로 바꾼 행: H(스텝당 표 수·확정 수), 조기 호출(초당 호출 예산 불변), FLIP_TH 끔(높은 flip_score에도 확정), C0·C1·C6 in-flight 1·C5 겹침 ≤ N_max(`tests/runtime/test_r7c10_prereg.py`). 정정 grep 목록(§69–§74)에 더한다: 사전 등록 변수의 **정의 문장**(예: "H=1은 앞당겨 묻기")이 인자 전달 말고 원장 결과로 확인되는지, 오프라인 분석에만 있는 매개변수(`lead_max`처럼)가 런타임에도 있는지.
- (§75 보충, 2026-09-25 07:44 UTC, 메인 결정) (1) **lead_max 기본 1.0 s 확정**(사전 등록 후보 {1.0, 1.5} 중; M4 §4.1 "2~3회"·E0.5 재생 3회와 일치). 최종값은 E0에서 d_p95가 나온 뒤 E §2.7-4 규칙(스텝당 표 중앙값 ≥ 2)으로 다시 정한다 — d_p95 > 약 0.67 s면 1.5 s. (2) **판정 7(H=3 대 H=1) → SCOPED**: 지금 결정 호출(단계 A DecCall)은 결정 종류마다 한 질문이 여러 스텝의 표가 되므로 H=1·H=3의 입력 길이·질문 내용 차이가 없다; 판정 7의 전제(H=3 = 3스텝 입력)는 다스텝 DecCall(학습 형식 변경)이 있어야 성립한다 → 그때 사전 등록 문구대로 시험. E2E 학습 준비(완료 정의 1–5)와 무관. (3) 창 안에 스텝이 없을 때 d̂ 뒤 첫 스텝에 투표하는 대체 규칙 채택(사전 등록 밖).
## 76. R7 11회차 정정 (2026-09-25 08:30 UTC, `docs/stage3/results/r7_cycle11.md`) [Claude 결정]
- **D-1**: `handoff.md` 10회차 줄과 `r7c10_fixes.md`(행 51, §5 1–3)의 판정 7·lead_max '결정 필요'에 §75 보충 해결 표시.
- **D-2 풀 '경계 사례' 층 정의(사전 등록 E §1.5 :121이 열어 둔 값)**: 경계 사례 = 그 스냅샷이 히스테리시스 띠 안(`ambiguous`)이거나, 그 스냅샷에서 시작하는 결정 스텝(다음 연속 스냅샷, 0.33 s) 동안 등록부 술어 값이 하나라도 바뀌는 경우(`harvest/sim/snapshot.py` `boundary_flags`, 과표집 30 %는 `select_decision`). 근거: near 띠만으로는 스냅샷의 4.6 %라 30 %를 채울 수 없고, 앞 스냅샷까지 세면 층이 전체의 35 %라 '과표집'이 아니게 된다(`pool.md` §1). **섭동 배정** = 시드로 정한 고정 순열, P0·P1·P2 각 40편, 분할 fit/eval 60/60(섭동별 20/20). 이 층은 E0.5·E1 기본 판정에 쓰이지 않고 E-M3-1·E3·`calib --decision-only`에서 쓰인다.
- **N4**: `M4Params.lead_max`는 유한한 양수만 받는다(`inf`·`nan` 거부 — 검증 통과 뒤 첫 호출에서 OverflowError가 나던 것, 시험 `test_lead_max_must_be_finite_and_positive`).
- **N1 문구 정정**: §75의 'H = 3 비트 동일'은 지연이 정확히 1.5 s(C2는 5.0 s)인 경계 판을 뺀 표본에서 참이다 — 경계 판은 §75 N1(나이 경계 일관화)이 의도적으로 바꾼다.
- **N3 (E0 뒤 lead_max 재결정 때)**: '"d_p95 > 약 0.67 s면 1.5 s"'는 호출이 스텝 격자에 맞을 때 기준이고, 격자 밖 호출이면 문턱이 약 0.34 s로 내려간다 → 재결정은 E0 오프라인 수와 함께 **런타임 원장의 스텝당 표 수 중앙값 ≥ 2**로 판단한다.

## 77. R7 12회차 결함 D1–D4 수정, (b) 범주 되먹임 형식(ser-A-min-2), 호출 기록 범위, NOTE 정리 (2026-09-25 10:00 UTC, `docs/stage3/results/r7_cycle12.md`, `docs/stage3/results/r7c12_fixes.md`) [Claude 결정, 메인 확인 요청 2건]
- 원칙(§72–§76과 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`, 조건 정의 `M4-overlap-commit.md`)은 구속한다. 코드는 사전 등록 문장을 그대로 구현하고, 문장이 열어 둔 값은 이 절에 적는다. 사전 등록 문서는 바꾸지 않았다. 코드 줄 번호는 이 수정 뒤 작업 트리 기준.
- **D3 (b) 범주 → 결정 모델 입력**: 사전 등록 문장 — M4 §4.2 의사코드(:232) `next_jev_input.add_line(f"last_step: {s.outcome}")  # (b) → Jev 입력 (C5'와 대조)`, §3 #10(:155) "Jev에 넘길 때는 범주 한 줄만", §0(:43) "범주를 다음 Jev 입력에 넣는다", 조건 표 :340 "C4 | 겹침 + (b)만(범주 + epoch 무효화, 가장 새 표)", :341 "C5 | 겹침 + (a) + (b) + TIDE 경고 (§4.2 전체)", :342 "C5' | C5에서 (b) 범주를 Jev 입력에서 뺌(코드 재계획만)", C6 "C5에서 겹침 끔", 판정 3(:361) "C5가 C5'보다 섭동 성공률 +5%p 이상이면 '(b) 범주를 결정 모델에 되먹임'을 주장". 정본 §58 "하나의 VLA = … typed 결정 토큰 … M4가 소비"(결정 모델 = 융합 모델, 모듈형 기준선은 Jev-L), §60·§61·§64 "(b) 측정 = 로봇 쪽 T1 고유 감각 코드 + 세계 쪽 확인 헤드 V1h"(범주의 출처). 구현: (i) **형식** — 모든 DecCall 상태(S1·IMG·S0 모두, 질문 바로 앞)의 마지막 줄이 `last_step: <범주>`, 범주 ∈ {none, OK, LAG, DEVIATE, CONTRADICT}(`serialize.with_last_step`, `deccall_snap.build_snapshot_request`, `runtime/models.build_live_request(last_step=)`). 행동 전문가 문맥(`ctx_text`)에는 넣지 않는다(사전 등록 대상 = Jev 입력 = 결정 질문). (ii) **런타임 값**(`core.OursRuntime.b_line`) = 마지막으로 끝난 결정 스텝의 (b) 범주(스텝 경계에서 `step_outcome`·융합 관절 잔차 → T1 CONTRADICT 덮어쓰기, 그 스텝의 늦은 T2 DEVIATE가 오면 OK·LAG를 DEVIATE로 올림), 아직 끝난 스텝이 없으면 none. 같은 틱에 보내는 호출은 경계 판정 전에 만들어지므로(`act` 순서) 다음 호출부터 실린다(DEVIATE·CONTRADICT는 조기 호출로 다음 틱). (iii) **조건**: C4·C5·C6(`feedback_b`)만 범주를 싣는다. C0–C3는 (b)가 없어 항상 none. **C5'를 런타임 조건으로 추가** = C5 + `RuntimeConfig.b_to_model=False`(원장 epoch·재개방·hold 그대로). [Claude 결정] C5'의 "뺌"은 **줄을 지우지 않고 값을 none으로** 둔다 — 학습 모델은 이 줄이 늘 있는 입력으로 학습되므로 줄을 지우면 판정 3이 (b) 정보가 아니라 입력 분포 이탈을 재게 된다; none은 학습 자료에서 "확인 없음"(첫 스텝·S-E2E)으로 본 값이라 범주 정보만 빠진다. 폴더·과제 이름에서는 `C5p`. (iv) **학습 자료 값**(`deccall_snap.annotate_last_step`·`last_step_of`, 단계 A `stagea_data.build_items`, 단계 B `stageb_data.load_stageb`, 오프라인 평가 `eval/common.load_episodes`·카나리): 그 편 **자신이 기록한 실행 뒤 확인**에서 런타임 규칙(`category_of_check`: 스텝 안 단계 전환 → 확인 없음 → OK, T1 술어 기대 어긋남 → CONTRADICT, T2 → DEVIATE, 아니면 OK)으로 낸다. R2(`datagen`) 줄은 이미 기록된 `verify.prev_step`(`datagen/rows.verify_prev_step`: 앞 결정 스텝 단계의 `m4b.spec.EXPECT`를 이번 관측의 참값 9술어로 판정)을 그대로 쓴다 — **지금 도는 R2_TRAIN 생성(6b013ac 고정)에 새 필드가 필요 없다**. POOL·DEV(`cli_pool`, 0.33 s 스냅샷, `verify` 없음) 줄은 앞 스냅샷의 단계 기대를 이번 스냅샷의 기록 술어(`truth9`, contact_stall 모름)로 같은 규칙에 넣는다. 첫 스냅샷 = none. S-E2E(ROBOTIS 원격 조작 공개 자료)는 (b) 확인이 없고 DecCall 상태가 아닌 자체 문맥(`se2e.*@v1`)이라 줄이 없다(= 확인 없음; `se2e_data.py`는 이 수정에서 건드리지 않음). (v) **알려 둘 차이(한계)**: 자료의 범주는 참값 술어 확인만이라 런타임 잔차 범주(LAG, 잔차 DEVIATE)는 학습 자료에 나오지 않는다; 런타임은 T1 고유 감각 규칙 + V1h conformal(unknown이면 판정 없음), 자료는 시뮬 참값; 모듈형 백엔드는 V1h가 없어 런타임 T2 DEVIATE가 없다. 섭동 P1(목표 2 cm 이동)·P2(방해물)는 단계 기대 술어를 거의 깨지 않아 자료의 DEVIATE는 드물 수 있다 — 본 학습 전에 R2 자료의 범주 분포를 센다(메인 확인 요청 1).
  - **형식 판본 = `SERIALIZER_VERSION` "ser-A-min-2"**(J1 "상태 직렬화기 판본" → `question_id@vN` 전부 바뀜 → E §3.7-7 재보정 조건 그대로: 보정 파일은 `Calibration.load`가 question_id 해시 불일치로 거부). 단계 A `prompt_config`에 `serializer` 필드. 단계 B `stageb_train.prompt_config`(S-E2E 작업 파일이라 이 수정에서 건드리지 않음)에는 필드가 없어 `stagea_train.serializer_of`가 **프롬프트 파일이 바이트 단위로 지금과 같을 때만** 현재 판본으로 읽는다(판본 상수가 그 파일 안에 있다); 다음에 `stageb_train.prompt_config`를 고칠 때 `"serializer": SERIALIZER_VERSION`을 넣는다. 거부: `eval/common.training_prompt_config`(e05·calib·rd·closed·canary가 먼저 읽음)와 융합 서버 `fused_model.check_prompt`(`serializer_ok`)가 옛 판본을 "trained with state serializer 'ser-A-min-1 …', this code feeds 'ser-A-min-2' (canon §77 …) -- retrain" 메시지로 거부.
  - **무효가 되는 것**: 지금까지의 모든 체크포인트(단계 A §55 스모크, 단계 B·R4 스모크, E-M4b-meas V1h 헤드 판, S-E2E 스모크 — 모두 §56 파이프라인 확인용이라 논문 수치 손실 없음). **진행 중인 S-E2E 학습(6b013ac 프롬프트 파일 고정, `prereg_se2e.md`)의 체크포인트도** 이 코드의 `check_prompt`·`training_prompt_config`에서 거부된다 — S-E2E 결정 항목은 원래 자체 질문(`se2e.*@v1`)이라 DecCall 런타임과 형식이 달랐다(SCOPED S7); S-E2E 평가를 어느 코드로 돌릴지는 메인 확인 요청 2. 보정 파일·카나리 기준일(질문 id·프롬프트가 바뀜 → 새 기준일)·qid 등록부 항목도 새로.
  - 무작위 대조(가짜 세계, 모의 선택기, 160판 = 8조건 × 20, H ∈ {1, 3}, W, 잡음, 지연 고정·흔들림, 강제 DEVIATE): 6b013ac와 비교 가능한 140판(C5' 제외) 모두 행동 바이트·원장 counts·epoch·호출 수·`last_step` 줄을 뺀 요청 상태가 같다; 달라진 것은 요청 상태의 마지막 줄뿐(C4·C5·C6 요청 2,068개 중 범주(OK·DEVIATE)를 실은 것 1,968개 — 나머지는 첫 스텝이 끝나기 전 none; C0–C3·C5' 요청 3,438개는 모두 none). 모의 선택기는 줄을 읽지 않으므로 학습 모델에서만 행동이 달라진다.
- **D4 호출 재현 기록**(E §1.6 :125–127, 정본 §28 A6 :255, §42): 결정 호출 행에 `probs`(질문별 보기 확률, option_key 기준; 보정 시 `probs_cal`), `last_step`, `request_blob`(= `request_sha256`: 해시하는 정규 JSON 본문 그대로 — 결정 요청 JSON·질문·상태, 이미지는 바이트 해시), `response_blob`(백엔드 원응답: Jev-L은 CallRecord의 보기 이름별 확률·확신·n_seq·토큰·시각, 융합은 서버 `/decide` 응답(이름별 확률 + 확인 헤드 로짓 + meta), 모의는 규칙 답). Astra 행에 `output_text`(원응답), `max_output_tokens`, `effort`, `request_blob`(요약 텍스트가 든 요청 본문), 머리캠 JPEG 원본(`image_sha256`의 blob). blob은 사이드카 옆 `<run>/blobs/<sha256>.{json,jpg}`에 내용 주소로 한 번만 쓴다(`ir_policy.write_blobs`, `trial_metadata.ours_blobs`). 크기(가짜 세계 60 s): 결정 요청 약 2.4–2.8 KB·응답 약 0.55 KB × 181회 + Astra ≈ 0.55–0.6 MB/편(+ Astra JPEG). [Claude 결정] **결정 호출의 이미지 바이트는 저장하지 않고 해시만**(머리 + 손목 약 100 KB × 초당 3회 ≈ 18 MB/편, E-M4 수천 편이면 수십 GB; E §1.6의 "원문 JSON"은 텍스트 Jev 시절 목록이고, "이미지 바이트 해시 + 원본"은 §28 A6의 Astra 규칙이라 Astra만 원본 저장). vLLM 전송 본문(트라이 시퀀스별 base64 요청)은 요청 JSON + 이미지 + 토크나이저의 결정적 함수(`jevl._body_mm`)라 따로 두지 않는다. 검사기 결과·계획 서명·patch 편집 거리는 §67 C8 SCOPED 그대로.
- **D1 FLIP_TH 후보**: `e05` `c_flip.flip_th_candidates` = 성공 궤적 1−α 분위, α ∈ {0.01(사전 등록, E :487·M4 :285), 0.001, 0.05}(M4 §4.4 α 범위) → 키 q0.99·q0.999·q0.95, `flip_th_initial` = α 0.01(M4 :287 "E0.5 재생 자료로 먼저 잡는다"). 범위 밖 α 0.1은 뺐다.
- **D2 E0.5 지표**: `same_time_flip_by_trajectory` = {success: P0이며 성공한 편, perturbed: P1·P2 편의 모든 스텝}(E §2A.4 "성공 궤적(P0 무섭동)과 섭동 궤적(P1~P2)을 나눠", 실패한 P0는 어느 쪽도 아님), 판정 6은 사전 등록 문장대로 합친 값 그대로. 층별 `a1flip`(= `subst`)·`a2flip`·`a3flip`·`a4flip` = A0 대비 option_key flip(정본 §27).
- **DOC**: D-1 handoff 정본 범위 §1~§77; D-2 `r7c9_fixes.md:88` 해결 표시; N1 `r7c10_fixes.md:72` 표시를 넷째 칸 안으로. **D-3(`harvest/sim/snapshot.py:31`·`:119-121` 주석·인자 이름의 "boundary (ambiguous)")는 보류** — 수정 시점에 재생 조사 에이전트가 `harvest/sim/*`을 쓰고 있어 건드리지 않았다(다음 `harvest/sim` 정리 때 정본 §76 D-2 정의로). N2 `closed.py` 주석에 "finite" 추가.
- **N3 FROZEN 읽기**: 고정 구간 "시작 시각이 now + d̂_p95 이전"(M4 §3 #5 :150)의 now = **요청 송신 시각**(표 대상 = `t_start ≥ t_send + d̂`, `target_slots`), 원장은 이미 시작한 슬롯만 불변(`m4.py` `t_start <= now`). 도착 시각으로 읽으면 H = 3 스텝당 표가 약 2개가 되어 M4 §4.1 :189 "약 3표"·E §2A.3 재생·E §2.6 계수와 어긋나므로 송신 기준이 사전 등록 수치와 맞다.
- **N4 E0.5·E1 채점 정답 = labels_v2**(§54·§65; 결과 기반 라벨은 plan 규칙이 판별력이 없어 거부권 용도). 판정 7(C2' − LA-2 ≥ 2%p·하한 > 0)은 labels_v2 정답에서 **순환**이다 — labels_v2와 C2'의 S1이 같은 코드 규칙이라 C2'가 이기면 "정답 규칙과 섞었더니 맞았다"일 뿐 C2'가 강한 기준이라는 증거가 아니다; 보여 줄 수 있는 것은 반대 방향(정답 규칙을 섞고도 LA-2를 못 넘으면 C2' 쪽 이득이 없다는 것)과 `--truth outcome:plan` 판의 값뿐. E0.5 결과 문서에 이 문장을 싣는다.
- **N5 질문 집합**: E0.5·E1은 DecCall 5질문(dir_xy·dir_z·mag_coarse·target·phase). 사전 등록 E0.5 표현(:239)의 접촉 근처 `fine_dir`는 DecCall·labels_v2에 질문이 없어 묻지 않는다 → SCOPED(접촉 근처 세밀 질문이 DecCall에 들어갈 때). E1 질문군의 progress·Noul 계열은 §71·§73 N5 그대로.
- **N6 카나리 표류 → J5 끔(구현)**: E §3.7-8(:347) "매일 카나리가 '표류 의심'을 내면 판정 7대로 끄고 재보정한다", §1.8(:139) "E1 보정 부분을 다시 돌린 뒤에 게이트를 재사용" → `closed.j5_after_canary`: 모델의 최신 카나리가 `drift_suspect: true`이고 보정 파일의 `utc` 날짜가 그 카나리 날짜보다 앞이면 그 판의 J5를 끈다(spec·meta `j5_alpha` None, `j5_canary_gate` 이유, 요청 값은 `j5_alpha_requested`); 그날 이후 다시 적합한 보정 파일이면 켠다.
- **N7 E0 판정 4 집계(구현)**: `analysis/latency.step_votes`(기록 지연을 기록 순서대로 H = 1 일정 −lead_max … −d̂에 넣어 스텝 시작 전 도착 수) + `judgment4`(중앙값 ≥ 2, lead 1.5·1.0 s), `judge_e0` 출력 `j4`(파드 S1 지연, d̂ = L1). §75 보충 (1)·§76 N3의 lead_max 재결정에 쓴다. **Jev-L용 E0 측정 도구**(`cli_e0`은 Jev API 판)는 여전히 없다 → E0 전에 필요(SCOPED, 완료 정의 밖).
- **N8 SCOPED 모음(§67–§77, 한 곳)**: (1) §67 C8 — M9 복구·T_fail 뒤 하트비트 정지, patch/replace의 A5′ 검사·M2 R3 합치기(검사기 결과·계획 서명·편집 거리 포함), 모듈형 스택 세계 쪽 측정(unknown), §52 VQA 보존 공동학습, 융합 런타임 토크 입력·확인 헤드 보정 파일·동시 부하 지연, 실물 서빙(RTX PRO 6000). (2) §68 K6 CONTRADICT-soft. (3) §71 보충 `LatencyChargingController` 스텁·EVAL H1–H3 기준선 비교. (4) §66 본 단계 B·R2_TRAIN 대량 생성(메인 승인으로 진행 중), CAL 보정·TEST 평가(메인 세션). (5) §63-(6) RB1 라이선스. (6) §67 보충 Astra 카나리(유료, "none"). (7) S-E2E 체크포인트 런타임 tau 마스크 차이(`r7_fixes.md` §7)와 S-E2E의 DecCall 형식 차이(이 절). (8) §73 D5 M4 설계 확장(경계 직후 W 2·γ 1.0, 큐 임계 g, 장면 변화 조기 호출, 미확정 실행 (b) 엄격화). (9) §73 N5 E1 범위(`calib` = 판정 1·8, 2일 반복 없음). (10) §74 보충 E-M4-lat 비례 STALE_MAX(지연 큐와 함께). (11) 런타임에 없는 조건 C2'·C2'-S·C2-match·C3'·C3''·C5-A3·C-FIX(C5'는 이 절에서 추가). (12) §75 보충 (2) E-M4 판정 7(다스텝 DecCall 전까지). (13) 이 절: E0.5 `fine_dir`(N5), Jev-L E0 측정 도구(N7), 결정 호출 이미지 원본 저장(D4), 완료 정의 밖 사전 등록 실험 E2a·E3·E-M3·E-M7(소프트 채널 AND·`C_flip` 채널)·E-M8·E-AE·E-R·E-link(계획 완료 정의 4가 범위를 정함). 보류(DOC): D-3 `snapshot.py` 주석.
- **메인 확인 요청**: (1) 본 단계 A·B 학습 전 R2 자료의 `last_step` 범주 분포(DEVIATE·CONTRADICT 희소 여부)를 보고 섭동 설계나 가중을 정할지. (2) 진행 중인 S-E2E 체크포인트를 옛 프롬프트 코드(6b013ac)로 평가할지, 이 형식 변경 뒤 코드에서 `strict_prompt=False` 진단으로만 볼지.
- (§77 보충, 2026-09-25 10:25 UTC, 메인 결정) (1) **(b) 범주 희소성**(R2 DEV 결정 스냅샷 1,081 중 DEVIATE 21·CONTRADICT 0, POOL CONTRADICT 1)은 본 단계 B 학습 사전 등록 때 정한다(비 OK 가중·섭동 설계); 성능이 부족하면 user-log 70대로 1.5년·신뢰도 규칙의 선행 기법을 찾아 새 판·새 사전 등록으로 적용. (2) **S-E2E(09:40 시작, 6b013ac 고정) 판정은 그 고정 사본으로** 한다 — 파이프라인 시험이라 새 형식 거부(ser-A-min-2)와 무관; 이 체크포인트는 폐루프에 쓰지 않는다. (3) **C5'는 `last_step: none` 줄을 남기는 읽기 확정**(줄을 지우면 판정 3이 (b) 정보가 아니라 입력 형식 변화를 잰다). (4) stageb_train.py의 prompt_config에 `serializer` 필드 추가는 다음 그 파일 수정 때.
- (§77 보충 2, 2026-09-25 11:07 UTC, 메인) **S-E2E 판정 스크립트를 결과 전에 저장소에 고정**(R7 13회차 N3): `tools/se2e/se2e_verdict.py`(사전 등록 `prereg_se2e.md` 문턱 그대로). 바뀐 것은 둘뿐 — (1) "≤" 비교에 §74 CMP_EPS = 1e-12(상대) 적용(0.65·0.65·0.80 평균 0.7000000000000001이 0.70 문턱에 떨어지던 문제), (2) (a)의 "rc 0"을 driver 출력(`main/evalck/resume rc=`)에서 직접 판정. 시험 `tests/test_se2e_verdict.py`(옛 스크립트 실패 → 새 스크립트 통과). 이 커밋 시각에 학습은 아직 끝나지 않았고(끝 예정 11:45 UTC) 판정은 이 판본으로만 한다.

## 78. 시뮬 재현성: PhysX 장면 재생성(하드 리셋) (2026-09-25 11:14 UTC, `docs/stage3/results/pool_replay_debug.md`, `physx_hard_reset.md`) [Claude 결정]
- **사실**: CPU PhysX는 `env.reset()`(자세 쓰기) 뒤에도 장면 내부 상태를 이어 가, 같은 시드가 프로세스 이력에 따라 몇 개의 이산 궤적 중 하나로 간다(풀 재실행 불일치 20편 최대 37 mm, 같은 워커의 C5→C5' 비교에서도 스텝 17부터 갈림 — R7 13회차 N2). 스레드 수·난수·코드 판본은 원인이 아니다.
- **결정**: 모든 에피소드 시작에서 PhysX 장면을 다시 만든다 — `harvest/sim/scene.py` `Env.reset()`이 `_recreate_physx_scene()`(타임라인 정지 → 이 시드의 물체 USD 자세 기록 → `sim.reset(soft=False)` → 카메라 렌더 제품 재생성 막기 → 스텝 계수 0). `make_env(hard_reset=True)` 기본, 모든 호출자(cli_pool, datagen, labeler, aiworker, run_dev, m4b)가 받는다. 증거: 이력 3종 × DEV 4시드 × 카메라 유무 24/24 새 프로세스와 비트 동일, 비용 에피소드당 약 +0.28 s. 렌더 픽셀은 비트 동일이 아니다(평균 약 1/255 — 렌더러 비결정, 물리와 무관).
- **데이터 처리**: (1) 라벨 신뢰 기준 = 재생 **비트 동일**(1 mm 아님); 풀 `replay_maxabs > 0` 886행(29편) 거부권 제외(labeler.md). (2) 기존 풀·R2 DEV·E0.5 스냅샷은 옛 물리로 녹화 → 새 코드로 재생되지 않는다; 풀 29편 재라벨은 `hard_reset=False`(옛 체제)로 하거나 풀과 라벨을 함께 다시 만든다(R2_TRAIN 뒤 결정). (3) R2_TRAIN은 옛 코드 파일럿(`train_pilot_prefix_6b013ac`)을 버리고 하드 리셋 빌드로 재기동. (4) 짝 비교(같은 시드의 조건 C0–C6, std↔rnd)는 하드 리셋 빌드에서만 유효하다.
- **남은 것**: dr·random 변형, CUDA PhysX, P2의 결정성 행렬은 아직 없음(SCOPED 아님 — 본 실험 전에 확인). prefix/warmup 선행 실행은 이제 결과에 영향이 없어 제거 후보.

## 79. R7 13회차 결함 D1–D3 수정, 경계 틱 호출의 `last_step` 짝 맞춤(N1), 빈 입력 거부(N5), 문서 정정 (2026-09-25 11:50 UTC, `docs/stage3/results/r7_cycle13.md`, `docs/stage3/results/r7c13_fixes.md`) [Claude 결정]
- 원칙(§72–§77과 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`, 조건 정의 `M4-overlap-commit.md`)은 구속한다. 코드는 사전 등록 문장을 그대로 구현하고, 문장이 열어 둔 값은 이 절에 적는다. 사전 등록 문서는 바꾸지 않았다(`tools/prereg_hash.py --check` OK). 코드 줄 번호는 이 수정 뒤 작업 트리 기준.
- **D1 FLIP_TH 초기값 = 질문별 split conformal**: 사전 등록 문장 — M4 §4.4 FLIP_TH 행(:287) "성공 실행 flip_score의 1−α 분위(split conformal) … E0.5 재생 자료로 먼저 잡는다 … `question_id@vN`별로 잡는다(J4)", M4 :44 "임계는 성공 실행으로 conformal 보정", q̂ 행(:283)·E §3.5(:330)의 순위 "⌈(n+1)(1−α)⌉", 정본 §28 J4(:260) "확률 게이트와 감시는 `question_id@vN`별로만", §12(:111) "임계값은 성공 실행으로 conformal 보정", E §2A.5(:253) "두 식의 성공 궤적 1−α 분위(FLIP_TH 초기값 후보)". 구현: `e05.flip_th_conformal(scores, α)` = 성공 스텝 점수의 ⌈(n+1)(1−α)⌉번째 작은 값(α는 소수 그대로 유리수 계산); 순위가 n을 넘으면 **유한 문턱 없음** — 값 `None`, `finite` false, `n_needed` = 유한 순위가 나오는 최소 n = ⌈1/α − 1⌉(α 0.01 → 99, 0.001 → 999, 0.05 → 19). 점수는 **질문마다** 따로(`analyze`의 `succ_q`: 그 질문의 (i) 표 분포 TV·(ii) 한 답 대 한 답 flip, P0 성공 편의 스텝만) — 이전의 "5질문 평균 점수 + `np.quantile` 선형 보간"(한 질문 TV 1/3이 평균 1/15)은 버렸다. 출력 `c_flip.flip_th_candidates[q0.99|q0.999|q0.95][질문]` = {tv_distance, one_flip, finite, n, rank, n_needed}, `flip_th_initial` = {alpha 0.01, key q0.99, method, per_question}, e05 `c_flip.question_ids`(그 판의 `question_id@vN`). 13회차 파드 모의 판(성공 스텝 47)은 이 규칙이면 α 0.01에서 문턱 없음(n 99 필요). [Claude 결정] (1) 판정 4의 AUROC(두 식 비교, E §2A.6-4 :259)는 사전 등록이 질문별을 적지 않아 지금처럼 스텝의 질문 평균 점수로 둔다(FLIP_TH만 질문별). (2) E0.5 점수의 "직전 창"(직전 스텝 표 3개)과 런타임 `flip_score`의 창(그 질문의 최근 표 4개 대 그 앞 4개)은 §73 열린 값 그대로 — 둘 다 질문별 TV라 척도([0, 1])는 같다. (3) 런타임 `M4Params.flip_th`는 한 값(모든 질문 공통)이고 지금은 꺼져 있다(None, E0.5 전) — J4대로 켤 때는 `question_id@vN`별 값을 받게 바꾼다(켜기 전 할 일, 지금 동작 없음).
- **D2 카나리 기준일 = 같은 세트 × 같은 `question_id@vN` 집합의 첫날**: 사전 등록 E §1.8(:137) "고정 스냅샷 × 고정 `question_id@vN` 소수 세트를 돌려 … 기준일과 비교", (:140) "기준일 = 카나리 세트를 처음 돌린 날", §3.7-7(:345–346) 판본(직렬화기 판본 포함)이 바뀌면 끄고 재보정, 정본 §77 "카나리 기준일(질문 id·프롬프트가 바뀜 → 새 기준일)". 구현: `canary.select_baseline(runs, set_sha, question_ids)` — 날짜 순으로 세트 해시와 `question_ids`(질문 → `question_id@vN`)가 **모두 같은** 첫 카나리; 없으면 그 판이 새 판본의 기준일(`baseline` None, 비교·표류 판정 없음). `question_ids` 필드가 없는 옛 카나리는 기준이 되지 않는다. 그래서 ser-A-min-2 판은 ser-A-min-1 기준(예: 파드 `canary_20260924_mock.json`)과 비교되지 않는다.
- **D3 표류 의심 뒤 J5는 재보정 전까지 꺼짐**: 사전 등록 E §1.8(:139) "표류 의심 → 그날 결과를 분리 보고하고, E1 보정 부분을 다시 돌린 뒤에 게이트를 재사용", §3.7-8(:347) "판정 7대로 끄고 재보정한다". 구현: `canary.latest_canary`가 `last_drift`(그 모델 지문의 가장 최근 `drift_suspect: true` 카나리 {id, date_utc, file} 또는 None)를 싣고, `closed.j5_after_canary`는 최신 카나리 하나가 아니라 이 `last_drift`와 보정 파일 `utc` 날짜를 비교한다 — 보정 날짜 ≥ 그 표류 카나리 날짜일 때만 켠다. 표류 다음 날의 깨끗한 카나리만으로는 켜지지 않는다. 날짜 단위(같은 날 경계)와 모든 질문을 끄는 범위는 §77 N6·13회차 N7 그대로. 다른 모델 지문의 표류는 세지 않는다(카나리는 지문별).
- **N1 경계 틱 호출 = 방금 끝난 스텝의 범주**: 사전 등록 M4 §4.2 `on_step_executed`(:232) `next_jev_input.add_line(f"last_step: {s.outcome}")` — 스텝 확인 뒤에 만드는 Jev 입력이 "다음 입력"이다. 학습 자료는 스냅샷 k에 k에서 끝난 스텝의 범주를 싣는다(§77 (iv), `deccall_snap.annotate_last_step` — **바꾸지 않음**). 구현(`runtime/core.OursRuntime.act`): 결정 호출을 경계 판정 **뒤에** 만든다(순서: Astra → 배달 → 경계 → 호출). 틱 시작 기준으로 그대로 둔 것: in-flight 수·N_max·d̂·대상 슬롯(`target_slots`)과 조기 호출 깃발 — 그래서 경계에서 생긴 DEVIATE·CONTRADICT의 조기 호출은 전처럼 **다음 틱**이다(§77 (ii)의 조기 호출 문장 그대로). 경계 틱 호출은 `last_step` 줄·premise epoch·S0 상태가 경계 확인 뒤의 값이다. **§77 (ii) 정정**: "같은 틱에 보내는 호출은 경계 판정 전에 만들어지므로 다음 호출부터 실린다" → 이 절부터 경계 틱의 호출에 실린다. 무작위 대조(가짜 세계 160판 = 8조건 × 20, `r7c12` 대조 도구에 경계 틱 호출 기록을 더함) 93c68d7 대비: **156판 비트 동일**(C0–C3 80/80, C5' 20/20, C4 18/20, C5 19/20, C6 19/20 — 행동 바이트·원장 counts·epoch·호출 수·호출 시각·요청 상태); 달라진 4판은 모두 주기 호출이 앞 스텝과 범주가 다른 스텝의 경계 틱에 걸린 판이다 — c11_C4·c15_C5는 그 호출의 줄만, c3_C4·c10_C6은 DEVIATE 경계 틱 호출이 새 epoch를 달아 표가 버려지지 않음(`dropped_epoch` 45 → 30, 15 → 0)에 따라 원장·행동도 다름; 성공 여부는 160판 모두 같다. 경계 틱 호출의 줄 = 방금 끝난 스텝 범주: 93c68d7 C4 2/4·C5 0/1·C6 37/38 → 이 수정 4/4·1/1·38/38(C0–C3·C5'는 늘 none).
- **N5 빈 입력 거부**: `eval/common.load_episodes`가 폴더 목록이 비었거나, 폴더가 없거나, `ep*.jsonl`이 없으면 SystemExit(rc 1) — `e05`·`rd`·`calib`·카나리 세트가 0편으로 rc 0을 내던 것. `rd.expand_dirs`는 P0/P1/P2도 에피소드도 없으면 폴더 이름 그대로 넘겨 같은 거부 문구를 받는다. 곁에서 고친 것: `e05`가 `question_id@vN`을 기록하게 되면서 qid 등록부 기본값을 실행 폴더(`<out>/qid_registry.json`, `calib`·카나리와 같음)로 둔다(작업 폴더 `docs/stage3/qid_registry.json`에 쓰지 않게).
- **Isaac 워커 환경(메인 요청)**: `closed.worker_cmd`의 워커 환경에 `OMP_WAIT_POLICY=PASSIVE`(libgomp 대기 회전이 프로세스마다 약 11코어를 쓰던 것; 결과 불변).
- **DOC**: D-1 `handoff.md` 12회차 줄·`r7c12_fixes.md` :20·:22·§8(1–3)에 [해결 R7 13회차 D-1: §77 보충] 표시; D-2 `r6_eval.md:46` "런타임에 없는 것 … C5'"에 [정정 R7 13회차 D-2, 정본 §77] 표시. (D-3·D-4는 메인이 93c68d7에서 고침.)
- **논문 갱신 항목(매시간 갱신 때)**: FLIP_TH = 질문별 split conformal(n이 모자라면 문턱 없음, α 0.01은 성공 스텝 99개 이상; `X_suppl.tex:27`), 카나리 기준일은 판본별·표류 뒤 J5는 재보정 전까지 끔, 경계 틱 호출도 방금 끝난 스텝 범주를 싣는다.
- (§79 보충, 2026-09-25 11:54 UTC, 메인 결정) 런타임 `M4Params.flip_th`가 질문 공통 한 값인 것은 J4(`question_id@vN`별 감시, 정본 §28 :260)와 어긋난다 — J4를 켜기 전(E0.5 뒤 FLIP_TH 확정 시점)에 질문별 사전(dict)으로 바꾸는 것을 **필수 작업**으로 둔다(지금은 FLIP_TH 끔이라 동작 영향 없음). E0.5 점수 창(직전 스텝 3표)과 런타임 창(최근 4표 대 그 앞 4표)의 차이(§73)는 FLIP_TH를 E0.5에서 정해 런타임에 쓰는 순간 척도 차이가 되므로 같은 시점에 한쪽으로 맞춘다.

## 80. R7 14회차 결함 D1 수정(결과 라벨 신뢰 기준을 읽는 쪽에 적용), S-E2E 판정 스크립트 입력 검사(N1), `--seeds` 0편 거부(N3), 문서 정정 (2026-09-25 13:10 UTC, `docs/stage3/results/r7_cycle14.md`, `docs/stage3/results/r7c14_fixes.md`) [Claude 결정]
- 원칙(§72–§79와 같음): 사전 등록(`E-first-experiments.md`·`EVAL-evaluation-design.md`·`prereg.json`·`prereg_labeler.md`·`prereg_se2e.md`, 조건 정의 `M4-overlap-commit.md`)과 정본은 구속한다. 사전 등록 문서는 바꾸지 않았다(`tools/prereg_hash.py --check` OK).
- **D1 결과 라벨 = 재생 비트 동일 행만**: §78 (1) "라벨 신뢰 기준 = 재생 **비트 동일**; `replay_maxabs > 0` 행 거부권 제외"를 라벨을 **읽는** 모든 곳에 건다 — 판정 함수 하나 `harvest/train/stagea_data.replay_bit_identical(row)` = `replay_maxabs`가 수이고 정확히 0. 적용처: `eval/common.load_truth`의 `outcome:<rule>`(= `e05`·`rd`·`calib`의 `--truth outcome:<rule>`), 단계 A `stagea_data.OutcomeLabels`(`outcome_factory`, `stagea_train --target-source outcome`), `tools/labels_v2_eval.py` 자기 점검 대조(`selfcheck_rows`). 뺀 행 수를 결과에 싣는다: `e05`·`calib` `meta.truth_label_trust` = {rule, rows_kept, rows_excluded_replay_not_bit_identical}(labels_v2 정답이면 null), `rd`는 변형별, 단계 A `config.json` `label_trust`, `labels_v2_eval` `outcome.n_excluded_replay_not_bit_identical`. [Claude 결정] (1) `replay_maxabs` 필드가 없거나 null·NaN인 행은 **신뢰하지 않는다** — §78 (1)은 "> 0 제외"만 적었지만 기준이 "비트 동일 확인"이므로 확인 증거가 없는 행은 기준을 통과하지 못한다. 지금 파드 라벨은 모두 필드가 있어(풀 `labels/` 6,627행 중 > 0 886행·29편, `pool_selfcheck` 4,970행 중 > 0 597행·13편, 누락 0) 실제로 빠지는 것은 > 0 행뿐. (2) 단위는 **행**(질문 하나): 같은 스냅샷의 다른 질문 행은 비트 동일이면 남는다(`labeler.md` "행 전부"). (3) 단계 A `partial` 파일의 스냅샷 완결 판정(행 ≥ 5)은 거르기 전에 한다(거른 뒤 세면 완결 스냅샷이 잘린 것으로 보임). 라벨을 **쓰는** 쪽(`cli_label` `summarize`·`poolsum` 요약)은 바꾸지 않았다 — 라벨러 자체의 품질 요약이고 `poolsum`은 재생 최댓값을 보고한다.
- **`stagea_data.py` = 프롬프트 파일(§71 `stagea_train.PROMPT_FILES`) 변경**: 파일 해시(LF 블롭 sha256 `99b17048…`, 이전 e6ab856 `bc052491…`)가 바뀌어 이 파일로 계산한 `files_sha`를 가진 체크포인트(단계 A `prompt_config`, 단계 B `prompt_config` = PROMPT_FILES + PROMPT_FILES_B)는 `fused_model.check_prompt`의 `files_ok`가 거짓이 되고, `serializer` 필드가 없는 단계 B 설정은 `serializer_of`가 ser-A-min-1로 분류한다. 기존 체크포인트는 §77(ser-A-min-2)로 이미 모두 무효라 새로 무효가 되는 것은 없다. S-E2E 주 학습·진단(D1–D3)은 고정 사본(`/data/harvest/code_se2e_run`·`code_se2e_diag`, 그 `stagea_data.py` = e6ab856 이전 블롭 `a8c7f527…`)에서 돌고, `stageb_data`만 쓰며(결과 라벨을 읽지 않음) 판정은 체크포인트 적재 검사를 거치지 않으므로 영향 없음. 다음 단계 A·B 학습은 새 해시로 한다.
- **N1 S-E2E 판정 스크립트 입력 검사**(`tools/se2e/se2e_verdict.py`, 사전 등록 `prereg_se2e.md` §3·§4): (b) "처음" = step 0 eval, "끝 3점" = 사전 등록 eval 일정(`EVAL_EVERY` 500마다 + 마지막 스텝)의 마지막 세 스텝(총 4686 → 4000·4500·4686)이 아니면 `b.steps_ok` 거짓 → (b) 불합격(빠진 4500 → [3500, 4000, 4686], 두 번 기록된 4686 → [4500, 4686, 4686] 모두 불합격); (e) 재개 기록의 스텝이 정확히 mid+1 … mid+50(2001–2050)이 아니면 `e.window_ok` 거짓 → "fail"(같은 50스텝이라도 1–50·3001–3050이면 불합격); 드라이버 폴더(`driver_A.out`·`driver_B.out`, (a) rc 0)는 **필수** — 인자가 없거나 폴더가 없으면 스크립트가 rc 1로 멈춘다. 문턱은 그대로. 실제 S-E2E 로그(읽기 전용)에 다시 돌린 결과(`/data/harvest/logs/se2e/verdict_v2.json`): 커밋 판(0b056b8) 출력 `verdict.json`의 모든 값이 같고 더해진 키만 `steps_ok` 참·`window_ok` 참(두 시드) → (d) 참, (e) "tolerance"·"tolerance", **S-E2E 판정(통과) 불변**. 사전 등록 판정 기록은 결과 전에 고정한 0b056b8 판의 `verdict.json` 그대로다(v2는 입력 검사 확인용).
- **N3 빈 선택 거부**: `eval/common.load_episodes`는 모든 폴더를 합쳐 한 편도 고르지 못하면(`--seeds`가 있는 편과 겹치지 않음) SystemExit(rc 1) — `e05`·`rd`·`calib`·카나리 세트(§79 N5와 같은 문구 종류). 한 폴더에서만 고른 경우는 그대로 받는다. `harvest.sim.determinism`은 `fresh`의 `--seed`(없으면 거부)·`history`의 `--first`·`--partial`·`--seeds`를 DEV 검사한 **뒤에** `--out` 폴더를 만든다(`compare`는 그대로).
- **DOC**: D-1 `r7c12_fixes.md:17`에 [정정 R7 14회차 D-1, 정본 §79 N1]; D-2 `labeler.md:40`, `pool_replay_debug.md:17`·`:135`, `r2_datagen.md:147`에 [정정 R7 14회차 D-2, 정본 §78 (2)](옛 물리 녹화는 하드 리셋 빌드로 재생되지 않음 — 29편 재라벨은 `hard_reset=False` 또는 풀·라벨 재생성, R2_TRAIN 뒤 결정; `cli_label`에는 지금 `hard_reset` 옵션이 없음). N4 `handoff.md` 정본 범위 §1~§80·§2.8 줄(13회차 수정 커밋 2c4a30d·태그 `stage3-r7fix13`, §78 하드 리셋 e8e1864, S-E2E 통과 bc1bd54, 진단 a4a6257, 14회차).
- **논문 갱신 항목(매시간 갱신 때)**: 결과 라벨(거부권)은 재생이 비트 동일한 행만 쓰고 뺀 수를 보고한다(현 풀 886 / 6,627행 제외, 29편 재라벨 방식은 R2_TRAIN 뒤).

## 81. R7 15회차 문서 정정(D-1 고정 선행 실행 서술, D-2 자체 점검 편 수), 재개 검사 키(N2), 라벨 쓰기 쪽 불신 기록(N10), 뺀 수의 질문 범위(N3), 기록 시각(N15) (2026-09-25 13:42 UTC, `docs/stage3/results/r7_cycle15.md`, `docs/stage3/results/r7c15_fixes.md`) [Claude 결정]
- 원칙(§72–§80과 같음): 사전 등록과 정본은 구속한다. 사전 등록 문서는 바꾸지 않았다(`tools/prereg_hash.py --check` OK). 앞 절 본문은 고치지 않고 이 절에 정정만 적는다.
- **D-1 정정 표시(§78 사실)**: 고정 선행 실행(`cli_pool.canonical_prefix`·`warmup`)은 이력 의존을 없애지 못한다(`pool_replay_debug.md:15`) — 다시 만든 풀에서도 라벨 886행(29편)이 비트 동일이 아니고, 비트 동일 재실행은 하드 리셋 빌드(e8e1864)에서만 성립한다. 이와 어긋나게 "선행 실행으로 비트 동일"로 읽히는 곳에 [정정 R7 15회차 D-1, 정본 §78] 표시: `pool.md:32`·`:33`·`:57`, `e_m4b_meas.md:19`, `D28-m4-critic-measurement.md:149`, 같은 사실 grep으로 찾은 `r2_datagen.md:99`, `draft-log.md:412`, 코드 주석 `harvest/cli_pool.py` `canonical_prefix`·`warmup` 설명과 `warmup(env)` 호출 줄, `harvest/datagen/gen.py:266`(동작 변화 없음).
- **D-2 정정(§80 [Claude 결정] (1) 괄호의 근거 수치)**: "`pool_selfcheck` 4,970행 중 > 0 597행·13편" → **597행·13시드·20편**(자체 점검의 편 = 시드 × 섭동, DEV 30시드 × P0–P2 = 90편 — §65 단위). 파드 재확인(읽기 전용, 13:32 UTC): `data/pool_selfcheck/dev*_P*.jsonl` 90파일·`.done` 90, 불신 597행(필드 없음·null 0)이 3P1·4P1·6P0·6P2·9P0·9P2·10P1·13P0·13P2·16P1·17P1·20P1·21P1·24P0·24P1·24P2·25P1·26P0·26P1·26P2에 있다(질문별 dir_xy 120·dir_z 108·mag_coarse 110·target 101·phase 101·fine_dir 57). 14회차 수정의 집계 스크립트가 시드만 셌다. `r7c14_fixes.md:24`에 같은 표시. 판정에 쓰는 수가 아니다.
- **N3 두 "뺀 수"의 질문 범위**: 같은 풀에서 `eval/common.load_truth`(e05·rd·calib `--truth outcome:`)는 5질문(QUESTIONS: dir_xy·dir_z·mag_coarse·target·phase) 행만 세어 불신 **805**행이고, 단계 A `OutcomeLabels`(`label_trust`)는 라벨 파일의 모든 행 — 접촉 근처 `fine_dir` 포함 — 을 세어 **886**행이다(차 81 = `fine_dir` 불신 행; 파드 재확인 5질문 6,000행 중 805, 전체 6,627행 중 886). §78 (1)·§80의 "886행"은 전체 행 기준이다. [Claude 결정] 두 기록에 범위를 싣는다: `truth_label_trust.questions` = 5질문 목록, 단계 A `label_trust.questions` = `stagea_train.STAGEA_TRUST_QUESTIONS`("all label rows … + fine_dir near contact"). 세는 방식은 바꾸지 않았다(`stagea_data.py` = 프롬프트 파일 §71이라 건드리지 않음 — 프롬프트 해시 불변). `labels_v2_eval` 자체 점검 대조의 뺀 수(597)도 모든 행 기준이다.
- **N2 재개 검사 키** [Claude 결정]: `stageb_train.RESUME_KEYS`에 학습 궤적을 바꾸는 진단 옵션(`lr_schedule`·`warmup_steps`·`train_subset`·`train_subset_seed`·`train_fraction`)과 시간 맥락 옵션(`camera_layout`·`motion_line`·`motion_bins`·`motion_dropout`·`se2e_t_root`)을 더한다. `eval_train_subset`·`eval_train_per_kind`는 학습 궤적을 바꾸지 않지만(`evaluate`는 자기 생성기를 씀) 기록되는 평가 집합을 정하므로 `val_per_kind`·`val_seed`와 같은 규칙으로 넣는다. 나머지 `train` 옵션은 `RESUME_FREE`(`run`·`out_root`·`resume`·`stop_at`·`overwrite`·`reload_check`·`save_every`·`eval_every`·`grad_ckpt`·`init_weights`(재개와 함께 쓰면 거부, 가중치는 체크포인트에서))로 분류하고, 시험이 파서의 모든 옵션이 둘 중 하나에 있는지 확인한다. 저장된 인자에 키가 없으면(옵션이 생기기 전 체크포인트) 그 옵션의 기본값으로 본다 — 모든 옵션이 이전 동작을 지키는 기본값으로 더해졌기 때문(`prereg_se2e_diag.md` §1). 기본 동작(재개 없는 학습, 기본값 재개)은 바뀌지 않는다. 파드의 시간 맥락 실험은 고정 사본(`/data/harvest/code_se2e_temporal`)에서 돌아 이 변경과 무관하다.
- **N10 라벨 쓰기 쪽** [Claude 결정]: `cli_label.label_snapshot`은 행의 `replay_maxabs`(와 `replay_obj_mm`·`replay_jpos_rad`)를 결과마다의 값의 최댓값으로 적되, 수가 없는 결과가 하나라도 있으면 **null**로 적는다(`cli_label.replay_max`; 이전에는 `or 0.0`으로 0.0 = 신뢰가 됐다). §80 (1)대로 읽는 쪽은 null을 불신한다. 지금 두 호출(`label_pool`·`selfcheck`)은 늘 기준 상태와 `replay`를 넘겨 모든 결과에 값이 있으므로(`labeler._replay_rollout`이 분기 전에 적음; 파드 풀 6,627행·자체 점검 4,970행 필드 없음·null 0) 실제 값을 잃는 경로는 없다. `summarize`·`poolsum` 요약은 그대로(`or 0.0`은 null 행을 0으로 요약 — 라벨러 품질 요약이라 §80대로 둠).
- **N15 기록 시각 정정**: §80 제목·`r7c14_fixes.md:3`·`handoff.md:3`의 "2026-09-25 13:10 UTC"는 처음 짐작 시각으로 잘못 적었다 — 그 내용을 담은 커밋 0790dd4(태그 `stage3-r7fix14`)의 시각은 **12:57:49 UTC**이므로 §80의 기록 시각은 12:57 UTC 이전이다(§21–§24 관례; §80 본문은 고치지 않음). `handoff.md`는 머리 시각을 이번 갱신 시각으로 바꾸고 14회차 줄에 커밋·태그를 적었다.
- **DOC(handoff 등)**: `handoff.md:108`의 14회차 수정 "커밋 안 함" → 0790dd4·태그 `stage3-r7fix14`로 커밋됨 표시, §2.8에 15회차 줄(FAIL, DOC 2 → 이 절), `direction-log.md` 15회차 행, `draft-log.md` 줄.
- **논문 갱신 항목(매시간 갱신 때)**: 자체 점검 불신 597행은 13시드·20편; 거부권 제외 수 886은 `fine_dir` 포함 전체 행(5질문 기준 805).
