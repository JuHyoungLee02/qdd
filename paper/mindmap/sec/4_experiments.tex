\section{Experiments --- 평가 방향}
\label{sec:experiments}

\begin{itemize}
  \item 평가 비교 대상: \intent{Astra만 / Jev만 / VLA / 룰베이스(스킬 다발) / LLM+스킬을 연결한 기존 논문 / Astra를 쓰는 기존 논문}
  \item \idea{``Astra를 쓰는 기존 논문''이 이제 있음~\cite{agp2026,harnessvla2026} $\rightarrow$ 재현 가능한 것을 고름}
  \item \idea{AGNOSTOS~\cite{agnostos2025}의 LLM 방법(X-ICM, LLM+스킬 1년 창 밖)은 정답 좌표를 받음 $\rightarrow$ 정답 상태 / 인식 상태 두 조건으로 나눔}
  \item \idea{과제 데이터로 미세조정한 학습 정책(VLA 포함)이 장면이 바뀌면 무너진다는 근거는 강함: RoboDojo에서 $\pi_{0.5}$ 20.92$\rightarrow$5.82 ($-$72\%)}
  \item \chg{공개 원자료로 직접 계산하니 같은 변화에서 Astra는 35.3$\rightarrow$31.4 ($-$11\%)만 떨어짐($\pi_{0.5}$는 $-$72\%). 사용자 주장의 첫 정량 근거. 다만 조건이 달라 예비 결과(레시피 정보, seed 1개, 인식 조건)}
  \item \dirn{가장 깨끗한 비교: 같은 인식 상태 위에서 ``학습한 결정 층'' 대 ``LLM 결정 층''}
  \item \idea{같은 Astra가 RoboDojo에서 22.5\%(원문 22.48\%, 자체 평가) 대 35.7\%(RoboDawn 하네스) $\rightarrow$ 인터페이스가 점수를 크게 바꿀 수 있음(표본 수·effort가 달라 인터페이스 효과만의 차이는 아님). 그래서 앞단을 같게 맞춰야 공정함}
  \item \chg{``모두 무너진다'' 같은 전칭 주장 대신 ``상대 하락폭이 작다''로 씀. 절대 성공률 우위는 주장하지 않음}
  \item \dirn{주 벤치마크는 RoboDojo-Sim(환경 축 Gen + 작업 축 Open), 보조는 LIBERO-Plus. 환경 축은 인식 상태 조건에서만 잼}
  \item \dirn{초기·모듈 실험은 단일 팔 자작 장면, 본 평가는 RoboDojo(둘을 섞지 않음)} \decide{본 평가의 정보 경계(트랙 O/D), 주장 문구}
  \item \chg{(09-24 사용자 결정: 논문 한 편) RoboDojo 새 장면 실시간 트랙에서 M4 켬/끔(E-link)과 M4 지연 스윕이 필수. Astra effort는 low가 기본, low·high 둘 다 보고}
  \item \intent{스테레오캠을 쓸 생각이고 그 로보티즈에 AI 워커가 있어 그거를 사용할 생각이야 로봇은 거기에 보면은 제드캠을 사용을 했거든 우리도 제드캠 사용해가지고 하는 걸로 할 거야 AI 워커를 기준으로 할 거고 그러면 스테레오캠이니까 뭐 뎁스나 이런 것도 가능은 할것 같고 그런 식으로 해서 한번 진행을 해보자 이미지도 당연히 되고}
  \item \intent{Sg2야}
  \item \intent{에이아이워커 기본으로}
  \item \chg{(09-24 사용자 결정) 로봇은 ROBOTIS AI Worker(실물 FFW-SG2, 베이스 고정), 카메라는 ZED 스테레오(깊이 켬). 결정 층 비교는 RoboDojo 양팔 로봇 그대로, 개발·정답 궤적·모듈 실험은 AI Worker 시뮬 쌍둥이 한 팔, 실물 확인은 AI Worker 최소판. 손목 힘 센서는 달지 않고 잔차 보정기는 관절 전류를 씀}
  \item \intent{아니, 카메라를 붙이거나 이러지 말고 그냥 AI 워커의 기본으로 부착되어 있는 카메라들 그대로 쓸 거야. 손목캠도 그렇고 헤드캠도 그렇고.}
  \item \chg{(09-24 §43) 카메라 추가·교체·이동 없음. ZED\_M 디지털 쌍둥이와 가상 스테레오 보조 트랙(D-stereo)은 폐기}
  \item \intent{우손목캠 있음 https://github.com/kairobahq/humanoid-challenge-env 여기에 있는 로봇 설정 그대로 가져와서 복사해서 써보자}
  \item \chg{(09-24 §47) 시뮬 로봇·카메라 = humanoid-challenge-env 설정 복사(로봇은 시뮬 설정 그대로, 카메라만 실물 사양): 머리 ZED Mini 왼쪽 정류 672$\times$376, 85$^\circ$, fx 367 / 우손목 D405 424$\times$240, 87$^\circ$. 스냅샷 풀은 새 장면(v2)으로만 만듦}
  \item \chg{(09-24 §48) 풀·라벨·폐루프 시뮬은 CPU PhysX(옛 장면 DEV 0--4 P0에서 1.3--3.9배 실시간 대 GPU 0.18--0.56배, 5/5 성공). 조건: v2에서 DEV P0 $\ge$ 90\% 재확인. 한 실험 안에서 물리 장치를 섞지 않음. 라벨 점수식은 v2 자체 점검 전 사전 등록 규칙으로 고름(옛 장면 스모크에서 ``10 s 안 성공 = 1''은 거의 모든 보기 동점)}
  \item \intent{3단계 실험 계획을 아주 철저하게 짠 다음에 실행에 들어가자 주기적으로 방향성이 맞는지에 대한 검사를 계속 해줘야 돼 틀리면은 그냥 처음부터 돌아가도 돼}
  \item \intent{필요한 큰거나 기본은 전부다 data 경로인건 알지? 쿠베의 h200의}
  \item \intent{폴더 이름은 harvest 여야지}
  \item \idea{(09-24 단계 3 예비 결과, 논문 \S 예비 결과) Inspect Robots v0.59.0: Isaac Sim 5.1 + Isaac Lab 2.3 부팅, sync·simlat·wall 16/16, 오버헤드 57--59 $\mu$s/스텝. 옛 장면(v1): 머리캠 켬 RTF 0.536 $\rightarrow$ simlat 기본, 오라클 플래너 DEV P0 30/30·P1 29/30·P2 30/30, 스냅샷 복원 최대 0.513 mm(쥐는 순간). v2(챌린지 설정·CPU 물리) 재측정은 진행 중}
  \item \intent{후보는 로봇 옆 GPU 서버 pro6000에서 돌릴예정임.}
  \item \intent{젬마4E4B하고 qwen3vl4b 차이 확인하고 성능좋은걸로 선정해봐봐}
  \item \chg{(09-24 §50) Qwen3-VL-4B 유지: Gemma 4 E4B는 이미지 모드 p95가 허용선(0.33 s) 초과, 정답률도 낮음(0.425 대 0.483)}
  \item \intent{사실 이건 실험인거고 제대로 된 데이터 를 제대로 넣은 실험은 아닌거 아는거지? 지금한건 소규모 테스트로 실제 동작하는지를 본거지?}
  \item \chg{(09-25 §56) 단계 A SFT는 파이프라인 시험. 정답률 수치는 논문 결과·근거로 쓰지 않음(참값 좌표 입력, 과제 1개, 무작위화 없음)}
  \item \intent{이렇게 모듈 각각 검증해보고 다하고나면 한번 돌아다니면서 객관 검증해보고 2연속 사이클 도는데 문제가 없으면, 이제 다음인 소규모 엔드투 엔드 준비해서 데이터 오픈소스 된것중에 해서 한 10gb정도로만해서 소규모 엔드투엔드 학습한번해보자.}
  \item \intent{저건 이제 추론에서의 검증인데 그것도 일단은 여기서 다하자 프로600에서 하는걸 가정해서 여기서 다할거고 여기서는 이제 파이프라인 완성과 엔드투엔드 학습 검증까지 해주면되는거야 최대 gpu 효율로 4장가진걸루}
  \item \dirn{(09-25 계획 e2e-ready) R1--R6 관문 $\rightarrow$ R7 객관 검증 순회 2회 연속 무발견 $\rightarrow$ S-E2E: ROBOTIS AI Worker 공개 데이터 약 10 GB로 융합 모델 소규모 학습(소규모 시험으로 표기). 추론 검증은 H200에서 PRO 6000 조건을 가정}
  \item \dirn{(09-25 논문 판) 주 표 = standard/random 짝 RD, 축 = 위층 Astra 켬·끔 × 아래층 C·A·A+RL·B, 기준선 = Astra만(Inspect Robots agent)·코드 정책·$\pi_{0.5}$ 미세조정·룰·모듈형. 지연 충실 트랙과 동기 공정 트랙 병기}
\end{itemize}

\subsection{단계 3 진행과 논문 갱신 (09-24 -- 09-25)}
\begin{itemize}
  \item \intent{지금까지 된 거 어떤식으로 할건지 해서 실제 논문형식으로 오버리프 다시 확 다듬어줘 아직 확실한 자료가 아닌건 임시로 넣어두고 하는 것도 괜찮아}
  \item \intent{예상 원하는 자료 그리고 비교 논뮨이나 이런거까지}
  \item \intent{논문 잘쓰는 법을 참고해서 작성을 해주면 더욱 좋을 듯}
  \item \chg{(09-24) 논문형 판(main.tex): 확실하지 않은 값은 주황 \tentative{임시}, 예상 결과 표(결과 칸 비움)와 가장 가까운 선행 비교 표, 표 캡션마다 기대 결과와 그것이 지지·반박하는 주장. 파이프라인 시험 수치는 결과로 쓰지 않음(§56)}
  \item \intent{해두돼}
  \item \intent{파이프라인이나 피규어도 더 다듬어줘 업뎃됐는지 봐주고}
  \item \chg{(09-24) GPU 2 = 렌더 없는 학습·추론 허용(렌더 금지는 그대로). (09-25) 개요·모델 그림을 두 호출(decide $\rightarrow$ M4 $\rightarrow$ chunk)과 확인 헤드 V1h로, M4 그림에 $H=1$ 앞당김·2/3·$W=1$을 넣어 다시 그림}
  \item \intent{직므은 단순이니까 10으로 하되 나중에는 우리가 데이터하고 할떄는 30으로 모을거}
  \item \chg{(09-24 §62·§63) S-E2E = ROBOTIS AI Worker 공개 두 과제 1,575편·\tentative{약 9.0\,GB}·10\,Hz 그대로(청크 5스텝 = 0.5\,s), 우리 데이터는 30\,Hz. 팔별 정규화, 그리퍼 [0, 1] 열림 정도, 토크 없음 $\rightarrow$ 마스크, 과제 지시문을 계약 요약 자리에. 결정 라벨은 휴리스틱(labels\_v2와 비교 안 함). RB1은 라이선스 미표기라 내부 시험만}
  \item \intent{gpu최대 효율분산해서 최대로 사용하고 있음?}
  \item \chg{(09-25 선택 ``지금 병행 시작'') S-E2E를 R7 2회 연속 무결 전에 비어 있는 GPU로 병행 시작. 두 시드, 학습 전 파이프라인 판정 기준 사전 등록. 결과 \tentative{아직 없음}}
  \item \intent{지속적으로 오버리프 업데이트 하면서 하는중?}
  \item \intent{1시간마다 논문 업데이트하기로해 앞으로는 꼭}
  \item \dirn{논문·마인드맵은 1시간마다 갱신한다. 이번 판(09-25 09:40 UTC 무렵): 정본 §56--§76, R7 12회차까지 반영, 12회차 수정 결과는 \tentative{다음 판}}
  \item \idea{(R7 객관 검증 순회) 12회 중 통과 1회(6회차), 연속 무결 \tentative{0}. 1--6회차는 명령이 끝까지 도는지 위주, 7회차부터 사전 등록 숫자·조건 문장을 코드와 한 줄씩 대조 $\rightarrow$ 부트스트랩 2,000 $\rightarrow$ 10,000, Holm·ECE 애매 제외·카나리 규칙·접촉 근처 $\tau$, $\gamma$ 0.67 $\rightarrow$ 2/3·C2 원문판, $H=1$ 앞당김, (b) 되먹임·기록 필드. 논문 부록 ``사전 등록 적합성 감사''로 평가 프로토콜 기여의 일부}
  \item \chg{(09-24 §66) R2 데이터 생성기: 30\,Hz, 머리 + 활성 손목 원본, 세 과제(머그-트레이·머그-표적·병-트레이), DR, LeRobot 내보내기, R2\_TRAIN 시드 10000--59999. 본 생성 \tentative{약 6,000편 진행 중}}
  \item \idea{(09-25 풀 라벨 최종화) 120편·6,627행·분기 롤아웃 25,650, 판별력 \tentative{0.006--0.069} $\rightarrow$ 거부권. 복원 1\,mm 초과 \tentative{410행(6.2\%, 20편)} 무효. 원인 조사: 같은 시드가 직전 실행 이력에 따라 몇 개의 이산 궤적 중 하나로 재생(PhysX 장면 내부 상태로 추정, 가설) $\rightarrow$ 재라벨 \tentative{준비 중}}
  \item \chg{(09-25 09:50 재생 조사 결론) CPU PhysX는 \texttt{env.reset()}을 넘어 장면 내부 상태를 이어 감: 같은 시드 $\rightarrow$ 직전 실행에 따라 이산 궤적 중 하나(녹화 + 대안 1--2개, \tentative{4--39\,mm}). 스레드 수·향상된 결정성·잠자기는 무관, 같은 호출 순서는 프로세스를 넘어 비트 동일. PhysX 장면 재생성(0.16\,s)이면 이력 무관 비트 동일. 라벨 신뢰는 편차 크기가 아니라 비트 동일 여부(1\,mm 이하 행에서도 최선 집합 바뀜) $\rightarrow$ 비트 동일 아닌 \tentative{886/6,627행(29편)} 거부권 제외, 라벨러는 비트 동일까지 이력 바꿔 재시도(시연 120행 편차 0). 29편은 장면 재생성 빌드로 재라벨}
  \item \dirn{매 편 앞 PhysX 장면 재생성 빌드(\tentative{구현 중})로 풀·라벨·R2\_TRAIN 생성·폐루프 평가를 모두 돌림 --- 짝지은 폐루프 비교의 재현성 조건. 논문: 실험 설정 ``시뮬 재현성'', 한계, 부록 ``시뮬 재현성 감사''}
  \item \idea{(R2\_TRAIN 생성 파일럿, §56 파이프라인 수치) P0 \tentative{120편}: 구조 검사 \tentative{120/120}, 성공 \tentative{107}(머그-트레이 41/41, 머그-표적 39/39, 병-트레이 27/40). 병 실패 13편은 모두 놓기(트레이 위 넘어짐 7, 트레이 벗어남 3, 탁자 밖 낙하 3). 생성은 멈추고 장면 재생성 빌드로 재기동}
  \item \idea{(S-E2E, 09:40 UTC 시작) 두 시드 학습 \tentative{진행 중}. 판정 = 사전 등록(\texttt{prereg\_se2e.md}) 그대로, 코드는 6b013ac 고정 사본(§77 보충 (2): 새 형식 거부와 무관한 파이프라인 시험, 폐루프엔 안 씀). 결과 \tentative{아직 없음}}
  \item \idea{(R7 12회차 수정 뒤) 결함 4건 모두 고침(정본 §77), 코드가 바뀌어 연속 무결 \tentative{0} $\rightarrow$ 13회차. 12회차 결함은 모두 대조표에 처음 옮긴 사전 등록 절차(지표 목록·조건 의사코드·기록 필드)에서 나옴}
  \item \intent{문제가 있다면 은 만약에 성능이 너무 낮다면 은 기존에 최근 1년 반 이후 논문에서는 그러한 성능들을 비슷한 논문을 찾아가지고 그러한 성능들을 어떻게 올렸는지 찾아봐서 적응을 해도}
  \item \dirn{(user-log 70, §77 보충 (1)) 성능이 너무 낮으면 1.5년 안·신뢰도 규칙을 지킨 비슷한 논문에서 그 성능을 어떻게 올렸는지 찾아 적용. 적용은 결과를 본 뒤 기준을 바꾸는 것이 아니라 새 판·새 사전 등록으로(원래 판 결과도 함께 보고). 첫 후보 자리: (b) 범주 희소성, 본 단계 B}
  \item \dirn{이번 판(09-25 10:50 UTC 무렵): 정본 §77·보충, 재생 결정성(pool\_replay\_debug·labeler), S-E2E 사전 등록·진행, R2 파일럿, R7 12회차 수정 반영. 되먹임·기록 필드는 ``구현 중''에서 구현 서술로 바꿈(결과 대기인 곳만 주황)}
  \item \chg{(09-25 11:14 §78) PhysX 하드 리셋 구현: \texttt{Env.reset()}이 매 에피소드 타임라인 정지 $\rightarrow$ 이 시드의 물체 자세를 USD에 기록 $\rightarrow$ PhysX 장면 재생성 $\rightarrow$ 렌더 주기 계수 0. 기본값 켬, 생성·라벨·폐루프 등 모든 호출자가 받음. 자세 기록 없이 재생성만 하면 한 시드가 새 프로세스와 첫 틱부터 \tentative{10\,mm} 다름(장면이 처음 만들어진 자세가 궤적을 바꿈). 증거: 이력 3가지 $\times$ 개발 시드 4 $\times$ 카메라 유무 \tentative{24/24} 새 프로세스와 비트 동일, 비용 \tentative{+0.28\,s}/편. 렌더 픽셀은 비트 동일 아님(평균 \tentative{약 1/255}, 렌더러 비결정, 물리 무관)}
  \item \dirn{(§78 데이터 처리) 라벨 신뢰 = 재생 비트 동일(1\,mm 아님), 풀 \tentative{886행(29편)} 거부권 제외. 옛 풀·R2 DEV·E0.5 스냅샷은 옛 물리 녹화라 새 코드로 재생 안 됨 $\rightarrow$ 풀 29편 재라벨은 옛 리셋으로 하거나 풀·라벨 함께 재생성(\tentative{R2\_TRAIN 뒤 결정}). R2\_TRAIN은 옛 파일럿 버리고 새 빌드로 재기동(\tentative{예정}). 짝 비교(C0--C6, std 대 rnd)는 새 빌드끼리만 유효. dr·random·CUDA PhysX·P2 결정성은 \tentative{본 실험 전 확인}}
  \item \chg{(§77 보충 2) S-E2E 판정 스크립트를 결과 전에 저장소에 고정(\texttt{tools/se2e/se2e\_verdict.py}, 문턱은 사전 등록 그대로): ``$\le$''에 상대 허용 $10^{-12}$(0.65·0.65·0.80 평균이 0.7000000000000001로 떨어지던 문제), (a)의 종료 코드는 구동기 출력에서 직접. 판정은 이 판본으로만}
  \item \idea{(S-E2E, 11:45 UTC 끝 예정) 이번 판 시점에 결과 문서 없음 $\rightarrow$ \tentative{진행 중}. 결과가 나와도 §56 파이프라인 판정일 뿐 논문 결과 아님}
  \item \idea{(R7 13회차, e6ab856 대상) \tentative{FAIL}: 결함 3(FLIP\_TH 초기값 추정식, 카나리 기준일의 질문 판본, 표류 의심 뒤 J5 재사용), 문서 4, 연속 무결 \tentative{0}. 12회차 수정(§77)의 핵심(\texttt{last\_step} 줄, 학습 자료 = 런타임 규칙, 옛 판본 거부, 기록 blob 해시)은 동작으로 확인. 논문 문장 지적(N11: C5$'$ 런타임, 기록 필드, 풀 제외 886행·29편)은 지난 판에서 이미 고쳐져 있었음. 수정 \tentative{진행 중}}
  \item \dirn{이번 판(09-25 11:45 UTC 무렵): 정본 §78(하드 리셋, ``구현 중'' $\rightarrow$ 구현 + 증거, 남은 것만 주황), §77 보충 2, R7 13회차 표·FLIP\_TH 정의·카나리 문장 정정 반영}
  \item \idea{(S-E2E 판정, 11:57 UTC, §56 파이프라인 확인이라 수치는 임시) 두 시드 모두 사전 등록 (a) 완주 (b) 감소 (c) 재적재 통과, (e) 재개는 비트 동일이 아니라 허용 기준으로 통과(중앙 \tentative{0.46\%·0.39\%}) $\rightarrow$ \textbf{파이프라인 확인 통과}. 검증 300개: 결정 NLL \tentative{4.62 $\rightarrow$ 0.68/0.69}, fm \tentative{2.2 $\rightarrow$ 0.06/0.05}, 결정 정확도 \tentative{약 0.72}, 학습 $\approx$ 검증(과적합 없음). GPU 사용률 \tentative{약 46\%} --- 같은 시간 파드 CPU 할당량 포화(Isaac 병행), 입력 전처리가 CPU에 묶인 것으로 추정(원인 미측정) $\rightarrow$ 본 학습 사전 등록 때 처리량 목표와 함께 정함. 논문 결과 아님}
  \item \idea{(S-E2E 진단 D1--D3, 사전 등록 11:47 UTC, 수치 임시) D1 상수 lr 5e-5로 +1,200스텝: 검증 결정 NLL \tentative{0.678 $\rightarrow$ 0.738} $\rightarrow$ ``이 lr에서 계산 한계 아님''(앞선 개선은 lr 감쇠 구간). D2 1,000개 8 에폭: 학습 정확도 \tentative{0.885}(아직 상승), 검증 \tentative{0.56} $\rightarrow$ ``불확정''. D3 오답 중 경계 밴드 \tentative{34\%} $<$ 50\% $\rightarrow$ ``라벨 모호성 한계 아님'', 0.1\,s 이웃 라벨 불일치로 잡은 상한 \tentative{약 0.93}, 오답은 움직임 유무·크기 한 칸이 대부분(부호 반대 \tentative{8/155}). 종합 = \textbf{판정 불가}}
  \item \dirn{(진단 뒤 추가 사전 등록 12:33 UTC) 같은 2,000스텝에서 학습 표본 1,000·25\%·50\%·100\% 데이터 규모 곡선(정확도 대 $\log_{10}N$ 기울기 규칙), N 1,000 판으로 D2 해소 --- \tentative{진행 중}. 결정 입력이 한 시점뿐이라 움직임 시작·멈춤이 애매하다는 시간 맥락 가설은 \tentative{문헌 조사 중}(프롬프트 파일을 바꾸면 새 \texttt{question\_id@vN}·새 사전 등록)}
  \item \idea{(R2\_TRAIN) 옛 파일럿을 버리고 하드 리셋 빌드로 재기동 --- 파드에서 \tentative{진행 중}}
  \item \dirn{이번 판(09-25 12:45 UTC 무렵): S-E2E 파이프라인 확인 통과와 진단(주황), 정본 §79·보충(FLIP\_TH·카나리·J5·경계 틱의 ``수정 중'' $\rightarrow$ 구현 서술), R7 14회차 행, R2\_TRAIN 재기동. user-log 70 뒤 새 항목 없음}
  \item \idea{(09-25 시간 맥락 문헌 조사, user-log 70 적용) 먼저 해볼 것 3개: (1) 카메라마다 과거 프레임 1장을 Qwen3-VL 2프레임 비디오 $[t-0.3\,\text{s},\,t]$로, (2) 움직임 텍스트 줄 + 드롭아웃, (3) 에피소드 다양성 기준 데이터 규모 곡선. 근거(1.5년 안·학회 확인): HAMLET(ICLR 2026), RoboMME(ICML 2026), GAP(ICLR 2026), Data Scaling Laws(ICLR 2025 Oral). MEM 등 학회 미확인 프리프린트는 인용 안 함}
  \item \intent{1순위 아이디가 좋다잉}
  \item \intent{저거는 지금 당장 오버리프에 업뎃해줘도 좋겠는데}
  \item \intent{피규어나 이런거에도}
  \item \chg{(user-log 71) 1순위(2프레임 비디오)를 논문에 계획된 설계 변경으로 넣음: 방법 ``시간 맥락: 과거 프레임''(\tentative{검증 중}), 계획 E-TC 2$\times$2, 관련 연구 ``시간 맥락과 이력'', 한계 한 줄. 개요·모델 그림에 카메라별 2프레임 입력을 주황 점선 ``시간 맥락(검증 중)''으로 표시(기준 한 시점 입력은 그대로 보임)}
  \item \dirn{(E-TC 계획) 같은 37k·같은 스케줄에서 \{한 시점, 2프레임 비디오\} $\times$ \{움직임 줄 없음, 있음(드롭아웃)\}, 시드 \tentative{2개 이상}. 지표 = 전체·전환 층(다음 0.33\,s 안에 라벨이 바뀌는 스냅샷)·정상 층·질문별 정확도, decide p95. 채택 = 전체 \tentative{$+0.02$} 이상, 전환 층 \tentative{$-0.01$}보다 안 떨어짐, p95 증가 \tentative{10\%} 이하. 전환 층을 따로 보는 이유 = GAP: 고유감각을 단순 연결하면 움직임 전환에서 시각을 덜 씀}
  \item \idea{(비용 사실 대 이득 가설) Qwen3-VL-4B \texttt{temporal\_patch\_size} 2, 이미지 처리기는 정지 이미지를 2프레임으로 복제 $\rightarrow$ 2프레임 비디오도 시간 격자 1칸, 시각 토큰(머리 252·손목 104)과 ViT 연산 같음, 타임스탬프 텍스트 몇 토큰만 늘어남. 실제 decide 지연은 \tentative{미측정}. 정확도 이득은 직접 잰 논문 없음 = 우리 가설. 단순 다중 프레임은 떨어진 사례(HAMLET RoboCasa 62.6 $\rightarrow$ 59.3) $\rightarrow$ 과거 프레임을 현재 복제로 바꾸는 드롭아웃}
  \item \dirn{(판본 규칙) 켜야만 쓰이는 선택 형식. 카메라 구성이 \texttt{question\_id} 해시 입력이라 켜면 새 카메라 구성·새 직렬화 판본, 다른 형식 체크포인트·보정 파일은 거부(\texttt{last\_step} 줄과 같은 규칙). 채택 전 기본 입력은 한 시점}
  \item \chg{(09-25 13:05 UTC E-TC 사전 등록, 어느 칸도 학습 전, 커밋 0b853c2) 위 계획에서 바뀐 것: 시드 \tentative{2개 이상} $\rightarrow$ \tentative{시드 0 하나}(§56 입력 절제, 검증 300). 전환 층 = 어느 질문이든 프레임 라벨이 $k$와 $k+1..k+3$(0.1--0.3\,s)에서 다르면, 검증 행의 \tentative{65.1\%}(층이 크다는 것을 미리 적음). 채택은 조건별이 아니라 \emph{요인별 주효과}: 전체 $\ge$ \tentative{$+0.02$}, 전환 층 $\ge$ \tentative{$-0.01$}, 전처리 포함 decide p95 증가 $\le$ \tentative{10\%}. 상호작용·NLL은 판정 밖. 표준오차 \tentative{0.02--0.03} $>$ 문턱도 미리 적음, 부트스트랩 구간 함께 보고. 판정 스크립트 결과 전 고정. 움직임 줄에 방향은 없음(팔 세 구간 + 그리퍼 열림·닫힘)}
  \item \dirn{(E-TC 실행) (한 시점, 없음) 칸 = 데이터 규모 곡선 100\% 판 재사용(같은 인자, 이 사본 예측 = 그 판 step 2,000 평가일 때만; 다르면 새로 돌림). 나머지 세 칸은 규모 곡선 판이 끝난 뒤 GPU 2·3, 먼저 (비디오, 없음) 단순 효과를 판정 밖으로 중간 보고. 결과 \tentative{아직 없음}}
  \item \dirn{(데이터 규모 곡선, 12:33 UTC 등록) $N$ = 1,000 / 9,371 / 18,742 / 37,484, 모두 2,000스텝. 규칙: 기울기 $b\ge$ \tentative{0.05}/10배 그리고 100\%$-$25\% $\ge$ \tentative{0.03} $\rightarrow$ 레버 확인, 100\%$-$50\% $<$ \tentative{0.01} $\rightarrow$ 포화 표시, 그 밖 약함. 논문 계획 절에 표(\texttt{tab:scale})로 넣음. 결과 문서 \tentative{아직 없음} $\rightarrow$ 중간 로그 값은 쓰지 않음}
  \item \dirn{이번 판(09-25 13:41 UTC 무렵): E-TC 사전 등록 값으로 ``사전 등록 예정'' 자리 교체(방법 시간 맥락·움직임 줄, 계획 E-TC 규칙·표에 주효과 줄), 규모 곡선 계획 표, 정본 §80(14회차 ``수정 중'' $\rightarrow$ 고침), R7 15회차 행(문서 정정 주황). user-log 71 뒤 새 항목 없음}
  \item \intent{한국시간으로 해줘 앞으론}
  \item \dirn{(user-log 72) 사용자 보고 시각은 한국시간(KST), 기록 문서는 UTC 그대로}
  \item \intent{논문 업뎃주기는 4시간으로 하자}
  \item \dirn{(user-log 73) 논문·마인드맵 갱신 주기 4시간(user-log 69의 1시간을 바꿈)}
  \item \idea{(데이터 규모 곡선 결과, 사전 등록 판정, §56 수치) 같은 2,000스텝에서 $N$ 1,000 / 9,371 / 18,742 / 37,484 $\rightarrow$ 검증 정확도 \tentative{0.552 / 0.682 / 0.686 / 0.683}, 기울기 \tentative{0.090}이지만 100\%$-$25\% \tentative{+0.001} $\rightarrow$ ``레버 약함(같은 스텝), 50\%$\rightarrow$100\% 포화''. D2 해소: $N$ 1,000 학습 \tentative{1.000} / 검증 \tentative{0.552} = 라벨 학습 가능·일반화 간극. 데이터 늘리기는 약 9k까지만 듣고 그 위는 스텝(계산)이 막음(9,371 판 학습 \tentative{0.706} $\approx$ 검증 \tentative{0.682}). 논문 표 \texttt{tab:scale} 채움(주황)}
  \item \idea{(E-TC 사전 등록 판정, §56 수치) 움직임 줄 M \textbf{채택}: 전체 주효과 \tentative{+0.025 [+0.003, +0.047]}, 전환 층 \tentative{+0.027}, p95 \tentative{$-$3.8\%}. 2프레임 비디오 V \textbf{불채택}: \tentative{+0.013} $<$ 0.02, p95 \tentative{+20.4\%}(GPU 순전파는 같고 CPU 전처리 몫). 이득은 주로 \texttt{mag}(``움직이냐·얼마나''), GAP식 전환 층 악화 없음. 시드 1·검증 300이라 하한이 0 근처 $\rightarrow$ 확인 실험. 논문 표 \texttt{tab:temporal} 채움, 판정 문장은 검정·수치는 주황}
  \item \intent{일단 움직임 줄 채택할게}
  \item \chg{(정본 §83) 움직임 줄 채택 + 속도 누수(중앙 차분) 발견 $\rightarrow$ 고친 데이터로 두 번째 시드 또는 검증 1,799 전체 확인(새 사전 등록, \tentative{진행 중}). V는 직전 프레임 재사용 판을 새 사전 등록으로만 재시험}
  \item \idea{(R7 16--19회차) 16 \textbf{통과}(15회차 수정 모두 동작 확인, 연속 1). 17 실패(문서 1: 진단 종합 칸이 D2 재판정 뒤에도 ``판정 불가''), 18 실패(문서 3: 정정 커밋이 기록 문서에 제어 문자 \texttt{\textbackslash r\textbackslash r\textbackslash n}을 넣어 문장이 끊김, 인계 문서 §82 미반영, 옛 하트비트 기본 줄 표시 없음), 19 실패(문서 2: 18회차 정정이 반만 됨, 새 조사 문서 §82 표시 없음). 17--19 모두 코드 결함 0, 연속 무결 \tentative{0}. 교훈: 문서 수정은 heredoc 없이, 고친 파일마다 제어 문자·줄 끝 검사}
  \item \dirn{이번 판(09-25 16:05 UTC 무렵, 4시간 주기): 중심 틀(조이스틱 + 순간 정밀 보정기, user-log 75·82), 보정된 가설(서론), 정본 §82(Astra 전용 일, 하트비트 = 기준선 K2, E-Astra-necessity 표)·§83(움직임 줄 채택, 속도 누수), E-TC·규모 곡선 표 채움(주황), 움직임 인터페이스 탐침·E-SR0(주황, 결과 없음), R7 16--19회차 행, 개요·모델 그림(과거 프레임 점선 삭제, 움직임 줄, J1--J6·J5 감사). 인용 추가: VLA-OS, Shortcut Learning, ECoT-Lite(모두 학회 확인), Steerable RSS 2026 표기}
  \item \idea{(움직임 줄 확인, 사전 등록 판정, §56 수치) 누수 고친 \texttt{se2e\_c1}, 시드 1·2, 검증 1,799: 없음 \tentative{0.672/0.677}, 있음 \tentative{0.707/0.708}, 합동 \tentative{+0.032 [+0.022, +0.043]}, 전환 층 \tentative{+0.030} $\rightarrow$ 채택 확인. 논문 표 \texttt{tab:motionconfirm}}
  \item \idea{(E-MA1 관문 G0 실패) 머리 FK 투영 대 Molmo2-ER 포인팅: 중앙값 \tentative{100.9\,px}(PnP 뒤 \tentative{86.9}), 문턱 12\,px. RB1 머리 관절 미기록이 주원인 추정. R2 미준비라 규칙대로 중단 $\rightarrow$ E-MA1b(로봇 기준 3D 궤적 보조, 카메라 불필요, 기준 칸 = 확인 실험 체크포인트) \tentative{진행 중}}
  \item \intent{그 돈쓸떼는 정말 이게 유익한 실험이 되는지도 중요해}
  \item \intent{고 나에게 승인은 안받아도되지만 자체적으로 저걸 해보라는거지}
  \item \intent{그리고 만약 중간에 잘못되면 다시 새롭게 설계헤ㅐ서 낭비 없게 해야해}
  \item \intent{매번 자체검사를 해야해 알겠지?}
  \item \dirn{(user-log 87, CLAUDE.md) 모든 실험 자체 검사: 시작 전(바꾸는 결정·표본 충분성·무료 사전 실행), 도중(관문 결함이면 멈추고 재설계), 끝(검증 뒤 보고). 유익성 판단은 Claude, 사용자 승인 불필요}
  \item \chg{(탐침 재범위) 유료 13회 \tentative{204원}에서 정지. 실측 low \tentative{15.7원/회}, p50 \tentative{4.2\,s}. 유료 = G1(약 40장 $\times$ 4조건) + S2(F0·F1 각 3편) + G2(low 오답에만 high $\le$8), S1 Astra 뺌, 상한 \tentative{15,000원}}
  \item \intent{지금의 가설을 기반으로 지금 아직 진행안된거 기바ㅏㄴ으로도 해서 제일 이상적인걸로 정리최대해서 논문 ㅈㅇ리해서 지금바로 줘봐보바 당연히 나중에 계속 업뎃 동일하게해주고}
  \item \dirn{이번 판(09-25 18시 UTC 무렵, user-log 88): 논문을 가설 기반 이상적 완성판으로 재구성 --- 제목, 초록, 서론(기여 4개: 결합·M4·평가·수단), 방법(직렬 흐름 절, MolmoAct 학습 보강 절), 실험 지도 표, E-Couple·E-MA 표(빈 칸), 움직임 줄 확인 표(채움), 결론·한계, 부록(구현 계획·비용 모형·R7 20--25), 개요 그림(직렬 흐름). 인용 추가: MolmoAct(ICRA 2026), MolmoAct2(기술 보고서), HAMSTER(ICLR 2025), GPT 6 Astra as Policy(기술 보고서). 안 한 실험은 결과 칸 비움 --- 앞으로 4시간 갱신도 같은 형식}
  \item \idea{(E-Astra-motion 결과, 총 \tentative{6,933원}) 정지 파지 40장 $\times$ 4조건(low 160회, \tentative{16.8원/회}, p50 \tentative{3.5\,s}), 직렬 흐름 F0·F1 각 3편(\tentative{49.7/58.0원/회}, p50 \tentative{8.9/10.0\,s}, 로봇 1분 \tentative{350--500원}), 표적 high 8회(\tentative{27.9원}). Astra 단독 조종 \tentative{0/6}, Qwen 동기 \tentative{0/20}. 덧그림 효과 가를 수 없음(\tentative{3} 대 \tentative{3}). 캐시 적중 0. E-Astra-necessity 첫 짝 참조: 과잉 주장 Astra \tentative{0/15} 대 8B \tentative{7/15}, 놓침 \tentative{13/25} 대 \tentative{4/25}}
  \item \chg{(정본 §86) F0 · 나이 상한 15\,s · 파지 확정 T1·V1h · E-Couple 판 수는 실측 단가로 다시 등록(상한 25,000원 $\approx$ 로봇 \tentative{50--70분})}
  \item \intent{결과들은 모두 효율적으로 정보 저장해둬 나중에 같은씰수안하게 동일 md를 써도되고 효율적으로 분산해서 책처럼 기록 남겨둬두되고}
  \item \intent{업데이트 기록 필수임}
  \item \dirn{(user-log 89, CLAUDE.md) \texttt{docs/book/} 연구 기록책: 실험 장부·함정(56개 $\rightarrow$ 탐침 뒤 62개)·결정 연표·지도·비용·갱신 기록. 결과·실수는 같은 커밋에 책 갱신, 책을 고치면 갱신 기록 한 줄 필수, 새 작업 전 함정 장 먼저}
  \item \dirn{이번 판(09-25 19:50 UTC 무렵, 4시간 주기): 정본 §85(자체 검사가 승인 대체, 논문 완성판 형식, E-MA1 G0 실패 $\rightarrow$ E-MA1b)·§86(탐침 결과로 흐름 상수 확정) 반영 --- 초록·서론 지연 수치, 방법(F0, 15\,s, 파지 확정 T1·V1h, 부드러운 반영 재생 결과, 덧그림, 비용), 실험 지도·탐침 절 완료, E-Couple 흐름 상수·예산, 결과 절·요약 표, 결론·한계, 부록 비용 모형, 그림(시간 축 L$\approx$9--10\,s, 지연 예산에 탐침 두 줄)}
  \item \idea{(MolmoAct 조각 시험 결과) E-MA1b 3D 궤적 보조 \tentative{+0.0031} [\tentative{$-$0.0022, +0.0085}] 불채택; E-MA3 층별 KV 청크 오차 \tentative{$-$2.19\%} [\tentative{1.48, 2.88}] $<$ 5\% 불채택(expert·LoRA 기울기 자르기 결합 주의, 책 P71). 남은 것: R2에서 trace5·덧그림(E-MA1 재등록), E-MA2 명령 추종(\tentative{예정})}
  \item \idea{(R2\_TRAIN 결과) 6,000편 생성, 유효 \tentative{5,126}(85.4\%; 병 과제 \tentative{58.5\%}), 단계 B 행 \tentative{1,495,348}, 결정 \tentative{152,409}, 병합 18/18 검증, 로더 전체 적재 확인. LeRobot 내보내기 \tentative{진행 중}}
  \item \dirn{이번 판(09-25 23:56 UTC, 4시간 주기): E-MA1b·E-CAM3·E-MA3 불채택 반영(실험 지도·표·방법·서론·결론), R2\_TRAIN 결과, R7 25--34회차 요약(코드 결함 0, 기준점 태그 전)}
  \item \intent{Siglip을 쓸 방법도 찾아봐 어떻게 엮을 수 있을지}
  \item \intent{나는 저걸 우리vla에 쓰는게 어떨가 싶은데}
  \item \intent{그래c,e반영가능성도 검사해보고 결정하자 일단ema2 다되고 나면 그거보고 ㄱ}
  \item \intent{만약 시그립이 그래서 필요없다면 굳이 더하란 내말은 무시해두돼}
  \item \chg{(정본 §84 보충 6) SigLIP 별도 통합 안 함 --- 융합 VLA의 영상 탑이 이미 SigLIP2-Large 계열(24층·폭 1024·패치 16, 동결). 손목 패치를 expert에 직접 주는 판은 결정 준수 해결 뒤 근거 있을 때만}
  \item \intent{x2점유시작}
  \item \intent{준수율이 0.8은 될방법론알 찾아야해}
  \item \intent{근데 그냥 소능 움직일때 물체가 근접하지 않았다면 상관 없지만 물체에 가깝게 다가가서 미세조정이 필요한데 아스트라의 반응성이 너무 느리면 그걸 보조하는 역할이라서 어떤걸 잡아야한다 이때 놓아야한다등에 대한 지식을 가져야하는데}
  \item \dirn{(user-log 93) 거리에 따른 권한 분배: 물체에서 멀면 조이스틱·Astra 조종을 강하게 따르고, 접촉 근처(5\,cm 안)에서는 VLA 자신의 판단(무엇을 잡고 언제 쥐고 놓을지)이 주도}
  \item \intent{그 잡고, 놓고 하는 판단 자체도 vla가 선체택지에 있어야한다는 뜻}
  \item \intent{vla자테가세부 조종스틱이기도하지 물론}
  \item \chg{(정본 §87) 잡기·놓기 판단을 VLA 선택지에: 그리퍼 질문 close/open/keep, 실행 허가는 T1 관문, 움직임 줄과 같은 직렬화 판본 올림에서 \tentative{구현 예정}. 계층: Astra 큰 조종 $\rightarrow$ VLA 결정 = 세부 조종 스틱 $\rightarrow$ expert 실행}
  \item \idea{(E-MA2 NONE, R2 평가 1,200) 준수율 C0 \tentative{0.0008}, C1 문장 \tentative{0.177}, C2 화살표 \tentative{0.0008} $<$ 0.6 $\rightarrow$ VLA는 명령을 보지 않고 코드 편향만. 결정층은 명령 칸 \tentative{0.970}인데 청크는 \tentative{0.275}/\tentative{0.093}만 따름}
  \item \idea{(E-SR0 WEAK) expert 반사실 방향 준수율 공개 실물 \tentative{0.340}, 시뮬 \tentative{0.431}(우연 약 0.33); 하위 단계 토큰 그리퍼 \tentative{0.95}. 결합은 조건 강화 전까지 청크·손끝 기준 경유(정본 §84 보충 5)}
  \item \dirn{(조건 강화, \tentative{진행 중}) E-SR1b 결정 드롭아웃 + CFG·되짚기 결정 라벨, E-SR1c 먼 구간 반사실 분기 + 권한 게이트 adaLN. 목표 먼 구간 준수율 0.8, 접촉 근처 능력 비열등. 억지 반사실은 스트레스 시험, 그럴듯한 수정 준수율 따로}
  \item \intent{나는 몰모엑트가 우리가 제대로 준비를 안해놔서 도움이 안되는 교훈ㅇ,로 작동한듯}
  \item \intent{준비를 아ㅖ제대로해서 검증 필요. 시간 걸려두되니까}
  \item \idea{(MolmoAct 성립 조건 관문, 학습 없음) 조건 14개 추출. 카메라 투영 \tentative{0.21\,px} 통과, 궤적 라벨 손끝 영상 안 \tentative{0.994} 통과, URDF 대 시뮬 손끝 \tentative{9.5\,mm} 실패. 미비 조건 9개(궤적 끝, 색, 1:1 조건 데이터, 경로 다양화, 두 대상 장면 등) $\rightarrow$ E-MAR-AO·E-MAR-S \tentative{예정}}
  \item \idea{(R7 35--37회차) 35 실패(시뮬 결과 문서 수치 2), 36·37 연속 통과 $\rightarrow$ E2E 준비 기준점 태그 \texttt{stage3-e2e-ready}. 기준점 뒤 검증기 약점 9건 TDD로 고침(실데이터 342편 새 오류 0). R2\_TRAIN LeRobot 6개 \tentative{22.13\,GB} 검증 6/6. 결합 구현 과제 1--8 끝}
  \item \dirn{이번 판(09-26 03시 UTC 무렵, 4시간 주기): E2E 기준점, R2\_TRAIN LeRobot, E-MA2 NONE, E-SR0 WEAK와 조건 강화 절·표(\texttt{tab:adh}), 조이스틱 세 층·거리 권한·그리퍼 선택지(§87), SigLIP 판단(§84 보충 6), MolmoAct 성립 조건 관문, 모델 그림(SigLIP2 영상 탑, 준수 약함 표시), 초록·서론·결론·한계}
\end{itemize}
