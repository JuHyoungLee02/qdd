\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/model.pdf}
\caption{\textbf{2단계(조건부): 조이스틱 VLA와 상위 계획기의 연결.} 1단계의 한계가 측정으로 확인될 때만 넣는 구성이다(구현해 두고 보류). 상위는 1단계와 같은 원래 형식(손끝 목표 xyz, 그리퍼 의도, 검증)을 낸다. 모델이 아닌 코드가 매 0.33\,s 상위 목표를 VLA 결정 칸의 보기로 바꾸고, VLA 자신의 결정과 늘 연속 가중으로 섞는다. VLA는 머리 카메라, 지금 움직이는 팔의 손목 카메라, 상태 텍스트(과제 문장, 그리퍼 상태, 움직임 줄)를 받아 decide 순전파 한 번으로 typed 결정의 보기 확률(\cref{eq:trie})과 확인 헤드 V1h의 세계 쪽 술어를 낸다. 확정 규칙 M4가 결정을 확정하면 행동 전문가가 기울기 차단된 문맥($\mathrm{sg}[h]$)과 확정 결정을 조건으로 0.4\,s 청크를 내고, 청크는 안전 투영을 거쳐 실행된다. 상위는 T1과 V1h로 확정된 결과를 보고 검증한다. 헤드 크기와 손실 가중치는 \tentative{가정값}이다.}
\label{fig:model}
\end{figure}
