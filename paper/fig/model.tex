\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/model.pdf}
\caption{\textbf{융합 VLA.} 머리 카메라(252 토큰)와 활성 팔 손목 카메라(104 토큰)를 원본 해상도 다중 이미지로 넣고, 과제 문장, 계약의 현재 단계 요약, 그리퍼 상태, 채택한 움직임 줄(\cref{sec:temporal})을 붙인다. 결정 스텝마다 두 번 부른다. \textbf{호출 1 decide}: 한 번의 순전파에서 (1) 결정 토큰(보기 이름 트라이의 갈림 노드마다 자식 토큰끼리 재정규화한 보기 확률, \cref{eq:trie})과 (2) 확인 헤드 V1h(실행 뒤 다음 관측의 세계 쪽 술어, M4 (b)와 critic의 측정원)를 함께 낸다. (3) 보조 기하 헤드는 시뮬 특권 상태의 상대 위치를 예측하는 학습 신호이며 기울기가 백본으로 흐른다. \textbf{호출 2 chunk}: action expert가 캐시된 문맥 은닉 상태를 기울기 차단(sg, KI) 뒤 읽고, M4가 확정한 결정을 조건으로 flow matching 0.5\,s 행동 청크를 낸다. 백본 순전파는 스텝당 한 번이다. 헤드 크기와 손실 가중치는 \tentative{가정값}이다. 이미지는 한 시점이다. 카메라마다 0.3\,s 전 프레임을 더한 2프레임 비디오 입력은 사전 등록 시험에서 채택하지 않았고, 대신 인과 속도를 거친 구간으로 적은 움직임 줄을 채택했다(런타임 적용은 \tentative{다음 작업}).}
\label{fig:model}
\end{figure}
