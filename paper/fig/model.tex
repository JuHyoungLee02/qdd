\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/model.pdf}
\caption{\textbf{2단계(조건부): 조이스틱 VLA와 상위 계획기의 연결.} 1단계의 한계가 측정으로 확인될 때 실행기 자리에 넣는 구성이다(구현해 두고 보류). VLA는 머리 카메라, 지금 움직이는 팔의 손목 카메라, 상태 텍스트(과제 문장, 상위가 선언한 구간 의도 줄, 그리퍼 상태, 움직임 줄)를 받는다. decide 순전파 한 번으로 typed 결정의 보기 확률(\cref{eq:trie})과 확인 헤드 V1h의 세계 쪽 술어를 낸다. 확정 규칙 M4는 겹쳐 받은 결정의 합의와 실행 뒤 측정으로 결정을 확정한다. 그러면 행동 전문가가 기울기 차단된 문맥($\mathrm{sg}[h]$)과 확정 결정을 조건으로 0.5\,s 청크를 내고, 청크는 안전 투영을 거쳐 실행된다. 상위 계획기는 T1과 V1h로 확정된 결과를 보고 검증한다. 헤드 크기와 손실 가중치는 \tentative{가정값}이다.}
\label{fig:model}
\end{figure}
