\begin{strip}
\centering
\includegraphics[width=\linewidth]{figures/overview.pdf}
\captionsetup{hypcap=false}
\captionof{figure}{\textbf{Harvest 개요.} 느린 API 계획기 Astra가 세션 계약을 쓰고, 첫 계획 뒤에는 하트비트(\tentative{잠정 5\,s}), 단계 경계, 실패 때 비동기로 계약을 확인한다. 아래층은 하나의 융합 VLA(Qwen3-VL-4B)다. 머리 카메라와 활성 팔 손목 카메라를 직접 보고 typed 결정 토큰(방향, cm 구간, 대상, 단계)과 action expert의 연속 행동 청크를 함께 낸다. 결정 토큰 호출은 약 0.33\,s 간격으로 겹치며, 확정 규칙 M4가 호출 사이 합의와 실행 뒤 측정을 결합해 확정한다. 확정된 결정은 코드 안전 투영으로 행동 청크를 제한한다. 모델 밖에 남는 것은 Astra, M4, 안전 투영뿐이다. 오른쪽 아래의 모듈형 스택(명시적 인식 $\rightarrow$ 결정 $\rightarrow$ 스크립트 스킬)은 기준선이자 학습 데이터의 교사다. 로봇은 첫 계획 뒤 멈추지 않는다.}
\captionsetup{hypcap=true}
\label{fig:teaser}
\end{strip}
