\begin{strip}
\centering
\includegraphics[width=\linewidth]{figures/overview.pdf}
\captionsetup{hypcap=false}
\captionof{figure}{\textbf{Harvest 개요.} VLA를 과제 전체의 정책으로 쓰지 않는다. 과제의 흐름은 위층 Astra가 주고, 아래층 VLA는 조이스틱처럼 방향·크기 보기를 고르는 순간순간의 말단 정밀 보정기다. 느린 API 계획기 Astra는 작은 모델로 대신할 수 없는 일(J1--J6)에만 쓴다. 과제 전에 세션 계약을 컴파일하고(J1), 실패 때 진단과 복구안을 쓰며(J2), 저빈도로 진행을 감사하고(J5), 작은 층이 모를 때만 불린다. 5\,s 하트비트는 현재 구현이자 기준선이다(\cref{sec:astra}). 주황 글씨의 움직임 개입(M4 느린 투표, 물체 기준 경로)은 \tentative{시험 준비 중}이다. 아래층은 하나의 융합 VLA(Qwen3-VL-4B)다. 머리 카메라와 활성 팔 손목 카메라를 한 시점으로 보고, 상태 텍스트에는 사전 등록 시험으로 채택한 움직임 줄이 들어간다(\cref{sec:temporal}; 2프레임 비디오 입력은 채택하지 않음). 결정 스텝마다 두 번 불린다. decide는 한 번의 순전파로 typed 결정 토큰(방향, cm 구간, 대상, 단계)의 확률과 확인 헤드 V1h의 세계 쪽 술어를 내고, M4가 확정한 뒤 chunk가 캐시된 문맥에 확정 결정을 조건으로 action expert의 0.5\,s 행동 청크를 낸다. decide 호출은 약 0.33\,s 간격으로 겹치며, 확정 규칙 M4는 호출 사이 합의와 실행 뒤 측정(로봇 쪽은 고유 감각 T1, 세계 쪽은 V1h)을 결합해 확정한다. 행동 청크는 코드 안전 투영으로 확정 방향·구간 안에 제한하도록 설계했으나, 지금 투영은 모듈형 경로에만 구현되어 있다(\cref{sec:safety}). 모델 밖에 남는 것은 Astra, M4, 안전 투영뿐이다. 오른쪽 아래의 모듈형 스택(명시적 인식 $\rightarrow$ 결정 $\rightarrow$ 스크립트 스킬)은 기준선이자 학습 데이터의 교사다. 로봇은 첫 계획 뒤 멈추지 않는다.}
\captionsetup{hypcap=true}
\label{fig:teaser}
\end{strip}
