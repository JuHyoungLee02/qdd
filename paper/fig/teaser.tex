\begin{strip}
\centering
\includegraphics[width=\linewidth]{figures/overview.pdf}
\captionsetup{hypcap=false}
\captionof{figure}{\textbf{Harvest 개요.} 느린 API 계획기 Astra가 세션 계약을 쓰고, 첫 계획 뒤에는 하트비트(\tentative{잠정 5\,s}), 단계 경계, 실패 때 비동기로 계약을 확인한다. 아래층은 하나의 융합 VLA(Qwen3-VL-4B)다. 머리 카메라와 활성 팔 손목 카메라를 직접 보고, 결정 스텝마다 두 번 불린다. decide는 한 번의 순전파로 typed 결정 토큰(방향, cm 구간, 대상, 단계)의 확률과 확인 헤드 V1h의 세계 쪽 술어를 내고, M4가 확정한 뒤 chunk가 캐시된 문맥에 확정 결정을 조건으로 action expert의 0.5\,s 행동 청크를 낸다. decide 호출은 약 0.33\,s 간격으로 겹치며, 확정 규칙 M4는 호출 사이 합의와 실행 뒤 측정(로봇 쪽은 고유 감각 T1, 세계 쪽은 V1h)을 결합해 확정한다. 행동 청크는 코드 안전 투영으로 확정 방향·구간 안에 제한된다. 모델 밖에 남는 것은 Astra, M4, 안전 투영뿐이다. 오른쪽 아래의 모듈형 스택(명시적 인식 $\rightarrow$ 결정 $\rightarrow$ 스크립트 스킬)은 기준선이자 학습 데이터의 교사다. 로봇은 첫 계획 뒤 멈추지 않는다.}
\captionsetup{hypcap=true}
\label{fig:teaser}
\end{strip}
