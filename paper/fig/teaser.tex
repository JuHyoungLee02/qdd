\begin{strip}
\centering
\includegraphics[width=\linewidth]{figures/overview.pdf}
\captionsetup{hypcap=false}
\captionof{figure}{\textbf{PaceNotes: 의도 공유 계층.} 랠리의 코드라이버와 드라이버처럼 일을 나눈다. \textbf{상위 계획기}(코드라이버; 지금은 Astra, 목표는 학습한 오픈 VLM)는 한 번에 요청 하나만 보내는 직렬 흐름으로 카메라 세 대와 손끝 덧그림을 보고, 행동마다 다음 \textbf{구간 의도}(지금 구간·할 일·다음 구간)를 선언한다. 그리고 VLA가 실제로 한 결과를 \textbf{검증}해 다음 의도를 낸다. \textbf{VLA}(드라이버; Qwen3-VL-4B)는 구간 의도와 영상·상태를 받아 0.33\,s마다 방향·크기·대상·단계·그리퍼를 닫힌 보기로 고르고, 쥐기·놓기 시점을 스스로 정한다. \textbf{행동 전문가}는 확정된 보기를 0.5\,s 관절 청크로 채운다. 확정 규칙 M4는 겹쳐 부른 결정의 합의와 실행 뒤 측정(고유 감각 T1, 확인 헤드 V1h)을 결합한다. 느린 상위의 답은 도착 시점에 그사이 VLA가 한 일과 대조한 뒤, 두 답의 합의와 물체와의 거리 권한에 따라 속도 상한 안에서 서서히 얹힌다. 잡음·놓음의 확정은 T1과 V1h가 맡는다. (b) 시간 축: 상위의 답은 지연(탐침 p50 \tentative{약 9--10\,s})마다 하나씩 도착하고, 그사이 로봇은 멈추지 않는다. 오른쪽 아래의 모듈형 스택은 기준선이자 학습 데이터의 교사다.}
\captionsetup{hypcap=true}
\label{fig:teaser}
\end{strip}
