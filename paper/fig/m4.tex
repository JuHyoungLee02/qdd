\begin{figure*}[t]
\centering
\includegraphics[width=\linewidth]{figures/m4_commit.pdf}
\caption{\textbf{M4 확정 규칙.} (a) 결정 토큰 호출을 약 0.33\,s 간격으로 겹쳐 보낸다. 막대 길이는 설명용이며, 머리 + 손목 입력의 실측 p95는 \tentative{0.18--0.28\,s}다(\cref{sec:prelim}). (b) 각 호출은 앞으로의 $H$ 스텝(그림은 $H=3$)에 답하고, 답은 \texttt{option\_key} 기준으로 스텝별 표에 쌓인다. RTC의 구간을 이산판으로 옮겨 고정(송신 뒤 $d_{p95}$ 안), 중간(바꾸려면 세 표 중 두 표 합의와 유예 창 $W=1$), 끝(가장 새 표만)으로 나눈다. $H=1$이면 같은 스텝을 $\text{lead}_{\max}$(\tentative{1.0\,s}) 전부터 $T_c$마다 앞당겨 물어 스텝마다 2--3표를 모은다. (c) 호출 사이 합의(a)와 실행 뒤 예상 대 측정 확인(b)을 결합해 확정, 늦춰 확정, 교체, 수리 가운데 하나를 고른다(\cref{tab:m4rule}). 교체와 수리 때는 전제 판본을 올려 그 전제에 기댄 미확정 표를 버린다. (b) 범주는 다음 결정 호출의 입력에 한 줄로 되먹이며, 이 부분은 \tentative{구현 중}이다. 표의 A--C는 설명용 예시다.}
\label{fig:m4}
\end{figure*}
