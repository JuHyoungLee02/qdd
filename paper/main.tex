% CVPR 2026 Paper Template; see https://github.com/cvpr-org/author-kit
% [Harvest] 공식 author kit 그대로. 바꾼 곳: 제목, \input 절 목록, preamble.tex 끝의 한국어/색 설정. 컴파일러: XeLaTeX.
% [Harvest] 2026-09-24 사용자 지시(user-log 26)로 본문을 논문 형식으로 다듬었다. 기존 내용만 썼고 새 내용은 넣지 않았다.
% [Harvest] 2026-09-25 사용자 지시(user-log 63)로 융합 설계(정본 §58) 기준 논문 형식으로 다시 썼다. 임시 값은 \tentative(주황).
% [Harvest] 2026-09-25 09:40 UTC 갱신(user-log 68·69 지속 갱신): 정본 §56–§76 반영 — 스텝당 두 호출(decide → chunk), M4 사전 등록 적합화(2/3, W, τ, H=1 앞당김, STALE_MAX 척도, C2 원문판), (b) 측정원 V1h + T1, R7 감사, SCOPED.
% [Harvest] 2026-09-25 10:50 UTC 무렵 갱신(user-log 69 1시간 주기, 70): 정본 §77과 보충 — (b) 범주 되먹임 줄 last_step 구현 서술, 호출 기록 blob, FLIP_TH q0.99, E0.5 궤적별 뒤집힘, 카나리 표류 → J5 끔; 시뮬 재현성(CPU PhysX 이력 의존, 886행 거부권 제외, 장면 재생성 빌드 구현 중); S-E2E 학습 진행 중; R2 생성 파일럿; R7 12회차 수정.
% [Harvest] 2026-09-25 11:45 UTC 무렵 갱신(user-log 69): 정본 §78(PhysX 하드 리셋 구현, 24/24 비트 동일, +0.28 s, 렌더 픽셀 비결정, 옛 녹화 재생 불가, 짝 비교는 새 빌드만), §77 보충 2(S-E2E 판정 스크립트 결과 전 고정, CMP_EPS·rc), R7 13회차(결함 3, 수정 중), FLIP_TH 정의·카나리 문장 정정; S-E2E 결과 아직 없음.
% [Harvest] 2026-09-25 12:45 UTC 무렵 갱신(user-log 69): S-E2E 사전 등록 파이프라인 판정 통과(§56, 수치 주황)와 진단 D1–D3(판정 불가, 규모 곡선 진행 중), 정본 §79·보충(질문별 FLIP_TH, 판본별 카나리 기준일, 표류 뒤 J5 끔, 경계 틱 last_step — 수정 중 → 구현), R7 14회차(결함 1, 수정 중), R2_TRAIN 하드 리셋 빌드 재기동.
% [Harvest] 2026-09-25 13:00 UTC 무렵 갱신(user-log 71): 시간 맥락 1순위(카메라마다 [t-0.3 s, t] 2프레임 Qwen3-VL 비디오) — 방법 §시간 맥락(검증 중), 계획 E-TC 2×2와 채택 규칙, 관련 연구(HAMLET·RoboMME·GAP·Data Scaling Laws, 학회 확인), 한계 한 줄, 개요·모델 그림의 주황 점선 과거 프레임.
% [Harvest] 빨간 줄(사용자 의도)이 있는 마인드맵 판은 mindmap.tex(mindmap/sec/*.tex)에 그대로 보관한다.

\documentclass[10pt,twocolumn,letterpaper]{article}

%%%%%%%%% PAPER TYPE  - PLEASE UPDATE FOR FINAL VERSION
% \usepackage{cvpr}              % To produce the CAMERA-READY version
\usepackage[review]{cvpr}      % To produce the REVIEW version
% \usepackage[pagenumbers]{cvpr} % To force page numbers, e.g. for an arXiv version

% Import additional packages in the preamble file, before hyperref
\input{preamble}

% It is strongly recommended to use hyperref, especially for the review version.
% hyperref with option pagebackref eases the reviewers' job.
% Please disable hyperref *only* if you encounter grave issues, 
% e.g. with the file validation for the camera-ready version.
%
% If you comment hyperref and then uncomment it, you should delete *.aux before re-running LaTeX.
% (Or just hit 'q' on the first LaTeX run, let it finish, and you should be clear).
\definecolor{cvprblue}{rgb}{0.21,0.49,0.74}
\usepackage[pagebackref,breaklinks,colorlinks,allcolors=cvprblue]{hyperref}

%%%%%%%%% PAPER ID  - PLEASE UPDATE
\def\paperID{*****} % *** Enter the Paper ID here
\def\confName{CVPR}
\def\confYear{2026}

%%%%%%%%% TITLE - PLEASE UPDATE
\title{Harvest: 느린 API 계획기와 typed 결정 토큰 VLA를 잇는\\시간차 확정으로 멈추지 않는 로봇 실행}

%%%%%%%%% AUTHORS - PLEASE UPDATE
\author{First Author\\
Institution1\\
Institution1 address\\
{\tt\small firstauthor@i1.org}
% For a paper whose authors are all at the same institution,
% omit the following lines up until the closing ``}''.
% Additional authors and addresses can be added with ``\and'',
% just like the second author.
% To save space, use either the email address or home page, not both
\and
Second Author\\
Institution2\\
First line of institution2 address\\
{\tt\small secondauthor@i2.org}
}

\begin{document}
\maketitle
\input{fig/teaser}
\input{sec/0_abstract}    
\input{sec/1_intro}
\input{sec/2_related}
\input{sec/3_method}
\input{sec/4_setup}
\input{sec/5_plan}
\input{sec/6_prelim}
\input{sec/7_conclusion}
{
    \small
    \bibliographystyle{ieeenat_fullname}
    \bibliography{main}
}

% WARNING: do not forget to delete the supplementary pages from your submission 
\input{sec/X_suppl}

\end{document}
