\section{Related Work}
\label{sec:related}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.14\linewidth}p{0.14\linewidth}p{0.12\linewidth}p{0.14\linewidth}p{0.07\linewidth}p{0.15\linewidth}p{0.14\linewidth}@{}}
\toprule
방법 & 계층 구조 & 빠른 층 출력 & 상위 호출 시점 & 비정지 & 확정 규칙 & 같은 장면 짝 강건성 평가 \\
\midrule
Gemini Robotics 1.5 / ER~\cite{geminirobotics15_2025,geminier_docs2026} & 오케스트레이터 VLM + VLA & 연속 행동 & 하트비트(약 1\,Hz), 하위 과제 성공 감지 & 있음 & 가장 새 지시 & 없음 \\
$\pi_{0.5}$~\cite{pi05_2025} & 한 모델(하위 과제 문장 $\rightarrow$ 행동) & 연속 행동 & 추론 스텝마다 & --- & 없음 & 없음 \\
Hi Robot~\cite{hirobot2025} & 상위 VLM + 하위 VLA & 연속 행동 & 1\,s마다 또는 사용자 개입 & --- & 없음 & 없음 \\
HiVLA~\cite{hivla2026} & 상위 VLM + DiT 행동 익스퍼트 & 연속 행동 & --- & --- & 없음 & 없음 \\
JEV-Star~\cite{jevstar2026} & Astra + 빠른 typed 모델 (게임) & typed 선택 & 명목 주기 + 사건 & 있음 & 이전 유효 계획 유지 & 없음 \\
Slow Brain~\cite{slowbrain2026} & 느린 VLM + 빠른 계획기 & 빠른 층 점수 & 겹친 연속 요청 & 있음 & 가장 새 답 / 확률 융합 & 없음 \\
A3~\cite{a3commit2026} & 단일 VLA & 행동 청크 & --- & --- & 같은 시각 샘플 합의, 실행 뒤 확인 없음 & 없음 \\
RoboDawn~\cite{robodawn2026} & VLM 하네스 & cm 단위 수치 명령 & 결정마다 & --- & 없음 & 부분(깨끗한 장면 점수 없음) \\
Harness VLA~\cite{harnessvla2026} & 계획기 + 동결 VLA 프리미티브 & 프리미티브 호출 & 동작 결과마다 & 없음(동기) & 없음 & 없음 \\
CaP-X~\cite{fu2026capx} & LLM 코드 생성 + 인식 도구 & 코드 & --- & --- & 없음 & 없음 \\
RoboDojo Astra 평가~\cite{robodojo2026} & Astra 단독 & --- & --- & --- & 없음 & 부분(같은 인식 아님) \\
GPT 6 Astra as Policy~\cite{su2026astra} & Astra + $\pi_{0.5}$ 후보 검토 & 연속 행동(교정은 Astra) & 청크 경계마다(동기 게이트) & 없음(동기) & 실패·불일치일 때만 교정 & 없음 \\
MolmoAct~\cite{molmoact2026} & 한 모델(깊이 $\rightarrow$ 궤적 $\rightarrow$ 행동) & 행동 토큰 & --- & --- & 없음 & 없음 \\
\midrule
\textbf{PaceNotes (ours)} & Astra + 융합 VLA & 조이스틱형 결정 토큰 + 연속 행동 & 직렬 흐름(요청 1개) + 과제 컴파일·실패·저빈도 감사 & 있음 & \textbf{M4: 시간차 합의 + 실행 뒤 측정; Astra 편향은 두 답 합의·손목 근거} & \textbf{있음(위층·학습량 축, 계획)} \\
\bottomrule
\end{tabular}
\caption{\textbf{가장 가까운 선행과의 비교.} ``---''는 해당 없음 또는 원문에서 확인하지 못함을 뜻한다. 계층 구조, 비정지 실행, 하트비트는 이미 있으므로 우리는 이것을 새로움으로 주장하지 않는다. 우리 행이 다른 곳은 확정 규칙과 평가 두 열, 그리고 느린 상위 모델의 교정을 멈추지 않는 실행 위에 합의와 근거를 거쳐 얹는 방식이다. RoboDawn과 RoboDojo Astra 평가는 같은 방향의 비교를 했지만, 깨끗한 장면 점수가 없거나 두 조건의 인식이 같지 않다.}
\label{tab:related}
\end{table*}

\paragraph{API 모델로 로봇 제어.}
AGP~\cite{agp2026}와 RoboDojo Astra 평가~\cite{robodojo2026}는 Astra로 로봇을 제어한다(둘 다 심사 전). RoboDawn~\cite{robodawn2026}은 Astra 같은 VLM이 cm 단위 숫자로 로봇을 조종하는 하네스이며, RoboTwin 2.0 C2R(깨끗한 장면 학습 $\rightarrow$ 무작위 장면 평가)에서 Astra 0-shot 53.2\%, $\pi_{0.5}$ 46.0\%를 보고했다. 깨끗한 장면 점수가 없어 하락 폭은 계산할 수 없다. Harness VLA~\cite{harnessvla2026}는 계획기가 동결 VLA 프리미티브를 부르고 결과 파일을 기다리는 동기 방식이며, CaP-X~\cite{fu2026capx}는 코딩 에이전트가 인식 도구와 스킬 함수를 불러 로봇을 제어하는 틀이다. Inspect Robots~\cite{inspectrobots2026}는 LLM 정책, 코드 정책, VLA를 같은 로그로 돌리는 오픈소스 평가 하네스이고, 루프는 동기식이다. 우리는 이 하네스를 1차 평가에 쓰되 시계를 바꿔 지연을 실제대로 반영한다(\cref{sec:setup}).

\paragraph{프런티어 모델의 일반 조종과 교정.}
GPT 6 Astra as Policy~\cite{su2026astra}(기술 보고서)는 Astra에 이미지와 도구를 주고 로봇을 직접 조종하게 한 판(Direct)과, $\pi_{0.5}$가 낸 후보 행동을 Astra가 검토해 실행이 실패했거나 의도와 어긋날 때만 말단 이동·회전·그리퍼를 고치는 하이브리드를 RoboDojo 10과제에서 비교했다. 하이브리드는 성공률 48\%로 Direct 26\%보다 높았고, 교정은 실행 스텝의 14.4\%였다. 불확실함만으로는 가로채지 않는다는 원칙도 이 게이트에서 왔다. 다만 그 하이브리드는 청크 경계에서 Astra의 답을 기다리는 동기 게이트라 느린 답이 곧 로봇의 정지다. PaceNotes는 같은 일반 조종 명령 공간(계속, 말단 수정, 멈춤과 진행 평가)을 쓰되, 로봇을 세우지 않고 직렬 흐름의 답을 도착 시점 예상 상태 기준으로, 합의와 근거를 거쳐 서서히 얹는다(\cref{sec:stream}).

\paragraph{계층형 VLA와 상위 호출 주기.}
Gemini Robotics 1.5~\cite{geminirobotics15_2025}의 오케스트레이터는 하위 과제 성공 감지로 다음 단계로 넘어갈 때를 정한다. Gemini Robotics-ER 개발자 문서~\cite{geminier_docs2026}는 최신 카메라 프레임과 짧은 질문을 주기적으로 보내 모델이 명시적으로 결정하게 하는 하트비트를 두고, 한 턴이 끝나기를 기다리면서 약 1\,Hz를 목표로 보내며, 타이머만 믿고 보내면 취소가 반복되는 고리에 빠질 수 있다고 경고한다. $\pi_{0.5}$~\cite{pi05_2025}는 추론 스텝마다 하위 과제를 다시 예측하고, Hi Robot~\cite{hirobot2025}(기간 밖, 기초 문헌)은 1초마다 또는 사용자 개입 때 상위 추론을 다시 돌린다. $\pi_{0.5}$에서는 학습한 상위 단 대신 영점 GPT-4를 쓰면 가장 나빴다. 즉 분포 안에서는 학습한 결정이 강하다. 우리의 주장은 절대 성능이 아니라 분포가 바뀐 뒤의 하락 폭에 관한 것이며, 이를 같은 장면 짝으로 잰다. CheckVLA~\cite{checkvla2026}와 Event-triggered~\cite{eventtrig2026}는 주기 호출과 사건 호출을 비교했다.

\paragraph{결정과 행동을 한 백본에서.}
$\pi_{0.5}$는 같은 백본이 하위 과제 문장을 내고 action expert가 flow matching으로 연속 행동을 낸다. Knowledge Insulation(KI)~\cite{kinsulate2025}은 expert를 그냥 붙이면 학습 속도와 지식 전이가 모두 나빠진다고 보고하고, expert에서 백본으로 가는 기울기를 막아 백본의 지식을 보호한다. HiVLA~\cite{hivla2026}는 물체 crop과 스킬 문장을 조건으로 받는 DiT 행동 익스퍼트를 쓴다. 로봇 데이터로 미세조정하면 분포 밖 성능이 떨어질 수 있고, SFT를 오래 하면 분포 밖에서 무너지며 RL은 덜 잊는다는 결과가 있다~\cite{chu2025sftrl}(기간 밖, 기초 문헌). 깨끗한 시연만으로 학습하면 무작위 장면에서 크게 떨어진다~\cite{robotwin2_2025,liberoplus2025}. 우리는 결정을 자연어 문장이 아니라 typed 보기 토큰으로 내게 하여 확률을 보기 집합 안에서 정의하고, 이 확률을 확정 규칙과 보정에 쓴다.

\paragraph{VLM을 조종기로: 조종 표현.}
VLM이 짧은 동작 명령을 고르고 저수준 정책이 그것을 연속 행동으로 옮기는 인터페이스에는 근거가 있다. Gemini Robotics 1.5의 Thinking VLA는 ``그리퍼를 왼쪽으로'' 같은 동작 문장을 거쳐 행동한다~\cite{geminirobotics15_2025}. Steerable VLA Policies~\cite{steerable2026}는 과제, 하위 과제, 동작, 픽셀 점과 궤적 명령을 모두 받도록 학습한 저수준 정책에서 한 가지 형식만 쓸 때는 동작 명령이 가장 효과적이었고, 처음 보는 물체에서는 점과 궤적이 앞섰으며, 모든 상황을 이기는 형식은 없다고 보고했다. 그 정책은 ``왼쪽으로''를 무조건 왼쪽이 아니라 물체 쪽으로 가는 뜻으로 재해석하기도 했다. VLA-OS~\cite{vlaos2025}는 같은 구조와 데이터에서 계획 표현만 바꾸어, 시각에 근거한 표현(상자, 말단 궤적)이 언어 표현보다 저수준이 더 잘 따르고, 계획을 먼저 생성해 행동 머리가 원 입력을 보지 못하게 하면 계획 오류가 누적된다고 보였다. 우리 조이스틱은 동작 수준의 닫힌 보기를 생성 없이 확률 한 번으로 읽어 이 누적 비용을 피하지만, ``어디로''를 말하는 공간 목표가 없고 연속 행동이 선택을 따르는지를 아직 재지 않았다. 추론이나 단계 라벨을 학습 때만 쓰고 실행 때 생성하지 않아도 전체 추론과 같은 성능이 나왔다는 결과~\cite{ecotlite2025}는 Astra를 실행 중 해설자가 아니라 학습 라벨의 교사로 두는 근거다.

\paragraph{공간 추론과 궤적 표현.}
MolmoAct~\cite{molmoact2026}(ICRA 2026)는 한 모델이 매 스텝 깊이 인식 토큰(이미지당 100개, RGB에서 단안 깊이 추정기로 만든 라벨을 VQ-VAE로 압축), 이미지 위 손끝 궤적(1--5점, 0--255 좌표), 행동 토큰을 차례로 생성한다. 그린 궤적으로 조종하면 언어로 조종할 때보다 진행 점수가 높았다(한 과제 15회, \tentative{0.747} 대 \tentative{0.413}; 완전 성공 \tentative{7/15} 대 \tentative{2/15}). 그러나 깊이나 궤적 단계를 뺀 절제가 없어 사슬 각 단계의 이득은 입증되지 않았다. 후속작 MolmoAct2~\cite{molmoact2_2026}(기술 보고서)는 우리 융합 VLA와 같은 계열의 구조(4B LLM, 흐름 정합 expert, 이산 행동 토큰 병행)로 가면서 궤적 생성을 버렸고, 깊이 생성 경로는 기본 경로보다 느렸다(\tentative{0.787\,s} 대 \tentative{0.179\,s}). HAMSTER~\cite{hamster2025}는 상위 VLM이 그린 2D 경로를 3D 저수준 정책이 따르게 했다. 우리는 이 계열에서 실행 지연이 없는 조각만 가져온다. 미래 손끝 궤적을 보조 손실로 배우고(VLA-OS~\cite{vlaos2025}의 보조 손실형 시각 계획, ECoT-Lite~\cite{ecotlite2025}의 학습 때만 쓰는 추론과 같은 방향), 그린 명령을 따르도록 명령 조건 데이터로 학습하며, Astra 영상에 손끝 이력을 덧그린다(\cref{sec:molmo}). 사용자 결정에 따라 목표를 공간에 표현하지는 않는다.

\paragraph{비동기 실행과 확정.}
RTC~\cite{black2025rtc}, SmolVLA~\cite{shukor2025smolvla}, VLASH~\cite{vlash2025}, A2C2~\cite{a2c2}는 행동 청크를 비동기로 실행한다. Slow Brain~\cite{slowbrain2026}은 느린 모델을 멈추지 않고 겹쳐 요청하며, 가장 새 답만 쓰거나(VLM Stream) 가장 새 답에 빠른 층 점수를 감쇠 가중으로 섞는다(Probability Fusion). A3~\cite{a3commit2026}는 VLA 청크를 같은 시각에 여러 개 뽑아 합의한 앞 구간을 확정하며, 저자는 정책 내부 신호만으로는 자신 있게 틀린 예측을 잡을 수 없고 외부 증거가 향후 과제라고 밝혔다. WCD~\cite{wcd2026}는 실행 뒤 관측과 상상한 결과의 불일치로 다음 후보 선택 예측기를 학습하고, SMC~\cite{smc2026}는 도구 사용 에이전트에서 두 모델의 다음 행동이 일치하면 미리 실행한 단계를 확정한다. 한 관측에 대한 반복 추론은 증거의 출처를 늘리지 않는다는 지적도 있다~\cite{pact2026}. M4는 시간차 합의와 실행 뒤 측정을 결합해 이 빈자리를 채운다. 측정원으로는 VLA 실패 감지 연구를 참고했다. SAFE~\cite{safe2025}는 VLA 내부 특징으로 실패를 감지하며 토큰 불확실성 기준선이 약하다고 보고했고, Self-Improving EFM~\cite{selfimprovingefm2025}은 정책과 같은 모델의 남은 스텝 예측을 성공 판정기로 써서 따로 둔 성공 분류 목표보다 안정적이었다고 보고했으며, TA-VLA~\cite{tavla2025}는 모터 전류에서 얻은 관절 토크가 접촉과 말단 상태를 구분한다고 보였다. 결정 스텝마다 예상 결과 술어를 같은 VLA의 헤드로 재서 확정 원장을 갱신한 직접 선례는 찾지 못했다.

\paragraph{typed 결정, 보정, 서빙.}
KnowNo~\cite{knowno2023}(기간 밖, 기초 문헌)는 LLM 객관식 계획을 conformal 예측 집합으로 바꾸어 집합이 원소 하나이면 실행하고 아니면 도움을 요청했고, CoFineLLM~\cite{cofinellm2025}은 단계별 집합으로 계획 전체의 커버리지를 보장했다. 온도 조정~\cite{guo2017calib}(기간 밖, 기초 문헌)은 검증 집합 NLL로 확률을 보정한다. 백본은 원본 해상도 다중 이미지 입력을 지원하는 Qwen3-VL~\cite{qwen3vl2025}이고, 서빙은 배치 불변 모드와 접두 캐시를 쓰는 vLLM~\cite{vllm2026}이다.

\paragraph{시간 맥락과 이력.}
과거 관측을 입력으로 쓰는 VLA는 이력 의존 과제에서 큰 이득을 보고했다. HAMLET~\cite{hamlet2026}은 시점마다 압축한 moment token과 작은 메모리 모듈로 GR00T N1.5의 이력 의존 실제 과제 성공률을 29.2\%에서 76.4\%로 올렸지만, 과거 프레임을 그냥 더 넣은 판은 RoboCasa에서 62.6\%에서 59.3\%로 떨어졌고 지연도 1.35배가 되었다. RoboMME~\cite{robomme2026}는 $\pi_{0.5}$ 위의 여러 기억 변형을 비교해 모든 과제를 이기는 방식은 없지만 지각(프레임) 기억이 움직임 중심·시간 민감 과제에서 가장 효과적이라고 보고했다. GAP~\cite{gap2026}는 시각과 고유감각을 함께 쓰는 정책이 움직임 전환 구간에서 시각을 덜 쓰게 되는 실패를 보였다. 우리가 시험한 두 변경(\cref{sec:temporal})은 기억 모듈을 더하지 않는 가장 가벼운 형태다. 백본이 이미 가진 2프레임 시간 패치로 0.3\,s 전 한 장을 보게 하는 것과, 인과 속도를 거친 구간으로 적은 움직임 줄 한 줄이다. 전환 층을 따로 재어 GAP식 실패를 확인했으며, 사전 등록 판정에서 움직임 줄만 채택했다. 데이터 규모 쪽으로는 모방 학습의 일반화가 시연 수보다 환경·물체 수에 대해 대략 거듭제곱으로 는다는 결과~\cite{datascaling2025}가 있어, 시간 맥락 시험과 별도로 규모 곡선을 잰다.
