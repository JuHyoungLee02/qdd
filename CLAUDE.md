# Harvest 작업 규칙

사용자가 준 주의사항과 제약을 모은 파일이다. 모든 작업, 특히 자율로 진행하는 작업에서 항상 먼저 읽고 어기지 않는다.
사용자가 새 주의사항이나 제약을 말하면 바로 이 파일에 추가한다.

## 브랜치와 푸시
- 모든 작업은 `dev` 브랜치에서 하고 `dev`에만 푸시한다.
- `main`에는 절대 커밋, 푸시, 병합하지 않는다. 사용자가 `dev`를 확인한 뒤 직접 `main`에 반영한다.
- 저장소 이름은 나중에 사용자가 직접 `harvest`로 바꾼다.

## 논문 초안 (`paper/main.tex`)
- Overleaf에 그대로 복사해 붙일 수 있는 LaTeX 형식으로 쓴다.
- 처음부터 완성하지 않는다. 대화가 진행될 때마다 조금씩 쌓아간다.
- 논문 문장으로 쓰지 않는다. 마인드맵처럼 "이렇게 할 거다" 수준의 짧은 항목으로 쓴다.
- 들어갈 수 있는 내용은 두 가지뿐이다.
  1. 사용자가 말한 내용
  2. 코드를 실제로 돌려서 확인한 사실
- Claude의 제안, 추측, 조사 결과는 사용자가 넣으라고 하기 전에는 넣지 않는다.
- 아직 정해지지 않은 것은 아예 언급하지 않는다.
- 관련 없거나 이상한 문장을 넣지 않는다.

## 연구 주제에서 확인된 제약
- GPT-6 Astra는 API라서 응답이 느리다.
- Jev(TypeSafe AI)는 텍스트 입력만 받는다. 카메라 프레임은 텍스트나 JSON으로 바꿔서 넣어야 한다. 바꾸는 방법은 아직 정해지지 않았으므로 사용자가 정하기 전에는 임의로 정하지 않는다.

## 연구 방향 (사용자가 정한 것)
- 항상 기억하고 강조할 점: VLA는 다른 환경으로 일반화가 안 되는 큰 단점이 있다. LLM은 일반화가 된다.
- 개념은 VLA 논문에서 많이 가져온다. 특히 "계획대로 진행되는지"의 정의, 실패 판정 후 되돌아가는 기준, 실패 복구 방법.
- Jev 입력: VLA에 국한하지 않고 AI 전 분야에서 이미지를 텍스트로 가장 효율적으로 바꾸는 방법을 찾아 쓴다. Astra가 Jev에게 전달하는 방식도 같은 문제로 본다.
- 행동 표현은 촘촘하게 가도 된다. Jev라면 충분히 가능하다고 본다.
- 연속 동작 만들기는 두 단계로 한다.
  1. RTC와 비슷한 방법: 1초에 Jev를 한 번이 아니라 여러 번(예: 3번) 계단식으로 겹쳐 호출한다. 겹치는 구간의 움직임이 제대로 반영됐는지 스스로 확인하고 업데이트하는 시스템이 반드시 있어야 한다.
  2. 액션 스무딩 등 VLA에서 쓰는 방법으로 아주 작은 움직임을 부드럽게 만든다.
- 기존 스킬은 Jev와 최대한 밀접하게 엮는다. LLM이 스킬을 불러오고 끝나는 방식이 아니라 잘게 엮는 방식을 참고한다.
- Astra 호출: 최소한 실패 판정이 나면 부른다. 그때 한 장이 아니라 연속 프레임을 준다(예: 한 이미지에 프레임 10개를 붙인 형태).
- Astra 호출 시점은 비교 실험으로 정한다: 일정 주기(Hz), 사용자가 정한 순간마다, "이때쯤 결과가 나왔어야 하는데 안 나왔다"는 자체 판단.
- Astra가 처음 계획할 때는 로봇이 멈춰도 된다. 그 이후에는 웬만하면 멈추지 않는다. 실패로 다시 생각해야 할 때만 예외다.
- 경험 축적이 제대로 되어야 한다. API 모델 기반의 경험 축적 방법을 참고한다.
- 비교 대상: Astra만, Jev만, VLA, 룰베이스(스킬 다발만), LLM+스킬을 연결한 기존 논문, Astra를 쓰는 기존 논문.
- 안전은 크게 다루지 않는다.

## 조사 규칙
- 참고 논문은 1년 반 이내 것으로 찾는다. (LLM+스킬 결합 논문은 1년 이내, GitHub 스타 상위 기준)

## 논문 초안 추가 규칙
- 사용자가 계획을 올리라고 하면 계획 초안도 넣는다.
- 초안에는 사용자가 말한 내용이 사용자의 의도로 드러나게 들어가야 한다.

## 계획 문서
- 전체 계획과 참고 문헌은 `docs/plan.md`에 있다. [사용자]와 [제안]을 구분해 두었다. 새로 정해지는 내용은 여기와 `paper/main.tex`에 함께 반영한다.
