# Harvest 작업 규칙

사용자가 준 주의사항과 제약을 모은 파일이다. 모든 작업, 특히 자율로 진행하는 작업에서 항상 먼저 읽고 어기지 않는다.
사용자가 새 주의사항이나 제약을 말하면 바로 이 파일에 추가한다.

**새 세션은 가장 먼저 `docs/handoff.md`를 읽는다.** 사용자 발언 전체는 `docs/user-log.md`에 있다. 새 발언이 생기면 거기에 덧붙인다.

## 브랜치와 푸시
- 모든 작업(글 작성, 코드 수정 포함)은 `dev` 브랜치에서 시작하고 `dev`에만 푸시한다. [사용자, 여러 번 강조]
- `main`에는 절대 커밋, 푸시, 병합하지 않는다. 사용자가 `dev`를 확인한 뒤 직접 `main`에 반영한다.
- 저장소 이름은 나중에 사용자가 직접 `harvest`로 바꾼다.

## 논문 초안 (`paper/main.tex`) — 2026-09-23 개정 [사용자]
- Overleaf에 그대로 붙일 수 있는 LaTeX 형식이다. **CVPR 공식 Overleaf 템플릿(github.com/cvpr-org/author-kit)을 파일 구조 그대로 쓴다** [사용자]: main.tex(review 모드), preamble.tex, sec/*.tex, fig/teaser.tex, main.bib. 절 파일에 내용을 쓰고, 템플릿 자체는 제목·절 목록·preamble 끝(한국어)만 바꾼다. 컴파일러 XeLaTeX. 양식만 CVPR이고 내용은 여전히 마인드맵 초안이다.
- (2026-09-24, user-log 26) 위 줄의 "내용은 여전히 마인드맵 초안"은 이제 마인드맵 판(`paper/mindmap.tex`)에만 적용한다. `paper/main.tex`는 논문형 글이다.
- 그 시점까지의 최신 생각을 잘 정리된 글로 계속 업데이트한다. 조사 중에도 멈추지 않는다.
- **색 구분**(마인드맵 판에 적용): 사용자가 의도하고 말한 내용은 빨간색(`\intent{}`), 그 밖의 내용(조사, 판단, 제안)은 검정색이다. 사용자 발언과 Claude 내용을 한 항목에 섞지 않는다.
- (마인드맵 판) 논문 문장이 아니라 "이렇게 할 거다" 수준으로 쓴다. 논문 판은 실제 논문 문장으로 쓰되 문서에 있는 내용만 쓴다. 관련 없거나 이상한 문장은 넣지 않는다.
- 확인되지 않은 수치를 사실처럼 쓰지 않는다. 신뢰도 낮은 참고 문헌은 넣지 않는다.
- 상세한 조사 기록과 근거는 `docs/`의 MD 파일에 둔다. `main.tex`는 정리본이다.
- [사용자, 두 번째 세션] 한국어로 쓴다. 논문이 아니라 마인드맵 초안이다. 처음부터 다 채우지 않고 부분 부분만 채운다. 각 부분에서 무엇을 생각했고, 어떻게 바뀌었고, 어느 방향으로 가는지만 보이면 된다. 예쁠 필요 없다.

- [사용자, 2026-09-24, user-log 26] "실제 논문처럼 글을 써주면 돼." / "지금 있는 내용에서만 글을 다듬어 달라는 얘기야 … 추가로 뭘 넣거나 이런다는 얘기가 아니라". → 본문(`paper/main.tex` + `sec/*.tex`)은 논문 형식 한국어 글이다. 새 주장·결과를 넣지 않고 있는 내용만 쓴다. 빨간 줄이 있는 마인드맵 판은 `paper/mindmap.tex` + `paper/mindmap/`에 보관하고 계속 갱신한다(빨간 줄 규칙은 마인드맵 판에 적용).
- 빨간색(사용자 의도) 줄은 요약하거나 줄이지 않는다. "짧게"는 검정 줄에만 적용한다. 양식을 옮기거나 초안을 고칠 때마다 빨간 줄을 `docs/user-log.md` 원문과 대조한다(2026-09-23, 8곳이 줄어 있었음).

- [사용자, 2026-09-25, user-log 68·69] "지속적으로 오버리프 업데이트 하면서 하는중?" / "1시간마다 논문 업데이트하기로해 앞으로는 꼭" → 논문 본문·마인드맵·PDF를 **1시간마다 반드시** 갱신해 dev에 푸시한다(세션마다 매시 반복 작업을 건다). 큰 작업 중에도 멈추지 않는다.
- [사용자, 2026-09-25, user-log 73] "논문 업뎃주기는 4시간으로 하자" → 위 1시간 주기를 **4시간 주기**로 바꾼다(매 4시간 :17 반복 작업). 큰 결과(판정·실험 결과)가 나오면 주기와 별도로 반영해도 된다.
- [사용자, 2026-09-25, user-log 70] "문제가 있다면 은 만약에 성능이 너무 낮다면 은 기존에 최근 1년 반 이후 논문에서는 그러한 성능들을 비슷한 논문을 찾아가지고 그러한 성능들을 어떻게 올렸는지 찾아봐서 적응을 해도" → 성능이 낮으면 1년 반·신뢰도 규칙에 맞는 선행 연구의 개선법을 찾아 적용한다. 적용은 사전 등록 문턱을 결과 뒤에 바꾸지 않고 새 판·새 사전 등록으로 한다(정본 §77 보충).

- 임시 파일(원문 HTML·PDF·추출 텍스트·그림 미리보기)은 `D:\tools\scratch_qdd`에 둔다. C 드라이브(세션 임시 폴더, 홈 캐시)에 쓰지 않는다. matplotlib는 `MPLCONFIGDIR=D:/tools/mplcache`로 실행한다(2026-09-24 위반 뒤 추가).

- [사용자, 2026-09-25, user-log 76] "지금 10만원 정도는 비용 한도 책정이 가능해" → 유료 API(Astra 등) 비용 한도는 약 10만 원. 실행 전 예상 비용 계산, 실행 중 누적 기록, 한도 전 정지(정본 §82 보충).

## 대화
- [사용자, 2026-09-25, user-log 72] "한국시간으로 해줘 앞으론" → 사용자에게 하는 보고의 시각은 한국시간(KST, UTC+9)으로 쓴다. 저장소 문서·정본·로그의 기록 시각은 혼동 방지를 위해 지금처럼 `date -u`(UTC)로 남긴다(09-24 KST·UTC 혼동 사고, 정본 §67).
- 사용자에게는 한국어로 답한다.

## 연구 주제에서 확인된 제약
- GPT-6 Astra는 API라서 응답이 느리다.
- Jev(TypeSafe AI)는 텍스트 입력만 받는다. 카메라 프레임은 텍스트나 JSON으로 바꿔서 넣어야 한다. 바꾸는 방법은 Claude가 정해 진행한다(사용자 결정 2026-09-24, user-log 25: "사용자가 정한다"는 사용자가 한 말이 아니었음). 기본 = M1 후보 A, B·C는 E3 비교.

## 연구 방향 (사용자가 정한 것)
- 항상 기억하고 강조할 점: VLA는 다른 환경으로 일반화가 안 되는 큰 단점이 있다. LLM은 일반화가 된다.
- 개념은 VLA 논문에서 많이 가져온다. 특히 "계획대로 진행되는지"의 정의, 실패 판정 후 되돌아가는 기준, 실패 복구 방법.
- Jev 입력: VLA에 국한하지 않고 AI 전 분야에서 이미지를 텍스트로 가장 효율적으로 바꾸는 방법을 찾아 쓴다. Astra가 Jev에게 전달하는 방식도 같은 문제로 본다.
- 행동 표현은 촘촘하게 가도 된다. Jev라면 충분히 가능하다고 본다.
- 연속 동작 만들기는 두 단계로 한다.
  1. RTC와 비슷한 방법: 1초에 Jev를 한 번이 아니라 여러 번(예: 3번) 계단식으로 겹쳐 호출한다. 겹치는 구간의 움직임이 제대로 반영됐는지 스스로 확인하고 업데이트하는 시스템이 반드시 있어야 한다.
  2. 액션 스무딩이나 VLA에서 쓰는 부드럽게 하는 방법으로 아주 작은 움직임을 처리한다(user-log 원문, D11 정정).
- 기존 스킬은 Jev와 최대한 밀접하게 엮는다. LLM이 스킬을 불러오고 끝나는 방식이 아니라 잘게 엮는 방식을 참고한다.
- Astra 호출: 최소한 실패 판정이 나면 부른다. 그때 한 장이 아니라 연속 프레임을 준다(예: 한 이미지에 프레임 10개를 붙인 형태).
- Astra 호출 시점은 비교 실험으로 정한다: 일정 주기(Hz), 정해 둔 필요한 순간마다(user-log 원문, 누가 정하는지는 [결정 필요] D23, D11 정정), "이때쯤 결과가 나왔어야 하는데 안 나왔다"는 자체 판단.
- Astra가 처음 계획할 때는 로봇이 멈춰도 된다. 그 이후에는 웬만하면 멈추지 않는다. 실패로 다시 생각해야 할 때만 예외다.
- 경험 축적이 제대로 되어야 한다. API 모델 기반의 경험 축적 방법을 참고한다.
- 비교 대상: Astra만, Jev만, VLA, 룰베이스(스킬 다발만), LLM+스킬을 연결한 기존 논문, Astra를 쓰는 기존 논문.
- 안전은 크게 다루지 않는다.

## 조사 규칙
- 참고 논문은 1년 반 이내 것으로 찾는다. (LLM+스킬 결합 논문은 1년 이내, GitHub 스타 상위 기준)
- 예외 [사용자, 2026-09-23 두 번째 세션]: 아주 큰 주제를 포괄하는 기초 문헌은 1년 반이 아니어도 된다. 예외로 쓸 때는 "기간 밖, 기초 문헌"이라고 표시한다.
- 모듈 설계 = 모듈마다 어디의 어떤 연구가 최고였는지, 무엇을 가져올지, 우리 구조에 어떻게 접목할지를 모으는 과정이다. 파이프라인 설계가 아니다. 로봇·VLA 분야로 한정하지 않고, 아직 로봇에 안 쓰인 LLM/VLM 방법을 적극 찾는다.

## 자율 진행 [사용자, 2026-09-23 두 번째 세션]
- 사용자 허락 없이 자동으로 계속 진행한다. 결정이 필요한 항목은 [결정 필요]로 기록해 두고 멈추지 않는다.
- 진행하면서 MD를 계속 쓴다(`docs/draft-log.md`, `docs/research/`). `paper/main.tex`는 그때그때 최신 정리본으로 갱신한다.


## 계획 문서
- 전체 계획과 참고 문헌은 `docs/plan.md`에 있다. [사용자]와 [제안]을 구분해 두었다. 새로 정해지는 내용은 여기와 `paper/main.tex`에 함께 반영한다.

## 진행 단계와 조사 원칙 [사용자]
- "마차를 이어서는 기차를 만들 수 없다." 처음이 틀어지면 아무리 고쳐도 좋은 결과가 안 나온다. 그래서 초안을 제대로 잡는다.
- 끊임없이 스스로를 의심하고 검증한다. 다만 과한 의심으로 없는 사실을 만들어내지(할루시네이션) 않는다. 확인 못 한 것은 확인 못 했다고 적는다.
- 단계 1, 초안 조사: 약 3시간(±1~2시간).
  - 인용한 내용이 틀리지 않았는지 확인한다.
  - 정말 최고 성능인지 확인한다.
  - 저자의 블로그, 발표, 게시물까지 찾아본다.
- 단계 2, 상세 설계: 최소 6시간에서 최대 24시간.
  - (2026-09-24, user-log 27·28) 03:37 UTC 1차 마감 뒤 1시간 연장: 오류를 잡고, 오류가 없으면 Overleaf에 방법을 더 강건하고 상세하게 쓰며 그림과 계속 맞춘다. 그림은 완벽해질 때까지 검토를 반복하고, 실제 CVPR·VLA 논문 그림 형식(구글 슬라이드 느낌)을 따른다(user-log 29).
  - 파이프라인을 짜는 것보다, 각 부분에 어떤 모듈을 장착하면 좋을지에 집중한다.
  - 파이프라인은 생각은 하되 깊게 들어가지 않는다.
- 시간은 정확히 맞출 필요는 없지만 ±1~2시간 안에 들어와야 한다.
- 모든 과정과 결정은 MD로 기록한다. 초안 버전 기록은 `docs/draft-log.md`에 남긴다: 언제 무엇을 어떻게 했는지, 무엇이 틀렸는지, 같은 실수를 반복하지 않기 위한 교훈.

## 참고 문헌 신뢰도 기준 [사용자]
- 신뢰도가 낮은 논문과 저장소는 최대한 쓰지 않는다. 쓰느니만 못하다.
- 신뢰도가 반드시 있어야 한다: 검증된 학회나 저자, 인용 수, 커뮤니티 반응(좋아요, 추천), 어느 정도의 GitHub 스타.
- 정답이 로봇 분야에서만 나온다고 생각하지 않는다. LLM, VLM 분야의 방법이 로봇 분야에 들어오기까지 시간이 걸리는 경우가 많다. LLM, VLM, 로봇, VLA 전 분야를 살펴서 각 부분에 최고의 모듈을 만드는 데 집중한다.

## 모델 사실에서 나온 설계 제약 (2026-09-23 검증)
- Jev는 수치 정밀도, 수치 비교, 산술에 약하다(공식 jaggedness 문서). 기하 계산은 코드에서 하고, Jev에게는 범주화한 관계와 술어로 묻는다.
- Jev 상태에 관련 없는 내용을 넣지 않는다(정확도가 떨어진다).
- Jev 확률 보정은 업체 주장뿐이다. 확률을 쓰는 모든 모듈보다 먼저 우리 데이터로 측정한다.
- Astra는 비디오 입력이 없다. 연속 프레임은 여러 이미지나 격자로 준다. Astra effort별 첫 토큰 시간은 날마다 바뀐다(2026-09-23: low 약 2.8~3.0초, high 73초 — Artificial Analysis 기준 읽은 값, 하한 45초는 출처 문서 미확인). 같은 날 직접 잰다.
- (2026-09-23 v3 원문 확인) Jev 공식 #2(c): Score 기댓값으로 두 단계 사이 값을 보간하지 않는다(임계값 판정에만). #8: 질문 사이 확률 항등식이 성립하지 않는다 → 확률 비교는 같은 문구·같은 보기·같은 유형 질문끼리만.
- (2026-09-23 v3 원문 확인) Astra는 effort `none`이 없어 logprob를 쓸 수 없다. effort 지연은 날마다 바뀐다(같은 날 직접 잰다).
- Astra effort 기본은 **low**, 실험에서는 low와 high를 둘 다 비교한다(사용자 결정 2026-09-24, user-log 25: high는 첫 토큰 약 73초라 너무 느림). 다른 작업의 "effort 낮추지 말 것" 발언은 이 프로젝트에 적용하지 않는다.
- Astra + Jev 계층 구조 자체는 이미 공개 담론이다(JEV-Star 2609.27331). 새로움은 M4 확정 규칙과 같은 인식 앞단 위 평가에서만 주장하고, M8·M6·M10은 부수 요소(M8 비교·M9·M10은 첫 논문에서 최소판, 정본 §26)로 둔다.
- H200 파드(`juhyoung-native-7a2a` 등)에서는 **모든 파일을 `/data` 아래에만** 쓴다(user-log 45). 실행 전 `source /data/harvest/env.sh`(HF·torch·pip·matplotlib 캐시, TMPDIR, pyc를 `/data/harvest/` 아래로). 홈(`~/.cache`)·`/tmp`·`/root`에 쓰지 않는다.
